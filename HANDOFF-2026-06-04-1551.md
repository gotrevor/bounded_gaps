# Handoff: `exists_separable_F_of_Mk_gt` narrowed deep-NT axiom → pure-density axiom (continuity proven in-kernel)

**Date**: 2026-06-04 15:51 UTC · **Branch**: `path-a-selberg-nu` · HEAD `cf6c3d1`
**7 commits this lap** (`15d2335`…`cf6c3d1`; 6 Lean+docs, all green at the pre-commit full-build gate, 8280 jobs). Never pushed. **Supersedes `HANDOFF-2026-06-04-1435.md`.**

## 🎯 What we're doing
Analytic/functional-analysis axiom burn-down for the GPY/Maynard sieve. The unconditional
`bounded_gap_of_Mk_200` rests on 4 mathematical axioms: `BombieriVinogradov` (external PNT,
cite-only), `s1_holds_from_nonprime_asym` + `s2_holds_from_prime_asym_under_EH` (the GPY sieve
asymptotics — leaf-1 GPY port is **paper-gated** for the singular-series constant, see prior handoff
+ `ON-LINE-REQUEST.md`), and **`exists_separable_F_of_Mk_gt`** (separable realisation of `M_k`).
This lap attacked the last one — the only non-paper-gated, non-external one — and reduced it to its
irreducible core.

## 🧠 Context to carry forward
- **Why this thread, not the GPY port:** the GPY-port asymptotic *constant* is genuinely paper-gated
  (I re-confirmed I'd be guessing the singular-series normalization / power of `log R`; past-me's
  `ON-LINE-REQUEST.md` stands — do NOT guess it). Per the unblock guide I worked a non-blocked thread.
  `exists_separable_F_of_Mk_gt` was flagged "most tractable" and is standard functional analysis.
- **Key realisation that unlocked it:** `MkF k G` depends ONLY on `G`'s values on the simplex (both
  `mkF_numerator`/`mkF_denominator` integrate over the simplex), so the `support G ⊆ simplex` clause
  is *decoupled* from the ratio value. And `MkSet_nonempty`'s witness `F t = ∏ i, b(t i)` is already
  `IsFiniteSeparable`. So the genuine content is: (continuity of `MkF` in `F`) + (sup-norm density of
  separable simplex-supported functions). I proved the continuity; density is the lone residue.
- **The chain built (all in `Sieve.lean`, `#print axioms`-verified):**
  `exists_separable_F_of_Mk_gt` ⟸ `exists_F_of_Mk_gt` (pre-existing `sSup` extraction) + `sep_approx`
  (now a THEOREM) ⟸ **`mkF_sub_lt_of_sup_le`** (the `MkF`-ratio sup-continuity, **FULLY axiom-clean**)
  + **`separable_dense_sup`** (the one remaining axiom: pure sup-norm density, NO number theory).
  Continuity was built from `mkF_denominator_sub_le_const`, `mkF_numerator_sub_le_const` (= sum of
  `J_i_sub_le_const`, which uses `Ji_integrand_integrableOn` — the parametric-integrand integrability
  extracted from `J_i_le_denom`'s `volume_preserving_piFinSuccAbove`+`integral_prod_right` machinery),
  then the quotient rule (denominator stays `≥ den F/2` for small `δ`; `le_div_iff₀`/`div_lt_iff₀`+`nlinarith`).
- **Avoided a dead end:** the original instinct (L²/Cauchy–Schwarz continuity) is heavier than needed —
  **sup-norm** continuity via plain `setIntegral_mono` + `norm_integral_le_integral_norm` is enough and
  much cleaner. Use that.

## ✅ State (full `lake build` green, 8280 jobs, observed at the pre-commit gate)
- `exists_separable_F_of_Mk_gt`: `#print axioms = [propext, Classical.choice, Quot.sound, separable_dense_sup]`.
- `exists_separable_F_truncated_of_Mk_truncated_gt`: now on `separable_dense_sup_truncated` (reuses the
  SAME continuity, since `simplex_truncated ⊆ simplex` and `MkF` is identical).
- `bounded_gap_of_Mk_200` axiom set (verified this lap): `[…, BombieriVinogradov,
  s1_holds_from_nonprime_asym, s2_holds_from_prime_asym_under_EH, separable_dense_sup, native_decide…]`
  — the old deep §6 `exists_separable_F_of_Mk_gt` axiom is GONE, replaced by `separable_dense_sup`.
- **ε-sister NOT yet narrowed:** `exists_separable_F_eps_of_Mk_eps_gt` still rests on `sep_approx_eps`
  (combined density+continuity). `MkF_eps` uses `simplex_eps` (∑≤1+ε) / `simplex_shrunk` / inner limit
  `1+ε-∑s`, so it needs the continuity lemmas replicated over those domains. **Template ready**:
  `J_i_eps_le_denom` (line ~1427) + `isClosed_simplex_shrunk` mirror everything I used.
- **Aristotle — TWO of my jobs running** (poll with `aristotle list`, the one-shot): `c46a7778`
  (`aristotle-sepdense`) grinding `separable_dense_sup` itself; `b6429b9f` (`aristotle-phisq`) grinding
  the squarefree `φ(r)/r²` Mertens asymptotic (a reusable GPY-NT brick). Others running are NOT mine.

## 🎬 Next actions
1. **Poll Aristotle** (`aristotle list`). If `c46a7778` (sepdense) or `b6429b9f` (phisq) returned:
   `aristotle download` → VERIFY in our v4.29.1 kernel + `#print axioms` (Aristotle pins v4.28; a
   passing `grind` can inject `sorryAx` — always check) → port. sepdense discharges the LAST axiom of
   `exists_separable_F_of_Mk_gt`; phisq is a reusable brick (note: `simplex` in my sepdense problem is a
   local def — re-prove against the repo's `Sieve.simplex`; `ContDiff ℝ ⊤` there = repo's `ℝ ∞`).
2. **If sepdense hasn't returned**, discharge `separable_dense_sup` locally via box-tiling: cover
   `closure(support F)` (compact, in the open simplex) by small cubes (side `<1/k`) inside the simplex;
   smooth tensor-bump partition of unity (each bump a *product* of 1-D `ContDiffBump`s = separable);
   `G := ∑ F(box-centre)·(tensor bump)`; sup-error `≤ δ` by uniform continuity of `F` (mesh from `δ` via
   `Metric.uniformContinuousOn_of_continuous`). See `PENDING_WORK.md` §B for all 3 attack paths.
3. **OR** narrow the ε-sister: replicate the 6 continuity lemmas for the eps domains (mechanical, the
   `J_i_eps_le_denom` template + my main lemmas are the exact pattern), then rewire `sep_approx_eps`
   onto a `separable_dense_sup_eps` axiom + `mkF_eps_sub_lt_of_sup_le`.
4. Keep ≥1 Aristotle job in flight on this thread when slots free.

## ⚠️ Gotchas
- **Full `lake build` OOM is transient process-pressure** (box has 31Gi; heavy `native_decide` modules
  OOM when compiled concurrently). Fix: build the failing heavy module ALONE, then re-run full build —
  it converges. Iterate proofs cheaply with `taskset -c 0-3 lake build BoundedGaps.Sieve` (Sieve pulls
  NO heavy `native_decide` modules, ~1 min, never OOMs). Only pay the full-build tax at commit.
- mathlib v4.29 lemma-name friction (flushed to staging memory `lean-setintegral-measurereal-div-gotchas`):
  `setIntegral_const` → `μ.real` (need `measureReal_def`); use `le_div_iff₀`/`div_lt_iff₀`;
  `(hf.sub hg).abs` needs `simpa only [Pi.sub_apply]`; `integrableOn_const` wants a `≠⊤` *term*.
- `M ROADMAP.md` + `scratch_*.lean` + `aristotle-*/` dirs in `git status` are pre-existing / scratch.

## 📁 Key files
- `BoundedGaps/Sieve.lean` — the whole separable narrowing: `mkF_{denominator,numerator}_sub_le_const`,
  `Ji_integrand_integrableOn`, `J_i_sub_le_const`, `mkF_sub_lt_of_sup_le`, `sep_approx`(thm),
  `separable_dense_sup`(axiom), `exists_separable_F_of_Mk_gt`(thm) + truncated sister. `J_i_le_denom`
  (~827) and `J_i_eps_le_denom` (~1427) are the integrability templates.
- `PENDING_WORK.md` §B — `separable_dense_sup` 3 attack paths + ε-sister TODO.
- `ON-LINE-REQUEST.md` — the GPY-port singular-series asymptotic (still open, paper-gated).
- `aristotle-sepdense/Problem.lean`, `aristotle-phisq/Problem.lean` — the two in-flight problems.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (separable-
realisation axiom burn-down; `exists_separable_F_of_Mk_gt` is now down to the pure-density
`separable_dense_sup`). Start at Next action #1: `aristotle list` and harvest sepdense/phisq if ready;
else discharge `separable_dense_sup` (box-tiling) or narrow the ε-sister.**

🧵 ➡️ 🧵
