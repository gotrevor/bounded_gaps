# Headline theorems whose #print axioms footprint must NOT change across a mathlib bump.
# Read by `lean-bump` (one fully-qualified name per line; # comments + blanks ignored).
# These are the unconditional, load-bearing results — the faithfulness surface of the repo.
# A green build does NOT prove the proofs are unchanged; this axiom gate is the real check.

# Flagship: H_1 ≤ 246 (Polymath8b, unconditional bounded gap between primes)
BoundedGaps.Polymath8b.H1_le_246

# Maynard's H_1 ≤ 600
BoundedGaps.Maynard.H1_le_600

# Zhang's 70,000,000 routed through the Polymath8b bound
BoundedGaps.Zhang.H1_le_70M_via_Polymath8b

# Twin-primes characterization (liminf gap = 2 ↔ twin-primes conjecture)
BoundedGaps.TwinPrimes.twinPrimes_iff_liminfGap_one

# The unconditional asymptotic DHL theorem (core sieve path)
BoundedGaps.Polymath8b.dhl_asymptotic_unconditional
