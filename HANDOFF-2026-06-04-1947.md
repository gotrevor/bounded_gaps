# Handoff: 3-D weighted-Riemann limit PROVEN + generic k-D framework down to ONE lemma

**Date**: 2026-06-04 19:47 UTC · **Branch**: `path-a-selberg-nu` · HEAD `207d38b`
**14 commits this lap** (`80b9500`…`207d38b`; every Lean commit green at the pre-commit full-build
gate, 8282 jobs). Never pushed. **Supersedes `HANDOFF-2026-06-04-1818.md`.** (Master `HANDOFF.md` is
the post-merge state — left untouched, per repo convention.)

## 🎯 What we're doing
Analytic axiom burn-down for the GPY/Maynard sieve. The flagship `bounded_gap_of_Mk_200` still rests
on 3 math axioms — `BombieriVinogradov`, `s1_holds_from_nonprime_asym`, `s2_holds_from_prime_asym_
under_EH` (verified unchanged this lap, no regression). The s1/s2 axioms' remaining analytic core
("sub-step (c)") is the **k-D coupled log-weighted Riemann limit** `∑_{n₁⋯n_k ≤ R} ∏Fᵢ(log nᵢ/log
R)/nᵢ / (log R)^k → ∫_{simplex} ∏Fᵢ`. This lap drove that limit from "2-D done, k-D is multi-lap"
to **3-D proven + the FULL generic k-D reduced to a single lemma.**

## 🧠 Context to carry forward
- **The ladder & its one engine.** Every dimension is proven by the SAME pattern: `*_factor` (factor
  the outer index) → `perturbed_riemann` (Φ = the (k-1)-D cross-section integral) → Pólya
  (`PolyaUniform.polya_uniform`, monotone→uniform) → a `t`-rescaling **pointwise** step that feeds
  the (k-1)-D limit. The pointwise step (`psi3_pointwise`, generically `psi_k_pointwise`) is the only
  genuinely new work at each level; everything around it is mechanical mirroring.
- **`psi3_pointwise` (the 3-D nut) was cracked in-kernel** by mirroring the 1-D `psi_tendsto`
  (`InnerUniformReduction.lean:325`) one dimension up: rescale `Ft=G(t·)`, `Gt=H(t·)`, level
  `N=⌊R^t⌋`, `c_R=logN/logR→t`; feed `Ft,Gt` into `weighted_riemann_2d` at level `N`; absorb the
  `c_R→t` argument drift via a **product split** `|G(cu)H(cv)−G(tu)H(tv)| ≤ |G||H(cv)−H(tv)| +
  |H||G(cu)−G(tu)|` × a 2-D harmonic bound (from `weighted_riemann_2d 1 1`); reassemble
  `Psi3 = c_R²·(realSum/logN²)` with the change-of-variables `phi2_scale`. **This is the exact
  template for the remaining generic `psi_k_pointwise`.**
- **Two Lean traps that ate time** (flushed to the staging buffer, see step-2 insights): (i) `set C
  := |L|+1` where `L := ∫…` → downstream `positivity`/`field_simp`/`nlinarith` zeta-unfold and whnf
  the integral → **heartbeat timeout**; fix = obtain `C` opaquely (`obtain ⟨C, hC0, …⟩ : ∃ C, …`) and
  hand-build positivity facts from `hC0` (positivity ignores hypotheses). (ii) Parametric-primitive
  continuity needs `f.uncurry` continuous on **all** of ℝ×ℝ — state lemmas with `Continuous` (not
  `ContinuousOn (Icc 0 1)`); `intervalIntegral.continuous_parametric_primitive_of_continuous`,
  pin `μ := volume`. `psi3_pointwise` carries `set_option maxHeartbeats 1000000`.
- **The convention question I deliberately did NOT chase** (it's real but paper-gated): under the
  repo's `lambdaTransform = ∑_{d|n}μ(d)F(log d/logR)` (GPY λ-convention), the *classical* 1-D Selberg
  diagonal asymptotic is `∝ ∫F'²` (with a derivative), yet `mkF_denominator = ∫F²`. The repo's design
  (`ANALYTIC_AXIOM_BURNDOWN.md`) deliberately targets `∫F²` via the `(μ²/φ)F ~ (∫F)logR` Mertens
  route — a coherent Maynard-convention path, NOT the z_r route. I judged my ∫F'² recollection a
  GPY-vs-Maynard conflation and **deferred to the repo design**; don't re-litigate this without the
  paper. (`ON-LINE-REQUEST.md` covers the precise gated identity.)
- **Bare `1/n`-weighted vs `(μ²/φ)`-weighted.** `weighted_riemann_3d`/`_kd` are the *bare* `1/(∏nᵢ)`
  Riemann sums (the analog of the proven `weighted_riemann_2d`). The GPY connection ALSO needs the
  `(μ²/φ)`-weighted lift (singular series) + the correction→o(main) bound — those are separate, still-
  open pieces, NOT part of the k-D Riemann thread.

## ✅ State (all axiom-clean `[propext, Classical.choice, Quot.sound]`, full build green 8282 jobs)
- **`BoundedGaps/WeightedRiemann3D.lean`** — `weighted_riemann_3d` (+ `_simplex` form) PROVEN
  UNCONDITIONALLY for `G,H ≥ 0`, `F` continuous: `∑_{lmn≤R} F·G·H/(lmn)/(logR)³ → ∫_{x+y+z≤1} F·G·H`.
  Supporting: `Phi2`, `phi2_continuous`, `phi2_scale`, `three_d_factor`, `weighted_riemann_3d_of_
  inner`, `Psi3`/`psi3_monotoneOn`/`psi3_reparam`/`inner_uniform_3d_of_pointwise_nonneg`,
  `psi3_pointwise`, `integral_F_phi2_eq_simplex`.
- **`BoundedGaps/WeightedRiemannKD.lean`** — generic framework, DONE except one lemma. Capstone
  `weighted_riemann_kd_of_pointwise` proves the FULL k-D limit `nestedLogSum R Gs R/(logR)^|Gs| →
  nestedPhi Gs 0 = ∫_{simplex}∏Gs` for every nonneg continuous list `Gs`, **modulo the hypothesis
  `hpw` = the generic pointwise**. Everything else built: `nestedLogSum`/`nestedPhi` (+ specializations
  = Phi/Phi2), `nestedPhi_continuous`, `nestedLogSum_nonneg`/`_mono`, `nestedLogSum_factor`,
  `weighted_riemann_cons_of_inner`, `PsiK`/`psiK_monotoneOn`/`psiK_reparam`,
  `inner_uniform_kd_of_pointwise`, `weighted_riemann_kd_nil`.
- **Aristotle (account-shared):** `aristotle-phisq` (`b6429b9f`, ~5h) RUNNING — the squarefree
  `φ(r)/r²` Mertens singular-series brick, still relevant. `aristotle-psi3-pointwise` (`680977a8`,
  ~1h) RUNNING but **SUPERSEDED** (proven in-kernel this lap — ignore/let it lapse, harvest not needed).

## 🎬 Next actions
1. **Prove `psi_k_pointwise`** — the SINGLE remaining lemma for the unconditional generic k-D:
   `∀ gs nonneg continuous, ∀ t∈[0,1], PsiK R gs t → nestedPhi gs (1-t)`. By **strong induction on
   `gs.length`**, mirroring `psi3_pointwise` (the exact template) one structural level up: unfold
   `PsiK = nestedLogSum R gs ⌊R^t⌋/(logR)^|gs|`, rescale to level `N=⌊R^t⌋`, drift-bound the
   `c_R→t` arg drift over the nested sum, and feed the IH (`weighted_riemann_kd_of_pointwise gs`,
   available by strong induction). The generic drift over the *nested* sum is the new wrinkle vs the
   3-D double sum — expect to need a small inductive drift lemma. Then `hpw := psi_k_pointwise`
   discharges `weighted_riemann_kd_of_pointwise` ⟹ unconditional generic `weighted_riemann_kd`.
   (Fallback if it stalls: concrete `weighted_riemann_4d` — copy `WeightedRiemann3D.lean` one dim up,
   `psi4_pointwise` rescales to the now-available `weighted_riemann_3d`.)
2. **Keep Aristotle fed:** `phisq` is in flight. If it returns, VERIFY in-kernel + `#print axioms`
   (Aristotle pins v4.28; a passing `grind` can inject `sorryAx`), then port; submit the next brick.
3. **Beyond the Riemann thread** (the OTHER s1/s2 pieces, per `ANALYTIC_AXIOM_BURNDOWN.md` /
   `PENDING_WORK.md`): the `(μ²/φ)`-weighted lift (singular series) and the correction→o(main) bound.
   Don't fixate on k-D Riemann alone — these are equally needed.

## ⚠️ Gotchas
- **Cheap iterate:** `taskset -c 0-3 lake build BoundedGaps.WeightedRiemannKD` (~18s, no
  `native_decide` modules). Full build (the pre-commit gate) is ~8282 jobs.
- `set`-bound integrals + `positivity`/`field_simp`/`nlinarith` = whnf heartbeat timeout (see Context).
- `div_add_div_same` is gone → use `← add_div`. `MapsTo` → `Set.MapsTo`. List membership proofs:
  `List.mem_cons.mpr (Or.inl rfl)` / `(Or.inr hf)` (the bare `List.mem_cons_self g gs` "function
  expected"-errored in this mathlib).
- The "ring failed → try ring_nf" lines under `congr 1`/`ring` are **benign suggestion traces**, not
  errors — trust "Build completed successfully" + `#print axioms`.

## 📁 Key files
- `BoundedGaps/WeightedRiemann3D.lean` — the 3-D limit (the proof template for `psi_k_pointwise`).
- `BoundedGaps/WeightedRiemannKD.lean` — the generic framework; `psi_k_pointwise` is the only gap.
- `BoundedGaps/InnerUniformReduction.lean:325` (`psi_tendsto`) — the 1-D pointwise template.
- `PENDING_WORK.md` "Generic k-D FRAMEWORK — DONE except ONE lemma" — the exact recipe.
- `ANALYTIC_AXIOM_BURNDOWN.md` — the full s1/s2 decomposition (Riemann + (μ²/φ) lift + correction).
- Memory `[[kd-weighted-riemann-ladder]]` — the cross-lap state of this ladder.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (GPY sieve
axiom burn-down; the k-D weighted-Riemann limit is one lemma from fully generic). Absorb the context,
then start at Next action #1: prove `psi_k_pointwise` by strong induction, using `psi3_pointwise` as
the template and `weighted_riemann_kd_of_pointwise gs` as the IH.**

🧵 ➡️ 🧵
