# Handoff: s1 analytic MAIN TERM proven end-to-end (general-J, axiom-clean)

**Date**: 2026-06-05 06:15 UTC · **Branch**: `path-a-selberg-nu` · HEAD `d83e47c`
6 commits this lap (`bd35abf`…`d83e47c`), every Lean commit green at the pre-commit full-build gate
(8288 jobs). Never pushed. Supersedes `HANDOFF-2026-06-05-0512.md`. (Master `HANDOFF.md` = post-merge
state, left untouched per repo convention.)

## 🎯 What we're doing
Analytic axiom burn-down for the GPY/Maynard sieve. Flagship `bounded_gap_of_Mk_200` rests on 3 math
axioms — `BombieriVinogradov`, `s1_holds_from_nonprime_asym`, `s2_holds_from_prime_asym_at_level`
(re-verified this lap: `#print axioms bounded_gap_of_Mk_200` = those 3 + native_decide; UNCHANGED).
This lap closed the **entire `s1` analytic MAIN TERM** in Path-Y (`y_r`-space), axiom-clean, for the
**actual** sieve test-function shape `F = ∑_j c_j ∏_i Fs_{j,i}`.

## 🧠 Context to carry forward
- **The landmine is RESOLVED.** Trevor settled it 2026-06-04 (top of
  `archive/findings/ON-LINE-FINDINGS-2026-06-04-gpy-diagonal-asymptotic.md`): **Path Y + antiderivative
  convention** — feed `selberg_nu` the antiderivative `𝔉` of the variational `F` (`𝔉'=F`), keeping the
  s1 constant `∫F²` and the whole `M_k`/witness layer untouched. Do NOT restate as `∫(F')²`, do NOT
  evaluate signed `z_r` (PNT-strength). I built the antiderivative operator (`Antiderivative.lean`)
  exactly for this.
- **Why the lap mattered:** last handoff had reduced s1's main term to ONE lemma (the simplex-Fubini
  bridge), with k=1 proven and k-D on Aristotle. This lap I (1) proved the Fubini bridge LOCALLY (beat
  Aristotle, which independently confirmed the same strategy), and then (2) realized the real sieve
  weight is **general-J** (box-tensor witnesses `∑_φ a_φ ∏ g(φi)`, NOT a single product), so the s1
  square `(∑_j c_j ∏Fs)²` expands into **signed** cross terms `∏(Fs_j·Fs_{j'})`. The nonneg-only ladder
  couldn't handle those, so I built the **signed ladder**, then assembled the general-J main term.
- **The double-sum form is correct** (verified the architecture mentally): after diagonalization +
  smoothing, the sieve sum is `∑_r (μ²/φ)·(∑_j c_j ∏_i y^{(j)}_{r_i})²` with `y^{(j)}_{r_i} ≈
  Fs_j i(log r_i/log R)`; expanding the square gives exactly `∑_{j,j'} c_j c_{j'}·nestedLogSumW(μ²/φ)
  R (ofFn(Fs_j·Fs_{j'})) R`, which is what `s1_yr_mainTerm_eq_mkF_denominator_decomp` proves converges
  to `∫_simplex F² = mkF_denominator k F`.
- **3 deep gaps remain to fully discharge the s1 axiom** (all need sieve context, all multi-session):
  - **(A) Diagonalization bridge** — connect literal `sieveSum (selberg_nu …)` to the `nestedLogSumW`
    double-sum. `SieveExpansion.lean` already has `sieveSum_selberg_nu_expand` (gives the `(j,j')`
    lattice-count form), `lattice_count_main_term`, `gpy_diagonalize_moebius`, `gpy_yvar_substitution`.
    The chain lattice-count → `∏1/[d,e]` (CRT, sub-step b) → `μ²/φ` `y_r` double-sum is the work.
  - **(B) Smoothing `y_r ≈ Fs(log r/log R)`** (Maynard `PartialSummation`/`S1Summation2`) — THE deep
    analytic nut. 1-D Riemann-sum core ON ARISTOTLE (`678a9ed6`, brick_smooth, RUNNING).
  - **(C) `alphaMainTerm` normalization** + `o(1)` correction (case (ii) needs BV/EH).
- **What I deliberately did NOT do:** start (A)/(B)/(C) (genuinely multi-session, better fresh); touch
  any axiom or the `M_k` witness layer (untouched by design); attempt the smoothing locally (on
  Aristotle).

## ✅ State (all new results axiom-clean `[propext, Classical.choice, Quot.sound]`; full build 8288 jobs)
- **`S1Fubini.lean`** (NEW): `simplex_integral_prod_eq_nestedPhi` (the Fubini bridge) via head-outer
  fibration `simplex_scaled_fubini_head` + `integral_insertNth_eq'` over `Sieve.simplex_scaled`.
  Discharged `hFubini` ⟹ `S1ConnectionK1.s1_yr_mainTerm_eq_mkF_denominator_sep` now UNCONDITIONAL.
- **`WeightedRiemannSigned.lean`** (NEW): `weighted_riemann_kd_muphi_signed` (signed k-D ladder, no
  nonneg hyp) via `signed_expand` (head split `g=g⁺-g⁻`, per-truncation polarization).
- **`S1MainTermDecomp.lean`** (NEW): `s1_yr_mainTerm_eq_mkF_denominator_decomp` — the general-J s1 main
  term `(∑_{j,j'} c_j c_{j'}·nestedLogSumW(μ²/φ)(ofFn(Fs_j·Fs_{j'})))/(logR)^k → mkF_denominator k F`.
- **`Antiderivative.lean`** (NEW): `antideriv`, `hasDerivAt_antideriv`, `contDiff_antideriv`,
  `ftc_bridge_one`, `mkF_denominator_antideriv_sep`, `mkF_denominator_antideriv_decomp` (general-J).
- `BoundedGaps.lean` imports all four. `PENDING_WORK.md` updated (lap-2 section + gaps A/B/C).

## 🎬 Next actions
1. **Poll Aristotle brick_smooth** `678a9ed6` (`aristotle list`; the fast one-shot). If COMPLETE:
   download, VERIFY in-kernel + `#print axioms`, and check it against the repo's existing
   `WeightedMertens.riemann_sum_log_weight` (it may be a re-proof of essentially that). Then submit the
   next brick. If still RUNNING, leave it.
2. **Start gap (A) — diagonalization.** Read `SieveExpansion.sieveSum_selberg_nu_expand` (line 387,
   gives the per-`(j,j')` lattice-count form) and `lattice_count_main_term` (line 629). Goal: a
   per-`(j,j')` lemma turning the lattice-count sum into the `μ²/φ`-weighted `y_r` double-sum that
   `s1_yr_mainTerm_eq_mkF_denominator_decomp` consumes. This is the structural spine; (B) smoothing
   plugs into it. Decompose it into the narrowest next lemma and grind / scaffold.
3. **Keep Aristotle fed** — whenever no job runs, architect the next bounded self-contained brick
   (a piece of (A) or (B)) and submit it.

## ⚠️ Gotchas
- `rw` on `List.map`/`List.sum` goals is fragile — use `congrArg List.sum (List.map_congr_left …)`,
  `show … from lemma _` to bridge defeq, and a `show` to beta-reduce after `integral_finset_sum`.
  (Flushed to memory `[[lean-rw-list-sum-map-pitfalls]]`.)
- `Sieve.simplex_scaled k a` (budget simplex, already in Sieve.lean) is the right object for head-outer
  fibration; it had no compactness lemma, I added `isCompact/isClosed_simplex_scaled` in S1Fubini.
- Cheap iterate: `taskset -c 0-5 lake build BoundedGaps.S1MainTermDecomp` (~9s) etc. Full gate = 8288.
- `weighted_riemann_kd_muphi` REQUIRES nonneg functions; for signed use
  `weighted_riemann_kd_muphi_signed`.

## 📁 Key files
- `BoundedGaps/S1Fubini.lean` — Fubini bridge + budget-simplex head-outer fibration.
- `BoundedGaps/WeightedRiemannSigned.lean` — signed k-D `(μ²/φ)` ladder.
- `BoundedGaps/S1MainTermDecomp.lean` — general-J s1 main term (the headline).
- `BoundedGaps/Antiderivative.lean` — antiderivative convention + FTC bridges.
- `BoundedGaps/SieveExpansion.lean` — diagonalization algebra for gap (A).
- `PENDING_WORK.md` — lap-2 progress + gaps A/B/C attack notes.
- `archive/findings/ON-LINE-FINDINGS-2026-06-04-gpy-diagonal-asymptotic.md` — the SETTLED convention.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (GPY s1
axiom burn-down; the entire s1 MAIN TERM is now proven axiom-clean, general-J). Absorb the context,
then start at Next action #1: poll Aristotle brick_smooth `678a9ed6`, then begin gap (A)
diagonalization via `SieveExpansion.sieveSum_selberg_nu_expand`.**

🧵 ➡️ 🧵
