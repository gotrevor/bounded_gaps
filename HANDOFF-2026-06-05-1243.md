# Handoff: `hBaseW` FULLY DISCHARGED — contour-free y-space `s1` main term now unconditional

**Date**: 2026-06-05 12:43 UTC · **Branch**: `path-a-selberg-nu` · HEAD `bbb37f7`
13 commits this lap (`675ebc8`…`bbb37f7`), full `lake build` green (8300 jobs) at every pre-commit
gate. Never pushed. Supersedes `HANDOFF-2026-06-05-1119.md`. Master `HANDOFF.md` untouched per repo
convention.

## 🎯 What we're doing
Analytic axiom burn-down for the GPY/Maynard sieve, building the **contour-free Path-Y y-space `s1`**
ALONGSIDE the d-space flagship (do NOT touch the flagship; Trevor flips the axioms + the `B^{±k}`
normalisation later). Last lap's two sole remaining analytic inputs for y-space `s1` were `hBaseW`
(W-coprime sharp Mertens) and `hcorr` (off-diagonal correction). **This lap fully discharged
`hBaseW`** and narrowed `hcorr` to its irreducible PNT core.

## 🧠 Context to carry forward — read this
- **`hBaseW` is DONE, unconditional, axiom-clean.** `CoprimeMertens.hBaseW_of_primes_totient T hT h2`
  gives `(∑_{n≤N,(n,W)=1}μ²/φ)/log N → φ(W)/W` for `W = ∏_{p∈T} p` (T a finite set of primes, `2∈T`
  — covers the sieve's primorial). VERIFIED to compose: `weighted_mertens_coprime_sq hF
  (hBaseW_of_primes_totient T hT h2)` typechecks (see the `ConnectTest` example I ran). Memory:
  `[[hbasew-discharged]]`.
- **How it was cracked** (the lap-7 "p=2 nut"): the geometric Möbius route, prime-by-prime. The
  unblocker was the **second-order Mertens** `SharpMertens.sum_g_second_order` (`U(N)−log N → β`),
  built on the existing `sum_g_decomp`. Then `single_prime_coprime_mertens_two` (p=2) via the crux
  `halving_recursion_o_log`, which I proved IN-KERNEL using the **L=0 reduction** (constant `L/2`
  solves `g+g∘half=L`, killing the alternation — see `[[halving-recursion-L0-reduction]]`).
  Then `coprime_step` (abstract p≥3 inductive step) + `coprime_prod_limit` (Finset induction over
  odd primes) + `prod_primes_totient` (`φ(∏T)=∏(p-1)`). **Order matters: add `2` FIRST** (base `U`,
  where second-order is available), then sweep odd primes (DCT, first-order only).
- **`hcorr` is PNT-blocked — this is a genuine wall, not a milestone pause.** The off-diagonal
  correction's two SIZE estimates both reduce to the **smoothing estimate** `yLambda ≈
  (d/φ(d))·(−F′)/log R`. I proved the ELEMENTARY first reduction `S1Correction.yLambda_factor`
  (isolates the multiplicative `d/φ(d)`), but the residual `∑_t μ(t)F(log dt/L)/φ(t)` is a
  Möbius-weighted average with cancellation = PNT/`1/ζ(1+w)` strength. THIS mathlib (v4.29.1) has NO
  quantitative PNT and NO Möbius mean `∑μ(n)/n→0`. The lap-4 firm verdict (`[[s1-derivative-landmine]]`)
  already concluded there's no in-kernel route. **`ON-LINE-REQUEST.md`** (filed this lap) asks a
  networked session: does current mathlib master / `PrimeNumberTheoremAnd` / any public Lean repo have
  a portable PNT or Möbius-mean, or an elementary second-moment `o`-bound that sidesteps it?
- **The `B^{±k}` flip + axiom restatement stays Trevor's call** — don't force it. y-space main term is
  `B^{+k}·M`, Sieve.alphaMainTerm is d-space `B^{−k}` (`[[yspace-s1-normalization-Bpm-k]]`).

## ✅ State — all axiom-clean `[propext, Classical.choice, Quot.sound]`, no sorry
- `SharpMertens.lean`: `sum_g_second_order` (+ `r_e_tendsto_gamma`, `R_tendsto_gamma`, `Q_tendsto`,
  `P_minus_one_mul_log_tendsto_zero`, `exists_sum_g_second_order`).
- `CoprimeMertens.lean`: `halving_recursion_o_log` (in-kernel), `single_prime_coprime_mertens_two`,
  `coprime_mertens_recursion_general`, `coprime_geometric_inversion_general`, `coprime_step`,
  `coprime_prod_limit`, `prod_primes_totient`, `totient_div_eq_prod`, **`hBaseW_of_primes_totient`**.
- `S1HBaseW.lean` (NEW, in aggregator): `yspace_kd_box_product_tendsto_primorial` — the k-D y-space
  `s1` MAIN TERM, UNCONDITIONAL for primorial `W` (hBaseW discharged in-kernel).
- `S1Correction.lean`: `yLambda_factor` (smoothing's elementary `d/φ(d)` split).
- Aristotle bricks `9c12cf7c`/`571d06d1` (brick_p2, the crux) are now MOOT — solved locally.

## 🎬 Next actions
1. **Harvest `ON-LINE-FINDINGS-*.md` if present** at repo root (a networked session may have answered
   the smoothing request). If it gives a portable PNT/Möbius-mean or elementary `o`-bound, use it to
   discharge the two `hcorr` SIZE estimates (in `S1Correction`/`S1DiagonalSize`/`S1OffDiagSize`),
   completing the contour-free `s1`. Then `git mv` the findings into `archive/findings/`.
2. **If still no findings**: the `s1` thread is at its PNT frontier. Either (a) reindex `yLambda_factor`
   to the clean `t`-form `(d/φ(d))·∑_t μ(t)F(log dt/L)/φ(t)` and state the smoothing as a disclosed
   axiom with the PNT prerequisite named (further narrowing), or (b) pivot: thread
   `hBaseW_of_primes_totient` up through `S1MainLimit`/`S1FullLimit` so the top y-space `s1` limit
   loses its `hBaseW` hypothesis (leaving only `hcorr`) — verbose plumbing but unblocked.
3. Keep ONE Aristotle job in flight if egress allows (the crux is moot; a useful brick would be an
   elementary sub-lemma, NOT the PNT residual which Aristotle's mathlib also lacks).

## ⚠️ Gotchas
- `Finset.sum_image` takes `Set.InjOn g ↑s` DIRECTLY (no `∀x∈s∀y∈s` form needed). The abs triangle
  inequality is `abs_add_le` (not `abs_add`). `field_simp` often closes the goal — a trailing `ring`
  then errors "no goals". `Real.norm_eq_abs` needs the value positionally (`Real.norm_eq_abs (Real.log e)`).
- `P_minus_one_mul_log_tendsto_zero` needs `set_option maxHeartbeats 400000 in` (tail-tsum chain).
- Develop fast: `/tmp/x.lean` importing the module, `taskset -c 0-7 lake env lean /tmp/x.lean`; full
  `taskset -c 0-7 lake build`. `#print axioms` EVERYTHING before trusting. Long-line warnings tolerated.

## 📁 Key files
- `BoundedGaps/CoprimeMertens.lean` — the whole hBaseW route (recursion→inversion→step→prod→hBaseW).
- `BoundedGaps/SharpMertens.lean` — `sum_g_second_order` (the p=2 unblocker; built on `sum_g_decomp`).
- `BoundedGaps/S1HBaseW.lean` — unconditional primorial s1 main term.
- `BoundedGaps/S1Correction.lean` — `yLambda_factor` + the correction reduction; the hcorr SIZE bounds live here / `S1DiagonalSize` / `S1OffDiagSize`.
- `ON-LINE-REQUEST.md` — the smoothing/PNT research ask (open).
- `PENDING_WORK.md` — lap-8 status at top.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (contour-free
y-space `s1`: `hBaseW` DONE, only `hcorr`/smoothing remains, and it's PNT-blocked). Absorb the context,
then start at Next action #1: check for `ON-LINE-FINDINGS-*.md` and act on it; if none, do #2.**

🧵 ➡️ 🧵
