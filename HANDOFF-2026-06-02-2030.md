# Handoff: symmetric-reduction Gram assembly COMPLETE — mk_54_witness reduced to one rational native_decide

**Date**: 2026-06-02 ~20:30 UTC · **Branch**: `path-a-selberg-nu` · all work in
`BoundedGaps/SymmetricReductionOrbitFree.lean` (box-local, 10 commits this lap).

## 🎯 Headline
The **entire** symmetric-reduction pipeline is now built, **axiom-clean**, AND **validated
end-to-end** — from orbit symmetry down to a `native_decide`-ready discharge of
`mk_54_witness_under_EH`. A committed `example` proves `Mk 3 > 1` non-vacuously through the whole
chain with **both** `native_decide`s firing (the histogram `hR` and the Gram quotient). The only
remaining inputs are the concrete optimal `k=54` data `(R, c)` from the LDL tooling — then the
identical two `native_decide`s.

**Endgame one-liner** (in `BoundedGaps/Polymath8b.lean`, once `(R,c)` exist):
```
theorem mk_54_witness_under_EH := OrbitFree.mk_54_witness_under_EH_of_symWeight_computable
  R c (OrbitFree.disjoint_of_histogram R (by native_decide)) (by native_decide)
```

## 🔧 GATING FIX (important)
`SymmetricReductionOrbitFree.lean` was **not in the root `BoundedGaps.lean` import tree**, so the
pre-commit `lake build` (default target = root's transitive closure) did NOT build it — which is
exactly how last session's broken `pair_fiber_card_eq_multinomial` slipped through green. **Fixed**:
added `import BoundedGaps.SymmetricReductionOrbitFree` (+ the new ε file) to `BoundedGaps.lean`, so
both are now gated (full build 8266→8269 jobs). Always `lake build BoundedGaps.<Module>` the
specific module you touch, but now the gate covers these too.

## 🆕 ε-path progress (unconditional H₁≤246)
`BoundedGaps/SymmetricReductionEpsOrbitFree.lean` (NEW). The full **ε Gram bilinear structure** is
now in place on both sides:
- **ε-denominator: orbit-free + computable.** `orbitPair_denominator_eps_const` (the
  `(1+ε)^{k+|p+q|}` weight is orbit-pair-constant), `gramDenEntryEps a b ε =
  (1+ε)^{k+|a|+|b|}·gramDenEntry`, `polynomialMaynardDenominator_eps_symWeight` (bilinear).
- **ε-numerator: bilinear structure done.** `crossNumerator_eps`, `crossNumerator_eps_orbitSum`,
  `polynomialMaynardNumerator_eps_symWeight` (reuses `symWeight_double_sum`).
- **ε-ratio**: `polynomialMkF_eps_symWeight` (Gram quotient; denominator computable, numerator
  still via `crossNumerator_eps`).
- Diagonal ε orbit-sum reductions pre-existed in `SymmetricReductionEps.lean`
  (`polynomialMaynard{Denominator,Numerator}_eps_orbitSum`, `affineNum_orbitSum_const`).

**TODO: `crossNumerator_eps` orbit-free/computable re-index — and it is DENOMINATOR-GRADE per `m`
(not research-grade; earlier worry retracted).** `affineSlackRat(a,β,ε) = (1-ε)^{n+|a|} ∑_{m≤β}
(2ε)^m(1-ε)^{β-m}C(β,m)·dirichletIntegralWithSlack(a,β-m)`. Push `∑_m` to the outside. **Key
cancellation**: with `a = removeNthᵢ(p+q)`, `|a| = (|α|+|β|) - (pᵢ+qᵢ)`, so
- the `(1-ε)` prefactor `(1-ε)^{n+|a|}·(1-ε)^{(pᵢ+qᵢ+2)-m}` ⇒ the `(pᵢ+qᵢ)` **cancels** ⇒
  `(1-ε)^{n+|α|+|β|+2-m}` — constant per `m`;
- the Dirichlet denominator `(n+|a|+((pᵢ+qᵢ+2)-m))! = (n+|α|+|β|+2-m)!` — **also constant per `m`**.
So for each fixed `m`, factor out `(2ε)^m(1-ε)^{n+|α|+|β|+2-m}/(n+|α|+|β|+2-m)!` (constant) and the
inner combinatorial sum is EXACTLY a `numerator_summand_factor`-style joint-type regroup with the
`m`-local factor `M_m(a,b) = C(a+b+2,m)·(a+b+2-m)! / ((a+1)(b+1)(a+b)!)` (the non-ε case is `m=0`,
`M_0(a,b) = (a+b+2)!/((a+1)(b+1)(a+b)!)`). So the ε-numerator entry = `∑_m (const_m) · (∑_T
multinomial(T)·markedSum_m(tableToMultiset T))` — the SAME `MarginCorrectTables` machinery as the
non-ε numerator, just an extra `∑_m` and `M_m`. Plan: define `markedCellFactor_m`/`pairWeight_m`,
mirror `numerator_combinatorial_factored → numerator_summand_eq_pairWeight → numerator_orbitFree`
with the `m` parameter, sum over `m ∈ range(pᵢ+qᵢ+3)`. Multi-session but mechanical. After it lands:
ε-entry + ratio + witness mirror the EH path → discharge `mk_eps_50_witness` via
`EpsBridge.mk_eps_50_witness_of_poly` → unconditional H₁≤246.

**Keystone landed** (`SymmetricReductionEpsOrbitFree.lean`): `sum_removeNth_add` (`∑removeNthᵢ(p+q)
= |p+q|-(pᵢ+qᵢ)`) and `dirichletSlack_removeNth_factor` (`dirichletIntegralWithSlack(removeNthᵢ(p+q),
s) = ∏ⱼ(p+q)ⱼ!·s! / ((pᵢ+qᵢ)!·(n+|p+q|-(pᵢ+qᵢ)+s)!)` — the ε-analog of `numerator_summand_factor`,
exhibiting the `(pᵢ+qᵢ)` cancellation). **Next lemma**: `affineSlackRat(removeNthᵢ(p+q), pᵢ+qᵢ+2, ε)
= ∏ⱼ(p+q)ⱼ! · ∑_{m∈range(pᵢ+qᵢ+3)} const_m(|p+q|)·Mlocal_m(pᵢ,qᵢ)` where `const_m(d) =
(2ε)^m(1-ε)^{n+d+2-m}/(n+d+2-m)!` and `Mlocal_m(a,b) = C(a+b+2,m)(a+b+2-m)!/(a+b)!`. Substitute
`dirichletSlack_removeNth_factor` with `s = (pᵢ+qᵢ+2)-m` into `affineSlackRat`'s `∑_m`; the `(1-ε)`
prefactor and Dirichlet denom both cancel `(pᵢ+qᵢ)` to give `const_m` (needs `m ≤ pᵢ+qᵢ+2`, true on
the sum range — careful with ℕ subtraction). Then mirror the non-ε orbit-free chain per `m`.

## ⚠️ FIRST: the previous HEAD did not build
`HANDOFF-2026-06-02-1647.md` claimed `8360f8c` (and the d469875 it grew into) were green. **They
were not.** `pair_fiber_card_eq_multinomial` had never elaborated:
- a parse error (anon-constructor pair missing inner parens for a `: V` ascription),
- the `i`-map's `: V` ascription wrapped the whole lambda → Lean inferred `i : … → V` not
  `… → (Fin k → V)`, poisoning `card_bij'`,
- `marginCorrect_cells_sum`'s `rw [hfib]` motive-incorrect (`k` occurs in `Fin k`) → `conv_rhs`,
- `case hi/hj` wrong `jointMultiset_count_eq` args + unfillable subtype `_`s.
Fixed in `895f7b8`. **Always `lake build` before trusting a "green" claim** (the pre-commit gate
caught nothing because the broken commit predated this lap).

## ✅ DONE this lap (all `[propext, Classical.choice, Quot.sound]`, all real `lake build` green)
Numerator orbit-free re-index (finishes the prior "NUMERATOR partial"):
- `numerator_orbitFree` — pair-weighted double orbit sum re-indexed over `MarginCorrectTables`
  (numerator analog of `orbitCore_eq_multinomial_sum_orbitFree`). Reuses the denominator's table
  bijection via `pair_image_eq` (product-orbit joint-type image collapses to the single-orbit
  image, realizing the column as `q=β`) + `pair_fiber_card_eq_multinomial`. New general-column
  joint machinery: `joint_sum_over_col/_row`, `joint_entry`, `joint_multisetToTable_mem`,
  `tableToMultiset_jointTable`.
- `orbitPair_numerator_orbitFree` + `orbitPair_numerator_computable` — full numerator entry =
  `(∑_T multinomial·pairWeight)/(n+1+|α|+|β|+1)!`. **No monoOrbit, and unlike the denominator NO
  orbit-cardinality factor** (full joint-type regroup never uses the q-collapse). `pairWeightC`
  computable twin verified in-kernel (`pairWeightC {(2,1),(1,1),(0,0)} = 100`).

Gram-matrix assembly (NEW — the prior plan's "genuinely multi-session" step):
- `crossDenominator` / `crossNumerator` — bilinear forms on two weights (= `polynomialMaynard*`
  on the diagonal). `cross*_orbitSum` evaluate the orbit-basis entries to the orbit-pair sums;
  `cross*_orbitSum_computable` chain to the native_decide-ready closed forms. **These are the
  `Mr[λ][μ]` matrix entries**, computable from shapes alone.
- `symWeight R c` — the symmetric weight `∑_{λ∈R} c_λ orbitSum λ` (terms = disjoint union of
  per-orbit term sets). `symWeight_termSets_disjoint`, `symWeight_double_sum` (shared engine).
- `polynomialMaynardDenominator_symWeight` / `polynomialMaynardNumerator_symWeight` — the
  **bilinear Gram expansions**: `polynomialMaynard*(symWeight R c) = ∑_{a,b∈R} c_a c_b
  cross*(orbitSum a)(orbitSum b)` (double `sum_biUnion` + per-orbit `sum_image`; numerator also
  commutes the marked-coordinate `∑ i` inward).
- `polynomialMkF_symWeight` — the symmetric Maynard ratio as a Gram quotient.

Witness reduction (NEW — connects to the goal axiom):
- `monoOrbit_eq_or_disjoint` (orbits partition) + `disjoint_of_distinct_orbit` (discharge the
  `hR` disjointness from "distinct reps ⟹ distinct orbits", decidable for concrete `R`).
- `Mk_gt_four_of_symWeight_witness` — Gram quotient `> 4` ⟹ `Mk (n+1) > 4`. General-threshold
  `Mk_gt_of_symWeight_witness` (`> T : ℚ` ⟹ `Mk (n+1) > T`) via the real `Mk_ge_polynomialMkF` +
  `polynomialMkF_eq_MkF` (both axiom-clean).
- `exists_theta_of_Mk_gt` (general `T > 0`) and `exists_theta_of_Mk_gt_four` — `Mk k > T` ⟹ the
  EH-witness existential `∃ϑ∈(0,1), Mk k > T/ϑ`. `T = 2·m` serves ALL the EH witnesses
  (`mk_54` m=2, `mk_5511` m=3, `mk_41588` m=4, `mk_309661` m=5).
- **`mk_54_witness_under_EH_of_symWeight`** — the COMPLETE conditional discharge: given `k=54`
  reps `R` (distinct orbits) + coeffs `c` with Gram quotient `> 4`, produces exactly
  `mk_54_witness_under_EH`'s statement (`∃ϑ∈(0,1), Sieve.Mk 54 > 2*2/ϑ`). The general tools above
  give the same one-liner for the other four EH witnesses (just `T := 2*m`).

## 🎬 Next actions (the ONLY thing left for H₁≤270 under EH)
1. **Produce the `k=54` data** from `tools/mk/` (`mk_sym.py` / `_ldl.py`): the orbit
   representatives `R : Finset (MultiIndex 54)` (non-increasing exponent vectors, degree ≥ 9) and
   the optimal coefficients `c : MultiIndex 54 → ℚ` (LDL/eigenvector). Polymath8b reports
   `M₅₄ > 4.00238`, so the quotient genuinely clears 4.
2. **`native_decide` the rational quotient `> 4`** — the `hwit` hypothesis. This is the
   computational endgame: the Gram entries enumerate `MarginCorrectTables` (a Pi-Fintype filter)
   and `Nat.multinomial`/`pairWeightC` over them. **Likely host-better — the box OOMs on large
   native_decide.** Validate the entries `#eval` for the chosen shapes first (the denominator
   `#eval`s to `11/3360` at a Fin-3 toy; pattern is the same).
3. **`hR`** is now fully mechanical: `disjoint_of_histogram R (by native_decide)` (the histogram
   hypothesis is decidable — verified it closes by `decide` on a toy `R`). `mk_54_witness_under_EH_of_symWeight`
   takes the `Disjoint` form directly, so this drops straight in.
4. **Discharge the axiom**: replace `axiom mk_54_witness_under_EH` in `BoundedGaps/Polymath8b.lean`
   with `theorem … := mk_54_witness_under_EH_of_symWeight R c (disjoint_of_histogram R (by
   native_decide)) (by native_decide)`. (Editing Polymath8b.lean is fine — it's the goal; just
   don't break other axioms.) So the ENTIRE remaining task is: produce `(R, c)` (step 1) and run
   two `native_decide`s. Everything else is proven and axiom-clean.

### Computational-endgame findings (for step 2)
Probed in-kernel reduction at a Fin-3 toy (`α=![2,1,0]`, `β=![1,1,0]`):
- **`(MarginCorrectTables α β).card = 3` `#eval`s fast** — the subtype Pi-Fintype enumeration over
  `↥(univ.image α)`/`↥(univ.image β)` is NOT the bottleneck. The prior `denominator #eval =
  11/3360` confirms the whole denominator entry reduces.
- Building blocks confirmed in-kernel: `pairWeightC {(2,1),(1,1),(0,0)} = 100`, `jointWeightC = 12`,
  `markedSum = 25/3`.
- **The Gram entries are computable as parametrized `def`s** (`gramDenEntry`, `gramNumEntry`):
  `gramDenEntry ![2,1,0] ![1,1,0] = 11/3360`, `gramNumEntry = 7/2160` `#eval` fast. The earlier
  *standalone-def* timeout was ad-hoc instance synthesis, not fundamental. Use
  `mk_54_witness_under_EH_of_symWeight_computable` / `Mk_gt_of_symWeight_witness_computable` —
  their `hwit` is stated with `gram*Entry` (computable), so `native_decide` works (`crossNumerator`
  / `crossDenominator` are `noncomputable` — a witness with *them* can't `native_decide`).
- **End-to-end `native_decide` confirmed feasible**: the committed `Mk 3 > 1` example
  native_decides both `hR` and the quotient in seconds. k=54 is bigger (degree ≥9, more orbits)
  so watch for OOM/timeout on the host; if too slow, the fallback is a `Fin`-indexed table
  reformulation, but the toy suggests it may just work.
- **`hR` cannot use direct `decide` on `monoOrbit a ≠ monoOrbit b`** (`monoOrbit` is
  `noncomputable`, image over `Equiv.Perm`). **SOLVED this lap**: `disjoint_of_histogram` reduces
  `hR` to the decidable `∃ v ∈ image a ∪ image b, #{i:a i=v} ≠ #{i:b i=v}`; verified it closes by
  `decide` on a toy `R`. Use `disjoint_of_histogram R (by native_decide)`.

Same template discharges `mk_5511/41588/309661_witness_under_EH` (thresholds `2·3/ϑ`, `2·4/ϑ`,
`2·5/ϑ`) — generalize `exists_theta_of_Mk_gt_four` to `Mk k > 2·m`.

## 🧠 Context / gotchas
- **Section-variable auto-binding**: in `namespace OrbitFree` with `variable {k} (α β)`, a lemma
  binds only the vars it *uses*. `joint_sum_over_col` uses only `β` (call `joint_sum_over_col β
  …`), `joint_sum_over_row` only `α`. Watch this when adding lemmas.
- `jointWeight`/`pairWeight` carry a gratuitous `noncomputable`; use the `*C` twins
  (`jointWeightC`, `pairWeightC`) for anything `native_decide`/`#eval`.
- Fleet discipline: ALL this lap's work is in `SymmetricReductionOrbitFree.lean`. Did NOT touch
  shared `SymmetricReduction.lean`. Push is host-only.
- `div_lt_iff` is gone in this mathlib → `div_lt_iff₀`.

## 📁 Key files
- `BoundedGaps/SymmetricReductionOrbitFree.lean` — everything (now ~1160 lines, sorry-free,
  axiom-clean). The full chain ends at `mk_54_witness_under_EH_of_symWeight`.
- `BoundedGaps/Polymath8b.lean` — `axiom mk_54_witness_under_EH` (line ~107), the discharge target.
- `tools/mk/SYMMETRIC_REDUCTION.md` — design doc, updated with the completion section.
- `BoundedGaps/SievePolynomial.lean` — `Mk_gt_four_of_polynomial_witness`,
  `Mk_ge_polynomialMkF`, `polynomialMkF_eq_MkF` (the real bridges).

---
**→ The symmetric-reduction theory is DONE. What remains is data + one `native_decide`, then a
one-line axiom→theorem swap. Verify green (`lake build BoundedGaps.SymmetricReductionOrbitFree`)
before trusting this doc.**

🧵 ➡️ 🧵
