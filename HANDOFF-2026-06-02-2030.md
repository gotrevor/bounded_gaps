# Handoff: symmetric-reduction Gram assembly COMPLETE — mk_54_witness reduced to one rational native_decide

**Date**: 2026-06-02 ~20:30 UTC · **Branch**: `path-a-selberg-nu` · all work in
`BoundedGaps/SymmetricReductionOrbitFree.lean` (box-local, 10 commits this lap).

## 🎯 Headline
The **entire** symmetric-reduction pipeline is now built and **axiom-clean**, from orbit symmetry
down to a conditional discharge of `mk_54_witness_under_EH`. The only remaining inputs are the
concrete `k=54` data `(R, c)` and a single rational `native_decide`.

## ⚠️ FIRST: the previous HEAD did not build
`HANDOFF-2026-06-02-1647.md` claimed `8360f8c` (and the d469875 it grew into) were green. **They
were not.** `pair_fiber_card_eq_multinomial` had never elaborated:
- a parse error (anon-constructor pair missing inner parens for a `: V` ascription),
- the `i`-map's `: V` ascription wrapped the whole lambda → Lean inferred `i : … → V` not
  `… → (Fin k → V)`, poisoning `card_bij'`,
- `marginCorrect_cells_sum`'s `rw [hfib]` motive-incorrect (`k` occurs in `Fin k`) → `conv_rhs`,
- `case hi/hj` wrong `jointMultiset_count_eq` args + unfillable subtype `_`s.
Fixed in `895f7b8`. **Always `lake build` before trusting a "green" claim** (the pre-commit gate
caught nothing because the broken commit predated this lap).

## ✅ DONE this lap (all `[propext, Classical.choice, Quot.sound]`, all real `lake build` green)
Numerator orbit-free re-index (finishes the prior "NUMERATOR partial"):
- `numerator_orbitFree` — pair-weighted double orbit sum re-indexed over `MarginCorrectTables`
  (numerator analog of `orbitCore_eq_multinomial_sum_orbitFree`). Reuses the denominator's table
  bijection via `pair_image_eq` (product-orbit joint-type image collapses to the single-orbit
  image, realizing the column as `q=β`) + `pair_fiber_card_eq_multinomial`. New general-column
  joint machinery: `joint_sum_over_col/_row`, `joint_entry`, `joint_multisetToTable_mem`,
  `tableToMultiset_jointTable`.
- `orbitPair_numerator_orbitFree` + `orbitPair_numerator_computable` — full numerator entry =
  `(∑_T multinomial·pairWeight)/(n+1+|α|+|β|+1)!`. **No monoOrbit, and unlike the denominator NO
  orbit-cardinality factor** (full joint-type regroup never uses the q-collapse). `pairWeightC`
  computable twin verified in-kernel (`pairWeightC {(2,1),(1,1),(0,0)} = 100`).

Gram-matrix assembly (NEW — the prior plan's "genuinely multi-session" step):
- `crossDenominator` / `crossNumerator` — bilinear forms on two weights (= `polynomialMaynard*`
  on the diagonal). `cross*_orbitSum` evaluate the orbit-basis entries to the orbit-pair sums;
  `cross*_orbitSum_computable` chain to the native_decide-ready closed forms. **These are the
  `Mr[λ][μ]` matrix entries**, computable from shapes alone.
- `symWeight R c` — the symmetric weight `∑_{λ∈R} c_λ orbitSum λ` (terms = disjoint union of
  per-orbit term sets). `symWeight_termSets_disjoint`, `symWeight_double_sum` (shared engine).
- `polynomialMaynardDenominator_symWeight` / `polynomialMaynardNumerator_symWeight` — the
  **bilinear Gram expansions**: `polynomialMaynard*(symWeight R c) = ∑_{a,b∈R} c_a c_b
  cross*(orbitSum a)(orbitSum b)` (double `sum_biUnion` + per-orbit `sum_image`; numerator also
  commutes the marked-coordinate `∑ i` inward).
- `polynomialMkF_symWeight` — the symmetric Maynard ratio as a Gram quotient.

Witness reduction (NEW — connects to the goal axiom):
- `monoOrbit_eq_or_disjoint` (orbits partition) + `disjoint_of_distinct_orbit` (discharge the
  `hR` disjointness from "distinct reps ⟹ distinct orbits", decidable for concrete `R`).
- `Mk_gt_four_of_symWeight_witness` — Gram quotient `> 4` ⟹ `Mk (n+1) > 4`. General-threshold
  `Mk_gt_of_symWeight_witness` (`> T : ℚ` ⟹ `Mk (n+1) > T`) via the real `Mk_ge_polynomialMkF` +
  `polynomialMkF_eq_MkF` (both axiom-clean).
- `exists_theta_of_Mk_gt` (general `T > 0`) and `exists_theta_of_Mk_gt_four` — `Mk k > T` ⟹ the
  EH-witness existential `∃ϑ∈(0,1), Mk k > T/ϑ`. `T = 2·m` serves ALL the EH witnesses
  (`mk_54` m=2, `mk_5511` m=3, `mk_41588` m=4, `mk_309661` m=5).
- **`mk_54_witness_under_EH_of_symWeight`** — the COMPLETE conditional discharge: given `k=54`
  reps `R` (distinct orbits) + coeffs `c` with Gram quotient `> 4`, produces exactly
  `mk_54_witness_under_EH`'s statement (`∃ϑ∈(0,1), Sieve.Mk 54 > 2*2/ϑ`). The general tools above
  give the same one-liner for the other four EH witnesses (just `T := 2*m`).

## 🎬 Next actions (the ONLY thing left for H₁≤270 under EH)
1. **Produce the `k=54` data** from `tools/mk/` (`mk_sym.py` / `_ldl.py`): the orbit
   representatives `R : Finset (MultiIndex 54)` (non-increasing exponent vectors, degree ≥ 9) and
   the optimal coefficients `c : MultiIndex 54 → ℚ` (LDL/eigenvector). Polymath8b reports
   `M₅₄ > 4.00238`, so the quotient genuinely clears 4.
2. **`native_decide` the rational quotient `> 4`** — the `hwit` hypothesis. This is the
   computational endgame: the Gram entries enumerate `MarginCorrectTables` (a Pi-Fintype filter)
   and `Nat.multinomial`/`pairWeightC` over them. **Likely host-better — the box OOMs on large
   native_decide.** Validate the entries `#eval` for the chosen shapes first (the denominator
   `#eval`s to `11/3360` at a Fin-3 toy; pattern is the same).
3. **`hR`** is `decide`/`native_decide`-able for concrete `R` (`monoOrbit a ≠ monoOrbit b` ⟺
   different value-histograms). Or hand-prove via `disjoint_of_distinct_orbit`.
4. **Discharge the axiom**: replace `axiom mk_54_witness_under_EH` in `BoundedGaps/Polymath8b.lean`
   with `theorem … := mk_54_witness_under_EH_of_symWeight R c hR hwit`. (Editing Polymath8b.lean
   is fine — it's the goal; just don't break other axioms.)

### ⚠️ Computational-endgame finding (the real bottleneck for step 2)
I probed evaluating the Gram entry RHS in-kernel. The blocker is **elaboration**, not just
`native_decide` runtime: the entries are indexed over the **subtypes** `↥(univ.image α)` /
`↥(univ.image β)`, whose `Fintype`/`DecidableEq` instances are very expensive to synthesize and
reduce (`isDefEq` heartbeat timeout even at a Fin-3 toy with `maxHeartbeats 2000000`; the prior
denominator `#eval = 11/3360` succeeded only because it was set up differently). **Before the
k=54 `native_decide` can run, the Gram entries almost certainly need a `decide`-friendly
reformulation over plain `Fin`-indexed tables** (e.g. `Fin (image-card) → Fin (image-card) →
Fin (k+1)` with the image cardinalities precomputed as literals), not the subtype-indexed
`CTable`. That reformulation (+ an equality bridge to the proven `crossDenominator_orbitSum_*`)
is the natural next box-side task — it's pure Lean, no external data, and unblocks the host's
`native_decide`. Building blocks confirmed to reduce in-kernel: `pairWeightC {(2,1),(1,1),(0,0)}
= 100`, `jointWeightC … = 12`, `markedSum … = 25/3`.

Same template discharges `mk_5511/41588/309661_witness_under_EH` (thresholds `2·3/ϑ`, `2·4/ϑ`,
`2·5/ϑ`) — generalize `exists_theta_of_Mk_gt_four` to `Mk k > 2·m`.

## 🧠 Context / gotchas
- **Section-variable auto-binding**: in `namespace OrbitFree` with `variable {k} (α β)`, a lemma
  binds only the vars it *uses*. `joint_sum_over_col` uses only `β` (call `joint_sum_over_col β
  …`), `joint_sum_over_row` only `α`. Watch this when adding lemmas.
- `jointWeight`/`pairWeight` carry a gratuitous `noncomputable`; use the `*C` twins
  (`jointWeightC`, `pairWeightC`) for anything `native_decide`/`#eval`.
- Fleet discipline: ALL this lap's work is in `SymmetricReductionOrbitFree.lean`. Did NOT touch
  shared `SymmetricReduction.lean`. Push is host-only.
- `div_lt_iff` is gone in this mathlib → `div_lt_iff₀`.

## 📁 Key files
- `BoundedGaps/SymmetricReductionOrbitFree.lean` — everything (now ~1160 lines, sorry-free,
  axiom-clean). The full chain ends at `mk_54_witness_under_EH_of_symWeight`.
- `BoundedGaps/Polymath8b.lean` — `axiom mk_54_witness_under_EH` (line ~107), the discharge target.
- `tools/mk/SYMMETRIC_REDUCTION.md` — design doc, updated with the completion section.
- `BoundedGaps/SievePolynomial.lean` — `Mk_gt_four_of_polynomial_witness`,
  `Mk_ge_polynomialMkF`, `polynomialMkF_eq_MkF` (the real bridges).

---
**→ The symmetric-reduction theory is DONE. What remains is data + one `native_decide`, then a
one-line axiom→theorem swap. Verify green (`lake build BoundedGaps.SymmetricReductionOrbitFree`)
before trusting this doc.**

🧵 ➡️ 🧵
