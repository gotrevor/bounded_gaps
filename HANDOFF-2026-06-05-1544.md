# Handoff: COURSE CORRECTION — off-diagonal `s1` needs the smoothing estimate (PNT); PNF-free combinatorial scaffold now COMPLETE

**Date**: 2026-06-05 15:44 UTC · **Branch**: `path-a-selberg-nu` · HEAD `8553db8`
6 commits this lap (`379488d`…`8553db8`), 5 Lean (all axiom-clean `[propext, Classical.choice, Quot.sound]`),
full `lake build` green (8302 jobs) at every pre-commit gate. Never pushed. Supersedes
`HANDOFF-2026-06-05-1501.md`. Master `HANDOFF.md` untouched (repo convention).

## 🎯 What we're doing
Analytic axiom burn-down for the contour-free Path-Y y-space `s1`. Lap 10 reduced the whole y-space `s1`
to ONE target: `offDiagMass/B^{+k} → 0`. The lap-10 plan was to discharge that **elementarily** via the
per-prime mass-fraction "crux (B)". **This lap's fresh-mind review found that plan is wrong** — crux (B)
is NOT elementary — and pivoted: built the full PNF-free combinatorial scaffold, and isolated the genuine
remaining input as ONE PNT-strength smoothing axiom.

## 🧠 Context to carry forward — READ THIS (the pivot is the point)
- **crux (B) — `∑_{(d,e):p|lcm}|yLλ·yLλ|/[d,e] ≤ (C/p)·Q` — is FALSE as a ratio to the actual `Q`** for
  structure-free weights. Counterexample: a weight on a single `d₀` with `p|d₀` gives ratio `1`, not
  `1/p`. So it MUST use `yLambda`'s smooth structure. And the lap-10 "sharp" bound `abs_yLambda_le_sharp`
  (`|yLambda_d| ≤ (d/φd)·C·∑μ²/φ`) is **lossy by `(log)²` per coordinate**: `∑μ²/φ ≈ log N` stands in for
  the *cancelling* Möbius mean `∑_t μ(t)F/φ(t) ≈ 1/log R` (the y-variables are genuinely `O(1/log R)` —
  archived findings `(★)`). Two majorant-bounded coords per pair ⟹ `(log)⁴` loss vs `(log)²` from `B²` ⟹
  the elementary route **cannot close** for any sieve-admissible (poly-bounded) `W`. **This vindicates
  lap-4's `[[s1-derivative-landmine]]` over lap-9's "off-diagonal is elementary".** I verified
  `sieveB W x = (φW/W)·log x` and that this mathlib has NO PNT (only `Chebyshev`, `VonMangoldt`).
- **What DOES close it (verified-consistent roadmap):** the smoothing UPPER bound `|yLambda_d| ≤
  (d/φd)·κ/log R` (PNT-strength) makes the weight `≤ κ²/L²·Gmaj`; then the PNF-free `Gmaj_per_prime_le`
  gives the per-prime `O(1/(p-1))` fraction; the Mertens-product size `𝒢=∑Gmaj ≈ (log R/log D₀)³`
  (NOT in mathlib) makes `κ²/L²·𝒢 ≈ B`; and growing `W=W(N)` (`D₀(N)→∞`) drives the `∑_{p>D₀}1/(p-1)²`
  tail (`recip_sq_tail_tendsto_zero`, already proven) to 0. I worked the bookkeeping: per-pair
  `≈ (log D₀)^{k-1}/D₀ → 0`. So the off-diagonal closes modulo {ONE smoothing axiom + the `𝒢` Mertens
  size + growing-W restate}.
- **I deliberately did NOT introduce the smoothing axiom this lap** — its exact statement (the constant
  must fold `‖F‖∞` and `‖F'‖∞`) deserves care, ideally after the online request confirms whether PNT is
  in mathlib master / `PrimeNumberTheoremAnd` (so it can be DISCHARGED, not just stated). A wrong axiom
  under time pressure is worse than a precise spec. The spec is in `PENDING_WORK.md` lap-11 head.
- **Aristotle `17b8559c` (brick_reindex) is now REDUNDANT** — I proved `reindex_bound` AND the capstone
  locally. `0e387e89` (brick_localfactor) is also redundant (= `selberg_local_factor`, proved locally).
  Don't wait on either; don't port them. Submit a fresh brick (see Next actions #4).

## ✅ State — all axiom-clean, build green (8302) at every commit
New in `BoundedGaps/S1OffDiagSize.lean` this lap (all PNF-free, pure-Finset + elementary arithmetic):
- **`selberg_local_factor`** (`379488d`): `G(p·d,e)=G(d,e)/(p-1)` for prime `p∤d,e` (the per-term core).
- **`reindex_bound`** (`e712918`): `∑_{d∈R,p|d}∑_{e∈R,p∤e}Gmaj ≤ (1/(p-1))∑∑Gmaj`, reindex `d↦d/p`.
- **`Gmaj_symm`, `Gmaj_local_factor_both`, `reindex_bound_both`** (`c9a9510`): both-divisible factor
  `G(p·d,p·e)=(p/(p-1)²)G(d,e)` + 2D reindex.
- **`Gmaj_per_prime_le`** (`d0c9660`, THE CAPSTONE): `∑_{d∈R}∑_{e∈R,p|lcm(d,e)}Gmaj ≤ (6/(p-1))·∑∑Gmaj`
  for squarefree divisor-closed `R`. The complete PNF-free per-prime singular-series mass fraction.
- `Gmaj a e := (a/φa)(e/φe)/lcm(a,e)` is the named deterministic majorant.
- Docs: `PENDING_WORK.md` lap-11 head (the finding + roadmap + revised priorities), `ON-LINE-REQUEST.md`
  lap-11 (sharpened: PNT-in-mathlib? `PrimeNumberTheoremAnd`? Mertens 3rd thm? elementary large-sieve
  escape?), memory `[[hcorr-diag-pntfree-offdiag-growing-w]]` updated.

## 🎬 Next actions
1. **Check `ON-LINE-FINDINGS-*.md` at repo root** — if a networked session answered the lap-11 request
   (PNT availability / `PrimeNumberTheoremAnd` / Mertens 3rd thm), act on it: that decides whether the
   smoothing input is *discharged* (import PNT) or stays an axiom, and whether the `𝒢` Mertens size is
   importable. Harvest → `git mv` into `archive/findings/`.
2. **State the smoothing axiom `yLambda_smoothing_bound`** in `S1Correction.lean` (next to
   `abs_yLambda_le_sharp`): `|yLambda R F (log N) d| ≤ (d/φ(d))·C_F/log N`, `C_F` from `‖F‖∞,‖F'‖∞`.
   Document it as the narrowest PNT-strength core (Maynard `PartialSummation` / Möbius-mean cancellation).
   Only if you can't import PNT (action 1).
3. **Bridge + crux(B)-for-actual-weight**: the ABSTRACT bridge is DONE —
   `S1OffDiagSize.abs_mul_div_lcm_le_Gmaj` (`172cb35`): `|x·y|/lcm ≤ B²·Gmaj d e` given `|x|≤(d/φd)B,
   |y|≤(e/φe)B`. Just feed it `B = C_F/log N` (from #2's smoothing axiom) with `x=yLambda_d, y=yLambda_e`
   to get `|yLambda_d·yLambda_e|/lcm ≤ (C_F/log N)²·Gmaj`, then compose with `Gmaj_per_prime_le` → the
   per-prime fraction for the ACTUAL off-diagonal weight. (That composition file must import both
   `S1OffDiagSize` and `S1Correction`; `S1DiagFree` already does.)
4. **Keep Aristotle busy** (current jobs redundant): submit the Mertens-product `𝒢`-size brick, or — safer
   for v4.28 — the bridge from #3 (pure inequality, self-contained). One job in flight at a time.
5. Longer arc: the **Mertens-product `𝒢 ≈ (log R/log D₀)³` size** (check our `CoprimeMertens`/`SharpMertens`
   — we already have `∑1/p=loglog+O(1)` and sharp `∑μ²/φ~log`) and the **growing-`W=W(N)` restate** of
   `yspace_s1_sieveSum_div_tendsto_offDiagMass` (the hardest bookkeeping — uniform-in-W hBaseW double limit).

## ⚠️ Gotchas
- `Finset.sum_image (g := …)` named-arg breaks overload resolution → use `rw [Finset.sum_image hinj]`
  (infers `g` from the matched subterm). `rw [← h]` rewrites ALL occurrences (e.g. both `First`s in
  `First+First`) → use `nth_rewrite 1 [← h]`. Inline `(by omega : 1 ≤ p)` needs a numeric hyp (`2 ≤ p`)
  in scope — `p.Prime` won't feed omega. (Flushed to the personal staging buffer.)
- `field_simp` sometimes fully closes a cleared-denominator identity (no `ring` needed) and sometimes
  leaves a `ring` goal — depends on what's in scope; just try `field_simp` then add `ring` if a goal remains.
- `Nat.lcm_mul_left : lcm (p*d) (p*e) = p * lcm d e` exists; for `lcm (p*d) e = p*lcm d e` (p∤e) use
  `Nat.Coprime.gcd_mul_left_cancel` + `Nat.mul_div_assoc`.
- Long-line lints tolerated. Pre-commit runs full `lake build`. Dev-fast: `/tmp/x.lean` importing the
  module + `taskset -c 0-7 lake env lean /tmp/x.lean`; `#print axioms` before trusting.

## 📁 Key files
- `BoundedGaps/S1OffDiagSize.lean` — the COMPLETE PNF-free off-diagonal combinatorial scaffold (this lap).
- `BoundedGaps/S1DiagFree.lean` — `offDiagMass`, `yspace_s1_sieveSum_div_tendsto_offDiagMass` (full `s1`
  conditional only on `offDiagMass/B^{+k}→0`); the bridge from action #3 belongs here or adjacent.
- `BoundedGaps/S1Correction.lean` — `abs_yLambda_le_sharp` (the lossy bound); home for the smoothing axiom.
- `PENDING_WORK.md` lap-11 head — the finding, the verified closure roadmap, revised priorities.
- `ON-LINE-REQUEST.md` lap-11 — the sharpened literature/PNT question (act on any findings doc first).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (contour-free
y-space `s1`: PNF-free off-diagonal scaffold DONE, sole remaining input = the PNT-strength smoothing
estimate). Absorb the pivot in "Context to carry forward", then start at Next action #1: check for an
`ON-LINE-FINDINGS-*.md` and act on it; if none, state the smoothing axiom (#2) and build the bridge (#3).**

🧵 ➡️ 🧵
