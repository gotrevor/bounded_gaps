# Handoff: Leaf 1 SMOOTH CORE CLOSED — `weighted_riemann_2d` proven unconditionally in-kernel

**Date**: 2026-06-04 14:35 UTC · **Branch**: `path-a-selberg-nu` · HEAD `11b9e2b`
**~13 commits this lap** (`ab7dcc8`…`11b9e2b`; ~7 Lean + docs), every Lean commit green at the
pre-commit gate (full `lake build`, 8280 jobs), all new decls `#print axioms`-clean
(`[propext, Classical.choice, Quot.sound]`), never pushed. **Supersedes `HANDOFF-2026-06-04-1307.md`.**

## 🎯 What we're doing
Analytic-NT axiom burn-down for the GPY/Maynard sieve (`s1_holds_from_nonprime_asym` & sisters,
`Sieve.lean`). Post-Cauchy–Schwarz that axiom = 4 analytic leaves. Coming in, leaves 2&4 were DONE,
leaf 1 was *decomposed* to an `inner_uniform` Aristotle target, leaf 3 narrowed. **This lap CLOSED
leaf 1's entire smooth analytic core in-kernel.**

## 🧠 Context to carry forward (read this)
- **The headline: `InnerUniformReduction.weighted_riemann_2d` is now a PROVEN unconditional theorem**
  (the 2-D simplex weighted-Riemann limit `∑_m∑_{n≤R/m} F(log m/log R)·G(log n/log R)/(mn)/(log R)²
  → ∫₀¹ F·∫₀^{1-x} G`). This is *verbatim* the deep axiom / the old in-flight Aristotle target
  `3e2b6a8d` — now machine-checked, axiom-clean. The 3 redundant Aristotle leaf-1 jobs
  (`3e2b6a8d`, `79da4f45`, `1f84c4d6`) were **cancelled**.
- **How it was built (the chain, all axiom-clean, in `BoundedGaps/InnerUniformReduction.lean` +
  `BoundedGaps/PolyaUniform.lean`):**
  1. `PolyaUniform.polya_uniform` — **Pólya's theorem** (monotone-in-*argument* functions converging
     pointwise to a continuous limit on `[0,1]` ⟹ uniform). NEW; mathlib only has Dini
     (monotone-in-*index*). Finite-grid bracket + monotone sandwich.
  2. `floor_rpow_one_sub` (`⌊R^{1-log m/log R}⌋=R/m`, the exact reparametrisation), `psi_monotoneOn`
     (`Ψ G R ·` monotone for `G≥0`), `inner_uniform_of_pointwise_nonneg`, then
     `inner_uniform_of_pointwise` (arbitrary continuous `G` via the `G⁺/G⁻` split). These give the
     uniform `huni` from just a pointwise limit.
  3. `weighted_riemann_2d_of_psi_pointwise` composes that with the prior lap's
     `WeightedRiemann2D.weighted_riemann_2d_of_inner`.
  4. **`psi_tendsto`** (the pointwise scale-change limit `Ψ G R t = (∑_{n≤⌊R^t⌋}G(log n/log R)/n)/log R
     → ∫₀^t G`) — PROVEN by assembling: `tendsto_logFloor_rpow_div` (`c_R=log⌊R^t⌋/log R → t`) +
     `tendsto_harmonic_icc2_div_log` (`(∑1/n)/log N → 1`) + a uniform-continuity drift bound
     (`|G(c_R u)−G(t u)|≤ε/4` uniformly, error `≤ε/2`) + `riemann_sum_log_weight` on `F_t=G(t·)`
     composed with `⌊R^t⌋→∞` + substitution `t·∫₀¹G(t·u)du=∫₀^t G`
     (`intervalIntegral.mul_integral_comp_mul_left`).
  5. `weighted_riemann_2d` = `weighted_riemann_2d_of_psi_pointwise … (fun H hH t ht => psi_tendsto …)`.
- **Mental model of what's LEFT for leaf 1 — the GPY port (the new frontier).** `weighted_riemann_2d`
  is the *smooth core* but is currently **standalone** — the `s1` chain does NOT yet consume it. The
  bridge is the classical GPY computation: connect `SieveExpansion.gpy_diagonal_asymptotic_form`'s
  output `∑_{r sf}(φ(r)/r²) z_r²` (with `z_r=∑_{(s,r)=1}μ(s)g(rs)/s`, `g(d)=F(log d/log R)`) to the
  double-sum shape `weighted_riemann_2d` addresses, identifying the singular series `𝔖`. **The
  algebra (diagonalization) is already done; what remains is the analytic asymptotic + the exact
  constant/`𝔖`, which I'm NOT confident stating without the GPY/Maynard paper** — so I logged a
  precise `ON-LINE-REQUEST.md` entry rather than risk a wrong axiom-clean statement. Do NOT guess it.
- **Why I didn't push into leaf 3 or guess the GPY port:** leaf 3 (`∑_{¬diag}|w|·main=o(main)`) is
  documented in `PENDING_WORK.md` as coupled to `𝔖(H)` + the `W`/`D₀`/`M`/`R` plumbing — not a clean
  isolable lemma. The GPY port's *statement* is paper-uncertain. Both are genuinely multi-lap.

## ✅ State (full `lake build` green, 8280 jobs — verified at the pre-commit gate)
- NEW `BoundedGaps/PolyaUniform.lean`: `polya_uniform`.
- `BoundedGaps/InnerUniformReduction.lean`: `Psi`, `floor_rpow_one_sub`, `tendsto_logFloor_rpow_div`,
  `tendsto_harmonic_icc2_div_log`, `psi_monotoneOn`, `inner_uniform_of_pointwise_nonneg`,
  `inner_uniform_of_pointwise`, `weighted_riemann_2d_of_psi_pointwise`, `psi_tendsto`,
  **`weighted_riemann_2d`** (all axiom-clean). Imports added: `Mertens`, `RiemannSumLogWeight`,
  `Mathlib.NumberTheory.Harmonic.EulerMascheroni`. Both wired into `BoundedGaps.lean`.
- Docs updated: `ANALYTIC_AXIOM_BURNDOWN.md`, `PENDING_WORK.md` (leaf-1 CLOSED + GPY-port 3 paths),
  memory `gpy-diagonalization-route`, new `ON-LINE-REQUEST.md` GPY-port entry.
- **Aristotle**: my 3 leaf-1 jobs cancelled; only `a1c167e7` (`aristotle-ugen`, NOT mine) running.

## 🎬 Next actions
1. **Check for `ON-LINE-FINDINGS-*.md` in the repo root** (the GPY-port request may be answered). If
   present, harvest it: it should give the precise `∑_{r sf}(φ/r²)z_r²` asymptotic + `𝔖` identity.
   Then build the GPY port (connect `gpy_diagonal_asymptotic_form` → `weighted_riemann_2d`),
   rank-1/separable case first (path b in `PENDING_WORK.md` §1).
2. **If no findings yet:** the GPY-port statement is paper-gated — do NOT guess it. Instead advance
   leaf 3 OR a non-leaf thread. Per the unblock guide, keep Aristotle busy: architect a *correctly
   stated* bounded lemma (the algebra of the `z_r²` expansion + sum-swap is statable without the
   constant, if you want — but verify against the paper).
3. Keep ONE Aristotle job in flight on something useful (currently only the non-mine `ugen`).

## ⚠️ Gotchas
- **Full `lake build` OOM is transient process-pressure**, NOT memory exhaustion (box has 31Gi). It
  hit the aggregator `BoundedGaps.lean` (single process loading ALL oleans). Fix: `pkill -9 lean lake;
  sleep 3; taskset -c 0-3 lake build`. Lingering procs from killed builds eat RAM. My changed module
  always built fine alone.
- mathlib name gotchas this lap (now in staging memory): `ContinuousOn.max` → use `ContinuousOn.sup`
  (ℝ: `⊔=max` defeq); `abs_add` → `abs_add_le`; `tendsto_rpow_neg_atTop` is root-level (not `Real.`);
  `Real.tendsto_harmonic_sub_log` IS `Real.`-namespaced; repo's `riemann_sum_log_weight` lives in
  `BoundedGaps.WeightedMertens.Riemann`; `harmonic_eq_icc_sum` in `BoundedGaps.Mertens`.
- `M ROADMAP.md` + `scratch_*.lean` + `aristotle-*` dirs in `git status` are pre-existing / not mine.

## 📁 Key files
- `BoundedGaps/PolyaUniform.lean` — Pólya's theorem (the reusable engine).
- `BoundedGaps/InnerUniformReduction.lean` — the full leaf-1 closure (reduction chain + `psi_tendsto`
  + unconditional `weighted_riemann_2d`).
- `BoundedGaps/SieveExpansion.lean` — `gpy_diagonal_asymptotic_form` (the GPY-port source) + leaves 2,3.
- `ANALYTIC_AXIOM_BURNDOWN.md` / `PENDING_WORK.md` (§1) — leaf scoreboard + GPY-port 3 attack paths.
- `ON-LINE-REQUEST.md` — the GPY diagonal-asymptotic request (leaf 1's last piece).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (s1 analytic
burn-down; leaf 1's smooth core is now CLOSED). Start at Next action #1: check for an
`ON-LINE-FINDINGS-*.md` answering the GPY-port request; harvest it and build the GPY port. If none
yet, do NOT guess the GPY asymptotic — advance leaf 3 or another thread and keep Aristotle busy.**

🧵 ➡️ 🧵
