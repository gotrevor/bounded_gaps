# Handoff: both A.2 bridges turned axiom→theorem; gap = 2 atomic cores (on Aristotle) + parts-witness DONE

**Date**: 2026-06-03 03:20 UTC · **Branch**: `path-a-selberg-nu` · HEAD `6d4fde2` · 6 commits this lap (4 Lean, 2 docs), all green.

## 🎯 What we're doing
Discharging the `mk_*_witness` numerical axioms (→ bounded gaps) by *proving* `Mk k > 4` via the
symmetric-reduction → orbit-basis Gram-quotient → matchings-closed-form pipeline in
`SymmetricReductionOrbitFree.lean`. The chain `symWeight → Gram quotient → Mk(n+1)>T` is complete and
feeds the already-proven `Targets.H1_le_of_Mk_witness`. The endgame is a kernel-checked `Mk 200 > 4`.

## 🧠 Context to carry forward
The PRIOR handoff (`HANDOFF-2026-06-03-0237.md`) said the remaining gap was "2 validated bridge
axioms + re-key witness". **Both are now substantially done.** What I did and why:

- **The reduction pattern that worked** (apply it again for any "table-sum = closed-form" axiom):
  don't axiomatize the *whole* identity — axiomatize only the **atomic combinatorial core** and
  discharge all surrounding plumbing in-kernel. For the denominator: `gramDenEntry (ofParts La)
  (ofParts Lb) = orbit-sum / (k+|α|+|β|)!` via `orbitPair_denominator_computable` then
  `orbitPair_denominator_eq` (both already in repo); `ofParts_degree : degree (ofParts L) = L.sum`
  (new, via `sum_getD_fin`); then cancel `autParts>0`. The leftover atomic axiom `denom_bridge` is
  exactly `autParts La · autParts Lb · (∑_{p∈orbit}∑_{q∈orbit} ∏ᵢ(pᵢ+qᵢ)!) = matchDenSum`. The
  numerator is identical structurally, using `crossNumerator_orbitSum` ∘ `orbitPair_numerator_eq` ∘
  `numerator_combinatorial_factored`, plus `markedCellFactor_eq_gWeight` (the slack-factorial cell
  weight = matchData's `gWeight`, pure ℚ algebra: `push_cast; field_simp` — NO trailing `ring`, it
  closes and `ring` then errors "No goals").

- **Re-keying mental model**: `_witness_match` needs `c, P` as functions on `MultiIndex`, but
  evaluating them per summand = 45-way match over 200-entry functions → `#eval` crash (code 134).
  Fix: state the witness over `LCs : List (List ℕ × ℚ)` with the Gram quotient as a DIRECT list-form
  double sum, and make `c, P` **noncomputable** (`Classical.choose`-based `coefInv`/`partsInv`) — the
  `native_decide` only ever touches the list-form `hwit`, never `c`/`P`. The Finset-sum-over-`R` →
  list-sum reindex is `List.sum_toFinset` applied twice (no `sum_image`/`toFinset_map` needed);
  `ofParts` injectivity on `LCs` comes from `List.inj_on_of_nodup_map hnodup`.

- **Both atomic cores were numerically pre-verified before submitting** (brute-force Python over
  permutations): denom 4 cases, num 5 cases incl. img-3 + asymmetric (`[2,2,1]/[3,1]/k=5` etc). They
  are genuinely true; Aristotle just has to find the proof.

## ✅ State (real `lake build` green observed)
- Module green at **8255 jobs**; pre-commit full-build gate green at **8270 jobs** on every Lean commit.
- `c9cb696` **`gramDenEntry_eq_matchDenForm` is now a THEOREM** (was axiom). `#print axioms` =
  `[propext, Classical.choice, Quot.sound, denom_bridge]`.
- `8d46bcd` **`gramNumEntry_eq_matchNumForm` is now a THEOREM**. `#print axioms` = `[3 std, num_bridge]`.
- `ef2b071` **`Mk_gt_of_symWeight_witness_match_parts`** — parts-list-keyed (capstone-feasible) witness;
  `8a5a6ba` k=3 regression fires `native_decide` on the list-form quotient. Both depend on
  `[3 std, denom_bridge, num_bridge]`.
- `Mk_gt_of_symWeight_witness_match` now `[3 std, denom_bridge, num_bridge]` — no sorry.
- Docs: `PENDING_WORK.md` §A + `ROADMAP.md` ledger brought current (`4286778`, `6d4fde2`).
- **Aristotle IN FLIGHT** (both RUNNING as of 03:20): `0b5bf5be` (`aristotle-denbridge/`) proving
  `denom_bridge`; `9d05dbaa` (`aristotle-numbridge/`) proving `num_bridge`.

## 🎬 Next actions
1. **Poll Aristotle** (`aristotle list`, the one-shot — NOT the live TUI) for `0b5bf5be` / `9d05dbaa`.
   When EITHER returns: `aristotle download`, VERIFY the proof in our kernel + `#print axioms` clean,
   then PORT. Porting note: the job statement uses `orbitFin L k`; the repo axiom is on
   `monoOrbit (ofParts L)`. These are **defeq** (`monoOrbit (ofParts L) = Finset.univ.image (fun σ =>
   fun i => (ofParts L)(σ i))` and `orbitFin L k = Finset.image (…) Finset.univ`) — a `rfl`/`Finset`
   rewrite. Replace the `axiom denom_bridge`/`num_bridge` with the imported proof → that Gram side is
   fully kernel-clean. IMMEDIATELY submit the next job (keep one always in flight).
2. **Stage the real k=200 witness** (I was mid-way here): `tools/mk/gen_witness7.py` (D=7, K=200, 45
   orbits, exact LDL via `_ldl.ldl_inertia`) produces orbit coeffs `wit` and orbits `M.partitions_upto(7)`.
   Emit a Lean `def witnessLCs : List (List ℕ × ℚ)` = `[(parts_i, c_i)]` (strip the zero parts; coeffs
   are the LDL witness vector). Then `#eval` the list-form quotient (fast/computable, `matchForm`
   `#eval`s instantly) to confirm `> 4`. This validates the real data through the exact Lean matchings
   path and STAGES the capstone. (Don't commit a heavy `native_decide` you can't run — see Gotchas.)
3. **Capstone** (HOST-only): `Mk_gt_of_symWeight_witness_match_parts witnessLCs … (by native_decide)`
   → `(4:ℝ) < Mk 200`. The box OOMs on the heavy `native_decide` (2025 matchForm terms, 214!
   factorials). Leave it for the networked host or flag in `ON-LINE-REQUEST.md` if needed.

## ⚠️ Gotchas
- **Box intermittent OOM** on `lake build` of *Mathlib* files ("Cannot allocate memory" / "read failed:
  resource exhausted" on `.trace`/`.olean`). It's a flake — just re-run `lake build`. Hit it twice
  this lap; both cleared on retry. Don't `lean` a Mathlib-importing file directly.
- Only commit builds you SAW go green. The heavy capstone `native_decide` likely won't finish on-box —
  do NOT commit it as a proof; use `#eval` to validate the data instead.
- `field_simp` closing a goal makes a trailing `ring` error "No goals to be solved".
- `denom_bridge`/`num_bridge` are intentional DISCLOSED axioms (numerically validated), not accidental
  sorries — they are the entire remaining math gap on the Gram side.

## 📁 Key files
- `BoundedGaps/SymmetricReductionOrbitFree.lean` — the pipeline. New this lap: `autParts_pos`,
  `sum_getD_fin`, `ofParts_degree`, `denom_bridge`+`gramDenEntry_eq_matchDenForm` (~2073),
  `markedCellFactor_eq_gWeight`, `num_bridge`+`gramNumEntry_eq_matchNumForm` (~2152),
  `partsInv`/`coefInv`/`Mk_gt_of_symWeight_witness_match_parts` (~2237), 2 new regression examples (~2360).
- `aristotle-denbridge/DenBridge.lean`, `aristotle-numbridge/NumBridge.lean` — the two in-flight cores.
- `PENDING_WORK.md` §A — actionable inventory (updated). `ROADMAP.md` ledger (updated).
- `tools/mk/gen_witness7.py` + `_ldl.py` + `mk_sym.py` — witness generator (k=200 ratio 4.0029).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the
context, then start at Next action #1: poll Aristotle `0b5bf5be`/`9d05dbaa`; if neither is done yet,
go straight to #2 (stage the k=200 `witnessLCs` and `#eval`-validate > 4).**

🧵 ➡️ 🧵
