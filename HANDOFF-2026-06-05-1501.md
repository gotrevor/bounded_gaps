# Handoff: Leg 1 DISCHARGED — full y-space `s1` now conditional ONLY on the off-diagonal; all soft Leg-2 reductions landed

**Date**: 2026-06-05 15:01 UTC · **Branch**: `path-a-selberg-nu` · HEAD `9c39c87`
13 axiom-clean commits this lap (`e0cbde2`…`9c39c87`), full `lake build` green (8302 jobs) at every
pre-commit gate. Never pushed. Supersedes `HANDOFF-2026-06-05-1322.md`. Master `HANDOFF.md` untouched
(repo convention).

## 🎯 What we're doing
Analytic axiom burn-down for the contour-free Path-Y y-space `s1` (built ALONGSIDE the d-space
flagship; Trevor flips axioms + `B^{±k}` normalisation later). Lap 9 split the off-diagonal correction
`hcorr` into a **PNT-free diagonal leg** (Leg 1) + an **off-diagonal leg needing growing `W`** (Leg 2),
landing the abstract toolkit. **This lap WIRED Leg 1 end-to-end and pushed deep into Leg 2.**

## 🧠 Context to carry forward — READ THIS
- **`hcorr` is now HALF discharged in-kernel.** The diagonal leg (`S1DiagCorrection`) is fully
  machine-checked, PNT-free, via the **scale trick**: take `x = x(N)` poly-large (decoupled from level
  `N`), so the lattice density `M(N) ≥ N^{6k+1}` dominates the fixed-poly diagonal weight `((C·N³)²)^k`
  (x-independent) ⟹ ratio → 0. NO Möbius cancellation, NO PNT.
- **The whole contour-free y-space `s1` now reduces to ONE clean M-free target:**
  `offDiagMass / B^{+k} → 0`, where `offDiagMass = ∑_{¬diag}(∏λλ)/∏[d,e]`. The `M` genuinely cancels
  (W-trick ⟹ off-diagonal *count* = 0 ⟹ `offdiagCorr = −M·offDiagMass`), confirming in-kernel that the
  scale trick is useless off-diagonal (Leg 2 needs growing `W`, not scale).
- **ALL the "soft" Leg-2 reductions are done** (Finset combinatorics, 2 ported from Aristotle &
  verified): union over coordinate pairs → union over shared primes → per-coordinate factorization →
  `∑_{p>D₀}1/p²` tail. The ONLY remaining nut is **crux (B): the per-prime mass fraction**
  `∑_{(d,e): p|lcm}|λλ|/[d,e] ≤ (C/(p-1))·Q`.
- **KEY REALISATION this lap: crux (B) is ELEMENTARY (PNT-free).** I proved the sharp coefficient bound
  `|yLambda d| ≤ (d/φ(d))·C·∑μ²/φ` is elementary (`abs_yLambda_le_sharp`, via `yLambda_factor` +
  `moebius_sq_eq_one_of_squarefree`), answering lap-9's open sub-question. And (B)'s arithmetic core is
  a per-term **reindex** `d = p·d'`: `φ(p d')=(p-1)φ(d')`, `lcm(p d',e)=p·lcm(d',e)` ⟹ a clean
  `1/(p-1)` factor (Aristotle `0e387e89` is proving `selberg_local_factor`, RUNNING).
- Memory `[[hcorr-diag-pntfree-offdiag-growing-w]]` (updated), `[[yspace-s1-normalization-Bpm-k]]`.

## ✅ State — all `[propext, Classical.choice, Quot.sound]`, no sorry, build green (8302)
**NEW files this lap:**
- **`S1DiagCorrection.lean`** — `diagCorr` (opaque def, dodges the giant-goal whnf loop) +
  `abs_diagCorr_le` (`|diagCorr| ≤ ((C·N³)²)^k`) + **`diag_correction_ratio_tendsto_zero`** (Leg 1:
  diagonal correction `/(B^{+k}·M) → 0`, PNT-free).
- **`S1DiagFree.lean`** — `yspace_s1_heuristic_main_div_sieveB_tendsto_scale` (scale-general main, `x`&`M`
  free) · `offdiagCorr` · `fullCorr_eq_diag_add_offdiag` (split) · **`yspace_s1_sieveSum_div_tendsto_diagFree`**
  (`_primorial`: full `s1` conditional ONLY on off-diagonal, `hBaseW` discharged in-kernel for primorial
  `W`) · `offDiagMass` · `offdiagCorr_eq_neg_mul_offDiagMass` (M-cancellation) · `hoffdiag_of_offDiagMass` ·
  `abs_offDiagMass_le_sum_pairs` · **`yspace_s1_sieveSum_div_tendsto_offDiagMass`** (the cleanest
  statement: full `s1` conditional on M-free `offDiagMass/B^{+k}→0`).
- **`S1OffDiagSize.lean`** (additions) — `offdiag_le_sum_pairs` (Aristotle `c459c135`, verified) ·
  `pair_offdiag_le_sum_primes` · `piFinset_filter_prod_factor` (Aristotle `3de9feb2`, verified).
- **`S1Correction.lean`** (addition) — `abs_yLambda_le_sharp` (sharp PNT-free coefficient bound).
- `PENDING_WORK.md` (lap-10 head) + `ON-LINE-REQUEST.md` (narrowed) updated.

## 🎬 Next actions
1. **Harvest Aristotle `0e387e89`** (`selberg_local_factor`, the crux-(B) per-term reindex identity):
   `aristotle list` (look for IDLE); `aristotle download 0e387e89 --destination /tmp/lf.tar.gz`; verify
   in our kernel + `#print axioms`; port into `S1OffDiagSize`.
2. **Build crux (B) from `selberg_local_factor`:** the single-coordinate per-prime mass fraction
   `∑_{(d,e)∈R×R: p|d, p∤e}(d/φ(d))(e/φ(e))/[d,e] ≤ (1/(p-1))·∑_{(d',e)}(…)` via the reindex bijection
   `d ↦ d/p` on `{d∈R: p|d}` + `selberg_local_factor` per term, then handle the `p|e` part (symmetric,
   smaller) and combine with the sharp coefficient bound (`abs_yLambda_le_sharp`) to get
   `∑_{p|lcm}|λλ|/[d,e] ≤ (C/(p-1))·Q`. This is the last genuinely-mathematical brick.
3. **Assemble `offDiagMass/B^{+k} → 0`** from the soft chain (`abs_offDiagMass_le_sum_pairs` →
   `pair_offdiag_le_sum_primes` → `piFinset_filter_prod_factor` → crux (B) → `sum_finset_recip_sq_le_tail`
   + `recip_sq_tail_tendsto_zero`). **This needs the GROWING-`W` architecture** (`D₀=D₀(N)→∞`,
   `W=W(N)=∏_{p≤D₀(N)}p`) so the `∑_{p>D₀}1/p²` tail → 0. The bookkeeping: each `Q_l = O(log N)`,
   `B^{+k}=(φW/W)^k(log N)^k`, so `∏Q_l/B^{+k}=O(1)` and the tail kills it. A prerequisite sub-lemma:
   `φ(W(N))/W(N) → 0` ⟺ `∏_{p≤D₀}(1-1/p)→0` (∑1/p diverges; check mathlib `Nat.Prime` recip sum).
4. **Always keep ONE Aristotle job in flight** — after harvesting `0e387e89`, submit the next bounded
   brick (e.g. the single-coordinate mass-fraction reindex bijection from #2, or a growing-`W`
   sub-lemma). `c459c135`/`3de9feb2` already harvested; `8681a6f0` (brick_diagprod) was just a
   cross-check of an existing lemma (ignore).

## ⚠️ Gotchas
- **Giant tendsto goals whnf-loop** on `Finset.filter` `DecidablePred` unification (even at 1.6M
  heartbeats). Wrap the giant sum in a `noncomputable def` and state limits about it; unfold once in a
  separate bound lemma. (`[[lean-def-opaque-dodges-whnf-loop]]`)
- **Passing `(M := fun N => …)` to a limit lemma** leaves `(fun N=>…) N` beta-redexes that `ring`
  treats as distinct atoms → `ring failed`. Use `beta_reduce` before `ring`/`rfl`; `simp only []` to
  beta-reduce a hypothesis. To cancel a factor `field_simp` can't (another denom not known ≠0), use
  `mul_div_mul_right _ _ h`. (`[[lean-beta-reduce-before-ring-lambda-args]]`)
- **Aristotle**: `download --destination` wants a `.tar.gz` FILE (dir → IsADirectoryError); `IDLE` =
  finished; always verify in-kernel + `#print axioms` before porting (v4.28 vs our v4.29.1).
- `moebius_sq_eq_one_of_squarefree` / `abs_moebius_le_one` need the `ArithmeticFunction.` prefix here
  (only `moebius` is `open`ed). `pow_le_pow_left₀` (not `pow_le_pow_left`) in v4.29.
- Dev fast: `/tmp/x.lean` importing the module, `taskset -c 0-7 lake env lean /tmp/x.lean`; `#print
  axioms` before trusting. Long-line lint warnings tolerated. Pre-commit runs full `lake build`.

## 📁 Key files
- `BoundedGaps/S1DiagCorrection.lean` — Leg 1 (diagonal correction → 0, PNT-free).
- `BoundedGaps/S1DiagFree.lean` — the assembly: full `s1` conditional only on `offDiagMass/B^{+k}→0`.
- `BoundedGaps/S1OffDiagSize.lean` — the soft Leg-2 reduction chain (union/factorization/tail).
- `BoundedGaps/S1Correction.lean` — `abs_yLambda_le_sharp` (sharp coeff bound) + the lap-9 toolkit.
- `PENDING_WORK.md` (lap-10 head) — the corrected map + the precise remaining Leg-2 chain (A)–(D).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (contour-free
y-space `s1`: Leg 1 DONE, full `s1` conditional only on the M-free off-diagonal `offDiagMass/B^{+k}→0`,
all soft Leg-2 reductions landed, crux (B) on Aristotle). Absorb the context, then start at Next action
#1: harvest Aristotle `0e387e89` (`selberg_local_factor`) and build crux (B) from it.**

🧵 ➡️ 🧵
