# Handoff: s1 deep axiom — all 4 analytic leaves advanced (2,4 DONE in-kernel; 1 decomposed; 3 narrowed)

**Date**: 2026-06-04 13:07 UTC · **Branch**: `path-a-selberg-nu` · HEAD `765e498` ·
**9 commits this lap** (`691a239`…`765e498`; 5 Lean + 4 docs), every Lean commit green at the
pre-commit gate (full `lake build`, 8278 jobs), all new decls `#print axioms`-clean
(`[propext, Classical.choice, Quot.sound]`), all box-local (NEVER pushed).
**Supersedes `HANDOFF-2026-06-04-1159.md`.**

## 🎯 What we're doing
Analytic-NT axiom burn-down for the GPY/Maynard sieve. The central deep axiom
`s1_holds_from_nonprime_asym` (`Sieve.lean:1907`, + s2 sisters) was — after the prior lap's
Cauchy–Schwarz collapse — reduced to **four analytic leaves**. This lap advanced ALL FOUR.

## 🧠 Context to carry forward (read this)
- **The four-leaf model (post-Cauchy–Schwarz).** Main term collapsed to the DIAGONAL block alone;
  s1 = (1) diagonal asymptotic + (2) `∑_{diag}|coeff|=o(main)` + (3) off-diag heuristic main
  `=o(main)` + (4) `IsLittleO` glue. Scoreboard now: **(2) DONE in-kernel, (4) DONE, (1) DECOMPOSED,
  (3) NARROWED.** Only the *inner uniform convergence* (leaf 1's core) and the leaf-3 singular-series
  bound remain genuinely open — both deep.
- **★ Leaf 1 is the headline.** I built `BoundedGaps/WeightedRiemann2D.lean` (new file, axiom-clean)
  that PROVES the GPY 2-D simplex weighted-Riemann limit (`weighted_riemann_2d`, = the in-flight
  Aristotle job `3e2b6a8d`) **modulo ONE clean ingredient**: the inner uniform convergence. The hard
  *outer* assembly is fully proven: `perturbed_riemann` (the MAIN+ERROR analytic core — lifts the
  1-D `riemann_sum_log_weight` to an R-dependent integrand, error squeezed by uniform-bound × bounded
  `(∑|F|/m)/log R`), `two_d_factor` (double sum factors over the outer var), `phi_continuousOn`
  (Φ_G(x)=∫₀^{1-x}G continuous on [0,1]), capstone `weighted_riemann_2d_of_inner`. **So leaf 1's
  remaining nut is now just `inner_uniform` — a 1-D-shaped problem, MUCH cleaner than the monolithic
  2-D.** I wrote `aristotle-inner-uniform/Problem.lean` (typechecks; statement + `riemann_sum_log_weight`
  axiom + a split-at-`R^{1-δ}` proof sketch) ready to submit.
- **Why I didn't grind `inner_uniform` locally:** it's the deep uniform-convergence core (two regimes:
  bulk `m≤R^{1-δ}` via equicontinuity of `u↦G((1-s)u)`, boundary `m>R^{1-δ}` via `1-s` small ⟹ both
  sum and integral `O(δ)`). The crux is the boundary regime (small `R'=⌊R/m⌋`, floor/scale errors not
  uniformly small). It directly duplicates the running Aristotle 2-D job, so per the unblock-guide I
  worked the other leaves instead. The decomposition is also a **hedge**: if the monolithic 2-D job
  fails, submit `inner_uniform` (strictly easier) and combine with my proven outer.
- **Leaf 2 (DONE).** `SieveExpansion.hyperbola_count_le`: k-dim Dirichlet count `#{d∈[1,N]^k:∏dᵢ≤N}
  ≤ N(1+log N)^{k-1}`, proven in-kernel (induction on k + `Fin.cons`/`Fin.tail` `card_bij'` fiber
  bijection + `harmonic_le_one_add_log`). `lattice_count_le_hyperbola` bridges real-R/general lattice;
  `diagonal_weight_le_hyperbola` capstones to `∑_{diag}|coeff| ≤ C²⌊R⌋₊(1+log⌊R⌋₊)^{k-1}`. (Residual:
  the parameter tail `R=o(M log R)` is GPY plumbing, not analysis.)
- **Leaf 4 (DONE).** `Sieve.alphaBound_of_sub_littleO`/`betaBound_of_sub_littleO` (positive-part
  `max(f,0)=O f`, so two-sided diff o(main) ⟹ one-sided obligation) + `alphaBound_of_heuristic_correction`/
  `betaBound_of_heuristic_correction` (split `sieveSum=Aheur+Bcorr`; heuristic limit + correction bound
  ⟹ alphaBound). Machine-checks `leaf 1 ∧ leaf 3 ⟹ s1`. **These cover s2 too** (beta sisters).
- **Leaf 3 (NARROWED).** `SieveExpansion.offdiag_main_abs_le` + `correction_abs_bound_offdiag`: the full
  s1 correction is now bounded by `∑_{diag}|wₚ| + ∑_{¬diag}|wₚ|·mainₚ`, BOTH nonnegative size sums. So
  `correction=o(main)` reduces to leaf 2 (done) + the clean leaf-3 estimate `∑_{¬diag}|w|·main=o(main)`
  (singular-series discrepancy, multi-lap; coupled to the W-trick prime gain).
- **Witness axioms are gated, not lap-tractable.** Probed `mk_54_witness_under_EH` (needs `Mk 54>4`):
  `gen_witness7.py` (deg 7) prints "no positive pivot (M_K≤4)" for K<~160, so deg-7 doesn't reach 4 at
  k=54; needs deg≥8 (huge native_decide) AND rests on the `denom_bridge`/`num_bridge` axioms anyway.
  Defer until bridges land. (Details in `PENDING_WORK.md`.)

## ✅ State (full `lake build` green, 8278 jobs — verified at each pre-commit gate this session)
- `BoundedGaps/WeightedRiemann2D.lean` (NEW, in build graph via `BoundedGaps.lean`): `phi_continuousOn`,
  `perturbed_riemann`, `two_d_factor`, `weighted_riemann_2d_of_inner`.
- `BoundedGaps/SieveExpansion.lean`: `hyperbola_count_le`, `lattice_count_le_hyperbola`,
  `diagonal_weight_le_hyperbola`, `offdiag_main_abs_le`, `correction_abs_bound_offdiag`.
- `BoundedGaps/Sieve.lean`: `alphaBound_of_sub_littleO`, `betaBound_of_sub_littleO`,
  `alphaBound_of_heuristic_correction`, `betaBound_of_heuristic_correction` (after the `betaBound` def).
- Docs: `ANALYTIC_AXIOM_BURNDOWN.md` (2 new dated sections), `PENDING_WORK.md` (leaf scoreboard +
  3-paths-each + witness probe), memory `gpy-diagonalization-route` updated.
- **Aristotle**: monolithic 2-D job `3e2b6a8d` (`weighted_riemann_2d`) STILL RUNNING — ~30% at 2h52m,
  slow, may fail. `aristotle-inner-uniform/Problem.lean` is the prepared cleaner fallback.

## 🎬 Next actions
1. **Poll `aristotle show 3e2b6a8d-dcec-4471-889b-392652f2878a`** (short `timeout 20`, NOT the live
   TUI). If COMPLETE: download, compile in our v4.29.1 kernel, `#print axioms` MUST be clean, then port
   onto `WeightedRiemann2D.weighted_riemann_2d_of_inner`'s shape (or use directly). If FAILED: submit
   `aristotle-inner-uniform/Problem.lean` (the cleaner, strictly-easier target) and keep ONE job in flight.
2. **Then the leaf-1 GPY port** (non-overlapping, needed regardless): connect the repo's Selberg
   diagonal `gpy_diagonal_asymptotic_form` (`∑_{r sf}(φ/r²)z_r²`, `z_r=∑_{(r,s)=1}μ(s)g(rs)/s`) to the
   double-Riemann form `weighted_riemann_2d` addresses — the classical GPY Möbius/singular-series
   manipulation. This is the remaining bridge; substantial.
3. Leaf 3: build the singular-series discrepancy bound for `∑_{¬diag}|w|·main=o(main)` (W-trick prime
   gain + GPY parameter plumbing `W=∏_{p≤D₀}p`, `M=(B−A)/W`, `R=x^{θ/2}`).

## ⚠️ Gotchas
- **OOM on full `lake build`** is a transient fan-out spike (16 lean workers each load Mathlib). Use
  `taskset -c 0-3 lake build`; if it still OOMs, `pkill -9 lean lake` then retry on warm cache (the
  failures hit *downstream* siblings like SymmetricReductionOrbitFree, NOT my changed files).
- `Fin.tail_cons`/`Fin.cons_succ` have a dependent-motive that trips `exact`/`show` — use `change`
  with a type-ascribed `(Fin.cons m d' : Fin (j+2) → ℕ)` then `simp [Fin.tail_cons]`, or `funext`.
- `div_le_div_right` is GONE; use `div_le_div_of_nonneg_right (hab : a≤b) (hc : 0≤c)`.
- Interval-integral primitive continuity = `intervalIntegral.continuousOn_primitive_interval` (over
  `uIcc`); the `..._Icc`/`..._primitive` variants give SET integrals (`∫ t in Icc/Ioc`), wrong type.
- `M ROADMAP.md` + `scratch_*.lean` + `aristotle-*` in `git status` are pre-existing / not mine — leave them.

## 📁 Key files
- `BoundedGaps/WeightedRiemann2D.lean` — THE leaf-1 deliverable (2-D limit ⟹ inner uniform).
- `BoundedGaps/SieveExpansion.lean` — leaf 2 (`hyperbola_count_le`…`diagonal_weight_le_hyperbola`) +
  leaf 3 narrowing (`offdiag_main_abs_le`, `correction_abs_bound_offdiag`).
- `BoundedGaps/Sieve.lean` — leaf 4 glue (`alphaBound_of_*`, `betaBound_of_*`); axiom `s1_…:1907`.
- `aristotle-inner-uniform/Problem.lean` — the prepared cleaner Aristotle target for leaf 1's core.
- `ANALYTIC_AXIOM_BURNDOWN.md` (tail) / `PENDING_WORK.md` (tail) — full leaf scoreboard + attack paths.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (s1 analytic
burn-down; this lap closed leaves 2,4, decomposed leaf 1 to `inner_uniform`, narrowed leaf 3). Start at
Next action #1: poll/harvest Aristotle `3e2b6a8d`; if failed, submit `aristotle-inner-uniform`. Keep ONE
Aristotle job in flight.**

🧵 ➡️ 🧵
