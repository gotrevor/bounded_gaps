# Handoff: μ²/φ y_r-space k-D ladder PROVEN + s1 main term reduced to ONE lemma (Fubini bridge)

**Date**: 2026-06-05 05:12 UTC · **Branch**: `path-a-selberg-nu` · HEAD `540382d`
9 commits this lap (`710bf6b`…`540382d`); every Lean commit green at the pre-commit full-build gate
(8284 jobs). Never pushed. Supersedes `HANDOFF-2026-06-05-0257.md`. (Master `HANDOFF.md` = post-merge
state, left untouched per repo convention.)

## 🎯 What we're doing
Analytic axiom burn-down for the GPY/Maynard sieve. The flagship `bounded_gap_of_Mk_200` rests on 3
math axioms — `BombieriVinogradov`, `s1_holds_from_nonprime_asym`, `s2_holds_from_prime_asym_at_level`
(census re-verified this lap; `H1_le_246` uses the `_eps` variants). This lap drove the **s1** analytic
core (Path Y / `y_r`-space, the convention Trevor already chose). The handoff goal #1 — the `(μ²/φ)`
`y_r`-space k-D Riemann ladder — is **DONE**, and the entire s1 *main term* is now reduced to a single
clean lemma (the simplex-Fubini bridge), proven at k=1 and on Aristotle for k-D.

## 🧠 Context to carry forward
- **The big structural win: I did NOT copy the 850-line bare ladder for the μ²/φ weight. I abstracted
  the WHOLE ladder over an arbitrary nonneg weight `w : ℕ → ℝ`** (`WeightedRiemannGen.lean`), with ONE
  analytic hypothesis `Weighted1DLimit w` (`∀ G cont on [0,1], (∑_{2≤m≤N} G(log m/log N)·w m)/log N →
  ∫₀¹ G`) plus `0 ≤ w n`. Key insight that made this cheap: the **limit side** (`nestedPhi`,
  `nestedPhi_scale`, `nestedPhi_continuous`, `simplexPhi`, Pólya, `exists_list_*`) is
  **weight-independent and reused verbatim** from `WeightedRiemannKD`; only the **sum side** carries the
  weight (every `/(n:ℝ)`→`* w n`, `harmNest`→`harmNestW w`, `0≤1/n`→hyp). The drift heart
  `evalNestW_dist` and the strong-induction `psi_k_pointwise_w` port unchanged in shape. Both the bare
  `1/n` ladder AND the μ²/φ ladder are now instances of `weighted_riemann_kd_w`.
- **Why this matters for s1:** the GPY `s1` denominator constant is `mkF_denominator k F = ∫_{simplex}F²`
  — correct ONLY in `y_r`-space (NOT d-space `∫F'²`, the "derivative landmine" — see
  `[[s1-derivative-landmine]]`). In `y_r`-space the main-term sum is the positive `(μ²/φ)`-weighted
  simplex sum, whose limit IS `∫_simplex F²`. `weighted_riemann_kd_muphi` delivers exactly that.
- **The s1 main term is now reduced to ONE lemma.** `S1ConnectionK1.s1_yr_mainTerm_eq_mkF_denominator_sep`
  is a COMPLETE theorem (separable smooth `F = ∏ Fs i`) modulo a hypothesis `hFubini : ∫_{simplex k}
  ∏gᵢ = nestedPhi (ofFn g) 0`. The `k=1` instance of that bridge IS proven
  (`s1_yr_mainTerm_eq_mkF_denominator_one`, fully axiom-clean). So: **land the Fubini bridge ⇒ the k-D
  s1 analytic main term is PROVEN end-to-end.**
- **The Fubini bridge genuinely needs a Fubini ORDER-SWAP** and there's no cheap dodge: `simplex_fubini`
  peels the integrated coord INNER, while `nestedPhi`/`simplexPhi` are head-recursive (peeled coord
  OUTER). I confirmed the monomial `EpsScaling/dirichlet_slack` route avoids the swap ONLY because the
  monomial head-integral collapses to a closed form (`c^{slack}`) that re-merges as increased slack;
  for general continuous `gᵢ` the head-integral is a general function of the budget and the swap
  resurfaces. So it's ~60–80 lines of real measure theory (product-indicator + `integral_integral_swap`).
  I put it on **Aristotle** rather than duplicate it locally; 3 attack paths are in `PENDING_WORK.md`.
- **What I deliberately did NOT do:** attempt the Fubini bridge locally (on Aristotle, don't duplicate;
  it's a substantial measure-theory lemma). Touch BombieriVinogradov (not in the local PNT+ clone —
  checked; genuine deep wall). The 3 standalone sorries (`Prerequisites:155` geh→eh/Vaughan,
  `Polymath8b:795` twin∨Goldbach, `Zhang:34`) — all correctly triaged as multi-session with result-only
  corollaries already shipping. The `y_r ≈ F` smoothing (Maynard's lemma, the NEXT deep s1 piece after
  Fubini) — deep/architectural, not started.

## ✅ State (all new results axiom-clean `[propext, Classical.choice, Quot.sound]`; full build 8284 jobs)
- **`WeightedRiemannGen.lean`** (NEW, ~640 lines): generic-weight ladder.
  `weighted_riemann_kd_w` / `_aux` / `_head` (headline, unconditional given `hw0`+`Weighted1DLimit w`);
  full sum-side port (`nestedLogSumW`, nonneg/mono, `PsiKW`, factor, cons-of-inner via
  `perturbed_riemann_gen`, inner-uniform, `evalNestW`/`harmNestW`+bridges, `evalNestW_dist`,
  `psi_k_pointwise_w`). Instances: `weighted_riemann_kd_muphi` (μ²/φ), `weighted_riemann_kd_muphi_sep`
  (separable/squared = the s1 const shape), `weighted_riemann_kd_harmonic` (bare 1/n recovered =
  identical to `weighted_riemann_kd`, the validation). `weighted1DLimit_muphi`, `gMoebiusSqTotient_nonneg`.
- **`WeightedMertens.lean`**: NEW `weighted_mertens_continuous` — μ²/φ Mertens for ALL continuous F
  (Weierstrass `exists_polynomial_near_of_continuousOn` + total-mass + 3ε). Axiom-clean.
- **`S1ConnectionK1.lean`** (NEW): `s1_yr_mainTerm_eq_mkF_denominator_one` (k=1 s1 main term =
  mkF_denominator, PROVEN axiom-clean), `mkF_denominator_one_eq` (public re-derivation of the private
  Sieve simplex-1 reduction), `s1_yr_mainTerm_eq_mkF_denominator_sep` (k-D, modulo `hFubini`).
- `BoundedGaps.lean` imports the new modules. `PENDING_WORK.md` updated (progress + 3 Fubini attack paths).

## 🎬 Next actions
1. **Poll the Aristotle Fubini job** `58f76560-3dbb-46be-98ee-2bdf4325079c` (`aristotle tasks
   58f76560-...`; brick at `/tmp/brick_fubini/Fubini.lean`). If COMPLETED: download, VERIFY in-kernel +
   `#print axioms`, port `simplex_integral_prod_eq_nestedPhi` onto repo defs (it already uses
   `Sieve.simplex`, `WeightedRiemannKD.nestedPhi`, `simplexPhi`/`nestedPhi_eq_simplexPhi`,
   `SievePolynomial.simplex_fubini`), then **discharge `hFubini` in
   `s1_yr_mainTerm_eq_mkF_denominator_sep`** → the k-D s1 analytic main term is proven. Then submit the
   next brick (keep Aristotle fed).
2. **If Fubini still IN_PROGRESS / failed:** attack it locally via `PENDING_WORK.md` path 1 (product
   indicator + `MeasureTheory.integral_integral_swap`) — that's the order-swap, the only real gap.
3. **After the main term:** the s1 *correction* term (the `o(1)`) needs BV/EH; and the `y_r ≈
   F(log r/log R)` smoothing (Maynard PartialSummation) is the bridge from `selberg_nu`'s divisor sum
   to the smooth ladder input. Both deep — decompose/scaffold next.

## ⚠️ Gotchas
- **`open scoped ContDiff`** is required for the `∞` notation or `ContDiff ℝ ∞ F` fails with a bare
  "expected token". And `∞ ≠ ⊤` in `WithTop ℕ∞` — to drop `∞→1` use `.of_le (by norm_num)`, NOT
  `.of_le le_top` (which proves `≤ ω`). (Flushed to KB staging.)
- `positivity` ignores hypotheses — for the abstract weight `w` build `0 ≤ w n` facts by hand (`hw0 n`),
  never `positivity`. This recurs throughout `WeightedRiemannGen`.
- Cheap iterate: `taskset -c 0-3 lake build BoundedGaps.WeightedRiemannGen` (~17s) /
  `BoundedGaps.S1ConnectionK1`. Full build (pre-commit gate) = 8284 jobs.
- Re-deriving private Sieve lemmas: `mkF_denominator_one_eq` reproves the `private`
  `integral_simplex_one_eq` via `MeasureTheory.volume_preserving_funUnique (Fin 1) ℝ`.

## 📁 Key files
- `BoundedGaps/WeightedRiemannGen.lean` — the generic-weight ladder + μ²/φ + bare instances.
- `BoundedGaps/S1ConnectionK1.lean` — s1 main term ↔ mkF_denominator (k=1 proven, k-D modulo Fubini).
- `BoundedGaps/WeightedMertens.lean` — `weighted_mertens_continuous` (+ `_sq`, the y_r 1-D core).
- `BoundedGaps/SievePolynomial.lean` — `simplex_fubini`, `dirichlet_slack` (the monomial template).
- `PENDING_WORK.md` — Fubini-bridge 3 attack paths; memory `[[kd-weighted-riemann-ladder]]`,
  `[[s1-derivative-landmine]]`.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (GPY s1
axiom burn-down; the μ²/φ y_r-space ladder is DONE, s1's main term is reduced to the simplex-Fubini
bridge). Absorb the context, then start at Next action #1: poll the Aristotle Fubini job
`58f76560-3dbb-46be-98ee-2bdf4325079c` and, if it's done, port + discharge `hFubini`; otherwise attack
the bridge locally via PENDING_WORK path 1.**

🧵 ➡️ 🧵
