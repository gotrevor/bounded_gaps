# Handoff: contour-free Path-Y `s1` chain CONNECTED end-to-end + B^±k normalisation finding

**Date**: 2026-06-05 09:11 UTC · **Branch**: `path-a-selberg-nu` · HEAD `e2aa0e4`
13 commits this lap (`82debdc`…`e2aa0e4`), every Lean commit green at the pre-commit full-build gate
(8293 jobs). Never pushed. Supersedes `HANDOFF-2026-06-05-0813.md`. Master `HANDOFF.md` untouched per
repo convention.

## 🎯 What we're doing
Analytic axiom burn-down for the GPY/Maynard sieve. Flagship `bounded_gap_of_Mk_200` rests on 3 math
axioms (`BombieriVinogradov`, `s1_holds_from_nonprime_asym`, `s2_holds_from_prime_asym_*`). We build
the **contour-free Path-Y y-space `s1` main term** ALONGSIDE the PNT-gated d-space `selberg_nu` (do
NOT touch the flagship; Trevor flips the axioms later). Prior laps built the y-space sieve + the
diagonalisation capstone `S1YSpace.yr_heuristic_main_eq_muphi`. **This lap connected the whole chain
end-to-end** and found the one structural obstacle left.

## 🧠 Context to carry forward — read this
- **The chain is now machine-checked end-to-end** (all axiom-clean, `[propext, Classical.choice,
  Quot.sound]`, no PNT, no BV):
  `yr_heuristic_main_eq_muphi` (capstone, box product over `Rset_i`)
  → `S1CountReconcile.yr_heuristic_main_eq_quadForm_product` (with `R=N`, `= M·∏ᵢ quadForm`)
  → `S1KDBox.yspace_kd_box_product_tendsto` (`∏ᵢ quadForm/(log N)^k → (φW/W)^k·mkF_denominator`)
  → `S1BoxSimplex.box_product_eq_mkF_denominator` (box constant = simplex denominator).
  So: **y-space S1 heuristic main / (M·(log N)^k) → (φW/W)^k·mkF_denominator k F**, contour-free.
- **THE OBSTACLE — read `PENDING_WORK.md` lap-5 step 3d and memory `[[yspace-s1-normalization-Bpm-k]]`.**
  I tried to push to `alphaBound` and hit a *real* wall: the y-space S1 grows like `(x/W)·(log R)^k`
  (`+k` log power, with the lattice density `M = x/W`), but `Sieve.alphaMainTerm = sieveB^(−k)·x/W`
  carries `(log x)^(−k)` (`−k`, decaying). Opposite powers ⟹ no constant `α` matches. `B^(−k)` is the
  **d-space** convention (same coin as the off-by-a-derivative `∫(F')²` landmine). **⟹ the axiom flip
  must ALSO restate `alphaMainTerm`/`betaMainTerm` to `B^(+k)`** — a flagship convention change =
  **Trevor's architectural call**, NOT something to hand-wave or force. This is the genuine boundary;
  everything feeding it is done. Do NOT try to "make the normalisation work" by inserting fudge
  factors — that would be faking it.
- **Why count→M is otherwise done:** `lattice_count_main_term` (already proven, axiom-clean) gives the
  CRT count `|count − (B−A)/(W·∏q)| ≤ 1`. The handoff-flagged "delicate bit" (every small `r` actually
  appears as a divisor) is the new `S1CandidateSet` (CRT existence + inclusion). The only count→M
  pieces left are (i) the `B^(±k)` convention above and (ii) the off-diagonal `o(main)` correction,
  which is **BV-gated** (`SieveExpansion.correction_abs_bound` exists).

## ✅ State — 4 new files, all axiom-clean, full build green (8293 jobs)
- `S1BoxSimplex.lean` — gap-A4 analytic core. `setIntegral_box_prod` (box Fubini), `mkF_denominator_eq_box`
  (`∫_simplex F²=∫_box F²` for simplex-supported F), **`box_product_eq_mkF_denominator`**
  (`∑_{j,j'}cⱼcⱼ'∏ᵢ∫₀¹Fs_{j,i}Fs_{j',i} = mkF_denominator`).
- `S1KDBox.lean` — `quadForm`, `quadForm_div_log_tendsto`, `quadForm_eq_muphi_sum`,
  `intervalIntegral_eq_setIntegral_Icc`, **`yspace_kd_box_product_tendsto`** (k-D limit → `(φW/W)^k·mkF_den`).
- `S1CandidateSet.lean` — `exists_n_interval_crt` (CRT existence), `mem_sieveDivisors_of_coprime`,
  `filter_Icc_subset_filter_sieveDivisors`, `sieve_interval_lower_bound`/`sieve_interval_covers`,
  `coord_sum_restrict_to_Icc`(`_bilinear`).
- `S1CountReconcile.lean` — `yr_coord_factor_eq_Icc_sum`, `yr_heuristic_main_eq_Icc_product`,
  **`yr_heuristic_main_eq_quadForm_product`** (the chain-connecting capstone).
- `PENDING_WORK.md` lap-5 section + step 3d carry the full map and the B^±k finding.

## 🎬 Next actions
1. **Aristotle brick `65d11d89`** (`brick_coprime_mertens`, the W-coprime sharp Mertens base `hBaseW`
   that makes general-`W` unconditional) was RUNNING ~2.5h+ this whole lap. Poll
   `aristotle list | grep coprime`. If COMPLETE: download, `#print axioms`-verify, port (it discharges
   the `hBaseW` hypothesis threaded through the entire y-space chain). Then submit the next brick.
2. **Do NOT** try to finish `s1` by forcing the normalisation. The `B^(±k)` convention is Trevor's
   call (memory `[[yspace-s1-normalization-Bpm-k]]`). If you want to *advance* it without him: write
   the y-space `alphaMainTerm_yspace k W x := sieveB^((k:ℤ))·(x/W)` and a `selberg_nu_yr`-flavoured
   `alphaBound_yspace`, then assemble `yr_heuristic_main_eq_quadForm_product` + the tendsto into it —
   but flag clearly it's a NEW main-term def, not the flagship `alphaBound`.
3. **Other open work if you want a different thread:** the off-diagonal `o(main)` correction (BV-gated,
   gap C) is the other count→M half; `s2` mirrors `s1` and reuses ALL this machinery (its prime weight
   needs BV even for the main term). `geh_implies_eh` (sorry, Vaughan's identity) is deep/deferred.

## ⚠️ Gotchas
- `quadForm` uses scaling `Real.log N` (N nat); the capstone uses `Real.log R` (R real). They align
  only with **`R = (N:ℝ)`** (`yr_heuristic_main_eq_quadForm_product` sets this). Keep them identified.
- The y-space tendsto needs `Fs` `[0,1]`-supported (`∀ t, 1<t → Fs i t = 0`) so the level-cutoff kills
  large divisors `r>R` in `Rset_i` (`coord_sum_restrict_to_Icc`). The witness `Fs` must satisfy this.
- New `S1*` files: develop in `/tmp/*.lean` that `import BoundedGaps.S1CountReconcile` (built module),
  `lake env lean /tmp/foo.lean` — far faster than rebuilds. `taskset -c 0-7 lake build BoundedGaps.X`.
- `Nat.chineseRemainder` returns a subtype; extract `.prop`/`chineseRemainder_lt_mul` BEFORE
  `obtain`-destructuring or you lose the value-link (cost me one iteration this lap).

## 📁 Key files
- `BoundedGaps/S1CountReconcile.lean` — the chain-connecting capstone `yr_heuristic_main_eq_quadForm_product`.
- `BoundedGaps/{S1BoxSimplex,S1KDBox,S1CandidateSet}.lean` — the three supporting pillars.
- `PENDING_WORK.md` — lap-5 section (top) + step 3d (the B^±k finding, the actionable remaining map).
- `BoundedGaps/Sieve.lean:3024` — the `s1` axiom; `:113` `alphaMainTerm` (the d-space B^−k normaliser).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (contour-free
y-space `s1`, now connected end-to-end). Absorb the context, then start at Next action #1: poll the
`65d11d89` coprime-Mertens brick and port it if done. Do NOT force the `B^±k` normalisation — that's
Trevor's call.**

🧵 ➡️ 🧵
