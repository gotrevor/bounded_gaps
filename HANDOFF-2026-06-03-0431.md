# Handoff: `Mk 200 > 4` landed ON-BOX + unconditional bounded gaps + both bridge base cases proven

**Date**: 2026-06-03 04:31 UTC · **Branch**: `path-a-selberg-nu` · HEAD `b4883be` · 8 commits this lap, all green (8271 jobs).

## 🎯 What we're doing
Discharging the `Mk k > 4` endgame (→ bounded gaps) via the symmetric-reduction → orbit-basis Gram-quotient
→ matchings-closed-form pipeline. The chain `symWeight → Gram quotient → Mk(n+1)>T` feeds the proven
`Targets.H1_le_of_Mk_witness`. **This lap delivered the headline: a kernel-checked `Mk 200 > 4`.**

## 🧠 Context to carry forward
- **THE BIG CORRECTION: the capstone is NOT host-only.** Every prior handoff (through `0320`) claimed the
  heavy `native_decide` "OOMs on the box / is host-only". **That was wrong.** The failure was
  `deep recursion at 'interpreter'` (interpreter stack used in native_decide/decide *setup*), NOT OOM. The
  matchings closed form is genuinely cheap. Fix: `weakLeanArgs = ["--tstack=2000000"]` on `[[lean_lib]]` in
  `lakefile.toml` (weak ⇒ not in trace hash, no full rebuild). All three native_decides + two decides for the
  full k=200 witness run on-box in ~50s. **Do not re-assume host-only — bump tstack first.**
- **Degree-7 frontier is fully mapped & closed.** `M_k(7)` PEAKS at k≈200 (4.003) then *decreases*
  (M_1000≈3.97, M_5511≈3.91) — at fixed degree 7 the space is too small for large k. So k=200 is the optimal
  degree-7 result; no named flagship (M_54>4 needs degree ~20; M_5511>6 etc.) is reachable on-box (degree ~20
  → thousands of orbits AND the `[1^D]×[1^D]` orbit gives `D!` matchData entries → infeasible). The flagships
  remain genuinely out of reach here. Don't re-chase them at degree 7.
- **The ENTIRE remaining math gap on the Mk chain = two axioms**: `denom_bridge` / `num_bridge` (the
  permanent/rook identities, `orbit-sum of ∏(pᵢ+qᵢ)! [·∑g] = matchDenSum/matchNumSum`). Everything else is
  kernel-proven or cited external analytic NT (`BombieriVinogradov`, sieve `s1/s2`, `exists_separable_F` — out
  of scope to prove here, multi-session each). I proved BOTH bridge **base cases** (`La=[]`) by hand this lap;
  the open piece is the shared **inductive step** (the hard research core), which Aristotle is grinding.
- I deliberately did NOT attempt the full bridge inductive step by hand — it duplicates the running Aristotle
  jobs and is a multi-hundred-line orbit-recursion slog. The base cases + orbit machinery are the right
  complementary contribution (build the infra the job's output ports onto).

## ✅ State (all green observed via `lake build`, 8271 jobs)
- `72f80c3` **`MkWitness200.lean`**: `Mk_200_gt_4 : (4:ℝ) < Sieve.Mk 200` — first kernel-checked `Mk k > 4`.
  `#print axioms` = `[3 std, denom_bridge, num_bridge, native_decide]`. Real k=200 D=7 witness (`witnessLCs200`,
  45 orbits, exact-LDL, rational quotient 4.002898), validated through the exact Lean matchings path.
- `fff0e29`/`c2d762c` **`bounded_gap_of_Mk_200`** (∃ admissible 200-tuple H, liminfGap 1 ≤ diameter H) +
  **`liminfGap_one_lt_top`** (H₁ finite) — first UNCONDITIONAL bounded-gaps result (vs `Targets.H1_le_246`
  which is conditional on the unproven `Mk 50 > 4`). Rests on `[3 std, 2 bridges, BombieriVinogradov, sieve
  s1/s2, exists_separable_F, native_decide]`.
- `4fa053c` **`autParts_mul_orbit_card`** (`autParts L · |monoOrbit (ofParts L)| = k.descFactorial |L|`) —
  orbit-size lemma, kernel-clean.
- `2c32b78` **`denom_bridge_nil`** + `prod_getD_fin`; `b4883be` **`num_bridge_nil`** + `sum_getD_fin_gen` —
  both bridge base cases (`La=[]`) PROVEN, kernel-clean `[propext, Classical.choice, Quot.sound]`.
- **Aristotle IN FLIGHT** (both RUNNING ~2h as of 04:31): `0b5bf5be` (`aristotle-denbridge/`) → `denom_bridge`;
  `9d05dbaa` (`aristotle-numbridge/`) → `num_bridge`.
- Docs: `PENDING_WORK.md §A` updated with the on-box milestone + three attack paths for the bridges.

## 🎬 Next actions
1. **Poll Aristotle** (`aristotle list`, one-shot). When `0b5bf5be`/`9d05dbaa` returns: `aristotle download`,
   verify in our kernel + `#print axioms` clean, PORT (`orbitFin L k = monoOrbit (ofParts L)` is **rfl** — both
   are `univ.image (fun σ => fun i => (ofParts L)(σ i))`), replace the `axiom denom_bridge`/`num_bridge` with
   the proof → that Gram side fully kernel-clean → `Mk_200_gt_4` loses that axiom. IMMEDIATELY submit the next job.
2. **If Aristotle stalls, attack the bridge inductive step by hand** (PENDING_WORK §A P2): induction on `La`,
   base case DONE (`denom_bridge_nil`/`num_bridge_nil`). Need the orbit recursion lemma relating
   `monoOrbit (ofParts (a::L))` to `monoOrbit (ofParts L)` (head value `a` lands on a `q`-nonzero slot →
   `(a+b)!`, numPairs+1, OR a `q`-zero slot → `a!`), mirroring `matchData`'s two branches. Hard; consider a
   re-scoped Aristotle job giving it the base case + orbit machinery as `axiom`s.
3. **Optional gilding**: a narrow admissible 200-tuple (H(200)≈1500) via `admissible_of_check_small_primes`
   would make `bounded_gap_of_Mk_200`'s diameter concrete (currently the cheap factorial tuple, 199·200!).

## ⚠️ Gotchas
- **Box intermittent OOM** on `lake build` of *Mathlib* oleans ("Cannot allocate memory" on `.olean`/`.ir`) —
  a flake; **re-run, it clears**. Hit it twice this lap on the pre-commit gate; both cleared on immediate retry.
- The lakefile `--tstack=2000000` is load-bearing for `MkWitness200` (and any future heavy native_decide here).
  For ad-hoc runs: `lake env lean --tstack=400000 <file>`.
- `denom_bridge`/`num_bridge` are intentional DISCLOSED axioms (numerically validated), not accidental sorries.
- `field_simp` closing a goal makes a trailing `ring` error "No goals". `native_decide` triggers a mathlib
  style-linter *warning* (non-fatal; repo uses it elsewhere).

## 📁 Key files
- `BoundedGaps/MkWitness200.lean` — `Mk_200_gt_4`, `bounded_gap_of_Mk_200`, `liminfGap_one_lt_top`, `witnessLCs200`.
- `BoundedGaps/SymmetricReductionOrbitFree.lean` — pipeline; new this lap: `autParts_mul_orbit_card`,
  `prod_getD_fin`, `sum_getD_fin_gen`, `denom_bridge_nil`, `num_bridge_nil` (+ the `denom/num_bridge` axioms).
- `lakefile.toml` — the `weakLeanArgs` tstack.
- `PENDING_WORK.md §A` — milestone + three bridge attack paths. `tools/mk/emit_lean_param.py` — parametric
  witness emitter. `aristotle-{den,num}bridge/` — the in-flight cores.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the context,
then start at Next action #1: poll Aristotle `0b5bf5be`/`9d05dbaa`; if neither is done, go to #2 (bridge
inductive step) or port whatever returned.**

🧵 ➡️ 🧵
