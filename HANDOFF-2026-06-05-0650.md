# Handoff: gap (A) algebraic spine COMPLETE in Path-Y μ²/φ form — sieveSum = y_r-main + correction

**Date**: 2026-06-05 06:50 UTC · **Branch**: `path-a-selberg-nu` · HEAD `0391a3b`
5 commits this lap (`bb2ef25`…`0391a3b`), every Lean commit green at the pre-commit full-build gate
(8288 jobs). Never pushed. Supersedes `HANDOFF-2026-06-05-0615.md`. (Master `HANDOFF.md` = post-merge
state, left untouched per repo convention.)

## 🎯 What we're doing
Analytic axiom burn-down for the GPY/Maynard sieve. Flagship `bounded_gap_of_Mk_200` rests on 3 math
axioms — `BombieriVinogradov`, `s1_holds_from_nonprime_asym`, `s2_holds_from_prime_asym_at_level`
(re-verified this lap: `#print axioms` UNCHANGED). This lap closed the **entire algebraic spine of
gap (A)** (the diagonalization bridge for `s1`) in the Path-Y `μ²/φ` convention.

## 🧠 Context to carry forward
- **The big realization this lap:** the diagonalized Selberg form's NATURAL weight is `φ(r)`
  (`gpy_diagonalize_moebius*`), but the *entire* Path-Y ladder (`WeightedRiemann*`, `S1MainTermDecomp`,
  `gMoebiusSqTotient`) uses `μ²(r)/φ(r)`. There was an unbridged weight-convention gap. The fix is
  EXACT algebra: absorb one `φ(r)` into each inner divisor sum, defining the GPY y-variable
  `y_{i,r} := φ(r)·∑_{r∣d}μ(d)gᵢ(d)/d`, giving termwise
  `∑_{d,e}μ(d)g₁(d)μ(e)g₂(e)/[d,e] = ∑_r (μ²/φ)(r)·y₁ᵣy₂ᵣ` (squarefree: μ²=1; else y=0). This is now
  proven and lifted all the way to `sieveSum = ∑_{j,j'}cⱼcⱼ'·M·∏ᵢ∑_r(μ²/φ)y₁y₂ + correction`.
- **CRITICAL scaling fact — don't mis-state the smoothing:** `y_r = O(1/log R)`, NOT `≈ F(log r/logR)`.
  (For r=1, `y_1 = ∑_{d≤R}μ(d)g(log d/logR)/d = O(1/logR)`.) Path Y works by feeding the **antiderivative**
  𝔉 (𝔉'=F): then `y_r[𝔉] ~ (μ(r)/logR)·F(log r/logR)`, and `∑_r(μ²/φ)y_r² ~ (1/logR)∫F²`. The contour-free
  `μ²/φ` Mertens ladder finishes it. See memory `[[lean-gpy-yform-muphi-bridge]]`, `[[s1-derivative-landmine]]`.
- **brick_smooth (`678a9ed6`) was REDUNDANT** — confirmed this lap. It proved `riemann_log_weight`
  (the `1/n` log-weight Riemann sum → ∫₀¹F), which is **already** the repo's proven
  `WeightedMertens.Riemann.riemann_sum_log_weight` (`RiemannSumLogWeight.lean`, ported from an earlier
  Aristotle job). So gap (B) — the actual y_r smoothing — is **untouched**, NOT what brick_smooth did.
  Don't port it. Downloaded to `/tmp/brick_smooth_x/` if you want to double-check.
- **Antiderivative refactor is NOT safe yet (don't do it blind).** To make the s1 axiom TRUE (it's
  off-by-a-derivative as stated, the landmine), feed `selberg_nu k J c (fun j i => antideriv (Fs j i))`.
  BUT `antideriv F` is `[0,1]`-supported only if `∫₀¹F=0` per coordinate (proven: `antideriv_support_subset`).
  That holds automatically iff Fs is itself a derivative (Maynard §6: variational F = sieve weight f
  differentiated). **OPEN sub-question: does the M_k-optimal F satisfy ∫Fs=0?** If not, the naive
  antiderivative convention is also problematic. Resolve this BEFORE touching the axiom — a differently-false
  axiom is worse than the disclosed landmine. The axiom + its 2 consumers (`selberg_sieve_data_from_F`,
  `_truncated_from_F`, Sieve.lean:3119/3202) would all need the antideriv'd ν (s2 too).
- **Box→simplex coupling (A4):** the yform main term is a BOX product `∏ᵢ∑_{rᵢ∈sieveDivisors}`; the ladder
  wants the SIMPLEX-nested `nestedLogSumW` (`∏rᵢ≤R`). They reconcile because F's simplex support kills
  off-simplex box terms — but F's support is on the full `∑cⱼ∏Fs`, NOT individual products, so the
  reconciliation couples the j-sum with the box→simplex step. Non-trivial.

## ✅ State (all new results axiom-clean `[propext, Classical.choice, Quot.sound]`; full build 8288 jobs)
- **`SieveExpansion.lean`** (4 y-form lemmas): `gpy_diagonalize_moebius_bilinear_yform` (core),
  `gpy_diagonalize_moebius_yform`, `heuristic_main_term_diagonalized_bilinear_yform` (per-coord piFinset),
  **`heuristic_main_selberg_nu_yform`** (full `∑_{j,j'}c` heuristic main in μ²/φ form). Plus
  `gpy_diagonal_yform_eq_zform` (cross-validates y-form vs the existing z-form: `y_r=(μφ/r)z_r`), and the
  capstone **`sieveSum_selberg_nu_yform_add_correction`** (`sieveSum = μ²/φ-main + correction` — the
  entire gap (A) algebra, directly consumable).
- **`Antiderivative.lean`** (2 lemmas): `antideriv_eq_zero_of_vanishing`, `antideriv_support_subset`
  (the `∫F=0 ⟹ 𝔉 is [0,1]-supported` viability condition).
- **Aristotle**: brick_smooth done (redundant, ignore). NEW job submitted & IN_PROGRESS:
  `coprime_muphi_mertens` **UUID `65d11d89-0c78-4350-8aa8-408564d42b33`** (W-coprime sharp Mertens:
  `∑_{n≤N,(n,W)=1}μ²/φ /logN → φ(W)/W`). Stub at `/tmp/brick_coprime_mertens/CoprimeMertens.lean`.
  Genuinely-open, elementary (non-PNT), needed for the `B=φ(W)/W·logx` alphaBound normalization.

## 🎬 Next actions
1. **Poll the coprime-Mertens brick** `65d11d89-0c78-4350-8aa8-408564d42b33`
   (`aristotle tasks <uuid>`; terminal = COMPLETED/FAILED). If COMPLETE: download
   (`aristotle download <uuid> --destination /tmp/x`; it's a gzipped tar), VERIFY in-kernel +
   `#print axioms`, port into `WeightedMertens.lean` (mirrors `weighted_mertens` shape). Then submit the
   next brick (keep ONE in flight).
2. **Decide the next deep thread.** Two real options: (B) the y_r SMOOTHING — the genuine analytic nut,
   needs the antiderivative + Maynard `S1Summation2` partial-summation; hard to make a clean Aristotle
   brick (PNT-adjacent in the signed form, elementary only in the positive y-form). (C) the correction
   `=o(main)` — algebraic split DONE (`correction_abs_bound` etc.), reduces to the parameter tail
   `⌊R⌋(1+log⌊R⌋)^{k-1}=o(B^{-k}x/W)` with `R=x^{θ/2}` (a clean real-analysis inequality once the GPY
   parameters are pinned — but that pinning is part of the larger assembly). Pick one and decompose to
   the narrowest next lemma.
3. **Resolve the `∫Fs=0` sub-question** before any antiderivative axiom refactor (see Context).

## ⚠️ Gotchas
- `field_simp` may fully close a `μ²/φ` goal (no trailing `ring` needed); `rw [sq]` grabs the FIRST `^2`
  in the goal (the `μ(r)²` weight, not your `y²`) — use `ring` instead.
- Aristotle `list` STATUS `IDLE` is AMBIGUOUS (done/canceled/never-run); poll `aristotle tasks <id>` for
  the real terminal state (`COMPLETED|FAILED`). `download` writes a single gzipped-tar FILE to
  `--destination`, not a dir.
- Cheap iterate: `taskset -c 0-5 lake build BoundedGaps.SieveExpansion` (~9s). Full gate = 8288 jobs.

## 📁 Key files
- `BoundedGaps/SieveExpansion.lean` — the y-form diagonalization spine + capstone (this lap's core).
- `BoundedGaps/Antiderivative.lean` — antiderivative convention + support viability.
- `BoundedGaps/S1MainTermDecomp.lean` — the μ²/φ ladder target (`nestedLogSumW → ∫_simplex F²`, DONE).
- `BoundedGaps/Sieve.lean:3024` — the `s1` axiom (the landmine; antideriv refactor target).
- `PENDING_WORK.md` — refined gap (A) map (A1/A2 done, A3=B/A4/A5=C remain) at top.
- `archive/findings/ON-LINE-FINDINGS-2026-06-04-gpy-diagonal-asymptotic.md` — the SETTLED Path-Y convention.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (GPY s1 axiom
burn-down; the gap (A) algebraic spine is now COMPLETE in Path-Y μ²/φ form). Absorb the context, then
start at Next action #1: poll the coprime-Mertens brick `65d11d89-…`, then pick the next deep thread (B
smoothing or C correction tail).**

🧵 ➡️ 🧵
