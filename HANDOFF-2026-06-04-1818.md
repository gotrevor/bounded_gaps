# Handoff: `separable_dense_sup` family FULLY DISCHARGED in-kernel (box-tensor) — `bounded_gap_of_Mk_200` now 3 axioms

**Date**: 2026-06-04 18:18 UTC · **Branch**: `path-a-selberg-nu` · HEAD `5c3a83c`
**7 commits this lap** (`178d42a`…`5c3a83c`; all green at the pre-commit full-build gate, 8280 jobs).
Never pushed. **Supersedes `HANDOFF-2026-06-04-1551.md`.**

## 🎯 What we're doing
Analytic/functional-analysis axiom burn-down for the GPY/Maynard sieve. Last lap had narrowed the
separable-realisation of `M_k` down to a single pure-density axiom `separable_dense_sup`. **This lap
DISCHARGED it (and its truncated sister) completely in-kernel** via a box-tensor construction — no
more density axiom anywhere.

## 🧠 Context to carry forward
- **The win:** `separable_dense_sup` and `separable_dense_sup_truncated` are now THEOREMS
  (`#print axioms = [propext, Classical.choice, Quot.sound]`). Consequently
  `exists_separable_F_of_Mk_gt` is fully axiom-clean, and **`bounded_gap_of_Mk_200` dropped 4→3
  mathematical axioms** — now `[BombieriVinogradov, s1_holds_from_nonprime_asym,
  s2_holds_from_prime_asym_under_EH] + native_decide`. `H1_le_246` likewise no longer touches any
  separable-density axiom (verified `#print axioms`). The whole `separable_dense_*` family (base +
  eps + truncated) is machine-checked.
- **The construction (all in `Sieve.lean`):** (1) inward centroid expansion `E t = b + r·(t−b)`,
  `b=(1/(k+1))ᵢ`, `r=1+h_F` where `h_F` = modulus mesh of `F` at `δ/2` (`exists_uniform_modulus`);
  `F₁=F∘E` is smooth, `|F−F₁|≤δ/2` on the simplex (`‖E t−t‖_∞ ≤ r−1 = h_F` since `‖t−b‖_∞≤1` there),
  and support strictly interior (margin `≥(1−r⁻¹)/(k+1)` per face, got by inverting E:
  `cφ−b = (E cφ−b)·r⁻¹`). (2) box-tensor `G = ∑_φ F₁(c_φ)·∏ᵢ meshBump h (φ i)(t_i)`, grid
  `c_φ=(φ i·h)ᵢ`, mesh `h=min(h_F/r, m₀/(k+1))`, `N=⌈1/h⌉`; `|F₁−G|≤δ/2` (`box_tensor_approx`),
  separable+smooth (`isFiniteSeparable_/contDiff_tensor_sum`); margin > mesh keeps support(G)⊆simplex.
  Triangle ⟹ `|F−G|≤δ`. The **truncated** sister uses interior point `c·𝟙`, `c=min(α,1/(k+1))/2`,
  so `c≤α/2` and `kc≤1/2` make the single mesh bound `h≤c(1−r⁻¹)` dominate all THREE faces
  (`t_i=0`, `t_i=α`, `∑=1`); needs `0<α` (threaded from the caller).
- **Reusable in-kernel cores landed this lap:** `abs_sub_weighted_average_le` (convex-comb sup
  bound), `meshBump`/`_nonneg`/`_contDiff`/`_partition`/`_support` (mesh-h 1-D PoU family),
  `box_tensor_approx` (modulus-of-continuity glue), `exists_uniform_modulus`.
- **Aristotle sepdense `c46a7778` was a DEAD END — don't re-trust it.** It was submitted with
  `ContDiff ℝ ⊤` (= C^ω **analytic**, not C^∞); an analytic function with compact support is ≡0, so
  it proved a VACUOUS statement. Our axiom is `ContDiff ℝ ∞` (= C^∞, genuine). If you ever hand a
  density problem to Aristotle, write `ContDiff ℝ (⊤ : ℕ∞)` explicitly. (Memory `sepdense-aristotle-deadend`.)
- **Why I stopped here (not fixation):** I inventoried ALL open axioms. Everything remaining is
  genuinely (a) external (BV/GEH/GeneralizedBV/MPZ — cite-only), (b) paper-gated (s1/s2 GPY
  asymptotic — see `ON-LINE-REQUEST.md`, still open and accurate), or (c) multi-session-deep
  (`geh_implies_eh` needs a faithful body for the opaque `axiom GEH : ℝ→Prop` + Vaughan's identity;
  the k-D weighted-Riemann generalisation of the proven `weighted_riemann_2d` is a multi-lap
  induction). SieveExpansion / SingularSeries (1-D) / WeightedRiemann2D have **no** open
  sorries/axioms — the GPY *algebra* is all done; only the gated analytic constant remains.

## ✅ State (full `lake build` green, 8280 jobs, observed at pre-commit on every commit)
- `separable_dense_sup`, `separable_dense_sup_truncated`, `exists_separable_F_of_Mk_gt`,
  `exists_separable_F_truncated_of_Mk_truncated_gt`: all `[propext, Classical.choice, Quot.sound]`.
- `bounded_gap_of_Mk_200` axioms (verified this lap): `[…, BombieriVinogradov,
  s1_holds_from_nonprime_asym, s2_holds_from_prime_asym_under_EH, native_decide…]`.
- **Aristotle — ONE of my jobs running:** `b6429b9f` (`aristotle-phisq`, task IN_PROGRESS ~3h) — the
  squarefree `φ(r)/r²` Mertens asymptotic, a reusable GPY-NT brick. `c46a7778` (sepdense) is the dead
  end above — do NOT port it.

## 🎬 Next actions
1. **Poll Aristotle phisq** (`aristotle tasks b6429b9f-6798-47bc-b60f-ab2d8b1ed4f4` — the one-shot,
   task-level status; project-level `IDLE` is ambiguous). If COMPLETE: download, VERIFY in our v4.29.1
   kernel + `#print axioms` (Aristotle pins v4.28 — a passing `grind` can inject `sorryAx`), then port.
   Then submit the NEXT job (phisq frees the slot; rate-limit = one at a time).
2. **The real remaining wall = the GPY port (s1/s2).** It is paper-gated for the singular-series
   normalisation; `ON-LINE-REQUEST.md` has the precise ask (Polymath8b §3 / Maynard §5-6 intermediate
   identities). Do NOT guess the constant. If a networked lap left an `ON-LINE-FINDINGS-*.md`, harvest it.
3. **Otherwise pick a non-gated infrastructure thread** (per the unblock guide — inventory is in
   `PENDING_WORK.md` §B/§Z): the k-D weighted-Riemann generalisation of `weighted_riemann_2d`
   (`WeightedRiemann2D.lean` / `InnerUniformReduction.lean`) is the cleanest non-paper-gated analytic
   prerequisite — but it's a multi-lap induction (re-run the inner-uniform argument per level); "special
   case first" → try `weighted_riemann_3d`. Or the multidimensional singular series 𝔖(H) Euler product
   (reuse `SharpMertens.eulerProduct_tprod`; convergence is standard but the normalisation convention
   leans on the GPY paper).

## ⚠️ Gotchas
- **Cheap iterate:** `taskset -c 0-3 lake build BoundedGaps.Sieve` (~30s, pulls no heavy
  `native_decide` modules, never OOMs). Pay the full-build tax only at commit (pre-commit hook runs it).
- **Two Lean perf traps hit this lap** (flushed to staging buffer): (i) `refine lemma (lam) (lam) …`
  over a big `Finset.sum` → `isDefEq` heartbeat timeout; fix = prove side-conditions as `have`s first,
  then `exact lemma a _ _ … h1 h2 h3` (pins the lambdas by pattern). (ii) `nlinarith` times out in the
  huge local context of the density proofs; fix = explicit `ring`-identity `have` + plain `linarith`.
  Also `Nat.cast_nonneg k` needs ascription `(… : (0:ℝ) ≤ (k:ℝ))` or typeclass resolution gets stuck.
- `M ROADMAP.md` + `aristotle-*/` dirs in `git status` are pre-existing/scratch.

## 📁 Key files
- `BoundedGaps/Sieve.lean` — the box-tensor density: `meshBump*` (~2155), `abs_sub_weighted_average_le`,
  `box_tensor_approx`, `exists_uniform_modulus`, `separable_dense_sup` (~2314, the construction),
  `separable_dense_sup_truncated` (~2562), and the prior 4 cores (`tensor_partition_of_unity` etc).
- `PENDING_WORK.md` §B (separable family DONE) + §Z (GPY s1/s2 attack paths).
- `ON-LINE-REQUEST.md` — the GPY-port singular-series asymptotic (open, paper-gated).
- `BoundedGaps/{SieveExpansion,SingularSeries,WeightedRiemann2D,InnerUniformReduction}.lean` — GPY
  thread, algebra fully done, frontier = the gated analytic constant.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (sieve
axiom burn-down; the separable-density family is now fully discharged). Start at Next action #1:
poll Aristotle phisq and harvest if ready; then either advance the GPY port via `ON-LINE-REQUEST`
findings, or take a non-gated infrastructure thread (k-D weighted Riemann / singular series).**

🧵 ➡️ 🧵
