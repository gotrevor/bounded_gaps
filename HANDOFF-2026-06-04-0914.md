# Handoff: Sharp Mertens FULLY UNCONDITIONAL — sub-step (c) 1-D keystone DONE

**Date**: 2026-06-04 09:14 UTC · **Branch**: `path-a-selberg-nu` · HEAD `7b65419` ·
**10 commits this lap** (`c5ec483`…`7b65419`), all green (full `lake build`, 8275 jobs,
verified at every pre-commit gate), all axiom-clean `[propext, Classical.choice, Quot.sound]`,
all box-local (NEVER pushed). **Supersedes `HANDOFF-2026-06-04-0820.md`.**

## 🎯 What we're doing
Analytic-NT axiom burn-down: turn the GPY/Maynard sieve-asymptotic axioms (`Sieve.lean`
`s1`/`s2`) into real proofs. The prior handoff had sharp Mertens `∑_{n≤x}μ²/φ ∼ log x`
(coeff EXACTLY 1, the sub-step (c) 1-D keystone) reduced to 2 Aristotle "leaves". **This lap
cracked BOTH leaves directly and made sharp Mertens fully unconditional.**

## 🧠 Context to carry forward
- **The lap's headline correction**: prior laps believed the two Euler-product bounds were
  "genuinely hard locally, use Aristotle" (mathlib lacks the *partial-sum* Euler product). That
  was a **miscalibration**. The trick: `bAF e ≠ 0` forces all prime exponents ≤ 2, so any `e≤N`
  in the support **divides the primorial-squared `(N#)²`**; then the bound is a **divisor-sum**
  Euler product, which mathlib HAS (`multiplicative_factorization`, `coe_zeta_mul_apply`,
  `IsMultiplicative.map_prod`). Aristotle (babs/blog) was never needed — I cancelled blog.
  → flushed as a reusable KB insight `lean-primorial-sq-divisor-euler-product` (staging buffer).
- **babs `830e5129` independently corroborated bound #1** (it finished, axiom-clean, same
  Euler/primorial→exp strategy) — nice cross-check, but not used.
- **Front #4 (the genuine remaining sub-step (c) nut) is the next thread**, and it's blocked on
  an async Aristotle job I just dispatched (see Next actions). That's WHY this is a handoff point
  rather than continuing — front #4's analytic heart is grinding remotely.

## ✅ State (full `lake build` green, 8275 jobs, all new decls axiom-clean — verified this session)
All in `BoundedGaps/SharpMertens.lean` (NEW decls this lap):
- **`sharp_mertens_unconditional : (∑_{n≤N} μ²/φ)/log N → 1`** — the milestone, fully axiom-clean,
  NO hypotheses. Caps sub-step (c)'s 1-D keystone.
- `sum_norm_bAF_le : ∑_{e≤N}‖bAF e‖ ≤ exp 2` (bound #1) and
  `sum_norm_bAF_log_le : ∑_{e≤N}‖bAF e‖·|log e| ≤ 4·exp2·∑'4n^{-3/2}` (bound #2) — both proved
  directly on `bAF`, both axiom-clean.
- Reusable bricks: `gabs` (=`|bAF|` as a multiplicative `ArithmeticFunction`), `gabs_sum_divisors_mul`,
  `sum_gabs_divisors_primorial_sq_le`, `sum_gabs_divisors_multiples_le` (the (C) restricted sum),
  `prod_primes_factorization_le_one`, `primorial_factorization_le_one`, `primorial_sq_primeFactors`,
  `log_le_two_sum_log_primeFactors` (A), `term_log_div_le`/`summable_four_div_rpow`/
  `sum_log_div_consecutive_le`/`sum_log_div_primes_le` (analytic crux `∑_p (log p)/(p(p-1)) < ∞`),
  `gabs_prime_add_sq`, `summable_norm_bAF`, `sharp_mertens_of_log_bound`.
- Memory `sharp-mertens-route` + `mertens-sharp-vs-crude` updated to DONE; `ON-LINE-REQUEST.md`
  sharp-Mertens item marked FULLY RESOLVED (deletable, no online dep); `ANALYTIC_AXIOM_BURNDOWN.md`
  has the milestone + front-#4 plan appended.
- **Aristotle**: `aristotle-wmertens` **`930e468a` RUNNING** — the front-#4 analytic heart, a pure
  Riemann-sum lemma `(∑_{n≤R}(1/n)F(log n/log R))/log R → ∫₀¹F` (self-contained, in
  `aristotle-wmertens/Problem.lean`). babs/blog both cancelled/done (no longer relevant).

## 🎬 Next actions
1. **Poll `aristotle show 930e468a-8e55-472b-8915-140d1abd7b09`** (one-shot, not the live TUI).
   When it returns: download (gzip tarball → `tar xzf`), VERIFY `#print axioms` clean in our
   v4.29.1 kernel, then PORT the Riemann-sum lemma into the repo (it's number-theory-free).
2. **Build front #4 = the WEIGHTED Mertens** `∑_{d≤R}(μ²/φ)·F(log d/log R) ∼ (∫₀¹F)·log R`:
   Abel summation (`Mertens.abel_summation_identity:659`) against `sharp_mertens_unconditional`
   bridges the arithmetic weight `g=μ²/φ` to the `1/n` model, then the wmertens Riemann lemma
   closes it. This is what feeds `alphaBound` / `s1_holds_from_nonprime_asym` (`Sieve.lean`).
3. While #1 grinds, the Abel-summation REDUCTION (algebraic, `g → 1/n` model) is local work that
   does NOT depend on the Riemann limit — can be set up in parallel.
4. If front #4 stalls, alternative open threads (NOT this thread): SieveExpansion sub-step (b)
   off-diagonal/assembly glue (needs W/D₀ GPY-parameter plumbing), `num_bridge`/`denom_bridge`
   (combinatorial), numerical `Mk`-witness axioms (Family C, `native_decide` route).

## ⚠️ Gotchas
- API names that bit (all in the KB insight): `div_le_div_iff₀` (NOT `div_le_div_iff`),
  `Finsupp.notMem_support_iff` (NOT `not_mem_`), `Nat.Coprime.pow 2 2 h` (exponents explicit-first),
  inside mathlib `namespace Nat` a bare `Prime p` means `Nat.Prime p` (so pass `hp : p.Prime`
  directly, not `hp.prime`), `Real.log_le_rpow_div` (ε=½ ⟹ `log x ≤ 2√x`).
- Box OOM flake on `lake env lean` (~1/few runs) — re-run, `taskset -c 0-3` helps.
- Aristotle pins v4.28; this repo is v4.29.1 — ALWAYS `#print axioms` a ported result (grind can
  silently inject `sorryAx`). The wmertens lemma is pure analysis so the port should be clean.

## 📁 Key files
- `BoundedGaps/SharpMertens.lean` — THE deliverable; `sharp_mertens_unconditional` + all bricks.
- `BoundedGaps/Mertens.lean` — `abel_summation_identity:659` (for front #4), `mertens_theta_log`.
- `BoundedGaps/Sieve.lean` — `s1_holds_from_nonprime_asym:1907`, `alphaBound:132` (the target).
- `ANALYTIC_AXIOM_BURNDOWN.md` — the burn-down map (milestone + front-#4 plan at the bottom).
- `aristotle-wmertens/Problem.lean` — the in-flight Riemann-sum job (`930e468a`).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait
for instructions, and don't offer other projects from the KB — this doc IS the chosen thread
(analytic-NT sieve burn-down; sub-step (c)'s 1-D keystone `sharp_mertens_unconditional` just landed
fully unconditional). Start at Next action #1: poll/harvest Aristotle `930e468a`, port the
Riemann-sum lemma, then build front #4 (weighted Mertens via Abel summation). Keep ONE Aristotle
job in flight at all times.**

🧵 ➡️ 🧵
