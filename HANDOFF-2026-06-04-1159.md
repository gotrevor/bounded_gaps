# Handoff: s1/s2 main term — Cauchy–Schwarz COLLAPSE to diagonal + obligation #2 reduced to one count

**Date**: 2026-06-04 11:59 UTC · **Branch**: `path-a-selberg-nu` · HEAD `6c9f39e` ·
**13 commits this lap** (`af0a3e6`…`6c9f39e`), every Lean commit green (full `lake build`, 8277
jobs, verified at each pre-commit gate), all new decls `#print axioms`-clean
(`[propext, Classical.choice, Quot.sound]`), all box-local (NEVER pushed).
**Supersedes `HANDOFF-2026-06-04-1103.md`.**

## 🎯 What we're doing
Analytic-NT axiom burn-down toward the GPY/Maynard sieve asymptotics. The deep axiom
`s1_holds_from_nonprime_asym` (`Sieve.lean:1907`, + s2 sisters) is the central remaining gap. The
prior lap machine-checked the ENTIRE algebraic reduction for the **single-`F` `j=j'` diagonal**.
This lap did the **general (non-separable, `J`-basis) CROSS-TERM** algebra AND — the headline —
proved a **Cauchy–Schwarz collapse** that reduces the whole main-term asymptotic to the *diagonal
block alone*, then reduced the correction's diagonal size bound to a *single* divisor-count estimate.

## 🧠 Context to carry forward (the mental model — read this)
- **The s1 obligation now bottoms out in exactly FOUR analytic leaves** (everything algebraic is
  done): (1) the **diagonal asymptotic** `B(F,F) = ∑_{r sf}(φ(r)/r²)z_r² → I(F)·const` as `R→∞`
  (THE deep nut; smooth core = the in-flight Aristotle 2D job); (2) the **k-dim Dirichlet divisor
  count** `Dₖ(R) = #{d : ∏dᵢ≤R} ≍ R(log R)^{k-1}` (now the ONLY missing piece for the correction's
  diagonal size bound); (3) **off-diagonal heuristic main = o(main)** (singular-series discrepancy,
  needs the W-trick prime gain); (4) **`IsLittleO` glue** into `alphaBound`.
- **★ THE KEY INSIGHT this lap — Cauchy–Schwarz collapse.** The Selberg bilinear form
  `B(g₁,g₂)=∑μ(d)g₁(d)μ(e)g₂(e)/[d,e]` diagonalizes to a φ-weighted inner product `∑_r φ(r)y₁ᵣy₂ᵣ`,
  so `B(g₁,g₂)² ≤ B(g₁,g₁)·B(g₂,g₂)` (`gpy_bilinear_cauchy_schwarz`, via
  `Finset.sum_mul_sq_le_sq_mul_sq`). **Consequence:** the `J²`-block main term
  `∑_{j,j'}cⱼcⱼ'∏ᵢB(Fⱼᵢ,Fⱼ'ᵢ)` is controlled entirely by the DIAGONAL blocks — once the single
  diagonal limit `B(F,F)→I(F)` is known, the cross terms are automatic. **So leaf (1) is the only
  main-term analysis left; don't go hunting for a separate cross-term asymptotic.**
- **Obligation #2 chain (correction diagonal size).** Three bricks reduce it to leaf (2):
  `selberg_nu_basis_diagonal_reassemble` (the `j,j'`-sum on the diagonal reassembles the JOINT `F²`
  — this is *why* the earlier per-block weight factorization was loose: simplex support lives on the
  joint `F`, not the `Fⱼᵢ`), `support_simplex_prod_le` (`F(log d/logR)≠0 ⟹ ∏dᵢ≤R`),
  `support_simplex_bounded` (continuous simplex-supported `F` is bounded ⟹ `‖F‖∞`). Capstone
  `diagonal_weight_le_count`: diagonal weight `≤ ‖F‖∞²·#{d:∏dᵢ≤R}`. With `R=x^{θ/2}≪x≈MW`, leaf (2)
  `Dₖ(R)≍R(log R)^{k-1} = o(M(log R)^k)` closes #2.
- **Diagonal `O(1)` error is already wired:** `lattice_count_main_term` gives exactly
  `|count − M/∏[Pᵢ]| ≤ 1` (with `M=(B−A)/W`), the precise hypothesis `diag_error_bound` needs.
- **Why I didn't grind leaf (2) locally:** the bound is fully provable (`harmonic_le_one_add_log`
  exists in mathlib + `dirichlet_hyperbola` is already in `SingularSeries.lean`), but the k-induction
  needs fiddly `Fin.cons`/`piFinset` partition-by-coordinate-0 manipulation — an ideal *self-contained
  Aristotle* problem. I architected it (see Next actions / `aristotle-hypercount/Problem.lean`) rather
  than burn lap budget on `Fin` plumbing.
- **s2 is covered for free on the algebra side:** the bilinear diagonalization + Cauchy–Schwarz are
  the same quadratic form; s2 differs only in the count (EH/MPZ level of distribution). No separate
  s2 diagonalization lemmas needed.

## ✅ State (full `lake build` green, 8277 jobs — verified this session)
All new decls in **`BoundedGaps/SieveExpansion.lean`** (appended after `gpy_quadform_moebius_nonneg`):
- Cross-term ladder: `gpy_diagonalize_bilinear`, `gpy_diagonalize_moebius_bilinear`,
  `gpy_diagonalize_moebius_bilinear_squarefree`, `gpy_diagonal_asymptotic_form_bilinear`.
- Concrete lattice: `sieveDivisors_pos`, `sieveDivisors_dvd_closed`.
- Full main term (canonical): `heuristic_main_term_diagonalized_bilinear[_canonical]`,
  `heuristic_main_selberg_nu_diagonalized`, `heuristic_main_selberg_nu_canonical`.
- Correction weight: `piFinset_sum_abs_prod_factor`, `correction_weight_factor`,
  `sum_prod_abs_mul_factor`, `correction_weight_factor_split` (loose — see context).
- **`gpy_bilinear_cauchy_schwarz`** (the collapse).
- Obligation #2: `selberg_nu_basis_diagonal_reassemble`, `support_simplex_prod_le`,
  `support_simplex_bounded`, `diagonal_weight_le_count`.
- Docs: `ANALYTIC_AXIOM_BURNDOWN.md` (2 new dated sections), `PENDING_WORK.md` §Z update, memory
  `gpy-diagonalization-route` updated + staging insight `lean-selberg-bilinear-cauchy-schwarz-collapse`.
- **Aristotle**: 2D job `3e2b6a8d` (`weighted_riemann_2d`, the diagonal-asymptotic smooth core)
  STILL RUNNING — ~25% at 1h45m, slow, may fail. The older `627d10e3` (`dirichlet_hyperbola`)
  COMPLETED and was already harvested into `SingularSeries.lean` (no action needed).

## 🎬 Next actions
1. **Poll `aristotle show 3e2b6a8d-dcec-4471-889b-392652f2878a`** (short `timeout 15`, NOT the live
   TUI). If COMPLETE: download, compile in our v4.29.1 kernel, `#print axioms` MUST be clean, port,
   then build leaf (1) — the diagonal asymptotic `∑_{r sf}(φ(r)/r²)z_r² → const` on
   `gpy_diagonal_asymptotic_form`. If FAILED: re-architect smaller (rank-1 `∑F/n·∑G/n` first).
2. **Submit the queued k-dim count job** when the slot frees: `aristotle-hypercount/Problem.lean`
   already written (statement `hyperbola_count_le`, proof `sorry`, induction sketch + mathlib hints
   in the docstring). On return: verify + port; it discharges leaf (2) and (with
   `diagonal_weight_le_count`) closes obligation #2's diagonal size bound entirely.
3. Then attack leaf (3) off-diagonal singular series (`lattice_count_offdiag_vanish_Wtrick` +
   admissibility/W-trick plumbing) and leaf (4) the `IsLittleO` glue into `alphaBound`.

## ⚠️ Gotchas
- `SieveExpansion.lean` opens only `ArithmeticFunction (moebius)` — qualify `ArithmeticFunction.`
  for `moebius_eq_zero_of_not_squarefree`, `moebius_sq_eq_one_of_squarefree`.
- v4.29 renames that bit this lap: `Fintype.mem_piFinset` (not `Finset.`), `pow_le_pow_left₀`
  (not `pow_le_pow_left`), `Real.log_prod` takes `(hf)` with `s,f` *implicit*.
- Aristotle pins v4.28; repo is v4.29.1 — ALWAYS `#print axioms` a ported result.
- `M ROADMAP.md` + `scratch_*.lean` + `aristotle-*` in `git status` are pre-existing / untracked
  (not mine) — leave them.

## 📁 Key files
- `BoundedGaps/SieveExpansion.lean` — THE deliverable (now ~1830 lines; the full cross-term
  diagonalization + Cauchy–Schwarz collapse + obligation-#2 reduction chain).
- `BoundedGaps/Sieve.lean` — `s1_holds_from_nonprime_asym:1907`, `selberg_nu:1872`, `simplex:503`,
  `alphaBound:132`, `mkF_denominator:510`.
- `BoundedGaps/SingularSeries.lean:115` — `dirichlet_hyperbola` (the 1-D interchange; iterate for Dₖ).
- `ANALYTIC_AXIOM_BURNDOWN.md` (tail) — the four analytic leaves, precisely stated.
- `aristotle-hypercount/Problem.lean` — the queued k-dim count job (leaf 2).
- `aristotle-wmertens2d/Problem.lean` — the in-flight 2D job (leaf 1 smooth core).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (analytic-NT
sieve burn-down; this lap collapsed the main term to the diagonal via Cauchy–Schwarz and reduced
obligation #2 to a single divisor count). Start at Next action #1: poll/harvest Aristotle `3e2b6a8d`,
then build the diagonal asymptotic. Keep ONE Aristotle job in flight (submit `aristotle-hypercount`
when the 2D slot frees).**

🧵 ➡️ 🧵
