# Handoff: s1/s2 sieve axioms — entire ALGEBRAIC reduction (sub-step a→c) machine-checked

**Date**: 2026-06-04 11:03 UTC · **Branch**: `path-a-selberg-nu` · HEAD `e827b8c` ·
**13 commits this lap** (`6631c7c`…`e827b8c`), every Lean commit green (full `lake build`, 8277
jobs, verified at each pre-commit gate), all new decls `#print axioms`-clean
(`[propext, Classical.choice, Quot.sound]`), all box-local (NEVER pushed).
**Supersedes `HANDOFF-2026-06-04-1014.md`.**

## 🎯 What we're doing
Analytic-NT axiom burn-down toward the GPY/Maynard sieve asymptotics. The prior lap finished the
**1-D weighted Mertens** keystone. This lap attacked the multidimensional lift — the deep axioms
`s1_holds_from_nonprime_asym` / `s2_*` (`Sieve.lean:1907+`) — and **machine-checked their ENTIRE
algebraic content** (sub-steps (a) expand → (b) count → (c) diagonalize → reduction), leaving only
purely-analytic obligations.

## 🧠 Context to carry forward
- **Headline: sub-step (c) is no longer "multi-month" on the algebra side — it's DONE.** The GPY
  diagonalization of the Selberg quadratic form is *pure finite algebra* (`Nat.sum_totient`:
  `gcd=∑_{r∣gcd}φ(r)`, `Nat.gcd_mul_lcm`, `Finset.prod_univ_sum`: `∏∑=∑_P∏`). The old framing routed
  this to "deep machinery"; that was wrong, same recalibration story as sharp/weighted Mertens. I did
  all of it locally, no Aristotle.
- **The reduction strategy that worked:** isolate the analytic obligation by *exact algebraic
  identity*. `sieveSum = heuristic_main + correction` where `heuristic_main` replaces the lattice
  count by its GPY main value `M/∏ᵢ[Pᵢ]` (any `M`), and `correction = ∑_P coeff·(count − M/∏[Pᵢ])`.
  Then `correction_split_offdiag` splits the correction (given off-diagonal count = 0, the W-trick
  fact) into **diagonal `O(1)` error** + **off-diagonal heuristic main**, and `diag_error_bound`
  bounds the first by `∑_{diag}|coeff|`. Net: the deep axiom is reduced to 3 crisp analytic nuts.
- **What the heuristic main factors into:** `heuristic_main_term_diagonalized` ⇒ `M·∏ᵢ(∑_r φ(r) yᵣ²)`,
  and `gpy_diagonal_asymptotic_form` puts each 1-D factor in canonical form
  `∑_{r squarefree}(φ(r)/r²)·(∑_{(r,s)=1}μ(s)F(log rs/logR)/s)²` — the exact object the `R→∞`
  asymptotic must evaluate. `φ(r)/r²` is multiplicative; the inner `zᵣ` is a coprime-restricted
  Möbius sum.
- **Mental model of what's LEFT (all analytic, no algebra):** (1) **diagonal-sum asymptotic**
  `∑_{r sf}(φ(r)/r²)zᵣ² → I(F)`-type const — the genuine GPY main-term computation; its
  multidimensional *smooth* core is the in-flight Aristotle job. (2) **`∑|coeff| = o(main)`** —
  a divisor-sum size bound (needs `R = x^{θ/2}` small enough; the naive `∏|F|·R^{2k}` bound is too
  weak for `k≥2`, so the support restriction of `F` to the simplex matters). (3) **off-diagonal
  main = o(main)** — the singular-series discrepancy. (4) `IsLittleO` glue into `alphaBound`.
  Items (2)-(3) need the W-trick parameter plumbing: `W=∏_{p≤D₀}p`, admissible shifts `≤D₀`,
  squarefree moduli coprime to `W` (lemmas already exist: `lattice_count_offdiag_vanish_Wtrick`).
- **Why I stopped pushing s1 further:** the remaining is genuinely analytic and the smooth core is on
  Aristotle. I did NOT open a new axiom thread (e.g. `exists_separable_F_*`) because context was
  filling and the clean checkpoint is here — every remaining s1/s2 piece is now a *named* obligation.

## ✅ State (full `lake build` green, 8277 jobs — verified this session)
All new decls in **`BoundedGaps/SieveExpansion.lean`** (appended after `sum_restrict_offdiag_vanish`):
- Diagonalization: `gpy_diagonalize`, `gpy_diagonalize_moebius`, `gpy_diagonalize_moebius_squarefree`,
  `gpy_yvar_substitution`, `gpy_yvar_eq_zero_of_not_squarefree`, `gpy_diagonal_asymptotic_form`,
  `gpy_quadform_nonneg`.
- Main-term factorization: `piFinset_lattice_main_factor`, `heuristic_main_term_diagonalized`.
- Reductions: `sieveSum_separable_eq_heuristic_add_correction`,
  `sieveSum_selberg_nu_eq_heuristic_add_correction` (FULL weight — the form s1 uses),
  `sieveThetaSum_selberg_nu_eq_heuristic_add_correction` (s2 sister, abstract `mθ`).
- Correction organization: `correction_split_offdiag`, `diag_error_bound`.
- Docs: `ANALYTIC_AXIOM_BURNDOWN.md` (2 new dated sections), `PENDING_WORK.md` §Z updated, memory
  `gpy-diagonalization-route` (NEW) + `MEMORY.md`.
- **Aristotle**: `3e2b6a8d` (`aristotle-wmertens2d`) STILL RUNNING (~50 min, ~19%) — the 2-D
  simplex-coupled smooth Riemann model `∑_{mn≤R}F·G/(mn)/(log R)² → ∫₀¹F∫₀^{1−x}G`. Slow; may fail.

## 🎬 Next actions
1. **Poll `aristotle show 3e2b6a8d-dcec-4471-889b-392652f2878a`** (short `timeout 12`, NOT the live
   TUI). If COMPLETE: download, compile in our v4.29.1 kernel, `#print axioms` MUST be clean, port to
   a new module. If FAILED/timed-out: re-architect smaller (rank-1 separable `∑F/n·∑G/n` first), or
   split. Then IMMEDIATELY submit the next job (keep one in flight).
2. **Build the analytic asymptotic on top of `gpy_diagonal_asymptotic_form`**: evaluate
   `∑_{r sf}(φ(r)/r²)zᵣ² → const` as `R→∞`. This is the genuine nut; the 1-D reusable bricks are in
   `WeightedMertens.lean` (`riemann_sum_log_weight`, `weighted_mertens`, Cesàro/majorant lemmas).
   A good Aristotle target if the 2-D one returns.
3. **Discharge `∑|coeff| = o(main)`** (the divisor-sum size bound, `diag_error_bound` reduces to it)
   and the W-trick off-diagonal vanishing at the real expansion (`lattice_count_offdiag_vanish_Wtrick`
   + admissibility plumbing), then `IsLittleO` glue into `alphaBound`/`s1_holds_from_nonprime_asym`.

## ⚠️ Gotchas
- `SieveExpansion.lean` opens only `ArithmeticFunction (moebius)` — qualify `ArithmeticFunction.`
  for `isMultiplicative_moebius`, `moebius_eq_zero_of_not_squarefree`, `moebius_sq_eq_one_of_squarefree`.
- `Finset.mul_add` does NOT exist — use bare `mul_add`. `mem_filter` etc. need `Finset.` prefix here.
- Box OOM flake on `lake env lean` (~1/few runs): re-run; `taskset -c 0-2` helps.
- Aristotle pins v4.28; repo is v4.29.1 — ALWAYS `#print axioms` a ported result.
- `M ROADMAP.md` in `git status` is pre-existing (not mine) — leave it.

## 📁 Key files
- `BoundedGaps/SieveExpansion.lean` — THE deliverable (sub-step a→c algebra + reductions; ~1180 lines now).
- `BoundedGaps/Sieve.lean` — `s1_holds_from_nonprime_asym:1907`, `selberg_nu:1872`, `alphaBound:132`,
  `mkF_denominator:510` (= α = ∫_{simplex}F²).
- `BoundedGaps/WeightedMertens.lean` + `RiemannSumLogWeight.lean` — the 1-D analytic keystone (bricks).
- `ANALYTIC_AXIOM_BURNDOWN.md` (tail) — the 3 remaining analytic obligations, precisely stated.
- `PENDING_WORK.md` §Z — analytic-NT inventory.
- `aristotle-wmertens2d/Problem.lean` — the in-flight 2-D job.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (analytic-NT
sieve burn-down; this lap machine-checked the ENTIRE s1/s2 algebraic reduction, leaving only analytic
nuts). Start at Next action #1: poll/harvest Aristotle `3e2b6a8d`, then build the analytic asymptotic
on `gpy_diagonal_asymptotic_form`. Keep ONE Aristotle job in flight at all times.**

🧵 ➡️ 🧵
