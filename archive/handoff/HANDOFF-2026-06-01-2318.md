# Handoff: fiber-count split into 3 sub-pieces — 2 proven, keystone at Aristotle, β-product remaining

**Date**: 2026-06-01 23:18 UTC · **Branch**: `path-a-selberg-nu` · **HEAD** `1935c6f` (33 commits
ahead of `3180948`, all local-only — box can't push). Supersedes `HANDOFF-2026-06-01-1310.md`.

## 🎯 What we're doing
The shared symmetric-reduction kernel for `polynomialMkF` / `polynomialMkF_eps`. The cross-orbit
tower (prior sessions) reduced every cross-orbit denominator matrix entry to ONE single-orbit core
sum `S(α,β) = ∑_{p∈monoOrbit α} ∏ᵢ (pᵢ+βᵢ)!`. The contingency-table closed form for `S(α,β)` was
validated; this leg attacked its proof — **the fiber count, the last combinatorial theorem.**

## ✅ This session — decomposed the fiber count into 4 pieces, proved 2, offloaded 1
The target (design doc ⭐ section): `S(α,β) = ∑_X (∏_t multinomial(n_t;X(·,t)))·(∏_{u,t}((c_u+b_t)!)^{x_{u,t}})`.
Step-1 weight factorization `prod_eq_prod_pow_joint` was already done. The remaining "fiber count"
`#{p ∈ monoOrbit α : joint-type(p,β)=X} = ∏_t multinomial(n_t;X(·,t))` splits into:

- **(a) ✅ DONE `mem_monoOrbit_iff`** (commit `65687d5`, axiom-clean `[propext,choice,Quot.sound]`):
  `p ∈ monoOrbit α ↔ ∀ v, #{i:pᵢ=v} = #{i:αᵢ=v}` (same fiber sizes ⟺ in the orbit). Forward =
  `Equiv.sum_comp` (a perm preserves fiber sizes); **reverse** glues per-value fiber equivs
  (`Fintype.equivOfCardEq (card {i//pᵢ=v} = card {i//αᵢ=v})`) into one coordinate permutation via
  `Equiv.ofFiberEquiv` + `Equiv.ofFiberEquiv_map`. This is the orbit→function-counting bridge.
- **(b) ✅ DONE `card_filter_eq_sum_joint`** (commit `369c98a`, axiom-clean): row margin
  `#{i:pᵢ=c} = ∑_{b∈image β} #{i:pᵢ=c ∧ βᵢ=b}`, via `Finset.card_eq_sum_card_fiberwise` +
  `filter_filter`. With (a), pins the table `X`'s **row margins to α's fiber sizes** ⟹ the fiber is
  empty unless `X` has those margins. (Column margins = β's fiber sizes by the symmetric statement.)
- **(c) ⏳ OFFLOADED to Aristotle** — the atomic keystone:
  `#{f:ι→β | ∀ b, #{i:f i=b}=h b} = Nat.multinomial univ h` for finite `ι,β` when `∑ h = |ι|`.
  **Project `15ba0cd4-4b7d-445c-a105-169793b91368` ("keystone"), RUNNING as of 23:17.** Statement in
  `/tmp/keystone/Keystone.lean`. Numerically validated (2000 random brute-vs-formula cases). hsum is
  essential (else LHS=0, RHS>0). Aristotle pins mathlib v4.28.0 (we're v4.29.1 — expect minor drift).
  **Retrieve**: `aristotle list | grep 15ba0cd4`; when `COMPLETE`/`COMPLETE_WITH_ERRORS`,
  `aristotle download 15ba0cd4-4b7d-445c-a105-169793b91368 --destination /tmp/keystone-out.tar.gz`
  then `tar -xzf`. Vet per `[[aristotle-partial-verify-decomposition-assembles]]`: confirm it's a real
  proof (not a wrapper+sorry), port v4.28→v4.29 names, drop into `SymmetricReduction.lean` (or a small
  `Combinatorics`-style helper file imported by it).

## 🎬 Next actions
1. **(Host only) Push** — `git push -u origin path-a-selberg-nu` (33 commits). Split into PRs.
2. **Retrieve + vet the keystone (c)** from Aristotle (commands above). If Aristotle returns a
   sorry/junk, prove it locally: induct peeling one value, `Nat.multinomial_cons` recursion
   (`multinomial (cons a s) f = (f a + ∑_s f).choose (f a) * multinomial s f`); the `choose` factor =
   "pick which `h a` of the slots map to `a`." Mirrors `Multiset.countPerms_filter_ne`.
3. **(d) β-group product — THE remaining repo heart** (own session): the fiber count factorizes,
   `#{p:Fin k→ℕ | J_β(p)=X} = ∏_{b∈image β} #{q:{i//βi=b}→ℕ | fibers(q)=X(·,b)}`, then (c) per group.
   Route: fiber-decomposition equiv `(Fin k→ℕ) ≃ Π b:↥(image β), ({i//βi=b}→ℕ)` (restrict the base of
   `Equiv.sigmaFiberEquiv β` to the image so the Pi is a Fintype) + `Fintype.card_pi` +
   `Fintype.card_subtype` to go filter-card ↔ subtype-card. ⚠️ **Integration wrinkle**: keystone (c)
   has a **Fintype codomain**; the per-group count has codomain ℕ — bridge by restricting to the
   finite value-support (`Fintype.ofFinset`/a `{c//c∈S}` subtype) before applying (c).
4. **Assemble** `S(α,β) = ∑_X (fiber count)·(weight)`: `Finset.sum_fiberwise` over the joint-type map
   `p ↦ (vt ↦ #{i:(pᵢ,βᵢ)=vt})`, constancy of the weight on each fiber from `prod_eq_prod_pow_joint`.
   Then the design doc's full closed form, then `native_decide` at fixed k=50/54 (≥deg 9 witness).

## 🧠 Context to carry forward
- **Why monoOrbit not the all-perms sum**: the monoOrbit fiber count gives the CLEAN
  `∏_b multinomial(n_b;X(·,b))` (no extra `∏_c m_c!` α-side factor). Going via `∑_{σ∈Perm}` (i.e.
  `group_sum_eq_stab_smul_orbitSum`) drags in `|stab α|` = the stabilizer count (action #3, a separate
  `∏_v (fiber v)!`). Both routes need a counting argument; monoOrbit's is cleaner — stick with it.
- The keystone is the ONLY genuinely-atomic generic fact here (perfect Aristotle leaf); everything
  else glues repo definitions and stays in-house. Don't hand Aristotle the whole fiber count (it would
  come back wrapper+sorry — `[[aristotle-open-problem-returns-wrapper-sorry]]`).
- This kernel is the shared bottleneck for BOTH the EH-conditional plain-`Mk` ladder AND the
  unconditional `Mk_eps 50` flagship (`b`'s `EpsBridge` already done; only this orbit-level witness
  remains). The ε-side (`SymmetricReductionEps.lean`) reuses the same fiber count.

## ⚠️ Gotchas (cumulative, new on top)
- `MultiIndex k := Fin k → ℕ` (reducible abbrev) — filters `fun i => p i = v` and `Equiv.ofFiberEquiv`
  typecheck directly; `Equiv.ofFiberEquiv e : Fin k ≃ Fin k` unifies with `Equiv.Perm (Fin k)` (defeq).
- `Equiv.ofFiberEquiv` direction: `e c : {a//f a=c} ≃ {b//g b=c}` gives `g (ofFiberEquiv e a) = f a`.
  For `α(σi)=pᵢ` use `f:=p, g:=α`, i.e. `e v : {i//pᵢ=v} ≃ {i//αᵢ=v}`.
- `Aristotle submit "<prompt>"` crashes (Errno 36) when the prompt exceeds ~255 bytes (it stats the
  prompt as a path). Use `--project-dir <dir>` with the statement in a `.lean` file + a short prompt.
  (Refines `[[aristotle-submit-prompt-pathmax]]`.)
- Box: `taskset -c 0-3 lake build BoundedGaps.SymmetricReduction`; hook rebuilds on commit; box has
  python3+stdlib (bare `python3`, no nix/sandbox).

## 📁 Key files
- `BoundedGaps/SymmetricReduction.lean` — the tower + `prod_eq_prod_pow_joint` + **NEW**
  `mem_monoOrbit_iff`, `card_filter_eq_sum_joint`; status block at bottom.
- `tools/mk/SYMMETRIC_REDUCTION.md` — ⭐ design; the "Two clean theorems" → now 4 sub-pieces (a–d),
  status marked.
- `/tmp/keystone/Keystone.lean` — the exact keystone statement submitted to Aristotle.
- `BoundedGaps/SymmetricReductionEps.lean` / `EpsBridge.lean` — ε-side (reuses the fiber count).

---
**→ Next session: if on the host, push (#1). Then retrieve+vet the Aristotle keystone (#2), and the
real work is the β-group product (#3) — well-scoped, with the Fintype-codomain bridge flagged. This IS
the chosen thread — don't summarize back, don't offer other KB projects.**

🧵 ➡️ 🧵
