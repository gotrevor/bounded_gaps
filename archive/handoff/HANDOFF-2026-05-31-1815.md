# Handoff: Symmetric reduction — full ratio invariance + (b)/(c) foundations landed

**Date**: 2026-05-31 ~18:15 UTC · **Branch**: `path-a-selberg-nu` · **HEAD** `16b8186`
(10 commits ahead of `3180948`, local-only — box can't push)

## 🎯 What we did
Resumed from `HANDOFF-2026-05-31-1653.md`. Trevor said "Check Aristotle" → then "All 3!
You choose the order." So I attacked all three open symmetric-reduction threads, in order
**(a) → (b) → (c)**, and landed real, complete, **axiom-clean** progress on each (no new
sorries/axioms — respecting the repo's sorry-free ethos rather than scaffolding).

**Aristotle check** (first action): nothing pending for bounded_gaps. Our two jobs
(`aristotle-mk5`, `aristotle-5511`) are IDLE/done and already banked (`f405f27`, `b682f25`).
The one RUNNING job (`o30`, "replace sorry in O30.lean") belongs to a sibling
**finite_simple_groups** session (alongside `lt60`/`case1-4`), not us — nothing to harvest here.

## ✅ State (all observed this session, all axiom-clean = propext/Classical.choice/Quot.sound)
- `aa92ad5` — **(a) DONE**: `polynomialMaynardNumerator_permWeight` + `polynomialMkF_permWeight`
  in `SymmetricReduction.lean`. **Full Rayleigh-ratio permutation-invariance is now complete** —
  the "restrict to symmetric F is WLOG" bedrock. Verified green: `lake build BoundedGaps.SymmetricReduction`.
- `9a74394` — **(b)+(c) foundations**:
  - (b) `SymmetricReduction.placement_count`: `Fintype.card (Fin T ↪ Fin k) = k.descFactorial T`
    (the `ff(k,T)` factor; bijective core of the matching closed form).
  - (c) **new file `BoundedGaps/EpsScaling.lean`**: `simplex_eps_eq_smul` +
    `monomialIntegral_eps` (the homothety scaling law `∫_{(1+ε)R_k} ∏tᵢ^αᵢ = (1+ε)^(k+|α|)·monomialIntegral`).
    Wired into the `BoundedGaps` aggregator root.
- `16b8186` — doc update (`SYMMETRIC_REDUCTION.md` "Lean foundation status").
- **Verified green**: full `lake build` = "Build completed successfully (8263 jobs)". The fixed
  pre-commit gate (see Gotchas) re-ran it on commit and confirmed.

## 🧠 Context to carry forward
- **What's genuinely DONE vs. foundational.** (a) is *complete* — invariance is a finished,
  usable theorem. (b) and (c) are each still **multi-session kernels**; what landed is the one
  cleanly-tractable, on-critical-path lemma for each, fully proven. Do NOT oversell (b)/(c) as
  "done" — they are not. The honest framing stands from last session: this whole reduction only
  reaches the **EH-conditional** `mk_*` ladder; the unconditional flagship (H₁≤246) needs the
  full `Mk_eps` bridge, of which `EpsScaling.lean` is just the first ingredient.
- **The (a) trick worth remembering**: to show `dirichletIntegralWithSlack` invariant under the
  coordinate perm, I did NOT build the induced permutation on `Fin n` (the fiddly thing the prior
  handoff feared). Instead: `removeNth i (w∘σ)` and `removeNth (σi) w` delete the *same value*
  `w(σi)` from `w`, so they share `∑` and `∏(·)!` — proved by `Fin.sum/prod_univ_succAbove` +
  `Equiv.sum/prod_comp` and cancelling `w(σi)`. DIWS depends only on those two quantities. Clean.
- **(c) change-of-variables**: `Measure.setIntegral_comp_smul_of_pos` is the right tool (linear
  homothety for a Haar measure); `volume` on `Fin k → ℝ` is `IsAddHaarMeasure`; `finrank = k` via
  `Module.finrank_pi`+`Fintype.card_fin`. Needed `open scoped Pointwise` for the set-smul `•`.

## 🎬 Next actions (remaining kernels — pick per Trevor's steer; both host-better)
1. **The matching closed-form identity** (the real (b) kernel): for an orbit-symmetric weight,
   `polynomialMaynardDenominator P_sym = Σ_{λ,μ} c_λ c_μ · S_den(λ,μ,k)/(k+|λ|+|μ|)!` (+ numerator
   analog), `S_*` = the matching/overlap sums in `tools/mk/mk_sym.py`. Needs an
   orbit→labelled-placement bijection counted by `placement_count`, the weight `W(M)`, and
   `monomialIntegral`'s dependence only on the overlap type. Mathlib-grade combinatorics. After
   it: at fixed k, `native_decide` the N×N rational form > 4 (N = #orbits at the required HIGH
   degree — ≥8, likely teens, per the exact LDL table) → discharge `mk_54_witness_under_EH` etc.
2. **The rest of the `Mk_eps` bridge** (remaining (c)): slack-Dirichlet analog over `simplex_shrunk`,
   two-layer numer/denom bridges, cutoff/DCT `Mk_eps_ge_polynomialMkF_eps` — the `SievePolynomial`
   development re-run over the ε-enlarged simplex, with `monomialIntegral_eps` supplying closed forms.
   Only this path moves the unconditional flagship.
3. **Host-only**: push `path-a-selberg-nu` (HEAD `16b8186`, 10 ahead of `3180948`).

## ⚠️ Gotchas
- **Fixed a latent commit-gate bug.** `.githooks/pre-commit` did `ls lakefile.toml lakefile.lean`
  as one test — `ls` exits nonzero when *either* file is missing, so a repo with only
  `lakefile.toml` (this one) always hit "Could not locate a lakefile — skipping build gate": the
  green-build gate was **silently disabled**. Switched to `[ -f a ] || [ -f b ]`; gate now runs.
  **BUT `.githooks/` is UNTRACKED in this clone** (`git ls-files .githooks` is empty), despite
  decisions.md documenting it as "committed in-repo". So the fix isn't versioned — on the host,
  `git add .githooks/` (with the fix) to make it durable, or confirm the host clone already tracks
  a correct copy. `core.hooksPath=.githooks` IS set locally (re-set on fresh clones).
- **Box build = OOM-grind** (unchanged). Editing `SymmetricReduction.lean` only recompiles it +
  the aggregator root (cheap, ~10-50s) since `SievePolynomial` is upstream/cached. `EpsScaling.lean`
  hit one transient "Cannot allocate memory" loading an olean — `pkill -9 lean lake` + retry
  converged. Never trust a build that errored mid-OOM; re-run from clean.
- New `.lean` not imported by the aggregator root is skipped by `lake build` (false green) —
  `EpsScaling` is now imported into `BoundedGaps.lean`. Don't forget for further new modules.

## 📁 Key files
- `BoundedGaps/SymmetricReduction.lean` — permutation-invariance (a) COMPLETE + `placement_count` (b).
- `BoundedGaps/EpsScaling.lean` — ε-enlarged simplex scaling law (c) foundation.
- `BoundedGaps/SievePolynomial.lean` — the plain-`Mk` machinery the kernels build on / mirror.
- `tools/mk/mk_sym.py` — validated closed-form reduction (the (b) kernel to formalize).
- `tools/mk/SYMMETRIC_REDUCTION.md` — full scoping + updated Lean foundation status.

---
**→ Next session: this is your starting point. (a) is genuinely complete; (b)/(c) have their
foundations in and their remaining kernels precisely scoped above. Don't re-open the project
menu — continue this thread per Trevor's steer (kernel #1 vs #2 vs push vs pivot). Be honest:
(b)/(c) are multi-session and only the EH-conditional ladder is in reach without the full Mk_eps bridge.**

🧵 ➡️ 🧵
