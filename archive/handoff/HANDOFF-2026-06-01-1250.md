# Handoff: cross-orbit kernel reduced to the matching permanent (6-commit tower)

**Date**: 2026-06-01 12:50 UTC · **Branch**: `path-a-selberg-nu` · **HEAD** (29 commits ahead of
`3180948`, all local-only — box can't push). Supersedes `HANDOFF-2026-06-01-1230.md`.

## 🎯 What we're doing
Building the symmetric-reduction kernel for `polynomialMkF` / `polynomialMkF_eps` — the orbit-level
Rayleigh computation that both the EH-conditional plain-`Mk` ladder AND the unconditional `Mk_eps 50`
flagship need (they **share this one bottleneck** — see prior handoff; `b`'s `EpsBridge` is already
done, only the un-materializable witness remains, which needs this kernel). Trevor's steer: a & b in
parallel = work the shared kernel; "keep trucking."

## ✅ This session — 6 commits, all axiom-clean `[propext, choice, Quot.sound]`, hook-verified green
A coherent reduction tower from the abstract Rayleigh data down to a single classical object:
1. `polynomialMaynardNumerator_orbitSum` — plain **numerator** diagonal reduction (the `i`-sum is
   orbit-invariant even though per-`i` terms aren't). Diagonal `Mr`/`Ar` entry now closed form.
2. new `SymmetricReductionEps.lean` — **ε-side diagonal reductions** (denominator factors out the
   orbit-invariant `(1+ε)^(k+|p+q|)`; `affineSlackRat` inherits the removeNth-perm invariance;
   numerator analog). b's orbit foundation done.
3. `orbitPair_denominator_eq` — **cross-orbit denominator core**: factors out the constant
   `(k+|α|+|β|)!`, isolating the pure count `∑∑ ∏ᵢ(pᵢ+qᵢ)!`.
4. `orbitPair_core_const` — **collapses the double orbit sum to a single one**:
   `∑_{p∈orbit α}∑_{q∈orbit β} ∏ᵢ(pᵢ+qᵢ)! = |orbit β| · ∑_{p∈orbit α} ∏ᵢ(pᵢ+βᵢ)!` (inner sum constant
   over `q∈orbit β`, same reindex trick as the diagonal).
5. `group_sum_eq_stab_smul_orbitSum` — **orbit-stabilizer bridge**: `∑_{p∈orbit α} f p` ↔
   `∑_{σ∈Sₖ} f(α∘σ)` differ by the stabilizer card (every fiber of `σ↦α∘σ` is a coset of
   `{σ : α∘σ=α}`, via `σ↦σ*ρ⁻¹`, `Finset.card_bij`). Lets the matching enumeration run over the group.

**Net**: the cross-orbit denominator matrix entry is now
`|orbit β| · (∑_{σ∈Sₖ} ∏ᵢ(α(σ i)+βᵢ)!) / (stab α).card / (k+|α|+|β|)!` — everything reduced to one
**permanent** `∑_{σ∈Sₖ} ∏ᵢ(α(σ i)+βᵢ)!` over a matrix `M[i][j] = (αⱼ+βᵢ)!` with few distinct rows/cols.

## 🎬 Next actions
1. **(Host only) Push** — `git push -u origin path-a-selberg-nu` (29 commits). Split into PRs:
   sieve Tier-1/2, `Mk_eps` bridge, SymmetricReduction kernel, SymmetricReductionEps.
2. **The matching permanent enumeration** (the genuine combinatorial heart — multi-session,
   needs design): collapse `∑_{σ∈Sₖ} ∏ᵢ(α(σ i)+βᵢ)!` to `∑_M (k.descFactorial T)·W(M)` over overlap
   patterns `M` (partial matchings of `α`-parts to `β`-parts sharing a slot). This is the step that
   makes the final form `native_decide`-able at `k=50/54` (the sum over patterns is *small* —
   independent of `k`; only `ff(k,T)=k.descFactorial T` carries the `k`). Needs: an overlap-pattern
   **type**, the orbit→placement bijection counted by `placement_count` (already proven:
   `Fintype.card (Fin T ↪ Fin k) = k.descFactorial T`), and the weight `W(M)`. Oracle:
   `tools/mk/mk_sym.py`. **Don't rush the type design.**
3. **Companion: `(stab α).card` closed form** = `∏ᵥ (|α-fiber of v|)!` (stabilizer of `α` = product of
   symmetric groups on its level sets). Needed to evaluate the bridge's stabilizer factor. Self-
   contained (~bijection `stab ≃ Πᵥ Perm(fiberᵥ)`), more tractable than #2 — a good warm-up next.
4. After the plain permanent form: the ε-version reuses the same combinatorics with `affineSlackRat`.

## ⚠️ Gotchas (this session)
- `Fin.removeNth i` forces `Fin (succ _)` — numerator-side orbit lemmas at `n+1`, not general `k`.
- `rw` is syntactic, orbit terms only defeq (`(a∘σ)+(b∘σ)` vs `(a+b)∘σ`, `(p+q) i` vs `p i+q i`) —
  bind through `have : <exact form> := lemma` then `rw`, or close with `simp only [Pi.add_apply]`.
- `unfold MultiIndex.degree` expands ALL `.degree`s → use `show (∑ i, … i) = …` to keep RHS folded.
- Nat assoc `k+(α+β)` vs `(k+α)+β` → `← add_assoc`.
- **`Equiv.Perm.apply_inv_self` does NOT exist** (nor `inv_apply_self`). Prove `ρ(ρ⁻¹ i)=i` via
  `rw [← Equiv.Perm.mul_apply, mul_inv_cancel, Equiv.Perm.one_apply]`. (`group` closes `(τ*ρ)*ρ⁻¹=τ`.)
- Box: `taskset -c 0-3 lake build <Module>`; pre-commit hook rebuilds; new modules must be imported by
  `BoundedGaps.lean` or `lake build` silently skips them (false green).
- Untracked scratch: many `HANDOFF-*.md`, `aristotle-*/`, `tools/mk/__pycache__/`. Don't commit; don't
  touch master `HANDOFF.md`.

## 📁 Key files
- `BoundedGaps/SymmetricReduction.lean` — the tower (commits 1,3,4,5 + earlier); status block at bottom.
- `BoundedGaps/SymmetricReductionEps.lean` — ε-side reductions (the flagship foundation).
- `BoundedGaps/EpsBridge.lean` — the `Mk_eps` bridge (DONE through `mk_eps_50_witness_of_poly`).
- `tools/mk/SYMMETRIC_REDUCTION.md` / `mk_sym.py` — design doc + matching-count oracle + exact LDL table.

---
**→ Next session: if on the host, push (#1). Then the work is the matching permanent enumeration (#2)
— the single shared kernel for BOTH targets. `group_sum_eq_stab_smul_orbitSum` + `orbitPair_core_const`
+ `orbitPair_denominator_eq` reduced everything to `∑_{σ∈Sₖ} ∏ᵢ(α(σ i)+βᵢ)!`; the remaining work is
collapsing that permanent over overlap patterns (warm up with the `stab.card` formula, #3). This IS the
chosen thread — don't summarize back, don't offer other KB projects.**

🧵 ➡️ 🧵
