# Handoff: All-3 symmetric-reduction push — (a) done, (b) seeded, (c) both Rayleigh sides closed-form

**Date**: 2026-05-31 ~18:45 UTC · **Branch**: `path-a-selberg-nu` · **HEAD** `8c4281f`
(14 commits ahead of `3180948`, local-only — box can't push). Supersedes `HANDOFF-2026-05-31-1815.md`.

## 🎯 What we did
Resumed (from `HANDOFF-2026-05-31-1653.md`), checked Aristotle (nothing pending for us — `o30` is a
sibling finite_simple_groups job), then Trevor said **"All 3! You choose the order"** and **"continue —
plenty of tokens."** So I drove all three open symmetric-reduction threads, in order (a)→(b)→(c), all
**axiom-clean, no new sorries/axioms**.

## ✅ State (all observed, all `#print axioms` = propext/Classical.choice/Quot.sound)
**(a) DONE** — `SymmetricReduction.lean`: `polynomialMaynardNumerator_permWeight` +
`polynomialMkF_permWeight` (`aa92ad5`). Full Rayleigh-ratio permutation-invariance = the
"restrict to symmetric F is WLOG" bedrock, complete. (Trick: `removeNth i (w∘σ)` and `removeNth (σi) w`
delete the same value `w(σi)` → share ∑ and ∏(·)!; no induced perm on `Fin n`.)

**(b) seeded** — `placement_count` (`9a74394`): `Fintype.card (Fin T ↪ Fin k) = k.descFactorial T`
(the `ff(k,T)` factor; bijective core of the matching closed form).

**(c) BOTH Rayleigh sides reduced to closed form** — new `EpsScaling.lean` (`9a74394`→`f360e6d`):
- `simplex_eps_eq_smul` + `monomialIntegral_eps` — homothety scaling `∫_{(1+ε)R_k}∏tᵢ^αᵢ = (1+ε)^{k+|α|}·monomialIntegral`.
- `mkF_eps_denominator_poly` — `Mk_eps` denominator closed form (pure dilation).
- **`dirichlet_affine_slack`** — the ε-numerator KEYSTONE: `∫_{(1-ε)R_n}∏sⱼ^aⱼ(1+ε-∑s)^β =
  (1-ε)^{n+|a|}·Σ_m C(β,m)(2ε)^m(1-ε)^{β-m}·dirichletIntegralWithSlack(a,β-m)`. (Rescale `s=(1-ε)σ`,
  then `1+ε-(1-ε)∑σ = 2ε+(1-ε)(1-∑σ)`, `add_pow` → binomial sum of standard slack-Dirichlets.)
- `inner_eq_eps` / `isCompact_simplex_shrunk` / `Ji_bridge_eps` / `mkF_eps_numerator_poly` — the
  `Mk_eps` numerator for a polynomial weight = triple sum of those affine-slack integrals.
- **Verified green**: full `lake build` = "Build completed successfully (8263 jobs)" (the fixed
  pre-commit gate re-ran it on each code commit).

## 🧠 Context to carry forward
- **Honesty on scope (unchanged from before, but more is now done).** (a) is a finished theorem.
  (c) now has BOTH sides of the `Mk_eps` Rayleigh ratio reduced to closed forms — the genuinely hard
  math (the affine-slack keystone) is done. (b) still has its multi-session combinatorial kernel ahead;
  only `placement_count` is in. The whole reduction still only reaches the EH-conditional ladder UNLESS
  the `Mk_eps` bridge is *finished* (assembly below) — but that assembly is now mostly mechanical.
- **Why the keystone matters**: it's the one piece of the `Mk_eps` numerator that isn't a re-run of
  `SievePolynomial` (slack base 1+ε ≠ domain bound 1-ε). With it + the denominator dilation, the
  closed-form content of the ε-bridge is complete.
- **Box behaved this session** but `Ji_bridge_eps` took 508s (OOM contention) — see Gotchas.

## 🎬 Next actions (pick per Trevor's steer; all host-better)
1. **Finish the `Mk_eps` bridge** (now the shortest path to flagship H₁≤246; the hard parts are done):
   - Define a rational `polynomialMkF_eps` (for rational ε; `(1+ε)`-powers are then rational) wrapping
     `mkF_eps_numerator_poly` / `mkF_eps_denominator_poly`.
   - `polynomialMkF_eps_eq_MkF_eps`: the two `_poly` bridges feed straight in.
   - `Mk_eps_ge_polynomialMkF_eps` via cutoff/DCT — mirror `SievePolynomial`'s
     `denom_tendsto`/`J_i_tendsto`/`numer_tendsto`/`Mk_ge_polynomialMkF` over the ε geometry
     (`simplex_eps`/`simplex_shrunk`). This is the biggest remaining chunk (~the 200-line DCT dev).
   - Then `Mk_eps_gt_of_polynomial_witness` → discharge `mk_eps_50_witness` (needs the actual degree-50
     ε-witness, exact-arithmetic, large `native_decide`).
2. **The matching closed-form identity** (the real (b) kernel): orbit→labelled-placement bijection
   counted by `placement_count`, weight `W(M)`, overlap-type invariance of `monomialIntegral`. Reaches
   the EH-conditional `mk_*` ladder. Mathlib-grade combinatorics.
3. **Host-only**: push `path-a-selberg-nu` (HEAD `8c4281f`, 14 ahead of `3180948`).

## ⚠️ Gotchas
- **Commit-gate bug FIXED but UNTRACKED.** `.githooks/pre-commit` used `ls a b` (nonzero if *either*
  missing) → gate silently skipped for a `lakefile.toml`-only repo. Fixed to `[ -f a ] || [ -f b ]`;
  it now runs. BUT `.githooks/` is untracked in this box clone (`git ls-files .githooks` empty) despite
  decisions.md calling it committed-in-repo — on the host, `git add .githooks/` (with the fix) to make
  it durable, or confirm the host clone tracks a correct copy.
- **Box OOM-grind**: `Ji_bridge_eps` build took 508s under parallel pressure (transient OOM, not
  scarcity). `pkill -9 lean lake` + plain `lake build` retries converge. Run long builds in the
  background and wait on the output file.
- Per-term `ring` over `(a*b)^k` powers (affine-slack proof): use `simp only [mul_pow]` (splits ALL,
  both sides) then `ring` — a bare `rw [mul_pow]` hits only the first match. Lesson flushed.
- New `.lean` must be imported into `BoundedGaps.lean` or `lake build` skips it. `EpsScaling` is wired in.

## 📁 Key files
- `BoundedGaps/EpsScaling.lean` — the whole `Mk_eps` closed-form bridge (denominator + affine-slack numerator).
- `BoundedGaps/SymmetricReduction.lean` — (a) full ratio invariance + (b) `placement_count`.
- `BoundedGaps/SievePolynomial.lean` — the plain-`Mk` machinery the ε-bridge mirrors (DCT dev to copy).
- `tools/mk/SYMMETRIC_REDUCTION.md` — scoping + updated Lean foundation status.

---
**→ Next session: continue this thread. (a) complete; (c) both Rayleigh sides closed-form (hard math done) —
the shortest remaining path to the flagship is *finishing* the `Mk_eps` bridge (assembly + DCT mirror), not
new math. (b) needs its combinatorial kernel. Don't re-open the project menu; act on Trevor's steer.**

🧵 ➡️ 🧵
