# Handoff: A.2 matchings closed form landed — `Mk k > 4` endgame reduced to 2 validated bridge axioms

**Date**: 2026-06-03 02:37 UTC · **Branch**: `path-a-selberg-nu` · 6 commits this lap (5 Lean, 1 docs), all in `SymmetricReductionOrbitFree.lean` / `MultinomialFast.lean` / `PENDING_WORK.md`.

## 🎯 What we're doing
Discharging `mk_*_witness` (→ bounded gaps) via the symmetric-reduction pipeline: a symmetric weight whose orbit-basis Gram quotient `> 4` certifies `Mk k > 4`, which through the **already-proven** `Targets.H1_le_of_Mk_witness` gives the gap bound. The chain `symWeight → Gram quotient → Mk(n+1)>T` is complete and axiom-clean; the endgame is making the Gram-quotient inequality `native_decide`-feasible at the real witness scale (k=200, 45 orbits, degree 7).

## 🧠 Context to carry forward (READ — overturns the prior handoff's "A.1 is gating")
The 2026-06-03-0024 handoff said A.1 (the filter-card / `Fin k`-scan hoist) was THE gating step. **This lap built A.1, MEASURED it, and found that's wrong.** The empirical chain:
- **A.1 IS solved** (`partsHist` + `card_filter_ofParts`, k-independent margin, no `Fin k` scan). Cut an img-4 enumeration `card` at k=300 from >280s (timeout) → 41s.
- ⚠️ **`histArr` (a `let arr := scan; fun v => arr.getD v 0`) does NOT materialize in `native_decide`** — it re-ran the scan per lookup (cost ∝ k). Lesson saved to memory. The real fix was `partsHist` (genuinely k-independent via `L.count`).
- **The REAL wall is the windowed table COUNT itself**: an img-4 orbit (4 distinct exponent values) → 16 cells → `4·2^15 = 131072` windowed tables *per entry*, k-INDEPENDENT. ~41s each × 9 img-4×img-4 entries (+factorials) ⇒ full 45-orbit witness is hours/OOM. img-4 orbits are mathematically essential (img≤3 never clears 4).
- ⟹ **The matchings closed form (A.2) is the ONLY box-feasible route** — replaces the 131072-table enumeration with a k-INDEPENDENT ~34-term sum over partial matchings, using `Nat.descFactorial` (no big factorial). **I built and landed it this lap.** It `#eval`s INSTANTLY even at k=300 img-4.

Also: factor-3 bignums (`297!` per accepted table in the enumeration path) are now moot — matchForm avoids `Nat.multinomial` entirely. `MultinomialFast`/`multFold` (Aristotle) were for the now-abandoned enumeration path; don't invest more there. (`Finset.toList` is noncomputable — see memory; that's why the enumeration's fast multinomial was a dead end.)

## ✅ State (real `lake build` green observed this lap)
- Root build green: **8270 jobs** (pre-commit gate ran on every Lean commit). Module green at 8255.
- **A.2 matchings form committed** (`ca3d47f`): `matchData/matchDenSum/autParts/matchDenForm` (denom), `gWeight/matchDataN/matchNumSum/matchNumForm` (numer) — all kernel-clean (only propext/Classical.choice/Quot.sound). NUMERICALLY VALIDATED in Lean: `matchDenForm [2,1] [1,1] 3 = 11/3360`, `matchNumForm [2,1] [1,1] 3 = 7/2160` (= the repo's `gram*Entry` spot-checks); and vs `tools/mk/mk_sym.py reduced_closed` across k.
- **Two bridge identities as DISCLOSED AXIOMS**: `gramDenEntry_eq_matchDenForm`, `gramNumEntry_eq_matchNumForm`. Witness theorem `Mk_gt_of_symWeight_witness_match` wires them; `#print axioms` = the 3 std + those 2, **no sorry**. k=3 regression example fires through the match path.
- `ofParts_inj` committed (`e636efb`) — `ofParts` injective on positive-parts lists (foundation for re-keying the witness on parts-lists).
- Earlier commits: `f8cc399` MultinomialFast, `535c693` A.1 hoist (WH layer), `bee9672` card_filter_ofParts+partsHist, `75026b8` PENDING_WORK.
- **Witness data IN HAND**: k=200 D=7 → 45 orbits, ratio **4.002898 > 4** (Python `tools/mk/_ldl.py` + `reduced_closed`; regen script logic in `/tmp/gen_witness_lean.py`). k=300 → 4.006944.
- **Aristotle IN FLIGHT**: `0b5bf5be-2ed4-431e-8f25-cd9701619696` (`aristotle-denbridge/`) — proving the DENOMINATOR bridge `denom_bridge` (the core A.2 combinatorial identity `autParts·autParts·orbitSum = matchDenSum`, numerically verified across 4 cases before submitting). Done Aristotle jobs this lap: `f84ef793` multinomialFast (ported), `656d6b54` card_filter_ofParts (ported), `3591b35b` multfold (COMPLETE_WITH_ERRORS, NOT ported — dead path).

## 🎬 Next actions
1. **Poll Aristotle `0b5bf5be`** (`aristotle list`, the one-shot — NOT the live TUI). When done: download, verify `denom_bridge` in our kernel + `#print axioms`. It proves `autParts La · autParts Lb · (∑ p∈orbitFin La k, ∑ q∈orbitFin Lb k, ∏ i,(p i+q i)!) = matchDenSum La Lb k`. Port it, then connect to `gramDenEntry_eq_matchDenForm` (repo already has orbit-sum = table-sum via `orbitPair_denominator_shapeForm`; combine to discharge the denom axiom). The NUMERATOR bridge is the analog with `Gocc`/`g` weights — harder; submit it to Aristotle next (architect like `denom_bridge`, numerically pre-verify the exact statement).
2. **Re-key the witness on parts-lists** (CAPSTONE feasibility) — `Mk_gt_of_symWeight_witness_match` sums over `R : Finset (MultiIndex)` with `c, P` functions on `MultiIndex`; evaluating `c a`/`P a` per summand needs a 45-way match over 200-entry functions (the full-witness `#eval` CRASHED, code 134). Build `…_parts (Ls : List (List ℕ)) (cs : List ℤ) …` whose `hwit` sum is `∑ i j, cs[i]·cs[j]·matchForm Ls[i] Ls[j] (n+1)` (DIRECT indexing). Reduce to `…_witness_match` via `R = (Ls.map ofParts).toFinset`: use `ofParts_inj` (done) for Nodup, `List.sum_toFinset` for the reindex, `disjoint_of_histogram` for hR. See `PENDING_WORK.md §A` step 2.
3. **Capstone** (likely HOST-only): with bridges proven + parts-witness, `Mk_gt_of_symWeight_witness_match_parts witLs witCs … (by native_decide)` → `(4:ℝ) < Mk 200`. ⚠️ the box hit intermittent OOM/SIGABRT even on mathlib olean loads this lap; the heavy final `native_decide` (2025 matchForm terms, ~100-digit coeffs, 214! factorials) may need the host.

## ⚠️ Gotchas
- `native_decide` won't materialize a `let`-bound array across a closure (use genuinely-cheap k-independent computations). `Finset.toList` is noncomputable. `Nat.choose` naive recursion is exponential for balanced args (use `Nat.descFactorial`). (All in memory now.)
- `List.getD_eq_getElem`/`getD_eq_default` take `l` and `d` as EXPLICIT args (`List.getD_eq_getElem La 0 h`), not implicit.
- The box has intermittent OOM under memory pressure — a transient build failure on a *mathlib* file is usually a flake; re-run `lake build`. Don't `lean` a Mathlib-importing file directly (loads all oleans → OOM); use `lake build`.
- The 2 bridge axioms are intentional disclosed `axiom`s (numerically validated), not accidental sorries. They are the entire remaining mathematical gap to a kernel-checked `Mk > 4`.

## 📁 Key files
- `BoundedGaps/SymmetricReductionOrbitFree.lean` — the whole pipeline. A.2 matchings section (~line 1983+: `matchData`…`matchNumForm`, the 2 bridge axioms, `Mk_gt_of_symWeight_witness_match`). A.1 WH layer (~1620+). `ofParts_inj` (~1947). 3 regression examples at the bottom (computable/W/WH/match paths).
- `PENDING_WORK.md` §A — the actionable inventory: A.2 formula written out, the 2 remaining steps (prove bridges, re-key witness), capstone payoff.
- `tools/mk/{_ldl,mk_sym}.py` — witness data (`_ldl.py` for the LDL coeffs; `mk_sym.py reduced_closed`/`_entry_sums` = the matchings formula, validated).
- `aristotle-denbridge/DenBridge.lean` — the in-flight bridge job (self-contained statement, numerically verified).
- `BoundedGaps/MultinomialFast.lean` — verified `multinomialFast` (kernel-clean), for the abandoned enumeration path; not on the critical path.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the context (especially: A.2 matchings is LANDED and the endgame is now just 2 validated bridge axioms + re-keying plumbing), then start at Next action #1 — poll Aristotle `0b5bf5be` and continue the bridge proof.**

🧵 ➡️ 🧵
