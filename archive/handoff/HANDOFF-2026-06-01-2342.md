# Handoff: fiber count — β-group product DONE; only keystone (Aristotle) + compose + bridge remain

**Date**: 2026-06-01 23:42 UTC · **Branch**: `path-a-selberg-nu` · **HEAD** `ab01f3b`+1 doc commit
(36 commits ahead of `3180948`, all box-local — no push egress). Supersedes `HANDOFF-2026-06-01-2318.md`.

## 🎯 What we're doing
The cross-orbit kernel reduced every matrix entry to one core `S(α,β) = ∑_{p∈monoOrbit α} ∏ᵢ (pᵢ+βᵢ)!`.
The validated closed form is `S(α,β) = ∑_X (∏_b multinomial(n_b;X(·,b)))·(∏_{c,b}((c+b)!)^{X(c,b)})`.
This session built most of the Lean proof of the **fiber count** (the hard combinatorial heart).

## ✅ This session — 4 commits (3 axiom-clean lemmas + docs); the structural core is DONE
The fiber count `#{p∈monoOrbit α : joint-type(p,β)=X} = ∏_b multinomial(n_b;X(·,b))` splits into pieces
(a)–(f). **(a),(b),(d) proven this session; (c) offloaded; (e),(f) remain.**

- **(a) ✅ `mem_monoOrbit_iff`** (`65687d5`): `p ∈ monoOrbit α ↔ ∀ v, #{i:pᵢ=v}=#{i:αᵢ=v}`. Reverse
  glues per-value fiber equivs via `Equiv.ofFiberEquiv`. The orbit→function-counting bridge.
- **(b) ✅ `card_filter_eq_sum_joint`** (`369c98a`): row margin `#{i:pᵢ=c}=∑_b #{i:pᵢ=c∧βᵢ=b}`.
- **(d) ✅ `card_jointType_eq_prod`** (`ab01f3b`) — **THE STRUCTURAL CORE, general over Fintypes
  `ι,V,B`**: `#{f:ι→V | ∀ v b, #{i:f i=v∧g i=b}=X v b} = ∏_b #{q:{i//g i=b}→V | ∀ v,#{j:q j=v}=X v b}`.
  Via `fiberRestrictEquiv` ((ι→V)≃∀b,(fiber_b→V), from `sigmaFiberEquiv`+`piCurry`) + `Fintype.card_pi`
  + `subtypePiEquivPi`; per-coordinate fiber identity by `subtypeSubtypeEquivSubtypeInter`. **Stated
  over a Fintype codomain `V`, so it composes directly with the keystone — the earlier "Fintype vs ℕ
  codomain" wrinkle is gone: take `V=↥(image α)` throughout.**
- **(c) ⏳ keystone at Aristotle** — `#{f:ι→β | ∀ b,#{i:f i=b}=h b}=Nat.multinomial univ h` (Fintypes,
  `∑h=|ι|`). **Project `15ba0cd4-4b7d-445c-a105-169793b91368` ("keystone"), RUNNING at 23:42 (~32 min
  in).** Numerically validated (2000 cases). Statement: `/tmp/keystone/Keystone.lean`. Retrieve:
  `aristotle list | grep 15ba0cd4`; when done `aristotle download 15ba0cd4-4b7d-445c-a105-169793b91368
  --destination /tmp/k.tar.gz && tar -xzf`. Vet it's a real proof (not wrapper+sorry,
  `[[aristotle-partial-verify-decomposition-assembles]]`), port v4.28→v4.29 names, add to the module.
  If it returns junk, prove locally: peel one value, `Nat.multinomial_cons` recursion (the `choose`
  factor = "pick which `h a` slots map to `a`"), mirrors `Multiset.countPerms_filter_ne`.

## 🎬 Next actions
1. **(Host only) Push** — `git push -u origin path-a-selberg-nu` (36 commits). Split into PRs.
2. **Retrieve + vet the keystone (c)** from Aristotle (commands above).
3. **(e) compose** — *cheap*: in `card_jointType_eq_prod`, rewrite each factor
   `#{q:{i//g i=b}→V | ∀ v,#{j:q j=v}=X v b}` via the keystone (c) to `Nat.multinomial univ (X · b)`.
   Needs the per-fiber `∑ v, X v b = Fintype.card {i//g i=b}` (column margin = `n_b`; from the
   analog of `card_filter_eq_sum_joint`). Result: `#{f:ι→V | J_g(f)=X} = ∏_b multinomial(univ, X(·,b))`
   — a clean general lemma.
4. **(f) value-restriction bridge + assembly** — connect `(monoOrbit α).filter (joint-type vs β = X)`
   (codomain ℕ) to (e)'s Fintype form with `V=↥(image α)`, `B=↥(image β)`: lift `p` to `Fin k→↥(image α)`
   (every `p∈monoOrbit α` has values in `image α` by (a)); for a *realized* `X` the orbit constraint is
   redundant (row margins force it via (a)). Then `Finset.sum_fiberwise` over the joint-type map
   `p ↦ (vt ↦ #{i:(pᵢ,βᵢ)=vt})` + weight constancy from `prod_eq_prod_pow_joint` gives
   `S(α,β) = ∑_X (fiber count)·(weight)`. Then the design-doc closed form, then `native_decide` at k=50/54.

## 🧠 Context to carry forward
- The **scratch-prototype workflow worked great**: build pieces in `/tmp/*.lean`, typecheck with
  `taskset -c 0-3 lake env lean /tmp/x.lean` (no olean written, fast), then move into the module and
  `lake build BoundedGaps.SymmetricReduction`. Used it to nail `card_jointType_eq_prod` incrementally.
- `card_jointType_eq_prod` is **general combinatorics** (not orbit-specific) — could later move to a
  `BoundedGaps/FiberCount.lean` helper, but it's fine where it is (a `section FiberProduct` with its
  own `variable`s + `open Finset Equiv`).
- The keystone is the ONLY genuinely-atomic generic fact (perfect Aristotle leaf). Everything else is
  repo glue or the now-done structural core. Do NOT hand Aristotle the whole fiber count.
- Shared bottleneck for BOTH the EH-conditional plain-`Mk` ladder AND the unconditional `Mk_eps 50`
  flagship; the ε-side (`SymmetricReductionEps.lean`) reuses the same fiber count.

## ⚠️ Gotchas (new on top of cumulative)
- `MultiIndex k := Fin k → ℕ` (reducible abbrev). `Equiv.ofFiberEquiv e : Fin k ≃ Fin k` unifies with
  `Equiv.Perm (Fin k)` (defeq).
- `fiberRestrictEquiv g f b j = f j.val` proven by `simp [fiberRestrictEquiv, Equiv.piCurry,
  Equiv.arrowCongr, Equiv.sigmaFiberEquiv]; rfl` (simp leaves a defeq goal `rfl` closes).
- `Equiv.subtypeSubtypeEquivSubtypeInter p q : {x:{a//p a}//q x.val} ≃ {x//p x∧q x}` — for the fiber
  identity, `.symm` it and `subtypeEquivRight` to swap the conjunction order.
- ⚠️ Don't blanket `simp only [← Fintype.card_subtype]`: the predicates *contain* inner `(filter).card`
  (the `= X v b` comparisons) which would also get rewritten. Use explicit `(Fintype.card_subtype P).symm`
  on the outer predicate only (as `card_jointType_eq_prod` does).
- `Aristotle submit "<prompt>"` crashes (Errno 36) if the prompt exceeds ~255 bytes (it stats it as a
  path) — use `--project-dir <dir>` + a short prompt (refines `[[aristotle-submit-prompt-pathmax]]`).
- Box: `taskset -c 0-3 lake build BoundedGaps.SymmetricReduction`; hook rebuilds on commit; bare `python3`.

## 📁 Key files
- `BoundedGaps/SymmetricReduction.lean` — tower + `prod_eq_prod_pow_joint` + **NEW** `mem_monoOrbit_iff`,
  `card_filter_eq_sum_joint`, `fiberRestrictEquiv`, `card_jointType_eq_prod` (`section FiberProduct`).
- `tools/mk/SYMMETRIC_REDUCTION.md` — ⭐ design; sub-pieces (a)–(f) with status.
- `/tmp/keystone/Keystone.lean` — the keystone statement submitted to Aristotle.
- `BoundedGaps/SymmetricReductionEps.lean` / `EpsBridge.lean` — ε-side (reuses the fiber count).

---
**→ Next session: if on the host, push (#1). Retrieve+vet the Aristotle keystone (#2). Then (e) compose
(cheap) + (f) bridge+assembly. The hard structural core (β-group product) is DONE this session. This IS
the chosen thread — don't summarize back, don't offer other KB projects.**

🧵 ➡️ 🧵
