# Handoff: (f) shipped; session pivoted to orientation + axiom-debt taxonomy

**Date**: 2026-06-02 ~02:30 UTC · **Branch**: `path-a-selberg-nu` · **HEAD** `9a58807` (41 commits ahead of `3180948`, box-local)

## 🎯 What we're doing
Two-part session. **Part 1** finished the Lean work — step **(f)** of the symmetric reduction (the
cross-orbit core `S(α,β)` now has a fully-evaluated multinomial closed form). Full technical detail
is in **`HANDOFF-2026-06-02-0135.md` — read that first for the code state.** **Part 2** was
conversational: Trevor asked me to explain where the project stands and how the remaining axioms get
discharged. The successor is most likely picking up in *that* register (orientation/discussion), or
being told "ok, do Step 1."

## 🧠 Context to carry forward
- **Trevor's mode right now is understanding, not coding.** His last three messages: "explain where we
  are and what's next," "how many axioms / sorries," "how will the sieve/narrowness axioms be
  discharged." Answer at post-undergrad-math-major level, decode notation, give the split not the raw
  number, flag confidence. He gets algebra/Galois; pitch abstractions by name.
- **The axiom-debt taxonomy I built this session** (verified by reading docstrings, not guessing):
  - **37 axioms, 3 genuine sorries** on this branch (verified by grep this session).
  - 37 axioms split: ~5 deep external inputs (`EH`, `GEH`, `BombieriVinogradov`, `MPZ_polymath8a`) +
    ~10 `narrowness_*` (cited-external **by design** — Clark-Jarvis 2001 enumeration / Hensley-Richards
    constructions; docstrings say "out of scope, cited as leaves") + ~10 `s1/s2_holds_from_*` /
    `exists_separable_F_*` (the **real dischargeable analytic debt**) + ~13 `mk_*` witness targets.
  - **Discharge stories differ sharply**: `narrowness_*` mostly stay axioms (upper bounds
    mechanizable via explicit tuple + `admissible_pigeonhole` + `native_decide`; lower bounds out of
    scope). `s1/s2_*` = the deep GPY/Maynard sieve asymptotics under EH — dischargeable in principle
    but a multi-month singular-series/divisor-sum lift that **mathlib can't yet support** (no Mertens/
    Selberg/singular-series). `exists_separable_F_*` = the bridge, partly fed by the symmetric-reduction
    track. The 3 sorries: `Zhang.lean:36` (H₁≤70M placeholder), `Prerequisites.lean:155`
    (`geh_implies_eh`), `Polymath8b.lean:797` (twin-primes∨Goldbach).
- **Honest framing I committed to with Trevor** (hold this line): today's (f) work is axiom-clean
  *infrastructure* but **did not change the axiom/sorry count** — it shortens distance-to-discharge for
  the EH-conditional `mk_*` witnesses, nothing more. Don't let the successor inflate it.
- **The two tracks**: the symmetric-reduction / `Mk`-witness work (variational/combinatorial) is one
  track; the sieve-asymptotic `s1/s2` work (analytic, Tier 2/3 on this branch) is the parallel track.
  Both converge on `H₁ ≤ 246`. Today touched only the first.

## ✅ State
- (f) complete & committed `9a58807`; pre-commit hook rebuilt green (8265 jobs); `#print axioms` on
  `orbitCore_eq_multinomial_sum` / `jointType_fiber_card_eq_multinomial` / `orbit_fiber_eq_multinomial`
  = `[propext, Classical.choice, Quot.sound]` only. (All observed in real output this session.)
- Working tree clean except untracked `HANDOFF-*.md`. Branch is box-local (no push egress).

## 🎬 Next actions
1. **Read Trevor's next message and match register.** If he's still orienting, keep explaining (use the
   taxonomy above). If he says "go," start the coding **Step 1** below.
2. **Coding Step 1 (the real next bottleneck): make `orbitCore_eq_multinomial_sum` orbit-free.** It
   currently sums over `X ∈ (monoOrbit α).image (jointMultiset β)` — index references the orbit, so NOT
   yet `native_decide`-able at k=54. Re-index to the explicit Finset of **margin-correct contingency
   tables** (row margins = α's fiber-size vector, col margins = β's), computable from α,β shapes alone.
   The two index sets are equal (every realized table has those margins via `sum_joint_eq_fiber`/`_col`;
   every margin-correct table is realized since its multinomial count ≥1). ~1 session, ~80% confidence.
3. Then: Rayleigh-matrix assembly + `native_decide` at k=54 (degree ≥9 witness, **exact LDL inertia,
   not float** — float lies at factorial scale). This is >1 session; degree wall is real.

## ⚠️ Gotchas
- **Push is host-only** (no GitHub egress in the box). Don't attempt `git push`/`gh`.
- **`HANDOFF.md` (bare) is the master/post-merge doc — do NOT touch it.** Dated `HANDOFF-*.md` are
  per-session. This scheme is deliberate.
- Don't re-derive (f) — it's done. And don't trust a handoff's "what's left" without grepping the repo
  (last session's handoff under-described state; the `jointMultiset` (f1) was already committed).

## 📁 Key files
- `HANDOFF-2026-06-02-0135.md` — the (f) completion handoff (full code-state detail). **Read first.**
- `BoundedGaps/SymmetricReduction.lean` — `section FiberMultinomial` (today's work) + (f1) tower.
- `BoundedGaps/Sieve.lean` — the `s1/s2_holds_from_*` analytic axioms (docstrings explain discharge).
- `BoundedGaps/Polymath8b.lean` / `Basic.lean:42` — `narrowness_*` axioms + `narrowness` def.
- `tools/mk/SYMMETRIC_REDUCTION.md` — design doc; exact reaches-4 LDL table (deg ≤8 <4 ⟹ ≥9).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or offer other
KB projects — this is the chosen thread. First read Trevor's actual next message: if he's still
orienting, answer in that register using the axiom taxonomy above; if he says go, read
`HANDOFF-2026-06-02-0135.md` then start Coding Step 1 (orbit-free re-indexing).**

🧵 ➡️ 🧵
