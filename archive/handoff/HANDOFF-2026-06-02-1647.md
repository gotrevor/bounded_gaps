# Handoff: orbit-free Rayleigh matrix — denominator DONE + native_decide-ready, numerator unlocked

**Date**: 2026-06-02 ~16:47 UTC · **Branch**: `path-a-selberg-nu` · **HEAD** `8360f8c` (53 ahead of `3180948`, box-local)

## 🎯 What this session did
Built `BoundedGaps/SymmetricReductionOrbitFree.lean` (new, sorry-free, axiom-clean) — the
orbit-free re-index of the cross-orbit Rayleigh matrix entries, the prerequisite for
`native_decide`-ing `Mk 54 > 4`. Picks up the prior handoff's "Coding Step 1" (orbit-free
re-index) and carries it through Step 3's foundation.

## ✅ DONE this session (14 commits, all green via pre-commit, all `[propext, Classical.choice, Quot.sound]`)
- **Orbit-free re-index of `S(α,β)` — COMPLETE.** `orbitCore_eq_multinomial_sum_orbitFree`:
  re-indexes the cross-orbit core over `MarginCorrectTables` (a Fintype-bounded contingency
  Finset, computable from α,β shapes — NO `54!` orbit). Realizability converse
  (`table_realized_in_orbit`) discharged via **Aristotle** (`joint_realizability`, job
  `38089125`, ported v4.28→v4.29 with ZERO edits → `BoundedGaps/JointRealizability.lean`).
- **Denominator matrix entry — FULLY orbit-free.** `orbitPair_denominator_shapeForm`:
  `∑∑ monomialIntegral(p+q) = multinomial(β-fibers) • (table sum) / (k+|α|+|β|)!`, no
  `monoOrbit`. Last orbit ref killed by `monoOrbit_card_eq_multinomial`.
- **Denominator is `native_decide`-READY.** `jointWeightC` (computable twin of the
  gratuitously-`noncomputable` `jointWeight`, `= it by rfl`) + `orbitPair_denominator_computable`.
  VALIDATED: full RHS `#eval`s to `11/3360` at α=![2,1,0],β=![1,1,0] (Fin 3); the Pi-Fintype
  `MarginCorrectTables` filter enumerates in-kernel (card 3). **This is option 2 from Trevor's
  "1 & 2!" — done.**
- **Numerator — constant factored + UNLOCKED.** `orbitPair_numerator_eq` factors out the
  constant Dirichlet denominator `(n+1+|α|+|β|+1)!`. `numerator_summand_factor` is the key
  insight (see below).

## 🧠 Context to carry forward
- **The numerator is denominator-grade, NOT a harder pointed build.** I first thought the
  numerator needed a multi-session pointed-contingency / orbit-stabilizer build because the
  denominator's q-collapse (`orbitPair_core_const`) provably FAILS for it (the marked
  coordinate `i` couples `p`,`q`,`i`; reindexing `q=β∘τ` leaves `pᵢ` pinned). **That
  pessimism is superseded.** `numerator_summand_factor` proves
  `∏ⱼ(removeNthᵢ(p+q))ⱼ! · (pᵢ+qᵢ+2)! = (∏ₘ(p+q)ₘ!) · (pᵢ+qᵢ+2)!/(pᵢ+qᵢ)!` — a joint-type
  weight times a LOCAL factor in `(pᵢ,qᵢ)` only. So `∑ᵢ`(local) `= ∑cells Y_{a,b}·M(a,b)`
  where `M(a,b)=(a+b+2)!/((a+b)!(a+1)(b+1))` and `Y` is the (p,q) joint type. The whole
  numerator double-sum regroups over the SAME (p,q) joint-type contingency tables as the
  denominator. The q-collapse failing doesn't matter — joint-type regrouping doesn't use it.
- **The contingency machinery is all already in `SymmetricReduction.lean`**: `card_jointType_eq_prod_multinomial`,
  `card_jointType_eq_prod`, `card_fiberwise_eq_multinomial`, `mem_monoOrbit_iff`,
  `prod_eq_prod_pow_joint`, `monoOrbit_mem_degree`. The denominator re-index reused these; the
  numerator will too.
- **Fleet discipline**: all new work in NEW files (`SymmetricReductionOrbitFree.lean`,
  `JointRealizability.lean`). Did NOT touch shared `SymmetricReduction.lean` (other box
  sessions run on the same tree). Transient OOM on `lake env lean` → just retry.

## 🎬 Next actions (option 1: finish the numerator → then the matrix assembly)
1. **Numerator orbit-free sum.** Using `numerator_summand_factor`, express the numerator
   combinatorial sum (RHS of `orbitPair_numerator_eq`) as
   `∑_{p∈orbit α}∑_{q∈orbit β} (∏ₘ(p+q)ₘ!) · (∑ᵢ M(pᵢ,qᵢ))`, then regroup over the (p,q)
   joint type Y (margins = α-fibers × β-fibers). Reuse the `MarginCorrectTables` /
   `card_jointType` machinery; the per-table summand is `[∏cells((a+b)!)^Y] · [∑cells Y·M] ·
   (pair-count)`. Pair-count = #{(p,q)∈orbitα×orbitβ : jointtype=Y} — a `card_jointType`-style
   multinomial product. ~1-2 sessions, denominator-grade.
2. **Computable numerator** (mirror `jointWeightC`/`orbitPair_denominator_computable`) for
   `native_decide`.
3. **Then the matrix assembly** (doc Step 2, still genuinely multi-session): symmetric weight
   `P_sym = ∑ c_λ orbitSum_λ` → bilinear `∑ c_λ c_μ Mr[λ][μ]`, then at k=54 `native_decide`
   the LDL inertia at degree ≥9. Chain `Mk_ge_polynomialMkF` → `Mk 54 > 4` → discharge
   `mk_54_witness_under_EH`.

## ⚠️ Gotchas
- **Push is host-only** (no GitHub egress in box). Don't `git push`/`gh`.
- **Don't touch bare `HANDOFF.md`** (master/post-merge). Dated `HANDOFF-*.md` are per-session.
- `jointWeight` library marker is gratuitously `noncomputable` — use the `jointWeightC` twin
  for anything `native_decide`/`#eval`.
- `MultiIndex.degree` dot-notation may not resolve on a raw `Fin k → ℕ` literal in `#eval`;
  use `∑ i, α i`.

## 📁 Key files
- `BoundedGaps/SymmetricReductionOrbitFree.lean` — all this session's work.
- `BoundedGaps/JointRealizability.lean` — Aristotle's ported realizability proof.
- `tools/mk/SYMMETRIC_REDUCTION.md` — design doc; see the two 2026-06-02 status sections at
  the END (the second CORRECTS the numerator pessimism).
- `BoundedGaps/SymmetricReduction.lean` — the contingency-table toolkit + `orbitPair_*` /
  `dirichletNum_orbitSum_const` the work builds on (SHARED — read, don't edit).

---
**→ Next session: the orbit-free denominator (incl. native_decide-ready) is DONE; the
numerator is unlocked by `numerator_summand_factor` and is denominator-grade. Start Next
action #1: regroup the numerator over (p,q) joint types. Verify build green before trusting
this doc (`lake build BoundedGaps.SymmetricReductionOrbitFree`).**

🧵 ➡️ 🧵
