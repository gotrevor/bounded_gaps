# Handoff: cross-orbit kernel — contingency design validated, reduced to ONE counting theorem

**Date**: 2026-06-01 13:10 UTC · **Branch**: `path-a-selberg-nu` · **HEAD** (30 commits ahead of
`3180948`, all local-only — box can't push). Supersedes `HANDOFF-2026-06-01-1250.md`.

## 🎯 What we're doing
The shared symmetric-reduction kernel for `polynomialMkF` / `polynomialMkF_eps` — needed by BOTH the
EH-conditional plain-`Mk` ladder and the unconditional `Mk_eps 50` flagship (they share this one
bottleneck; `b`'s `EpsBridge` is already done, only the orbit-level witness remains). Trevor's steer:
"keep trucking — that work might be thinking deeply & coordinating carefully." So this leg was design
+ de-risking + one foundational proof.

## ✅ This session — 7 commits, all axiom-clean `[propext, choice, Quot.sound]`, hook-verified green
A complete reduction tower **plus** a validated design for the remainder:
- (commits 1–5, see prior handoff) plain numerator diagonal; full ε-side diagonal
  (`SymmetricReductionEps.lean`); `orbitPair_denominator_eq` (factor out `(k+|α|+|β|)!`);
  `orbitPair_core_const` (double orbit sum → single); `group_sum_eq_stab_smul_orbitSum`
  (orbit-stabilizer bridge). **Net: every cross-orbit matrix entry reduced to the single-orbit core
  `S(α,β) = ∑_{p∈orbit α} ∏ᵢ(pᵢ+βᵢ)!`.**
- **(NEW this leg) Validated the contingency-table closed form** for `S(α,β)`
  (`tools/mk/validate_contingency.py`, 25 structured+random cases vs brute, ALL MATCH):
  `S(α,β) = ∑_X (∏_t multinomial(n_t;x_{·,t})) · ∏_{u,t}((c_u+b_t)!)^{x_{u,t}}`, X = r×s nat matrix with
  row sums `m_u` (α-value mults) / col sums `n_t` (β-value mults). **This replaces `mk_sym.py`'s
  bespoke partial-matching type** — contingency tables are margin-constrained `ℕ`-matrices, far more
  Lean-friendly. The `ff(k,T)=descFactorial` re-emerges from the zero-margin multinomials; at fixed
  `k=50/54` the table sum is a concrete `native_decide`-able rational. Design recorded in
  `tools/mk/SYMMETRIC_REDUCTION.md` (⭐ section).
- **(NEW) Proved step 1 of that form**: `prod_eq_prod_pow_joint` (axiom-clean) —
  `∏ᵢ G(pᵢ)(βᵢ) = ∏_{vt} G(vt.1)(vt.2)^{#{i:(pᵢ,βᵢ)=vt}}` (the weight depends only on the joint type),
  via `Finset.prod_fiberwise_of_maps_to`. With `G a b=(a+b)!` this is the `∏((c_u+b_t)!)^{x_{u,t}}` factor.

## 🎬 Next actions
1. **(Host only) Push** — `git push -u origin path-a-selberg-nu` (30 commits). Split into PRs.
2. **The fiber count — the single remaining combinatorial theorem (the heart):**
   `#{p ∈ orbit α : joint-type(p,β) = X} = ∏_t multinomial(n_t ; x_{·,t})` for a table `X` with the
   correct margins (0 otherwise). Then `Finset.sum_fiberwise` over the joint-type map `p ↦ (vt ↦ #{i:(pᵢ,βᵢ)=vt})`
   assembles `S(α,β) = ∑_X (fiber count)·(weight)`, combining with `prod_eq_prod_pow_joint`.
   **mathlib toolkit (identified this session — use it, don't re-derive):**
   - `Nat.multinomial s f` + `Nat.multinomial_spec : (∏ i∈s, (f i)!) * multinomial s f = (∑ i∈s, f i)!`
     (`Mathlib/Data/Nat/Choose/Multinomial.lean`).
   - `Multiset.countPerms m = m.toFinsupp.multinomial` = "#lists inducing the multiset `m`" — this is
     both `|orbit α|` (lists/rearrangements of α's value-multiset) AND the per-β-group fill count.
     `countPerms_filter_ne` peels one value off (induction handle).
   - Structure: within each β-group `t` (the `n_t` slots with `β=b_t`), the value assignment with
     counts `x_{·,t}` is counted by `multinomial(n_t; x_{·,t})`; groups are independent; the global
     margins `∑_t x_{u,t}=m_u` make `p` a rearrangement of `α`. The fiber bijects to `∏_t (group-t fills)`.
   *Design the joint-type / contingency-table representation first (a `Fin r → Fin s → ℕ` or a Finsupp;
   the margins as a Finset filter). Don't rush the type.*
3. Companion (also via `countPerms`/`multinomial`): `(stab α).card = ∏ᵥ (|α-fiber v|)!` — evaluates the
   stabilizer factor `group_sum_eq_stab_smul_orbitSum` left open. More tractable; warm-up.
4. After the plain `S(α,β)` closed form: the ε-version reuses it (the `affineSlackRat` weight has the
   same joint-type dependence).

## ⚠️ Gotchas (cumulative)
- `Fin.removeNth i` forces `Fin (succ _)` (numerator-side lemmas at `n+1`).
- `rw` syntactic vs orbit-term defeq — bind via `have : <exact form> := lemma` then `rw`, or
  `simp only [Pi.add_apply]`. `unfold MultiIndex.degree` expands ALL `.degree`s — use `show`.
  Nat assoc `k+(α+β)` vs `(k+α)+β` → `← add_assoc`.
- `Equiv.Perm.apply_inv_self`/`inv_apply_self` DON'T exist — `ρ(ρ⁻¹ i)=i` via
  `rw [← Equiv.Perm.mul_apply, mul_inv_cancel, Equiv.Perm.one_apply]`; `group` closes `(τ*ρ)*ρ⁻¹=τ`.
- `Finset.prod_fiberwise` needs `Fintype` on the codomain — for `ℕ×ℕ` value pairs use the `_of_maps_to`
  variant with `t := univ.image …` (as `prod_eq_prod_pow_joint` does).
- Box: `taskset -c 0-3 lake build <Module>`; hook rebuilds; new modules must be imported by
  `BoundedGaps.lean`. Box has python3+stdlib (no `sandbox`/nix) — run validators with bare `python3`.

## 📁 Key files
- `BoundedGaps/SymmetricReduction.lean` — the tower + `prod_eq_prod_pow_joint`; status block at bottom.
- `BoundedGaps/SymmetricReductionEps.lean` — ε-side reductions.
- `tools/mk/SYMMETRIC_REDUCTION.md` — ⭐ validated contingency design (the Lean target) + matching oracle.
- `tools/mk/validate_contingency.py` — the brute-vs-closed-form validator (25 cases, ALL MATCH).
- `BoundedGaps/EpsBridge.lean` — the `Mk_eps` bridge (DONE through `mk_eps_50_witness_of_poly`).

---
**→ Next session: if on the host, push (#1). Then the work is the fiber-count theorem (#2) — the single
remaining combinatorial heart, now well-scoped with the `Nat.multinomial`/`Multiset.countPerms` toolkit
identified and the contingency design validated. This IS the chosen thread — don't summarize back, don't
offer other KB projects.**

🧵 ➡️ 🧵
