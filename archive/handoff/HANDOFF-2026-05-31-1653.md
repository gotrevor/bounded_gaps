# Handoff: Symmetric reduction — scoped + denominator invariance founded

**Date**: 2026-05-31 16:53 UTC · **Branch**: `path-a-selberg-nu` · **HEAD** `bec95b7` (7 commits ahead of `3180948`, local-only — box can't push)

## 🎯 What we're doing
Resumed from `HANDOFF-2026-05-31-1443.md` on the open question: build the symmetric-function reduction for `polynomialMkF` (to discharge the larger `mk_*` witnesses), or bank? Trevor said "2 or 3, your choice" then "keep moving." I chose to **scope it properly, then start the Lean foundation** — and along the way took a full inventory of the repo's remaining debt (he asked for the axiom/sorry summary at the end).

## 🧠 Context to carry forward
- **The symmetric reduction is correct + de-risked, but lower-value than it looked.** I built `tools/mk/mk_sym.py`: a *closed-form* (matching/overlap → falling factorials over a factorial) reduction of the Maynard Rayleigh quotient over degree-≤D symmetric polys, validated EXACTLY against the brute monomial oracle (`mk5_sym.py`) for k=2..9. Full verdict in `tools/mk/SYMMETRIC_REDUCTION.md`.
- **I got burned by float and corrected it — don't repeat this.** A float generalized-Rayleigh power iteration reported "degree-7 clears M₅₀>4 (sup≈4.36)." That is **false**: the orbit basis spans ~10⁸⁹ in L²-norm, so float silently lies. Exact rational LDL-inertia (`A−4M` negative definite) shows degree-7 is *below* 4 at k=50 AND k=54; true crossing degree is high (≥8, likely teens) because the true M₅₀ sits *just* above 4 (why Polymath8b needed k=50 exactly). **Lesson flushed to memory: `float-rayleigh-factorial-illconditioned`.**
- **What the reduction would actually buy:** only the **EH-conditional** plain-`Mk` ladder (`mk_54`>4, `mk_5511`>6, `mk_41588`>8, `mk_309661`>10). The unconditional flagship (`mk_eps_50` → H₁≤246) routes through `Mk_eps` (ε-trick) which has **no polynomial bridge** — that's separate, additional work. So this is not a path to the headline result, only to the EH-conditional rungs (same vein as the `mk_5` we did last session).
- **Why I stopped where I did:** the real Lean cost is a *parametric-in-k symmetric-function integration identity* (the matching closed form, proven generally) — multi-session, mathlib-grade, NOT Aristotle-shaped (not bounded/decidable). `native_decide` only finishes the final small orbit-matrix inequality; you can't materialize the witness's monomial Finset (orbit `(1⁷)` alone is C(54,7)≈177M monomials). I proved the *denominator permutation-invariance* (the clean foundational piece) and deliberately did NOT grind the numerator invariance: ~60-100 fiddly lines (induced perm on `removeNth`) that discharge nothing on their own, and each box build OOM-retries for many minutes. Low leverage. Queued for a host session.
- **The repo's easy vein is fully mined.** Every remaining axiom/sorry is research-grade (see State). The finite admissible-tuple discharges (50/51/54/5511) are all done.

## ✅ State (all observed this session)
- `f3653e0` — scoping: `mk_sym.py` (closed form, validated) + `_exact.py`/`_ldl.py` (exact LDL inertia) + `SYMMETRIC_REDUCTION.md` (corrected verdict).
- `a54d04e` — **`BoundedGaps/SymmetricReduction.lean`** (new, axiom-clean, sorry-free): `permWeight`, `permTerm_injective`, `monomialIntegral_comp_perm`, and `polynomialMaynardDenominator_permWeight` (denominator invariance). Wired into the `BoundedGaps` aggregator root. `#print axioms` = 3 standard only. Plus 4 stale-sorry docstring fixes (Engelsma/Polymath8b/Targets/SievePolynomial — tuple_5511/tuple_50 are theorems now, SievePolynomial is sorry-free).
- `bec95b7` — design-doc update recording Lean foundation status + the two next Lean steps.
- **Verified green**: full `lake build` = "Build completed successfully (8262 jobs)" (after several transient-OOM retries; memory is fine, ~28Gi free after `pkill`).
- **Inventory (told Trevor):** 37 axioms + 3 sorries. Axioms: Prerequisites 4 (BV/GBV/MPZ_polymath8a/GEH-opaque), Sieve 10 (s1/s2/exists_separable asymptotics), Polymath8b 23 (13 `mk_*` variational + 10 `narrowness_*`). Sorries: `geh_implies_eh` (Vaughan wall), `Zhang:36` (method-faithful, result shipped via Polymath8b), `Polymath8b:797` (twin-or-Goldbach, ~weeks).

## 🎬 Next actions
1. **Trevor's next message likely steers.** Default options to put to him (or act on if he already chose): (a) **numerator permutation-invariance** → full `polynomialMkF_permWeight` (recipe in `SYMMETRIC_REDUCTION.md` "Lean foundation status": `dirichletIntegralWithSlack` is multiset-invariant; `removeNth j (v∘σ)` has same value-multiset as `removeNth (σj) v` via `Fin.prod_univ_succAbove`+`Equiv.prod_comp`/`sum` + ℕ-cancel; then reindex i-sum by σ) — **better on the host** (box builds OOM-grind); (b) the **matching closed-form identity** (the real multi-session kernel); (c) the separate **`Mk_eps` polynomial bridge** (only thing that moves the unconditional flagship); (d) pivot / pause.
2. **Host-only**: push `path-a-selberg-nu` (HEAD `bec95b7`, 7 ahead of `3180948`). Box has no egress.

## ⚠️ Gotchas
- **Box build = OOM-grind.** Editing a docstring in a deeply-imported / `native_decide`-heavy file (SievePolynomial, Engelsma, Mk5Witness) forces a full downstream recompile incl. re-running the 5511 `native_decide` (minutes). OOM is *transient parallel pressure*, not scarcity — `pkill -9 lean lake`, then plain `lake build` retries converge (this lake rejects `-j`/`--jobs`). Lesson flushed: `lean-docstring-edit-recompile-cascade`.
- A new `.lean` not imported by the aggregator root is skipped by `lake build` (false green) — `SymmetricReduction` is now imported into `BoundedGaps.lean`, so it's covered. Don't forget that for any further new module.
- Don't touch a master `HANDOFF.md` (post-merge baseline). Dated `HANDOFF-*` are per-session.

## 📁 Key files
- `BoundedGaps/SymmetricReduction.lean` — the new foundation (denominator invariance proven; numerator is the next lemma).
- `BoundedGaps/SievePolynomial.lean` — `polynomialMaynardNumerator/Denominator`, `Mk_ge_polynomialMkF` (L501), `denom_bridge` (L719). The reduction builds on these.
- `tools/mk/mk_sym.py` — validated closed-form reduction (`validate` subcmd); `tools/mk/_ldl.py`/`_exact.py` — the trustworthy exact LDL path.
- `tools/mk/SYMMETRIC_REDUCTION.md` — full scoping verdict + exact numbers + Lean foundation status + the two next-step recipes.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the context above, then act on Trevor's steer re: Next action #1 (numerator invariance vs matching identity vs Mk_eps bridge vs pivot). If he hasn't steered, the honest framing is: the symmetric reduction is correct but multi-session and only reaches the EH-conditional ladder — say so, don't oversell it.**

🧵 ➡️ 🧵
