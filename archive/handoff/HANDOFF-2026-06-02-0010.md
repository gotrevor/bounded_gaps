# Handoff: GENERAL FIBER COUNT COMPLETE — keystone proven natively; only the monoOrbit bridge (f) remains

**Date**: 2026-06-02 00:10 UTC · **Branch**: `path-a-selberg-nu` · **HEAD** `95131fe`+1 doc commit
(43 commits ahead of `3180948`, all box-local — no push egress). Supersedes `HANDOFF-2026-06-01-2342.md`.

## 🎯 What we're doing
Cross-orbit kernel: every matrix entry reduced to one core `S(α,β) = ∑_{p∈monoOrbit α} ∏ᵢ (pᵢ+βᵢ)!`.
Validated closed form `S(α,β) = ∑_X (∏_b multinomial(n_b;X(·,b)))·(∏_{c,b}((c+b)!)^{X(c,b)})`. This
session **finished the entire general fiber count** — including proving the keystone that prior
sessions had deemed multi-session and offloaded to Aristotle.

## ✅ This session — the general fiber count is COMPLETE (6 axiom-clean lemmas, all hook-verified green)
The fiber count `#{p∈monoOrbit α : joint-type(p,β)=X} = ∏_b multinomial(n_b;X(·,b))` split into (a)–(f).
**(a)–(e) ALL DONE; only (f) remains.** Commits:
- **(a) `mem_monoOrbit_iff`** (`65687d5`): `p∈monoOrbit α ↔ ∀v,#{i:pᵢ=v}=#{i:αᵢ=v}`.
- **(b) `card_filter_eq_sum_joint`** (`369c98a`): row margin `#{i:pᵢ=c}=∑_b #{i:pᵢ=c∧βᵢ=b}`.
- **(d) `card_jointType_eq_prod`** (`ab01f3b`): β-group product (structural core), general Fintypes.
- **`exists_fiberwise`** (`84b11b1`): witness with prescribed fibers, via `ι ≃ Σv,Fin(h v)`.
- **(c) `card_fiberwise_eq_multinomial`** (`0282250`) — **THE KEYSTONE, PROVEN NATIVELY**:
  `#{f:ι→V | ∀v,#{a:f a=v}=h v} = multinomial univ h` when `∑h=|ι|`. Orbit–stabilizer: `σ↦f₀∘σ`
  surjects `Perm ι` onto the fiber set with fibers = stabilizer cosets, so `|ι|! = #{fibers=h}·∏(h v)!`
  (`DomMulAct.stabilizer_card`); cancel against `Nat.multinomial_spec`. **Aristotle project `15ba0cd4`
  was CANCELLED** — I landed it by hand first (no v4.28→29 port).
- **(e) `card_jointType_eq_prod_multinomial`** (`95131fe`): `#{f:ι→V | J_g(f)=X} =
  ∏_b multinomial(univ, X(·,b))`, given column margins `∑v X v b = |{i//g i=b}|`. Composes (d)+(c).
  **The general contingency-table closed form — the whole fiber count, over arbitrary Fintypes.**

All `#print axioms` = `[propext, Classical.choice, Quot.sound]`. All in a `section FiberProduct`
(`open Finset Equiv`, `open scoped Nat`) in `SymmetricReduction.lean`.

## 🎬 Next actions
1. **(Host only) Push** — `git push -u origin path-a-selberg-nu` (43 commits). Split into PRs (the
   `SymmetricReduction` fiber-count work is a clean standalone PR).
2. **(f) value-restriction bridge + S(α,β) assembly — THE LAST PIECE.** Connect the *general* fiber
   count `card_jointType_eq_prod_multinomial` (over Fintypes `ι,V,B`) to the *orbit-specific* sum
   `∑_{p∈monoOrbit α} ∏ᵢ(pᵢ+βᵢ)!` (codomain ℕ):
   - Specialize `ι := Fin k`, `V := ↥(image α)`, `B := ↥(image β)`. Lift `p : Fin k → ℕ`
     (values in `image α` by **(a)** `mem_monoOrbit_iff`) to `Fin k → ↥(image α)`; `β` likewise.
   - For a *realized* table `X` (right margins), `{p∈monoOrbit α : joint-type=X}` = `{f : Fin k→↥S |
     J_g(f)=X}` (the orbit constraint is redundant — the row margins force `p∈monoOrbit α` via (a);
     **(b)** `card_filter_eq_sum_joint` supplies the margin bookkeeping; the keystone's `hcol` column
     margin is the β-fiber-size = `n_b`).
   - Then `Finset.sum_fiberwise` over the joint-type map `p ↦ (vt ↦ #{i:(pᵢ,βᵢ)=vt})`, with the
     summand constant on each class = the weight from `prod_eq_prod_pow_joint` (already in the file),
     gives `S(α,β) = ∑_X (fiber count)·(weight)` = the design-doc closed form.
   - **Then** `native_decide` at fixed k=50/54 (≥deg 9 witness, per the exact LDL table).
   This bridge is intricate plumbing (the ℕ→↥S lift, the realized-X case analysis) — ~50-80 lines,
   own session. All the hard combinatorics is now behind us.

## 🧠 Context to carry forward
- **The keystone got proven by hand, fast** — orbit–stabilizer via `DomMulAct.stabilizer_card`
  (mathlib's "card of permutations preserving a function" = `∏ fiber!`) + `Nat.multinomial_spec`
  (`∏(h v)!·multinomial=(∑h)!`). The witness (`exists_fiberwise`) and the surjectivity
  (`Equiv.ofFiberEquiv` from equal fibers, = (a)'s reverse) were the only real sub-steps. NOT
  multi-session — the prior "offload to Aristotle" estimate was too pessimistic once `stabilizer_card`
  was found. Lesson: grep mathlib's `GroupTheory/Perm/` for the hard combinatorial cardinalities.
- The **scratch-prototype workflow** (`/tmp/*.lean` + `lake env lean`, then move into the module) was
  decisive for nailing the keystone's fiddly perm algebra incrementally.
- Shared bottleneck for BOTH the EH-conditional plain-`Mk` ladder AND the unconditional `Mk_eps 50`
  flagship; the ε-side (`SymmetricReductionEps.lean`) reuses the same fiber count.

## ⚠️ Gotchas (new on top of cumulative)
- `Equiv.Perm.apply_inv_self`/`inv_apply_self` DON'T exist (v4.29.1). For `(c y)((c y)⁻¹ a)=a` use
  `rw [← Equiv.Perm.mul_apply, mul_inv_cancel, Equiv.Perm.one_apply]`; for the round-trips, `group`
  closes Perm-group equalities (`a*b⁻¹*b=a` etc.) inside `Subtype.ext (by group)`.
- `DomMulAct.stabilizer_card f₀ : Fintype.card {g : Perm ι // f₀ ∘ g = f₀} = ∏ i, (#{a//f₀ a=i})!` is
  a **plain subtype card** — use it directly (no MulAction instance plumbing; the MulAction `orbit`
  route hits `Fintype (orbit …)`/`Fintype ((Perm ι)ᵈᵐᵃ)` synthesis failures, avoid it).
- For the per-fiber bijection in the keystone, sum over fibers with `Finset.card_eq_sum_card_fiberwise`
  + a per-`y` Equiv to the stabilizer (using a `choose`n preimage) — avoids the dependent-Prod
  bijection's right_inv pain.
- `Equiv.sigmaSubtype v : {s : Σ w, β w // s.fst = v} ≃ β v` (for `exists_fiberwise`'s fiber count).
- `aristotle cancel <project-id>` cancels a running job (sets it IDLE).

## 📁 Key files
- `BoundedGaps/SymmetricReduction.lean` — tower + `prod_eq_prod_pow_joint` + (a) `mem_monoOrbit_iff`,
  (b) `card_filter_eq_sum_joint`, and `section FiberProduct`: `exists_fiberwise`, `fiberRestrictEquiv`,
  (d) `card_jointType_eq_prod`, (c) `card_fiberwise_eq_multinomial`, (e) `card_jointType_eq_prod_multinomial`.
- `tools/mk/SYMMETRIC_REDUCTION.md` — ⭐ design; sub-pieces (a)–(e) DONE, (f) remaining.
- `BoundedGaps/SymmetricReductionEps.lean` / `EpsBridge.lean` — ε-side (reuses the fiber count).

---
**→ Next session: if on the host, push (#1). Then (f) the value-restriction bridge + `sum_fiberwise`
assembly into `S(α,β)` (#2) — the ONLY remaining piece; all hard combinatorics done. This IS the
chosen thread — don't summarize back, don't offer other KB projects.**

🧵 ➡️ 🧵
