# Handoff: the `Mk_eps` polynomial bridge is COMPLETE (axiom-clean)

**Date**: 2026-06-01 ~00:40 UTC · **Branch**: `path-a-selberg-nu` · **HEAD** `672ccc0`
(17 commits ahead of `3180948`, local-only — box can't push). Supersedes `HANDOFF-2026-05-31-1845.md`.

**UPDATE (commit `672ccc0`)**: the `>2/ϑ` generalization + the `mk_eps_50_witness` connector
are now DONE (see below). The only remaining work is the degree-50 polynomial + `native_decide`.

## 🎯 What we did
Resumed from `HANDOFF-2026-05-31-1845.md` (Next action #1: "finish the `Mk_eps` bridge").
**Done — the entire bridge, axiom-clean, zero sorries.** New file `BoundedGaps/EpsBridge.lean`
(40 decls) wires `EpsScaling`'s closed forms into a full reduction:

`polynomialMkF_eps P ε > 4`  ⟹  `Mk_eps k ε > 4`   (rational `0 ≤ ε < 1`).

## ✅ State (all observed)
- **`lake build` green** — pre-commit gate ran full `lake build` (8264 jobs) on each of the two
  code commits. `EpsBridge` builds clean (no warnings of ours after lint pass).
- **`#print axioms` = propext/Classical.choice/Quot.sound** on `Mk_eps_ge_MkF_eps`,
  `polynomialMkF_eps_eq_MkF_eps`, `Mk_eps_gt_four_of_polynomial_witness`. No new axioms/sorries.
- Two commits: `ca073e9` (DCT half) + `f8cffbd` (rational bridge + lint). Also committed the
  fixed `.githooks/` (was untracked in the box) so the gate is durable here.

### What `EpsBridge.lean` contains
1. **`simplexLE k B`** — the `B`-truncated simplex `{t≥0 : ∑≤B}`. `simplex_eps`/`simplex_shrunk`
   are `rfl`-instances at `B=1±ε`. Geometry proved ONCE, generally: `convex`, `isClosed`,
   `interior_simplexLE_subset` (metric perturbation, needs `0<B`), `simplexLE_diff_open_null`
   (`Convex.addHaar_frontier`), `ae_open_simplexLE`.
2. **`chiB k n B`** — smooth cutoff `(∏σ(n tᵢ))·σ(n(B-∑t))` for the `B`-simplex (general `chi`):
   nonneg, le_one, smooth, `support ⊆ simplexLE B`, `eventually_eq_one`/`tendsto_one` on interior.
3. **Two-layer DCT** (mirrors `SievePolynomial`): `FapproxEps = chiB(1+ε)·P`; `denomEps_tendsto`
   (over `simplex_eps`), `innerIntEps_tendsto`/`J_i_eps_tendsto`/`numerEps_tendsto` (over
   `simplex_shrunk`, inner bound `1+ε-∑s`), assembling **`Mk_eps_ge_MkF_eps`** (`0≤ε<1`).
4. **Rational bridge**: `affineSlackRat` + `affineSlack_cast` (real affine-slack integral = its
   rational cast, via `dirichlet_affine_slack`); `polynomialMaynardNumerator_eps`/`_Denominator_eps`/
   `polynomialMkF_eps`; `denomEps_bridge`/`numerEps_bridge`/**`polynomialMkF_eps_eq_MkF_eps`**.
5. **Discharge**: **`Mk_eps_gt_four_of_polynomial_witness`** — verified rational ratio `>4` ⟹
   `Mk_eps k ε > 4`. (ε-analog of `SievePolynomial.Mk_gt_four_of_polynomial_witness`.)

## 🧠 Context to carry forward
- **The bridge is a *reduction*, not a discharge.** The flagship `H₁≤246` routes through the
  AXIOM `Polymath8b.mk_eps_50_witness` (`∃ ε ϑ, 1+ε<1/ϑ ∧ Sieve.Mk_eps 50 ε > 2/ϑ`, with ϑ in the
  BV range `<1/2`). The bridge gives the *machinery* to discharge it — but only once an explicit
  degree-50 polynomial `P` + rational `(ε,ϑ)` exist with `polynomialMkF_eps P ε > 2/ϑ`. That
  witness is **not done** and is the next big chunk (see actions).
- **Threshold is `2/ϑ`, not `4`.** `mk_eps_50_witness` wants `Mk_eps 50 ε > 2/ϑ` for ϑ slightly
  `<1/2` (so `2/ϑ` slightly `>4`), coupled with `1+ε < 1/ϑ`. The current discharge lemma is
  hard-coded to `>4`; **generalize it to `> c`** (trivial: replace the `4` literal with a `c`
  param and thread `hP`/`linarith`) before wiring to the witness.
- **Why the bridge math was tractable**: everything mirrored `SievePolynomial`'s plain-case
  development; the genuinely new pieces (affine-slack numerator kernel + homothety denominator)
  were already closed-form in `EpsScaling`. The B-truncated-simplex generalization halved the
  geometry by sharing the `1±ε` cases.

## ✅ DONE since 0015 (commit `672ccc0`)
The mechanical wiring is complete and axiom-clean:
- `Mk_eps_ge_polynomialMkF_eps` — clean `(polynomialMkF_eps P ε : ℝ) ≤ Mk_eps k ε` lower bound.
- `Mk_eps_gt_of_polynomial_witness` — **general threshold** `> c` (the `> 4` lemma is now a wrapper).
- `mk_eps_50_witness_of_poly (P : PolynomialSieveWeight 50) {ε ϑ : ℚ} …` — its conclusion is
  **verbatim the body of `Polymath8b.mk_eps_50_witness`** (verified defeq by a scratch `example`).
  Hypotheses: `0<ε<1`, `0<ϑ<1/2`, `1+ε<1/ϑ`, and the one rational bound
  `polynomialMkF_eps P ε > 2/ϑ`. Supply a concrete `(P₅₀, ε, ϑ)` proving that last inequality and
  the axiom is discharged (replace it: `mk_eps_50_witness := mk_eps_50_witness_of_poly …`).

## 🎬 Next actions
1. **The degree-50 ε-witness** (the ONLY remaining work; large, possibly host/compute-bound):
   - **The hard part**: obtain/optimize the actual symmetric degree-50 polynomial coefficients
     (Polymath8b did this numerically in Maple). Then a *computable* mirror of `polynomialMkF_eps`
     (rationals are computable; `polynomialMkF_eps` is `noncomputable` only by inheritance —
     see how `Mk5Witness.lean` made the plain degree-5 case `native_decide`able) and a large
     `native_decide`/exact-rational check of `polynomialMkF_eps P₅₀ ε > 2/ϑ`. **Likely blocked
     offline** — the coefficient data / the optimization may need the host or internet. Consider
     Aristotle for sub-lemmas, NOT for the optimization itself.
2. **Host-only**: push `path-a-selberg-nu` (HEAD `f8cffbd`, 16 ahead of `3180948`). Also reconcile
   `.githooks/` — committed here in the box; confirm the host clone tracks the same (decisions.md
   says it should already).

## ⚠️ Gotchas
- **`rw [affineSlack_cast …]` closes the goal by `rfl`** (the earlier `push_cast` already made both
  sides identical), so a trailing `push_cast`/`ring` errors "No goals". Drop the closer. This is
  exactly `[[lean-rw-closes-by-rfl-trailing-closer-no-goals]]`.
- `interior_simplexLE_subset` needs `0<B` (the empty-`Fin` case reduces to `0<B`); the ε-instances
  get it from `0≤ε` (`1+ε>0`) and `ε<1` (`1-ε>0`). `J_i_eps_tendsto` therefore needs `ε<1`.
- `J_i_eps_tendsto` dominating constant is `(Cp·(1+ε))²` (inner integral over `[0,1+ε-∑s]`,
  `vol ≤ 1+ε` since `∑s≥0`), not the plain `Cp²`.
- `.githooks/` still **untracked on disk vs. tracked-in-commit**: I `git add`-ed it in commit
  `ca073e9`, so it IS in history now. Fresh box clones still need `git config core.hooksPath .githooks`.

## 📁 Key files
- `BoundedGaps/EpsBridge.lean` — the whole bridge (geometry + cutoff + DCT + rational + discharge).
- `BoundedGaps/EpsScaling.lean` — the closed forms it consumes.
- `BoundedGaps/SievePolynomial.lean` — the plain-`Mk` template it mirrors.
- `BoundedGaps/Polymath8b.lean:68` — the `mk_eps_50_witness` axiom this reduces.
- `BoundedGaps/Mk5Witness.lean` — model for the eventual `native_decide` witness.

---
**→ Next session: the `Mk_eps` bridge is DONE & axiom-clean. The only thing between us and
discharging the `mk_eps_50_witness` axiom is the explicit degree-50 polynomial witness +
`native_decide` (large, maybe host-bound). Generalize the discharge lemma to `>2/ϑ` first.**

🧵 ➡️ 🧵
