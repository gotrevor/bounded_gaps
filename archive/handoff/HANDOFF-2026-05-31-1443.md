# Handoff: Aristotle leverage + the `mk_*` witness vein (two axioms discharged)

**Date**: 2026-05-31 14:43 UTC · **Branch**: `path-a-selberg-nu` · **HEAD** `500df0e` (local-only — box has no egress; host must push)

## 🎯 What we're doing
Session started from `HANDOFF-2026-05-31-1311.md` with Trevor's question: "how can we use **Aristotle** (Harmonic's auto-formalizer) on bounded_gaps?" That turned into actually using it, and into a real-math thread: discharging the `mk_*` M_k variational-bound axioms via the repo's existing polynomial-Rayleigh machinery. Two axioms went axiom→theorem this session.

## 🧠 Context to carry forward
- **Aristotle is leverage on bounded, *architected* leaves + heavy compute — not a thinker.** It one-shot `tuple_5511_admissible` (a finite decidable check) following the strategy I handed it; ported zero-edit. Use it where the math is already decomposed and the cost is mechanical/compute. Ops details are now canonical in the KB: `core/projects/lean-journey/reference/reference_aristotle_cli.md` (I updated it this session). **That file is co-edited by a parallel finite-simple-groups box session** — the `ARISTOTLE_API_KEY` is **shared across box sessions with ~1 preemptive slot**: a fresh submit can CANCEL another session's RUNNING task. My M_5 Aristotle job came back `CANCELED` (preempted, NOT a merit failure) — check `aristotle tasks <proj-id>` for the real terminal state, don't trust `list`'s ambiguous `IDLE`.
- **The `mk_*` reframing (the session's real insight):** the M_k "numerical Maple" axioms are NOT irreducible. The repo already has axiom-clean `Mk_ge_polynomialMkF` (smooth-cutoff+DCT) + `polynomialMkF_eq_MkF`, so each reduces to: exhibit a polynomial `P`, prove the rational `polynomialMkF P > threshold`. I did this for M_5.
- **But M_5 was SPECIAL — do not over-generalize (I did, then corrected it).** `polynomialMkF` sums over *explicit monomials*; at k=5 that's 56, tractable for `native_decide`. M_50 is blocked on **two** independent walls: (1) the unconditional `H1_le_246`/`dhl_50_2` uses `Sieve.Mk_eps 50` (ε-trick) and **no polynomial bridge exists for `Mk_eps`/`Mk_truncated`** — only plain `Mk`; (2) plain `Mk 50 > 4` needs a *multivariate* symmetric witness (verified: `F = g(Σtᵢ)` plateaus at ≈3.23 < 4 for all degrees, `tools/mk/mk50_p1.py`), which at k=50 explodes to ~23k monomials → `native_decide` infeasible. Every other plain-`Mk` target is k≥54 → same wall.
- **The real unlock (if Trevor wants the family):** a **symmetric-function reduction for `polynomialMkF`** — compute a symmetric polynomial's Rayleigh ratio from its ~7 orbit/power-sum coefficients (closed forms in `k`) instead of N monomials. Collapses k=50 to k=5 size. Substantial, multi-session, theory-build (NOT Aristotle-shaped). This is the genuine next math, not a copy-paste.

## ✅ State (all observed this session)
- `b682f25` — `tuple_5511_admissible` axiom→theorem (Aristotle-assisted). `Engelsma.lean`.
- `f405f27` — `mk_5_witness_under_EH` (M_5 > 2) axiom→theorem. New `Mk5Witness.lean` + `Maynard.lean` axiom→import. Witness `polynomialMkF P5 = 12048682945/6016885374 ≈ 2.00248`.
- `2e0f3a1`, `500df0e` — docs (`tools/aristotle/QUEUE.md` target queue + corrected M_50 finding).
- **Verified**: `lake build BoundedGaps.Targets` green (8257 jobs); `#print axioms` on both new theorems = standard 3 + `native_decide` axioms, **no `sorryAx`**.
- Remaining `:= sorry` (unchanged, all off the H_m path): `Prerequisites:155` (geh_implies_eh / Vaughan wall), `Polymath8b:796`, `Zhang:36`.

## 🎬 Next actions
1. **Session ended on an open question to Trevor**: build the symmetric-function reduction (the real unlock for `Mk 50/54/…`), or bank here? His next message likely answers it. If "build it" → start by reading `SievePolynomial.lean` (`polynomialMaynardNumerator/Denominator`, the J_i structure) and design the orbit/power-sum reduction; `tools/mk/mk5_sym.py` has the orbit math in Python to mirror. If "bank" → nothing to do but confirm host-push.
2. **Host-only**: push `path-a-selberg-nu` (HEAD `500df0e`, 4 commits ahead of `3180948`). Box can't push.

## ⚠️ Gotchas
- **Box OOM** intermittently kills a whole-repo build ("Cannot allocate memory" opening oleans) — NOT a real error. `pkill -9 lean lake`, wait ~20s, rebuild the apex module (`Targets`), not the world.
- `native_decide` triggers a mathlib-linter *warning* here (lakefile `weak.linter.mathlibStandardSet`), NOT an error — the repo's tuple proofs already use it. Don't mistake the warning for a failure.
- `lake build` skips a module not imported by a default target (false green) — build new modules by name (`lake build BoundedGaps.Mk5Witness`).
- Don't touch master `HANDOFF.md` (post-merge state only). Dated `HANDOFF-SESSION-*`/`HANDOFF-*` are per-session.

## 📁 Key files
- `BoundedGaps/Mk5Witness.lean` — the M_5 discharge (witness P5 + computable mirror + `dsum` Finset↔List bridge + chain). The template for the pattern.
- `BoundedGaps/SievePolynomial.lean` — `polynomialMkF`, `Mk_ge_polynomialMkF` (L501), `polynomialMkF_eq_MkF` (L844). The reduction would live near here.
- `tools/aristotle/QUEUE.md` — target triage; M_50 wall analysis.
- `tools/mk/*.py` — witness search (`mk5_sym.py`) + M_50 probe (`mk50_p1.py`).
- `~/personal/claude/knowledge/core/projects/lean-journey/reference/reference_aristotle_cli.md` — canonical Aristotle ops (KB, co-edited).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor, don't wait for instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the context, then act on Trevor's steer re: Next action #1 (symmetric reduction vs bank). If he hasn't steered yet, the live question is exactly that — answer as Ren would: the reduction is the real unlock but genuinely multi-session, so be honest about cost, don't pretend it's quick.**
