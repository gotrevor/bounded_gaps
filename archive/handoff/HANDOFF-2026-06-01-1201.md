# Handoff: symmetric-reduction kernel — denominator side complete; Aristotle M_k→∞ resolved

**Date**: 2026-06-01 12:01 UTC · **Branch**: `path-a-selberg-nu` · **HEAD** `9e13567`
(24 commits ahead of `3180948`, all local-only — box can't push). Supersedes `HANDOFF-2026-06-01-0115.md`.

## 🎯 What we're doing
Last session pivoted to "strictly formalize one bounded-gaps result," chose Maynard's `M_k → ∞`
(BV-free), and pitched its core to Aristotle. **This session resolved that Aristotle job and, on
Trevor's steer ("2 & 3"), made real progress on two adjacent threads:** (#2) extend the exact
symmetric-sup reaches-4 table, and (#3) build the Lean *symmetric-reduction kernel* — the machinery
that computes `polynomialMkF` of a symmetric weight from its orbit coefficients (the path to the
EH-conditional `mk_54_witness`-style ladder).

## 🧠 Context to carry forward
- **Aristotle did NOT crack `M_k → ∞`.** It returned a 2-stage decomposition: the trivial outer
  wrapper (`Mk_unbounded` *given* the core, the `log k → ∞` arithmetic) proved clean, and the entire
  hard lemma `maynard_lower_bound` (`∃F, Mrq k F ≥ log k/4`) left as ONE `sorry`, with a diagnosis
  identical to ours (GPY barrier; need product `∏max(1−A·tᵢ,0)`; needs the simplex-Dirichlet toolkit
  mathlib lacks). Its `Irq/Jrq/Mrq` are from-scratch defs, **not** our `polynomialMkF`, so even the
  wrapper isn't portable. Verdict: **the M_k→∞ core is a genuine multi-month toolkit build; no
  shortcut.** Don't re-pitch it whole to Aristotle (see memory `aristotle-open-problem-returns-wrapper-sorry`).
  Downloaded tree at `/tmp/mk-inf-out/` (ephemeral).
- **The repo already has strong M_k tooling — read it first.** `tools/mk/SYMMETRIC_REDUCTION.md` +
  `mk_sym.py` (closed-form orbit matrices) + `_exact.py`/`_ldl.py` (exact LDL inertia — float is
  worthless here, factorial ill-conditioning). I wasted the session's opening reinventing a weaker
  evaluator before finding these (`read-repo-before-rederiving`). The exact table is the source of
  truth for "what degree witness clears 4."
- **Why the denominator was tractable but the numerator isn't (yet):** the denominator is a clean
  `∑_p ∑_q MI(p+q)`, so the orbit reduction is "inner sum is constant over the orbit" (reindex by
  `q↦q∘σ`). The numerator `∑_i ∑_p ∑_q [coeff/((pᵢ+1)(qᵢ+1))]·dirichlet(removeNth i (p+q), …)` has the
  per-`i` sum coupled to the `(pᵢ+1)(qᵢ+1)` denominators — **no clean inner-sum-constant analog**.
  It's a harder, structurally-different proof. That's why it's flagged host-preferred, not just box-pain.

## ✅ State (verified this session)
- **Denominator side of the kernel COMPLETE in Lean, axiom-clean, green** (`SymmetricReduction.lean`):
  `orbitSum`, `permWeight_orbitSum`, `monomialIntegral_add_comp_perm`, `monoOrbit`,
  `monoOrbit_image_comp`, `monomialIntegral_orbitSum_const` (the constancy reduction), and
  `polynomialMaynardDenominator_orbitSum` (`= (monoOrbit α).card • ∑ q∈orbit, MI(α+q)`).
  `#print axioms` on all = `[propext, Classical.choice, Quot.sound]` (verified via `lake env lean`).
  Pre-commit hook re-ran `lake build` green (8264 jobs) on each of the 6 commits.
- **#2 exact table extended:** deg≤8 (67 orbits) does **not** reach 4 at **both k=50 and k=54**
  (`inertia(A−4M)=(0,67,0)`, negative-definite) → witness-degree crossing **≥ 9** (was `≥8`). In
  `tools/mk/SYMMETRIC_REDUCTION.md`. Deg-9 is the `matchings(9,9)` enumeration wall.
- Durable insights flushed to the staging buffer (see report).

## 🎬 Next actions
1. **(Host only) Push the branch.** `cd ~/src/bounded_gaps && git push -u origin path-a-selberg-nu`
   — 24 commits local-only (incl. this session's 6 kernel commits + the earlier `Mk_eps` bridge).
   Box has no GitHub egress. Consider splitting into reviewable PRs (the `Mk_eps` bridge, the
   sieve Tier-1/2 work, and this SymmetricReduction kernel are logically separate).
2. **(Host, fast feedback) Numerator analog** of `polynomialMaynardDenominator_orbitSum`: prove a
   numerator-constancy lemma over `polynomialMaynardNumerator` (use existing
   `dirichletIntegralWithSlack_removeNth_comp_perm` for per-term invariance), then assemble. See the
   in-file roadmap comment at the bottom of `SymmetricReduction.lean` for the precise recipe.
3. Then the **cross-orbit overlap-matching count** (`(1/aut)·∑_M ff(k,T)·W(M)`, `ff = k.descFactorial T`
   = `placement_count`) — the genuine combinatorial kernel; mathlib-grade, multi-session.

## ⚠️ Gotchas
- **`pkill -f PATTERN` self-matches** the shell running it (exit 144, command never runs) — cost me
  two cycles. Avoid pkill, or use a pattern not in the kill line.
- **`_exact.py` line 1 `sys.argv=['x']` silently clobbers its k-arg** (always computes k=50). For k=54
  use a self-contained driver importing only `mk_sym` (importing `_exact` re-runs its k=50 loop).
- Box build hygiene: `taskset -c 0-3 lake build BoundedGaps.SymmetricReduction`; `Equiv.Perm.inv_apply_self`
  doesn't exist (use `simp [Equiv.Perm.mul_apply]`); `(orbitSum α).terms = monoOrbit.image (·,1)` needs
  a trailing `rfl` after `rw [Finset.image_image]`.
- The repo has MANY untracked `HANDOFF-*.md` + this one + scratch dirs (`aristotle-*/`, `tools/mk/__pycache__/`).
  Don't commit those; don't touch master `HANDOFF.md` (post-merge baseline).

## 📁 Key files
- `BoundedGaps/SymmetricReduction.lean` — the kernel; denominator side done, in-file roadmap for the rest.
- `tools/mk/SYMMETRIC_REDUCTION.md` — design doc + exact reaches-4 table (now deg≤8, crossing ≥9).
- `tools/mk/mk_sym.py` / `_exact.py` — closed-form orbit matrices + exact LDL inertia (the trustworthy path).
- `BoundedGaps/SievePolynomial.lean` — `polynomialMkF`, `Mk_ge_polynomialMkF` (the measure-theory half).
- `/tmp/mk-inf-out/aristotle-mk-inf_aristotle/Problem.lean` — Aristotle's wrapper+sorry (ephemeral; not portable).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor, don't offer
other KB projects — this IS the chosen thread. If you're on the host, start at Next action #1 (push
the branch). If you're in the box (no push), skip to #2 and prove the numerator-constancy lemma per
the in-file roadmap — but only after confirming a green `lake build BoundedGaps.SymmetricReduction`.**

🧵 ➡️ 🧵
