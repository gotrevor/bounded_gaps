# Handoff: general fiber count COMPLETE — only the monoOrbit bridge (f) remains for S(α,β)

**Date**: 2026-06-02 00:18 UTC · **Branch**: `path-a-selberg-nu` · **HEAD** `1ab707e`
(**39 commits** ahead of `3180948`, all box-local — no push egress; verified `git rev-list --count`).
Supersedes `HANDOFF-2026-06-02-0010.md` (which estimated the commit count as 43 — actual is 39).

## 🎯 What we're doing
Cross-orbit symmetric-reduction kernel for `polynomialMkF`/`polynomialMkF_eps`. Prior sessions
reduced every cross-orbit matrix entry to one core `S(α,β) = ∑_{p∈monoOrbit α} ∏ᵢ (pᵢ+βᵢ)!`, with the
validated closed form `S(α,β) = ∑_X (∏_b multinomial(n_b;X(·,b)))·(∏_{c,b}((c+b)!)^{X(c,b)})`. This
session **finished the entire general fiber count**, including proving the keystone that had been
offloaded to Aristotle.

## 🧠 Context to carry forward
- The fiber count `#{p∈monoOrbit α : joint-type(p,β)=X} = ∏_b multinomial(n_b;X(·,b))` was split into
  sub-pieces (a)–(f). **(a)–(e) are DONE and axiom-clean; only (f) remains.**
- **The keystone (c) was NOT multi-session.** It looked hard until `DomMulAct.stabilizer_card` turned
  up in `Mathlib/GroupTheory/Perm/` — then orbit–stabilizer makes it ~70 lines. I proved it by hand
  *while Aristotle was still running on it*, then `aristotle cancel`led the redundant job (project
  `15ba0cd4`). Lesson flushed to memory ([[search-specialized-mathlib-before-offload]]).
- **Why the monoOrbit route, not the all-perms sum**: the monoOrbit fiber count gives the clean
  `∏ multinomial` with no extra `∏ m_c!` α-side factor. Don't switch to `group_sum_eq_stab_smul_orbitSum`.
- **The Fintype-vs-ℕ codomain wrinkle is resolved by design**: every general lemma is stated over
  Fintypes `ι,V,B`; (f) specializes with `V = ↥(image α)`, `B = ↥(image β)` (finite value-sets).
- **Workflow that worked**: prototype each fiddly proof in `/tmp/*.lean` with
  `taskset -c 0-3 lake env lean /tmp/x.lean` (fast, no olean), then move into the module.

## ✅ State (all hook-verified green, all `#print axioms` = `[propext, choice, Quot.sound]`)
In `BoundedGaps/SymmetricReduction.lean`, `section FiberProduct` (`open Finset Equiv`, `open scoped Nat`):
- (a) `mem_monoOrbit_iff` (`65687d5`) · (b) `card_filter_eq_sum_joint` (`369c98a`)
- (d) `card_jointType_eq_prod` (`ab01f3b`) — β-group product (structural core)
- `exists_fiberwise` (`84b11b1`) — witness with prescribed fibers (`ι ≃ Σv,Fin(h v)`)
- (c) `card_fiberwise_eq_multinomial` (`0282250`) — **the keystone**, orbit–stabilizer
- (e) `card_jointType_eq_prod_multinomial` (`95131fe`) — **general fiber count = ∏ multinomial**
- docs (`1ab707e`). Build: `lake build BoundedGaps.SymmetricReduction` → green, 8253 jobs (verified
  this session). Working tree clean (only untracked `HANDOFF-*.md`).

## 🎬 Next actions
1. **(Host only) Push** — `git push -u origin path-a-selberg-nu` (39 commits). The `SymmetricReduction`
   fiber-count work is a clean standalone PR.
2. **(f) value-restriction bridge + S(α,β) assembly — THE LAST PIECE** (~50-80 lines, own session):
   - Specialize `card_jointType_eq_prod_multinomial` to `ι := Fin k`, `V := ↥(image α)`, `B := ↥(image β)`.
     Lift `p : Fin k → ℕ` (values ⊆ `image α` by (a) `mem_monoOrbit_iff`) to `Fin k → ↥(image α)`.
   - For a *realized* table `X`, `{p∈monoOrbit α : joint-type=X}` equals `{f : Fin k→↥S | J_g(f)=X}`
     (orbit constraint redundant — row margins force `p∈monoOrbit α` via (a); (b) supplies margin
     bookkeeping; the keystone's `hcol` column margin = β-fiber size `n_b`).
   - `Finset.sum_fiberwise` over the joint-type map `p ↦ (vt ↦ #{i:(pᵢ,βᵢ)=vt})`; summand constant on
     each class = the weight from `prod_eq_prod_pow_joint` (already in the file) ⟹
     `S(α,β) = ∑_X (fiber count)·(weight)` = the design-doc closed form.
   - Then `native_decide` at fixed k=50/54 (≥deg 9 witness, per the exact LDL table in the design doc).

## ⚠️ Gotchas
- `Equiv.Perm.apply_inv_self`/`inv_apply_self` DON'T exist (v4.29.1): use
  `rw [← Equiv.Perm.mul_apply, mul_inv_cancel, Equiv.Perm.one_apply]`; round-trips close with
  `Subtype.ext (by group)`.
- Use `DomMulAct.stabilizer_card` (plain subtype card) **directly** — the MulAction `orbit` route hits
  `Fintype (orbit …)` / `Fintype ((Perm ι)ᵈᵐᵃ)` synthesis failures.
- `Equiv.sigmaSubtype v : {s : Σ w, β w // s.fst = v} ≃ β v` (used in `exists_fiberwise`).
- Box: `taskset -c 0-3 lake build …`; hook rebuilds on commit; bare `python3`.

## 📁 Key files
- `BoundedGaps/SymmetricReduction.lean` — the tower + `prod_eq_prod_pow_joint` + (a),(b) + the whole
  `section FiberProduct` ((d),`exists_fiberwise`,`fiberRestrictEquiv`,(c),(e)).
- `tools/mk/SYMMETRIC_REDUCTION.md` — ⭐ design; sub-pieces (a)–(e) DONE, (f) flagged as the only remainder.
- `BoundedGaps/SymmetricReductionEps.lean` / `EpsBridge.lean` — ε-side (reuses the fiber count).

---
**→ Next session: this is your starting point. If on the host, push (#1). Then build (f) — the value-
restriction bridge + `sum_fiberwise` assembly into `S(α,β)`; all hard combinatorics is already done.
Don't summarize this doc back to Trevor, don't wait for instructions, and don't offer other KB
projects — this doc IS the chosen thread. Absorb the context, then start at Next action #1.**

🧵 ➡️ 🧵
