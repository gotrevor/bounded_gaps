# Handoff: Weighted Mertens (front #4) FULLY AXIOM-CLEAN — keystone DONE

**Date**: 2026-06-04 10:14 UTC · **Branch**: `path-a-selberg-nu` · HEAD `e50a776` ·
**11 commits this lap** (`eb19720`…`e50a776`), every Lean commit green (full `lake build`, 8277
jobs, verified at each pre-commit gate), all new decls axiom-clean, all box-local (NEVER pushed).
**Supersedes `HANDOFF-2026-06-04-0914.md`.**

## 🎯 What we're doing
Analytic-NT axiom burn-down toward the GPY/Maynard sieve asymptotics (`Sieve.lean` `s1`/`s2`).
The prior lap left **front #4** — the 1-D WEIGHTED Mertens `∑_{1≤n≤N}(μ²/φ)·F(log n/log N)/log N →
∫₀¹F` (sub-step (c) keystone, after sharp Mertens) — reduced to two Aristotle "leaves". **This lap
proved the ENTIRE thing and made it fully axiom-clean.**

## 🧠 Context to carry forward
- **The headline: front #4 is DONE, machine-checked, ZERO cited axioms.** `weighted_mertens` and
  `weighted_mertens_of_contDiff` (the sieve-ready C¹ form) `#print axioms` =
  `[propext, Classical.choice, Quot.sound]`. The old "sub-step (c) is multi-month" framing was a
  miscalibration (the SECOND time — same as sharp Mertens). The 1-D keystone was days-scale.
- **How it went:** I built the whole Abel-summation reduction LOCALLY and axiom-free
  (`weighted_mertens_of_riemann`): `g·F = (1/n)·F + (g−1/n)·F`; the discrepancy part `→0` by Abel
  summation (`abel_summation_identity` ⇒ `Bdisc N·F(1) − Abel-tail`, Term1 from sharp Mertens +
  harmonic∼log, Term2 = Lipschitz squeeze vs a weighted-Cesàro majorant average). The ONLY genuinely
  analytic piece — the `1/n` Riemann-sum model limit `riemann_sum_log_weight` — I first DISCLOSED as
  an axiom, then **Aristotle `930e468a` proved it**; I downloaded, **verified `#print axioms` clean
  in our v4.29.1 kernel** (the port was clean — pure analysis, no grind-injected sorryAx), and ported
  it to `BoundedGaps/RiemannSumLogWeight.lean`. That discharged the axiom.
- **Key reusable insight (also flushed to KB inbox `2026-06-04-lean-abelsummation-weighted-cesaro.md`):**
  mathlib HAS `Mathlib.NumberTheory.AbelSummation` (integral Abel summation, e.g.
  `sum_mul_eq_sub_integral_mul₀'`), `LSeries.SumCoeff`, and `Harmonic.Bounds`
  (`log(n+1) ≤ harmonic n ≤ 1+log n`). Prior handoffs wrongly believed these weren't available and
  routed such work to Aristotle. GREP mathlib's NumberTheory/ BEFORE outsourcing analytic-NT lemmas.
- **Next nut = the MULTIDIMENSIONAL / singular-series lift.** `weighted_mertens` is the 1-D factor;
  `s1_holds_from_nonprime_asym` needs the k-dimensional, simplex-coupled, coprimality-corrected
  version (→ `α = I(F)`). The pure-analysis CORE of that coupling (the 2-D simplex-constrained
  Riemann sum, NO number theory) is **on Aristotle now**: `3e2b6a8d` (`aristotle-wmertens2d/`), goal
  `weighted_riemann_2d : (∑_{mn≤R} F·G/(mn))/(log R)² → ∫₀¹F(x)∫₀^{1−x}G`. Verified the statement
  is true (F=G=1 ⇒ both sides 1/2).

## ✅ State (full `lake build` green, 8277 jobs — verified this session)
All in **`BoundedGaps/WeightedMertens.lean`** (NEW file) + `BoundedGaps/RiemannSumLogWeight.lean`:
- `weighted_mertens` / `weighted_mertens_of_contDiff` — capstone, AXIOM-CLEAN.
- `weighted_mertens_of_riemann` — the axiom-free arithmetic reduction.
- `riemann_sum_log_weight` — = `Riemann.riemann_sum_log_weight` (Aristotle proof, ported, verified).
- `discrepancy_weighted_tendsto_zero`, `abel_tail_tendsto_zero`, `abel_tail_majorant_tendsto_zero`,
  `discrepancy_div_log_tendsto_zero`, `Bdisc`, `sum_sub_eq_Bdisc`.
- Reusable analytic bricks: `weighted_cesaro_tendsto_zero`, `weighted_avg_majorant_tendsto_zero`,
  `harmonic_div_log_tendsto_one`, `sum_log_mul_log_diff_le_sq`, `riemann_sum_const_one` (F≡1 check).
- Memory `weighted-mertens-route` (NEW) + `MEMORY.md`, `ANALYTIC_AXIOM_BURNDOWN.md`,
  `PENDING_WORK.md` (§Z) all updated. KB inbox note flushed.
- **Aristotle**: `3e2b6a8d` (`aristotle-wmertens2d`) RUNNING — the 2-D coupled model (next nut's
  analytic core). Download artifacts of the finished `930e468a` are in `aristotle-wmertens-dl/`
  (untracked; the ported proof is committed in `RiemannSumLogWeight.lean`).

## 🎬 Next actions
1. **Poll `aristotle show 3e2b6a8d-dcec-4471-889b-392652f2878a`** (one-shot, not the live TUI).
   When COMPLETE: `aristotle download 3e2b6a8d --destination <x>.tar.gz`, `tar xzf`, compile the
   `Problem.lean` in our kernel (`lake env lean`), **`#print axioms` must be clean**, then port to
   a new module (mirror what I did with `RiemannSumLogWeight.lean`). If it FAILED/timed out, see
   `PENDING_WORK.md` §Z item-2 paths (do the rank-1 separable case, or split the 2-D lemma smaller).
2. **Build the arithmetic layer** on top of the 2-D analytic core toward `s1_holds_from_nonprime_asym`:
   the coprimality + singular-series correction (Polymath8b §3 sfg-1/lflg). `PENDING_WORK.md` §Z
   item-2 has three paths; recommended P3 = separable rank-1 (`J=1`) case first, where the sieve sum
   factors into 1-D `weighted_mertens` calls.
3. While #1 grinds, the singular-series Euler product `𝔖` (PENDING_WORK §Z item-2/P2) is local,
   self-contained infra (reuse `SharpMertens` `eulerProduct_tprod`) — build it in parallel.

## ⚠️ Gotchas
- Box OOM flake on `lake env lean` (~1/few runs): "Cannot allocate memory" — just re-run, `taskset -c 0-2` helps.
- Aristotle pins v4.28; repo is v4.29.1 — ALWAYS `#print axioms` a ported result. (The wmertens port
  WAS clean, but the `tar` content had `ring`→`ring_nf` advisory notes; harmless.)
- `RiemannSumLogWeight.lean` is dense Aristotle-generated tactic soup (`set_option maxHeartbeats
  800000 in`) with one harmless whitespace-linter warning at line ~170. Don't refactor it; it verifies.
- `M ROADMAP.md` in `git status` is pre-existing (not mine) — leave it.

## 📁 Key files
- `BoundedGaps/WeightedMertens.lean` — THE deliverable (capstone + full reduction + reusable bricks).
- `BoundedGaps/RiemannSumLogWeight.lean` — the ported Aristotle Riemann-sum proof (axiom-clean).
- `BoundedGaps/Sieve.lean` — `s1_holds_from_nonprime_asym:1907`, `alphaBound:132` (the eventual target).
- `BoundedGaps/Mertens.lean` — `abel_summation_identity:659`; `BoundedGaps/SharpMertens.lean` — sharp Mertens.
- `PENDING_WORK.md` §Z — analytic-NT inventory + 3 attack paths each; `ANALYTIC_AXIOM_BURNDOWN.md` — burndown map.
- `aristotle-wmertens2d/Problem.lean` — the in-flight 2-D job (`3e2b6a8d`).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (analytic-NT
sieve burn-down; front #4 weighted Mertens just landed FULLY axiom-clean). Start at Next action #1:
poll/harvest Aristotle `3e2b6a8d`, verify+port the 2-D model, then build the arithmetic
(coprimality + singular series) layer toward `s1`. Keep ONE Aristotle job in flight at all times.**

🧵 ➡️ 🧵
