# Handoff: Mertens analytic toolkit built (14 commits, axiom-clean); sharp route (von Mangoldt) in flight

**Date**: 2026-06-04 03:16 UTC · **Branch**: `path-a-selberg-nu` · HEAD `59c17ac` · 14 content commits this lap, all green (full build 8272 jobs, verified at each pre-commit gate), all box-local (NEVER pushed — host pushes).

## 🎯 What we're doing
Analytic-NT axiom burn-down (`ANALYTIC_AXIOM_BURNDOWN.md`): the repo's headline
`bounded_gap_of_Mk_200` rests on cited analytic axioms (s1/s2, BV) whose 1-D core is
Mertens-type summation `∑_{n≤N} μ²(n)/φ(n) ~ log N`. The lower half (`mertens_lower`)
landed last lap. **This lap built the entire toolkit for the upper half** in
`BoundedGaps/Mertens.lean` and discovered the route splits sharp/crude (see Context).

## 🧠 Context to carry forward
- **THE key insight (read `[[mertens-sharp-vs-crude]]` memory):** my `mertens_first_le`
  uses Chebyshev `θ(N) ≤ N·log 4`, so it carries a LOSSY `log 4 ≈ 1.386` coefficient
  (sharp = 1). Pushed through the 2nd Abel step the `log log N` coefficient inflates to
  `log4·(1+1/log2) ≈ 3.4`, and `∑μ²/φ ≤ ∏(1+1/(p-1)) ≤ exp(C·loglog N) = (log N)^C` then
  gives only **`∑μ²/φ = O((log N)^3.4)`, NOT the sharp `Θ(log N)`** the GPY sub-step (c)
  main term needs. So there are TWO tracks (both fully spec'd in `PENDING_WORK.md §C`):
  - **Crude/qualitative (optional milestone):** assemble `∑_{p≤N}1/p = O(loglog N)` from the
    landed pieces. Famous theorem, but non-sharp → does NOT give the headline.
  - **Sharp (the real path):** von Mangoldt `∑Λ(n)⌊N/n⌋ = ∑log m` → Stirling → ψ≤CN →
    `∑Λ(n)/n = log N + O(1)` (coeff 1) → drop prime-power tail → sharp Mertens 1st →
    sharp Mertens 2nd → `∑μ²/φ = Θ(log N)`.
- **Consumer-validation pattern (it works great):** before an Aristotle lemma returns,
  write the downstream proof with its *statement* as a temporary local `axiom` in a
  scratch; get it green; then port the real proof + consumer together. Did this for
  Mertens-1st (consumer green before Abel landed). Do the same for the von Mangoldt
  consumer (`∑Λ(n)/n` bounds — see Next actions).
- **Lean gotchas hit (now in personal memory):** `rw [← Finset.sum_image]` HO-trap →
  `[[lean-rw-sum-image-ho-trap]]`; v4.29 renames `div_le_div_iff→div_le_div_iff₀`,
  `le_div_iff→le_div_iff₀`; `Real.hasDerivAt_log` uses `⁻¹` not `1/`;
  `Finset.Ico_add_one_right_eq_Icc` + `Finset.sum_Ico_eq_sum_range` for Icc↔range reindex;
  Aristotle download is a gzipped tar → `[[aristotle-download-and-verify]]`.
- **Box OOM flake** ("Cannot allocate memory" loading oleans) hit ~4× this lap — always a
  flake, just re-run `lake build`.

## ✅ State — landed in `BoundedGaps/Mertens.lean` (all `#print axioms` = [propext, Classical.choice, Quot.sound], no native_decide)
- `mertensSummand_eq_prod`, `mertens_prod_upper` (`∑μ²/φ ≤ ∏(1+1/(p-1))`) — `a2e4c60`
- `chebyshev_theta_le` / `chebyshev_theta_le'` (`θ(N) ≤ N log 4`, plain + indicator) — `5cfec21`,`4abc26d`
- `telescope_tail_eq/le`, `prime_tail_le` (`∑(1/(p-1)−1/p) ≤ 1`) — `3963399`
- `abel_div_le` (Aristotle `32baa99f`, weight 1/n) + `mertens_first_le` + `mertens_first_le'` — `c8b7f1c`,`422a1f8`
- `hasDerivAt_log_log`, `continuousOn/antitoneOn_one_div_x_log_x`, `integral_one_div_x_log_x`
  (FTC `∫_2^N 1/(x log x)=loglogN−loglog2`), `sum_one_div_n_log_n_le` — `ec2196f`
- `abel_summation_identity` (Aristotle `431512dd`, general weight) — `8bf2cdf`
- `mertens_second_term_bound` (`(1+log n)(1/log n−1/log(n+1)) ≤ (1+1/log2)/(n log n)`) — `8295e10`
- `sum_log_le`, `le_sum_log` (Stirling `N log N−N+1 ≤ ∑log m ≤ N log N`) — `8c6d615`
- `cast_div_le_self`, `sub_one_le_cast_div` (floor `N/n−1 ≤ ⌊N/n⌋ ≤ N/n`) — `59c17ac`

**Aristotle IN FLIGHT:** `cc0dfbaf-...` (`aristotle-vmhyp/VMHyp.lean`) proving
`vonMangoldt_hyperbola : ∑_{1≤n≤N} Λ(n)·⌊N/n⌋ = ∑_{1≤m≤N} log m` — the sharp-route gate.

## 🎬 Next actions
1. **Poll `aristotle list | grep vmhyp`.** If `cc0dfbaf` is IDLE: download
   (`aristotle download cc0dfbaf --destination aristotle-vmhyp-out`, it's a gzipped tar →
   `tar xzf`), VERIFY in-kernel + `#print axioms` clean, fix any v4.28→v4.29 regressions,
   port `vonMangoldt_hyperbola` into `Mertens.lean`, commit. If still RUNNING, keep working
   locally and poll between steps.
2. **Assemble the sharp `∑_{n∈Icc 1 N} Λ(n)/n` LOWER bound NOW** (needs only landed pieces +
   hyperbola): from `vonMangoldt_hyperbola` + `cast_div_le_self` (⌊N/n⌋≤N/n, Λ≥0) get
   `∑_{m≤N}log m ≤ N·∑Λ(n)/n`, combine with `le_sum_log` ⇒ `∑Λ(n)/n ≥ log N − 1`. Validate
   in scratch with hyperbola as a temp `axiom` if cc0dfbaf hasn't landed.
3. **Next Aristotle brick (keep ONE in flight):** Chebyshev `ψ(N)=∑_{n≤N}Λ(n) ≤ C·N`
   (gives the `∑Λ(n)/n ≤ log N+C` UPPER bound via `sub_one_le_cast_div` + `sum_log_le`).
   Decompose `ψ−θ = ∑_{k≥2}θ(N^{1/k}) ≤ √N·log N`; mathlib has `vonMangoldt_le_log`,
   `primorial_le_four_pow`. (Architect + submit when cc0dfbaf returns.)
4. Then: prime-power tail `∑_{p,k≥2}(log p)/p^k = O(1)` → sharp Mertens 1st → reuse
   `abel_summation_identity` for sharp Mertens 2nd → `∑μ²/φ = Θ(log N)`.

## ⚠️ Gotchas
- `mertens_first_le`'s `log 4` constant is the whole reason the crude route is non-sharp —
  don't try to "fix" Mertens 2nd's constant downstream; the lossiness is upstream in θ.
- The 2nd Abel step's weight `1/log n` is JUNK at n=1 (`1/log 1 = 1/0 = 0`), so it's NOT
  antitone at the 1→2 step. `A_1 = 0` kills that term; split off n=1 before any
  termwise weight-monotonicity bound. (Relevant to the crude assembly, `PENDING_WORK §C`.)
- Index alignment in the crude assembly: `sum_one_div_n_log_n_le` is `∑_{n=3}^N`, the Abel
  difference-sum is `∑_{n=2}^{N-1}` — off-by-shift + the `1/(2log2)` n=2 term.

## 📁 Key files
- `BoundedGaps/Mertens.lean` — all landed bricks above (~700 lines now).
- `PENDING_WORK.md §C` — full Aristotle status + both-tracks recipe + landed inventory.
- `ANALYTIC_AXIOM_BURNDOWN.md` — the ladder map (updated: Mertens-1st done, Mertens-2nd decomp).
- `aristotle-vmhyp/VMHyp.lean` — the in-flight von Mangoldt hyperbola statement.
- `aristotle-abel-dl/`, `aristotle-abelid-dl/` — downloaded Abel proofs (provenance, untracked).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or
wait for instructions, and don't offer other projects from the KB — this doc IS the chosen
thread (the analytic-NT Mertens burn-down). Start at Next action #1: poll `cc0dfbaf`; while it
finishes, do action #2 (assemble the sharp `∑Λ(n)/n ≥ log N − 1` lower bound — all pieces
except the hyperbola are landed, so validate it in a scratch with the hyperbola as a temp axiom).**

🧵 ➡️ 🧵
