# Handoff: generic k-D weighted-Riemann PROVEN + (μ²/φ) y_r-space engine landed

**Date**: 2026-06-05 02:57 UTC · **Branch**: `path-a-selberg-nu` · HEAD `558250c`
**6 commits this lap** (`49b801d`…`558250c`); every Lean commit green at the pre-commit full-build
gate (8282 jobs). Never pushed. Supersedes `HANDOFF-2026-06-04-1947.md`. (Master `HANDOFF.md` is the
post-merge state — left untouched, per repo convention.)

## 🎯 What we're doing
Analytic axiom burn-down for the GPY/Maynard sieve. The flagship `bounded_gap_of_Mk_200` rests on 3
math axioms — `BombieriVinogradov`, `s1_holds_from_nonprime_asym`, `s2_holds_from_prime_asym_under_EH`
(unchanged this lap). The s1/s2 analytic core ("sub-step c") is the k-D coupled log-weighted Riemann
limit. **This lap CLOSED the bare k-D ladder (the documented "one remaining lemma" `psi_k_pointwise`)
and started the `(μ²/φ)` `y_r`-space lift** (Path Y), the next thread toward s1.

## 🧠 Context to carry forward
- **The bare k-D ladder is DONE, end to end.** `WeightedRiemannKD.weighted_riemann_kd` (unconditional,
  axiom-clean) proves `nestedLogSum R Gs R/(logR)^|Gs| → nestedPhi Gs 0 = ∫_{simplex}∏Gs` for every
  nonneg continuous list `Gs`. `psi_k_pointwise` (the former nut) fell to **strong induction on list
  length**: `psi_k_pointwise gs` reduces (rescale `N=⌊R^t⌋`, `c_R=logN/logR→t`, scaled list `gss`) to
  `weighted_riemann_kd` for the same-length `gss`+all-ones list, which by strong induction needs only
  `psi_k_pointwise` at length `< |gs|`. New machinery: `evalNest`/`harmNest` (generic nested-sum drift
  bound `evalNest_dist`), `nestedPhi_scale` (the generic `phi2_scale`, proved via the budget-form
  `simplexPhi` + `nestedPhi_eq_simplexPhi` + `simplexPhi_scale`), finite-list helpers
  `exists_list_bound`/`exists_list_unif`. `weighted_riemann_kd_head` relaxes the head to merely
  continuous (only the tail needs nonneg, for Pólya).
- **The s1 "off-by-a-derivative" landmine is REAL and is the strategic pivot.** Per the harvested
  findings (`archive/findings/ON-LINE-FINDINGS-2026-06-04-gpy-diagonal-asymptotic.md`): `selberg_nu`
  is built from `lambdaTransform` = Polymath8b's d-space `λ_F`, whose diagonal constant is `∫F'²`
  (a DERIVATIVE), but the `s1` axiom claims `∫F²` (= `mkF_denominator`). These reconcile ONLY in
  **Maynard's `y_r`-space**, where the sums are positive `(μ²/φ)` and the constant genuinely is `∫F²`.
  **Recommendation (findings + mine): take Path Y.** Do NOT evaluate the signed `z_r` directly — that
  step is PNT-strength (a contour in Polymath8b). The bare k-D Riemann ladder is Path-Y machinery,
  NOT wasted. Resolving the convention fully (feed `lambdaTransform` the *antiderivative* of the
  variational F, or restate the constant as `∫F'²`) is an **architectural call = Trevor's**; build the
  y_r machinery meanwhile.
- **What I did NOT do and why.** Did not touch s1/s2 (blocked on the convention call). Did not attempt
  the signed-`z_r` d-space route (PNT wall). Did not do the full μ²/φ k-D mirror (multi-lap) — instead
  landed its reusable ENGINE so the mirror is mechanical next lap.

## ✅ State (all axiom-clean `[propext, Classical.choice, Quot.sound]`, full build green 8282 jobs)
- **`WeightedRiemannKD.lean`**: `weighted_riemann_kd` (+ `_head`, `_aux`), `psi_k_pointwise`,
  `nestedPhi_scale` (+ `simplexPhi`/`nestedPhi_eq_simplexPhi`/`simplexPhi_scale`), `psiK_zero`,
  `evalNest`/`harmNest` + `evalNest_dist`/`_abs_le`/bridges, `harmNest_eq_nestedLogSum`,
  `exists_list_bound`/`exists_list_unif`. **The whole bare ladder is complete.**
- **`WeightedRiemann2D.lean`** — NEW y_r-space engine: `perturbed_riemann_gen` (abstract-weight
  perturbed Riemann: takes the two weighted 1-D limits as hyps, isolates the weight-specific input),
  `weighted_mertens_Icc2` (μ²/φ Mertens over `Icc 2`, weight-on-right), `perturbed_riemann_muphi`
  (the μ²/φ analog of `perturbed_riemann`, built from the gen engine + `weighted_mertens`).
- **`WeightedMertens.lean`**: `weighted_mertens_sq` (1-D W=1 Path-Y core: `∑(μ²/φ)F²/logN → ∫F²`).

## 🎬 Next actions
1. **Mirror the 2-D→k-D y_r ladder with the `μ²/φ` weight.** Copy the bare ladder
   (`WeightedRiemann2D`→`KD`) one cons up, swapping `perturbed_riemann` → `perturbed_riemann_muphi`
   and `/n` → `* gMoebiusSqTotient m`. Need: `two_d_factor`-analog for the μ²/φ weight, the Pólya
   inner-uniform reduction (the monotone step needs `gMoebiusSqTotient ≥ 0` ✓), and a μ²/φ
   `weighted_riemann_2d_muphi`. Target: `∑_{∏rᵢ≤R}(∏μ²(rᵢ)/φ(rᵢ))∏F(log rᵢ/logR)/(logR)^k →
   ∫_{simplex}∏F`. (Watch: `weighted_mertens` needs Lipschitz `F`, so the μ²/φ ladder carries Lipschitz
   hyps where the bare one needed only continuity.)
2. **Then connect to `SieveExpansion.gpy_diagonal_asymptotic_form`** (the algebraic z_r identity) — but
   the *asymptotic* should be taken in y_r-space (`weighted_mertens_sq` / the μ²/φ ladder), NOT by
   evaluating z_r². See findings §(c) Path Y for the exact composition.
3. **Surface the convention question to Trevor** (it's his architectural call) — needed before s1 can
   actually be discharged. Until then keep building y_r machinery (it's needed either way).
4. Keep Aristotle fed: at lap start most jobs were IDLE (finished), `aristotle-basis-summable`
   RUNNING (account-shared). Architect a bounded y_r brick if no job is running.

## ⚠️ Gotchas
- **`positivity` ignores hypotheses** — for `set`-bound vars (εpp, cR, M, C, abstract weights) build
  nonneg facts by hand (`mul_nonneg`, `.le`), never `positivity`. This bit `perturbed_riemann_gen`
  (used explicit `mul_le_mul_of_nonneg_right`, not `gcongr`) and `psi_k_pointwise` throughout.
- **`div_le_div_of_nonneg_right` wants `0 ≤ c`** (not `0 <`); pass `h.le`.
- **Higher-order unification timeout**: `weighted_mertens_Icc2 hLip hCont` for `G = F·Φ` timed out at
  `whnf` — give `(G := fun x => F x * Φ x)` explicitly.
- **Nested abs `|(|F x|)-|F y||` is a parse error** — don't state it; inline the proof via
  `abs_abs_sub_abs_le_abs_sub` and let Lean infer the type.
- Cheap iterate: `taskset -c 0-3 lake build BoundedGaps.WeightedRiemann2D` (~7s) /
  `BoundedGaps.WeightedRiemannKD` (~17s). Full build (pre-commit gate) = 8282 jobs.
- The "ring failed → try ring_nf" / linter long-line warnings are benign; trust "Build completed".

## 📁 Key files
- `BoundedGaps/WeightedRiemannKD.lean` — the complete generic bare k-D ladder.
- `BoundedGaps/WeightedRiemann2D.lean` — `perturbed_riemann{,_gen,_muphi}`, `weighted_mertens_Icc2`
  (the y_r-space engine; the template + engine for the μ²/φ ladder mirror).
- `BoundedGaps/WeightedMertens.lean` — 1-D μ²/φ (`weighted_mertens`, `weighted_mertens_sq`).
- `archive/findings/ON-LINE-FINDINGS-2026-06-04-gpy-diagonal-asymptotic.md` — Path Y vs Path D, the
  convention landmine, exact paper references. **Read before touching s1.**
- `PENDING_WORK.md` — full leaf inventory; memory `[[kd-weighted-riemann-ladder]]`,
  `[[s1-derivative-landmine]]`.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (GPY sieve
axiom burn-down; bare k-D Riemann DONE, now building the `μ²/φ` y_r-space lift). Absorb the context,
then start at Next action #1: mirror the 2-D y_r ladder with the `μ²/φ` weight, using
`perturbed_riemann_muphi` as the engine and the bare `WeightedRiemann2D`/`KD` ladder as the template.**

🧵 ➡️ 🧵
