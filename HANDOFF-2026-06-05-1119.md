# Handoff: s1 correction count-side fully discharged + the entire odd-prime `hBaseW` route COMPLETE

**Date**: 2026-06-05 11:19 UTC · **Branch**: `path-a-selberg-nu` · HEAD `4d9b59e`
14 commits this lap (`813f4eb`…`4d9b59e`), full build green (8299 jobs) at every pre-commit gate.
Never pushed. Supersedes `HANDOFF-2026-06-05-0956.md`. Master `HANDOFF.md` untouched per repo convention.

## 🎯 What we're doing
Analytic axiom burn-down for the GPY/Maynard sieve. Flagship `OrbitFree.bounded_gap_of_Mk_200` rests on
3 math axioms: `BombieriVinogradov`, `s1_holds_from_nonprime_asym`, `s2_holds_from_prime_asym_at_level`
(verified unchanged this lap via `#print axioms` — no regression). We build the **contour-free Path-Y
y-space `s1`** ALONGSIDE the d-space flagship (do NOT touch the flagship; Trevor flips the axioms +
the `B^{±k}` normalisation later). The sole remaining analytic inputs for y-space `s1` are the
off-diagonal correction `hcorr` and the W-coprime Mertens base `hBaseW`. This lap drove BOTH hard.

## 🧠 Context to carry forward — read this
- **The s1 correction is UNCONDITIONAL (not BV-gated)** — lap-6's strategic correction, now realised:
  this lap machine-checked the ENTIRE **count side** of `hcorr`. `S1Correction.yspace_correction_abs_bound_explicit`
  bounds the actual y-space correction by `∑_{diag}|∏λλ| + ∑_{¬diag}|∏λλ|·(M/∏[dᵢ,eᵢ])` with
  `M = (⌊2x⌋−(⌈x⌉−1))/W` the exact lattice density, **no BV and no count hypothesis** — only W-trick
  admissibility (`hWdvd`/`hshift_le`/`hshift_ne`/`hAB`). Both count-side legs are proven: off-diagonal
  vanishing (W-trick, `lattice_count_pair_offdiag_vanish_Wtrick`) AND diagonal `O(1)` error
  (`yspace_diag_count_err`, via `lattice_count_main_term`). **What's LEFT for `hcorr`: the two purely-
  analytic SIZE estimates** on the RHS sums (see Next actions).
- **`hBaseW` is no longer a black box** — the lap-6 Aristotle brick `65d11d89` failed because it tried
  to prove the *unrestricted* sharp Mertens from scratch; but the repo HAS that
  (`SharpMertens.sharp_mertens_unconditional`, `∑μ²/φ /log → 1`). The clean route is **geometric Möbius
  inversion per prime**, now built end-to-end in `CoprimeMertens.lean`. **The odd-prime single-prime
  case is COMPLETE & unconditional** (`single_prime_coprime_mertens`, p≥3). The remaining nut is
  genuinely `p = 2` (DCT fails: `1/(p-1)=1`, limit series diverges) — and I verified it needs the
  **second-order** convergent Mertens `U(M)=log M+β+o(1)`, NOT merely bounded-difference (the |·|-bound
  loses the cancellation; ~log N error terms of the same order). See PENDING_WORK for the worked-out
  diagnosis.
- **Why I didn't do the general-W induction or p=2:** p=2 needs a deeper analytic input (second-order
  Mertens or signed Abel) than the repo has; the general-W induction needs single-prime for ALL primes
  incl. 2, so it's blocked on p=2. Both are clean next sub-projects, not this-lap work.
- **B^{±k} flip stays Trevor's call** — don't force it. My y-space main term is `B^{+k}·M`.

## ✅ State — 2 new files + S1Correction extended, all axiom-clean `[propext, Classical.choice, Quot.sound]`
- **`S1Correction.lean`** (extended): `lattice_count_pair_offdiag_vanish_Wtrick`,
  `piFinset_prod_pair_sum_restrict`, `coprime_lcm_of_coprime`, `yspace_correction_abs_bound` (herr as
  hyp), `abs_yLambda_le` (|yLambda|≤d·C·∑1/φ), `yspace_diag_count_err` (the herr leg),
  **`yspace_correction_abs_bound_explicit`** (herr-free — the capstone).
- **`S1OffDiagSize.lean`** (NEW): `recip_sq_tail_tendsto_zero` (∑1/(k+D₀)²→0), `sum_finset_recip_sq_le_tail`,
  `summable_recip_sq_shift` — the `∑_{p>D₀}1/p²` engine for the off-diagonal SIZE leg.
- **`CoprimeMertens.lean`** (NEW): `per_term`, **`coprime_mertens_recursion`**,
  **`coprime_geometric_inversion`**, `inversion_summable`, `geom_value`, `U_div_log_tendsto_one`,
  `const_div_log_tendsto_zero`, `log_div_ratio_tendsto_one`, `U_shift_div_log_tendsto_one`,
  `U_mono`/`gMoebiusSqTotient_nonneg`, **`single_prime_coprime_mertens` (p≥3, COMPLETE)**.
- Both new files added to `BoundedGaps.lean` aggregator. Last green: `lake build` 8299 jobs (HEAD).

## 🎬 Next actions
1. **`hBaseW` p=2** (the unblocker). Either (a) extract the **second-order** μ²/φ Mertens
   `U(N)=log N+β+o(1)` from `SharpMertens`'s `bAF` partial-sum bounds, then pair the finite
   `coprime_geometric_inversion` sum (`U(N/2^{2j})−U(N/2^{2j+1})→log2`); or (b) signed Abel summation on
   the inverted series. Poll Aristotle `dd98b9c1` (brick_singleprime, RUNNING, `sharp_mertens` inlined) —
   it may crack p=2; if so download, `#print axioms`-verify, port.
2. **Then general-W `hBaseW`**: generalise `coprime_mertens_recursion` to `S_V(N)=S_{pV}(N)+(1/(p-1))S_{pV}(N/p)`
   (p∤V), induct over primes of squarefree `W` ⟹ `φ(W)/W`. Discharges `hBaseW` everywhere it's threaded.
3. **The two `hcorr` SIZE estimates** (elementary, no BV): diagonal `∑_{diag}|∏λλ|=o(M(log R)^k)` (use
   `abs_yLambda_le` + the smoothing `yLambda≈(d/φ(d))C/log R` = `brick_smooth` `678a9ed6` content — the
   naive d-factor is too lossy); off-diagonal `∑_{¬diag}|∏λλ|·M/∏[d,e]=o(M(log R)^k)` (assemble
   `S1OffDiagSize`'s 1/p² tail with the `Q'ᵢ=O(log R)` factors). Feeds `hcorr` → `S1FullLimit` unconditional.

## ⚠️ Gotchas
- Matching two `(Finset.filter p).card` that are beta/defeq-equal: `exact`/`rfl` **whnf-loops** on the
  `DecidablePred` instance (timeout even at 1M heartbeats). Use `convert h using 6` (depth past `.card`).
  Distilled to staging buffer `[[lean-convert-sidesteps-filter-decidable-whnf]]`.
- mathlib name traps (staging `[[lean-tsum-summable-name-gotchas]]`): `Real.summable_one_div_nat_pow`
  (bare fails — namespaced), `Summable.sum_le_tsum`/`.tsum_eq_zero_add` (method form), `tendsto_sum_nat_add`
  needs NO summability hyp.
- Develop fast: `/tmp/*.lean` importing the module, `taskset -c 0-7 lake env lean /tmp/x.lean`; full
  `taskset -c 0-7 lake build`. Long-line linter warnings tolerated (precedent).
- The "diag" predicate in the correction is CROSS-coordinate coprimality (`∀i≠j coprime(lcm(P i),lcm(P j))`),
  NOT the within-coord `dᵢ=eᵢ` GPY diagonal — so `diagonal_weight_le_count` (d-space) does NOT directly apply.

## 📁 Key files
- `BoundedGaps/CoprimeMertens.lean` — the hBaseW geometric route (recursion → inversion → p≥3 limit).
- `BoundedGaps/S1Correction.lean` — `yspace_correction_abs_bound_explicit` (herr-free correction bound).
- `BoundedGaps/S1OffDiagSize.lean` — the `∑_{p>D₀}1/p²` tail engine.
- `BoundedGaps/SharpMertens.lean` — `sharp_mertens_unconditional` (the base; check for a bounded/2nd-order form).
- `BoundedGaps/S1FullLimit.lean` — `yspace_s1_sieveSum_div_tendsto` (conditional on `hcorr`+`hBaseW`).
- `PENDING_WORK.md` — top section has the full lap-7 status + the precise p=2 / SIZE-bound diagnoses.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (contour-free
y-space `s1`: correction count-side DONE, odd-prime `hBaseW` DONE). Absorb the context, then start at
Next action #1: crack `hBaseW` for `p = 2` (poll Aristotle `dd98b9c1` first), or if blocked there pick
up a `hcorr` SIZE estimate (#3). Do NOT re-inherit the "BV-gated" framing and do NOT force the B^{±k} flip.**

🧵 ➡️ 🧵
