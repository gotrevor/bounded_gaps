# Handoff: GATE DISCHARGED — `∑_{n≤N} μ²(n)/φ(n) = Θ(log N)` is now FULLY UNCONDITIONAL & axiom-clean

**Date**: 2026-06-04 04:55 UTC · **Branch**: `path-a-selberg-nu` · HEAD `1a5bd9f` · **15 content commits this lap** (`12e307e`…`1a5bd9f`), all green (full repo build 8272 jobs verified at every pre-commit gate), all box-local (NEVER pushed — host pushes). **Supersedes `HANDOFF-2026-06-04-0328.md`.**

## 🎯 What we're doing
Analytic-NT axiom burn-down (`ANALYTIC_AXIOM_BURNDOWN.md`): the 1-D Mertens core
`∑_{n≤N} μ²(n)/φ(n) ~ log N` underlying the cited sieve axioms. **This lap CLOSED that thread
entirely** — the headline is now unconditional and machine-checked from `[propext,
Classical.choice, Quot.sound]` and nothing else.

## 🧠 Context to carry forward
- **The whole sharp chain is DONE.** Prior laps had the lower half (`mertens_lower`) and sharp
  Mertens 1st (`∑Λ(n)/n = log N + O(1)`). This lap built EVERYTHING above it AND discharged the
  last gate. The dependency ladder, all in `BoundedGaps/Mertens.lean`:
  `mertens_vonMangoldt_two_sided` → `vonMangoldt_split_prime` → **prime-power tail** →
  `mertens_prime_log_two_sided` → 2nd Abel step (`mertens2_abel`) → `mertens2_two_sided`
  (Mertens 2nd, coeff 1) → Euler product (`prod_euler_le_exp`) → **`mertens_theta_log`** /
  `mertens_isTheta_log` (the headline).
- **How the last gate fell (the key insight).** The prime-power tail
  `∑_{n≤N, IsPrimePow ∧ ¬Prime} Λ(n)/n` was the one remaining axiom (an Aristotle job `3f137191`
  was grinding it). I proved it **from scratch instead**, constant `≤ 3`:
  1. `Chebyshev.sum_PrimePow_eq_sum_sum` (mathlib HAS this!) regroups the tail as
     `∑_{k=2}^{⌊logN/log2⌋} ∑_{p≤⌊N^{1/k}⌋}(log p)/p^k` (filter-split off the k=1 prime slice).
  2. **The trick** (`[[lean-widen-then-sum-comm]]`): to bound that k-indexed double sum, do NOT
     attempt the interdependent Fubini. Instead **widen each k-slice's prime range to `[2,N]`**
     (`sum_le_sum_of_subset_of_nonneg`) so the `k` and `n` ranges become independent → then
     `Finset.sum_comm` swaps cleanly. Per-`n` geometric `∑_{k≥2}n^{-k} ≤ 1/(n(n-1)) ≤ 2/n²`
     (`geom_tail_le`) and `∑(log n)/n² = O(1)` (`sum_log_div_sq_le`, integral comparison) finish it.
  3. All-n (not prime-restricted) loses a constant (`≤3` instead of the sharp `≈0.76 ≤ 1`), but
     that's harmless for `O(log N)` — the headline's constant is just `exp(1 + D(log4+7))`.
- **Why coefficient 1 mattered all along** (`[[mertens-sharp-vs-crude]]`): `∑μ²/φ ≤ ∏(1+1/(p-1))
  ≤ exp(∑1/(p-1)) ≈ exp(C·loglog N) = (log N)^C`. The crude Chebyshev-θ route gives `C≈3.4` →
  only `O((log N)^3.4)`. The sharp von Mangoldt route gives `C=1` → genuine `Θ(log N)`. Done now.
- **`3f137191` (pptail, proving `≤ 1`) is SUPERSEDED.** If it returns it only tightens the
  additive constant (`log4+7 → log4+5`); not needed. Next session: free that Aristotle slot.

## ✅ State — `BoundedGaps/Mertens.lean` (~1700 lines, all theorems `#print axioms` =
`[propext, Classical.choice, Quot.sound]`, no `sorry`, no `native_decide`)
**Unconditional headline results (verified `#print axioms` clean this session):**
- `prime_power_tail_le_three` — the discharged gate (`tail ≤ 3`).
- `mertens_prime_log_two_sided` — `|∑_{p≤N}(log p)/p − log N| ≤ log4+7`.
- `mertens2_two_sided` — `∑_{p≤N} 1/p = log log N + O(1)` (coefficient 1, both halves).
- `mertens_theta_log` — `log N ≤ ∑μ²/φ ≤ exp(1+D)·log N`.
- `mertens_isTheta_log` — same as `Asymptotics.IsTheta atTop (∑μ²/φ) log` (idiomatic form).
**Reusable analytic bricks added:** `sum_log_div_sq_le`, `geom_tail_le`, `log_telescope_le`,
`leading_term_bound/_ge`, `leading_sum_bound/_ge`, `reindex_inv_log`, `reindex_log_sq`,
`mertens2_abel` (general 2nd Abel identity), `prod_euler_le_exp`, `euler_exponent_le`,
`tail_eq_kge2`, `tail_kge2_le`. Generic-in-`C` cores: `mertens2_upper_of/_lower_of`,
`mertens_upper_of`. (Conditional `*_of_tail` glue still present — documents the structure, now
subsumed by the unconditional versions; harmless, leave it.)

## 🎬 Next actions
1. **Start the NEXT RUNG: the GPY/Maynard `s1` divisor-sum expansion** — the actual consumer of
   `∑μ²/φ`. Target axiom: `s1_holds_from_nonprime_asym` in `Sieve.lean:1907` (and `s2_*`). It
   asserts `alphaBound` (`Sieve.lean:132`): `sieveSum ν b W y - α·alphaMainTerm` is `o(alphaMainTerm)`,
   where `ν = selberg_nu` (a squared sum of 1-D divisor sums) and `α = ∫_{simplex} F²`. Proving it
   = the GPY main-term computation (Polymath8b §3 eqns sfg-1, lflg): open the square, sum over
   `n ≡ b (W)` in `[x,2x]`, the divisor sums collapse to singular-series sums whose 1-D pieces are
   exactly `∑μ²/φ = Θ(log N)` (now available). This is a **major multidimensional effort** — read
   `ANALYTIC_AXIOM_BURNDOWN.md` "BV dependency tree" + the `selberg_nu_basis`/`alphaMainTerm`/
   `mkF_denominator` defs first. NOT a one-lemma lap; decompose it (good Aristotle fodder once scoped).
2. **Kill/repurpose Aristotle `3f137191`** (superseded) and architect the next bounded lemma toward
   action #1 — but only once you've scoped a clean self-contained sub-piece (don't submit half-baked).
3. (Optional, low priority) Simplify the now-redundant `mertens_theta_log_of_tail` /
   `mertens2_two_sided_of_tail` to derive from the unconditional versions, or delete them.

## ⚠️ Gotchas
- **Box OOM flake** ("Cannot allocate memory" on olean load) hits ~once per few `lake env lean`
  invocations — always a flake, just re-run.
- v4.29 names used this lap: `div_le_div_iff₀`, `le_div_iff₀`, `div_le_iff₀`; `Summable.sum_le_tsum`
  (NOT bare `sum_le_tsum`); `Real.add_one_le_exp` (`x+1 ≤ eˣ`); `Real.log_two_gt_d9`/`_lt_d9` for
  numeric `log 2` bounds; `antitoneOn_of_deriv_nonpos` needs a `DifferentiableOn` arg too.
- The peel `Icc 1 K = insert 1 (Icc 2 K)` needs `1 ≤ K` in scope for its `omega` (it's *false* for
  `K=0`) — `tail_eq_kge2` carries `hKpos`.
- Consumer-validation pattern worked great again: prove the downstream proof in scratch with the
  hard lemma's *statement* as a temp `axiom`, get green, then port real+consumer together.

## 📁 Key files
- `BoundedGaps/Mertens.lean` — everything above; the headline is `mertens_theta_log` / `mertens_isTheta_log`.
- `PENDING_WORK.md §C` — full lap inventory + "gate discharged" status + next-rung pointer.
- `ANALYTIC_AXIOM_BURNDOWN.md` — the BV/sieve ladder map (read before the GPY rung).
- `BoundedGaps/Sieve.lean:132` (`alphaBound`), `:1907` (`s1_holds_from_nonprime_asym`) — next target.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (the
analytic-NT burn-down). The 1-D Mertens core is DONE and unconditional; your next rung is the
multidimensional GPY `s1` expansion (Next action #1). Start by reading `ANALYTIC_AXIOM_BURNDOWN.md`
+ the `alphaBound`/`selberg_nu_basis` defs in `Sieve.lean` to scope a decomposition, THEN work.**

🧵 ➡️ 🧵
