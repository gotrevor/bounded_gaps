# Handoff: Both bridge axioms ELIMINATED — `Mk_200_gt_4` is axiom-clean

**Date**: 2026-06-03 20:41 UTC · **Branch**: `path-a-selberg-nu` · HEAD `1927c2e` · 6 commits this lap, all green (8271 jobs), all box-local (NEVER pushed — host pushes).

## 🎯 What we're doing
Discharging the `Mk k > 4` endgame → bounded gaps, via the symmetric-reduction → orbit-basis
Gram-quotient → matchings-closed-form pipeline. **This lap closed the headline gap: the two
permanent/rook bridge axioms `denom_bridge` and `num_bridge` are now FULLY KERNEL-PROVEN theorems.**

## 🧠 Context to carry forward
- **THE BIG WIN.** `#print axioms Mk_200_gt_4 = [propext, Classical.choice, Quot.sound, native_decide]`
  — **NO mathematical axioms remain** on the `Mk 200 > 4` result. Verified this lap. The whole
  symmetric-reduction chain is in-kernel; native_decide is only the concrete k=200 witness computation.
- **The proof technique that cracked it — the "permanent route" (NOT the orbit-recursion every prior
  handoff chased).** For an identity `autParts·autParts·(orbit double-sum) = matchSum`: define a
  **permanent** `permDenSum La Lb k = ∑_{ρ∈Perm(Fin k)} ∏ᵢ(ofParts La i + ofParts Lb(ρ i))!` (ℚ-valued
  `permNumSum` with the extra `∑ᵢ gWeight(pᵢ,qᵢ)` factor). Prove TWO halves that both equal `k!·perm*`:
  `lhs_*` (orbit double-sum·(k−|La|)!(k−|Lb|)! = k!·perm*, via the already-proven
  `ofParts_autParts_orbit_sum`) and `rhs_*` (matchSum·(...) = k!·perm*, by induction on `La`). They
  share `perm*` → cancel (it's a positive ℕ/ℚ). This sidesteps the brutal `monoOrbit (ofParts (a::L))`
  recursion entirely.
- **The hard core `perm*_laplace`** (Laplace expansion of the permanent): after `perm*_cons`
  (decompose ρ by `ρ 0` via `Equiv.Perm.decomposeFin` / `Fintype.sum_prod_type`), the inner perm-sum
  over the remaining columns collapses to `perm*` on the **column-removed list**. Key insight: the
  `swap 0 j ∘ succ` embedding differs from the canonical `Fin.succAbove j` by a permutation
  (`Fin.exists_succAbove_eq`), absorbed by permutation-invariance of the permanent
  (`permSum_perm_invariant` / `permSum_gw_invariant`); then the EXACT functional identity
  `ofParts Lb ∘ Fin.succAbove j = ofParts (Lb.eraseIdx j)` (`ofParts_comp_succAbove`, proven by
  `funext`+getD/eraseIdx/succAbove value computation) finishes it. The num Laplace's `gWeight a (·)`
  first-row term splits into a `permDenSum` part (uses the proven denom result) + a `permNumSum` part.
- **`descFactorial_succ_eq'`** (`k.descFactorial (n+1) = k*(k-1).descFactorial n`) is UNCONDITIONAL
  (truncated ℕ subtraction makes it hold for all k,n) — this is what makes `matchDenSum_cons_eq'` /
  `matchNumSum_cons_eq'` hold without an artificial `|La|+|Lb|<k` hypothesis (needed because
  `denom_bridge` is invoked with only `|La|,|Lb|≤k`).
- **Aristotle gave a head start, not the answer.** Jobs `0b5bf5be`/`9d05dbaa` (last lap) returned the
  *architecture* (`lhs_eq_perm`, `permDenSum_cons`, `matchDenSum_cons_eq'`) with 2-3 residual sorries;
  I ported the working lemmas (one aesop regression in 4.29.1 vs their 4.28.0, sidestepped by reusing
  the repo's existing orbit machinery) and **proved the residual sorries by hand**
  (`permDenSum_laplace`, `permNumSum_laplace`, `matchNumSum_cons_eq'`). I cancelled the redundant
  follow-up jobs (`d314bdc2` numrec).
- **Gotcha that ate time: `let`-lambdas in a def's `.map` function** block syntactic `rw`/matching.
  Fix: `have hMNS : matchNumSum (a::La) Lb k = (... inlined-lambda ...).sum := rfl` (defeq via zeta)
  to inline, then work with the inlined form; and `rw [List.map_congr_left (g := <target>) hproof]`
  lets Lean INFER the source function `f` (e.g. `W∘transform`) so you never write its exact cast form.

## ✅ State (all observed green this lap)
- `6d8e23f` **`matchNumSum_cons_eq'` PROVEN** → last math axiom gone. `1927c2e` docs.
- `d18ca93` **`num_bridge` theorem** (was axiom). `b8fd8f5` **`denom_bridge` theorem** (was axiom).
- Full `lake build` green (8271 jobs) — confirmed by the pre-commit gate on `6d8e23f`.
- `#print axioms Mk_200_gt_4` = `[propext, Classical.choice, Quot.sound, native_decide]` (verified).
- New proven lemmas live in `BoundedGaps/SymmetricReductionOrbitFree.lean`: `permDenSum`/`permNumSum`
  (+`_cons`, `_nil`, `_laplace`), `lhs_eq_perm`/`lhs_num`, `rhs_eq_perm`/`rhs_num`, `orbit_sum_eq_perm_sum(_Q)`,
  `permSum_perm_invariant`/`permSum_gw_invariant`, `succAbove_val`, `eraseIdx_getD`, `ofParts_comp_succAbove`,
  `descFactorial_succ_eq'`, `matchData_fst_le_la`/`matchDataN_fst_le`, `matchDenSum_eq_zero_of_lt`/
  `matchNumSum_eq_zero_of_lt`, `matchDenSum_cons_eq'`/`matchNumSum_cons_eq'`, `matchDataN_proj`,
  `matchDenSum_dataN`, `list_sum_flatMap`, `sum_range_eq_list`, `zip_range_eq_map`, `weight_elt`.

## 🎬 Next actions
The in-scope bridge work is **DONE and axiom-clean** — do NOT re-open it. The remaining open items
are all OUT of this thread's scope:
1. **Nothing on the Mk-bridge chain.** It's complete. If Trevor asks "what's left for bounded gaps?",
   the honest answer: the broader `bounded_gap_of_Mk_200` / `H1_le` rest on cited external analytic NT
   (`BombieriVinogradov`, sieve `s1/s2` = `s1_holds_from_nonprime_asym`, `exists_separable_F_of_Mk_gt`)
   and the flagship `mk_*_witness` axioms (degree ~20, infeasible on-box). These are the multi-month nut.
2. **Analytic-NT thread is OWNED ELSEWHERE.** `ANALYTIC_AXIOM_BURNDOWN.md` + Aristotle job `6c45fd6b`
   (`mertens_crux`, `scratch_mertens.lean`) are a parallel session's track — **do not disturb that job**.
3. **Optional gilding** (low priority, still analytic-axiom-bound): give `bounded_gap_of_Mk_200` a
   concrete narrow admissible 200-tuple (H(200)≈1500) via `admissible_of_check_small_primes` so its
   diameter is concrete (currently the cheap 199·200! factorial tuple). Needs Python (`tools/mk/`) to
   find an admissible tuple, then emit + native_decide. Self-contained but a fresh-lap task.

## ⚠️ Gotchas
- **Box intermittent OOM** on `lake build` of Mathlib oleans ("Cannot allocate memory") — a flake;
  re-run, it clears.
- Heavy `native_decide` (MkWitness200) needs `--tstack=2000000` (set in `lakefile.toml weakLeanArgs`).
  For ad-hoc: `lake env lean --tstack=400000 <file>`.
- Untracked scratch files (`scratch_*.lean`, `aristotle-*-job/`, `aristotle-*-result`) are this lap's
  dev artifacts — all content is now ported & committed; safe to ignore or `git clean` later.
- `native_decide` triggers a non-fatal mathlib style-linter warning (repo uses it elsewhere).

## 📁 Key files
- `BoundedGaps/SymmetricReductionOrbitFree.lean` — the whole reduction; `denom_bridge` (~L2689),
  `num_bridge` (~L3423) are now theorems. The permanent-route lemmas are between L2319 and L3480.
- `BoundedGaps/MkWitness200.lean` — `Mk_200_gt_4`, `bounded_gap_of_Mk_200`.
- `PENDING_WORK.md §A` — updated: both bridges proven, axiom-clean state recorded.
- `ANALYTIC_AXIOM_BURNDOWN.md`, `scratch_mertens.lean` — the parallel analytic thread (not yours).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. The
Mk-bridge work is COMPLETE and axiom-clean; if Trevor hasn't given a new direction, the only in-scope
follow-up is the optional admissible-tuple gilding (Next action #3) — otherwise report the milestone
and ask what he wants next. Do NOT touch the analytic-NT thread / job `6c45fd6b`.**

🧵 ➡️ 🧵
