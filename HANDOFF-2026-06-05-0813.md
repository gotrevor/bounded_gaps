# Handoff: contour-free Path-Y `s1` MAIN-TERM core COMPLETE (y-space sieve built end-to-end)

**Date**: 2026-06-05 08:13 UTC · **Branch**: `path-a-selberg-nu` · HEAD `33eb72d`
23 commits this lap (`4bf8830`…`33eb72d`), every Lean commit green at the pre-commit full-build gate
(8289 jobs). Never pushed. Supersedes `HANDOFF-2026-06-05-0650.md`. (Master `HANDOFF.md` untouched per
repo convention.)

## 🎯 What we're doing
Analytic axiom burn-down for the GPY/Maynard sieve. Flagship `bounded_gap_of_Mk_200` rests on 3 math
axioms — `BombieriVinogradov`, `s1_holds_from_nonprime_asym`, `s2_holds_from_prime_asym_at_level`.
This lap I **built the entire contour-free Path-Y `s1` MAIN TERM from scratch** (the deepest axiom),
as composable axiom-clean pieces. `Mk_200_gt_4` axioms verified UNCHANGED this lap.

## 🧠 Context to carry forward — read this, it's the whole mental model
- **FIRM VERDICT (don't re-litigate):** the repo's `selberg_nu` (built from `lambdaTransform` =
  Polymath8b `d`-space `λ_F`) has `s1` constant `∫(F')²` and is **genuinely PNT-gated** — NO
  contour-free route. Confirmed 3 ways: (1) papers (Polymath8b's own proof is a contour); (2) this
  mathlib (v4.29.1) has `LSeries/Nonvanishing` + `PrimesInAP` but **NO quantitative PNT / Möbius
  mean `∑μ(n)/n→0`**; (3) the **witness forces y-space** — the `d`-space sieve ratio is `M_k(F')`
  but `Mk_200_gt_4` proves `M_k(F)`, so `s1`'s `∫F²` is the *`y`-space* statement, and pairing it
  with the `d`-space `selberg_nu` IS the "off-by-a-derivative" landmine. The antiderivative relabel
  does NOT help (step (†) is PNT for any smooth weight). See memory `[[s1-derivative-landmine]]`.
- **⟹ The fix = a NEW `y`-space sieve `selberg_nu_yr`, built ALONGSIDE (do NOT touch the flagship).**
  Maynard's `y_r`-space: choose smooth `y_r`, derive `λ_d` by Möbius inversion; then the
  diagonalisation lands on `∑(μ²/φ)F²` DIRECTLY (contour-free, just sharp Mertens). I built this
  whole chain this lap. The witness layer (`mkF_denominator`/`MkF`/`Mk_200_gt_4`) is about `F`, NOT
  `ν` — it stays UNTOUCHED when Trevor eventually flips the axioms.
- **The candidate-set crux (worked out this lap):** the sieve expansion sums over `sieveDivisors`,
  but the diagonalisation needs a squarefree divisor-closed `R`. RESOLVED: use
  `Rset = sieveDivisors.filter(Squarefree · ∧ Coprime · W)` — divisor-closed, and `yLambda Rset … d = 0`
  for `d ∉ Rset`, so `∑_{sieveDiv} = ∑_{Rset}` (extra terms vanish). All glued.

## ✅ State — the contour-free `s1` MAIN-TERM chain, all axiom-clean, full build green (8289 jobs)
Built bottom-up (files: `WeightedMertens.lean`, `SieveExpansion.lean`, new `S1YSpace.lean`):
- `weighted_mertens_general(_of_contDiff)` — weighted Mertens for ANY weight `g` with base density `c`.
- `gMuSqTotientCoprime`, `weighted_mertens_coprime(_sq)`, `yspace_muphi_diagonal_tendsto(+_bilinear)`:
  `(∑_{r≤N,(r,W)=1}(μ²/φ)F²)/logN → (φ(W)/W)∫F²` (Maynard `S1Summation2`/GGPY L4 WITH singular series).
- `moebius_div_collapse`, `inner_moebius_collapse`, **`moebius_inversion_multiples`** (the exact
  `λ_d ↔ smooth-y_r` duality).
- `gpy_diagonalize_yform_smooth/_muphi(+_bilinear)`: `∑λλ/[d,e] = ∑_r(μ²/φ)F²`.
- `sieveSum_genProd_sq_expand` (general sieve opening, any coeff), `selberg_nu_yr_sep` (+nonneg,
  +expand, +`_eq_heuristic_add_correction` split), `yLambda` + vanishing/restriction lemmas,
  `filter_sf_coprime_hyps`/`sieveDivisors_yspace_hyps`.
- **CAPSTONE `yr_heuristic_main_eq_muphi`**: the y-space heuristic main `= M·∏ᵢ ∑_{r≤lvl,sf,(r,W)=1}(μ²/φ)Fᵢ²`,
  fully diagonalised, contour-free. Plus **`yspace_sieve_quadform_tendsto_one`** — the first FULLY
  UNCONDITIONAL contour-free `s1`-main-term-shaped result (`W=1`, `𝔖=1`, base = `sharp_mertens_unconditional`).

## 🎬 Next actions
1. **Poll the Aristotle brick** `aristotle tasks 65d11d89-0c78-4350-8aa8-408564d42b33` (W-coprime sharp
   Mertens `∑_{n≤N,(n,W)=1}μ²/φ /logN → φ(W)/W` — the `hBaseW` that makes general-`W` unconditional;
   was RUNNING ~1h+). If COMPLETE: download, `#print axioms`-verify, port; it discharges `hBaseW`.
   Then submit the next brick (keep ONE in flight).
2. **Finish `s1`** (a fresh structural lap — see `PENDING_WORK.md` top "What's LEFT"): (i) **count→`M`**
   pick `M`=lattice main density, bound correction `=o(main)` (BV-gated, gap C; infra exists:
   `lattice_count_main_term`, `correction_abs_bound`, `hyperbola_count_le`); (ii) **simplex assembly**
   (gap A4 — `∏rᵢ≤R` enters via the count, couples to `weighted_riemann_kd_muphi`); (iii) `hBaseW` from
   the brick ⟹ unconditional. THEN Trevor flips the 3 `s1`/`s2` axioms + `selberg_sieve_data_from_F`.
3. `s2` mirrors `s1` (same y-space machinery; the `θ`-weight on one coord adds the BV/EH content).

## ⚠️ Gotchas
- `field_simp` often FULLY closes `μ²/φ` goals — a trailing `ring`/`ring_nf` then errors "no goals".
- `Finset.sum_comm'` for interlocking-filter swaps needs the EXACT membership order
  `x∈s ∧ y∈t x ↔ x∈s' y ∧ y∈t'` (see memory `[[lean-finset-sum-comm-prime]]`).
- `aristotle tasks <uuid>` sometimes echoes a *different* recent task; cross-check with
  `aristotle list | grep coprime`. The `2329d005 aristotle_fibersum` IDLE entry is not ours.
- Cheap iterate: `taskset -c 0-5 lake build BoundedGaps.S1YSpace` (~3s) or develop in a `/tmp/*.lean`
  scratch that `import BoundedGaps.S1YSpace` (module already built) — far faster than full rebuilds.

## 📁 Key files
- `BoundedGaps/S1YSpace.lean` — NEW; the y-space sieve `selberg_nu_yr_sep`, `yLambda`, the heuristic-main
  capstone `yr_heuristic_main_eq_muphi`, the unconditional `yspace_sieve_quadform_tendsto_one`.
- `BoundedGaps/SieveExpansion.lean` — Möbius inversion + y-form diagonalisation + general expansion.
- `BoundedGaps/WeightedMertens.lean` — `weighted_mertens_general` + the W-coprime singular-series tendsto.
- `PENDING_WORK.md` — top section (lap 4) has the full assembled chain, the firm `s1` verdict, and the
  precise "What's LEFT" list (count→M, simplex, brick). Read it.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (GPY `s1`
burn-down; the contour-free y-space main-term core is now COMPLETE). Absorb the context, then start at
Next action #1: poll the coprime-Mertens brick `65d11d89`, then take the `s1` finish (count→M +
simplex + brick base) or work the next open axiom.**

🧵 ➡️ 🧵
