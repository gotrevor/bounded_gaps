# Handoff: Mertens 1D lower bound LANDED (axiom-clean) + concrete H₁≤1304 gilding; upper bound mid-flight

**Date**: 2026-06-04 02:00 UTC · **Branch**: `path-a-selberg-nu` · HEAD `1ea9815` · 6 commits this lap, all green (8272 jobs), all box-local (NEVER pushed — host pushes).

## 🎯 What we're doing
The `Mk 200 > 4` bridge chain is DONE/axiom-clean (prior lap). This lap pushed two fronts:
**(1)** gilded the headline `bounded_gap_of_Mk_200` to a *concrete* numeric bound, and **(2)** opened
the **analytic-NT burn-down** by landing the first real brick — the 1D Mertens lower bound — as a
kernel-clean theorem. The strategic frame (`ANALYTIC_AXIOM_BURNDOWN.md`): the repo's remaining
analytic axioms (`s1/s2_holds_*`, `BombieriVinogradov`, `exists_separable_F_of_Mk_gt`) are all gated
on Mertens/singular-series summation; Mertens 1D is the first brick of that multi-month ladder.

## 🧠 Context to carry forward
- **The s1/s2 axioms are BLOCKED, not lazy.** `s1_holds_from_nonprime_asym` (Sieve.lean:1907) is an
  `∀ᶠ x, alphaBound …(selberg_nu …)`, and **`selberg_nu` is an opaque axiom** (no body) — so the
  statement is literally unprovable until `selberg_nu` is given a faithful GPY def. That's the
  multi-month nut. Don't try to one-lap it. The Mertens ladder is the right *foundational* attack.
- **`Zhang.H1_le_70M` must STAY a sorry.** It's the *method-faithful* Zhang theorem (takes an MPZ
  hyp). Discharging it with my unconditional `liminfGap_one_le_1304` (1304 ≤ 70M) would be valid but
  **misrepresent it** — the repo deliberately separates it from the result-only `H1_le_70M_via_Polymath8b`.
  Don't fake it. (Also blocked by import order: Zhang is imported before MkWitness200.)
- **Aristotle ports need v4.28→v4.29 fixing.** `mertens_crux` came back from Aristotle (job
  `6c45fd6b`) sorry-free but with a `grind +suggestions` that FAILS under our v4.29.1. Fix pattern:
  replace the dead `grind` with an explicit derivation (here: `radical(p^k)=p` via
  `radical_pow _ hfp` + `radical_of_prime hp.prime` + `simp`, with
  `hfp := (hp.factorization_pos_of_dvd hm.ne' hpm).ne'`). **Always `#print axioms` after porting** —
  the broken `grind` had silently injected `sorryAx` while still "compiling".
- **mathlib v4.29.1 HAS `radical`** (`UniqueFactorizationMonoid.radical`; `radical_dvd_self`,
  `radical_pow`, `radical_of_prime`, `radical_mul`, `Nat.radical_eq_prod_primeFactors`,
  `squarefree_radical`). The burndown's old "no radical" claim was WRONG (now corrected in the doc).
- **The upper-bound proof is mid-flight LOCALLY** — see Next actions. I chose to prove it myself
  (not just wait on Aristotle `129257d7`) because Aristotle ports need fixing anyway and local guarantees
  v4.29.1. Helper (a) is already green in `scratch_mertupper.lean`.

## ✅ State (all observed green this lap)
- `60cd99c` **gilding**: `bounded_gap_of_Mk_200` now uses explicit narrow `tuple200` (diameter 1304);
  new `liminfGap_one_le_1304 : liminfGap 1 ≤ 1304` and `narrowness_200_le_1304`. `Mk_200_gt_4` stays
  axiom-clean. (Engelsma `checkAdm` bundled-`native_decide` admissibility pattern.)
- `b69dc99` **`BoundedGaps/Mertens.lean`**: `mertens_lower : Real.log N ≤ ∑_{Icc 1 N} μ²(n)/φ(n)`,
  `#print axioms = [propext, Classical.choice, Quot.sound]` — fully kernel-clean, NO native_decide.
  (radical-fiber partition; ported from Aristotle `6c45fd6b` + hand-proved `harmonic_eq_icc_sum`.)
- `1ea9815` corollaries: `log_le_sum_inv_totient` (log N ≤ ∑ 1/φ(n)), `tendsto_sum_mertensSummand_atTop`
  (∑ μ²/φ → ∞). All axiom-clean. Full build green **8272 jobs** (pre-commit gate confirmed).
- **Aristotle `129257d7-…`** (`aristotle-mertupper/`) RUNNING the upper bound `mertens_prod_upper`.

## 🎬 Next actions
1. **Finish `mertens_prod_upper` locally** (`scratch_mertupper.lean`, repo root — helper (a)
   `mertensSummand_eq_prod {n} (hsf : Squarefree n) : mertensSummand n = ∏ p ∈ n.primeFactors, 1/((p:ℝ)-1)`
   is GREEN there). Statement (already typechecks):
   `∑ n ∈ Icc 1 N, mertensSummand n ≤ ∏ p ∈ (Icc 2 N).filter Nat.Prime, (1 + 1/((p:ℝ)-1))`.
   **Plan**: (i) `rw [Finset.prod_one_add]` ⇒ RHS = `∑ t ∈ P.powerset, ∏ p ∈ t, 1/(p-1)` (P = the
   filtered primes). (ii) restrict LHS to squarefree via `Finset.sum_subset (filter_subset …)` +
   `mertensSummand_of_not_squarefree`. (iii) rewrite each squarefree term by helper (a). (iv) reindex
   `∑_{n sqfree} ∏_{p∈primeFactors n}` as `∑_{t ∈ image primeFactors} ∏_{p∈t}` via `Finset.sum_image`
   — injectivity from `Nat.prod_primeFactors_of_squarefree` (∏ primeFactors = n recovers n).
   (v) `Finset.sum_le_sum_of_subset_of_nonneg`: `image ⊆ P.powerset` (each prime factor p of n≤N has
   2≤p≤N, prime) and `∏ 1/(p-1) ≥ 0` (`Finset.prod_nonneg`). Then **CANCEL Aristotle `129257d7`**
   (`aristotle cancel …`) and **submit the next brick**.
2. **Port into `BoundedGaps/Mertens.lean`** (add `mertensSummand_eq_prod` + `mertens_prod_upper`),
   `#print axioms` clean, full `lake build`, commit. With lower+upper you have `∑ μ²/φ = Θ(log N)`.
3. **Next Aristotle brick** (architect + submit, keep ONE in flight): the SHARP
   `∑_{n≤N} μ²(n)/φ(n) = log N + O(1)` (the Mertens constant), or move to the **multidimensional**
   singular-series → ∫F² version (sub-step (c), the real GPY main-term nut).
4. If Aristotle `129257d7` returns BEFORE you finish #1: harvest it (`aristotle download … --destination …`,
   untar, **verify in-kernel + `#print axioms` clean**, fix any v4.29 `grind`/`simp` regressions),
   take whichever proof is cleaner.

## ⚠️ Gotchas
- **`simp only [Finset.prod_inv_distrib]` → "typeclass instance problem is stuck".** Workaround used in
  helper (a): turn `∏ (1/f)` into `1/∏ f` via `Finset.prod_div_distrib, Finset.prod_const_one` then
  `congr 1` + `Finset.prod_congr`. Also `((p-1:ℕ):ℝ) = (p:ℝ)-1` needs `Nat.cast_sub hp1` (`1 ≤ p`).
- **Box intermittent OOM** on `lake build`/`lake env lean` loading Mathlib oleans ("Cannot allocate
  memory") — a flake, just re-run (hit it twice this lap, cleared both times).
- Untracked scratch: `scratch_mertupper.lean` (helper a, green), `scratch_mertens_verify.lean`
  (full mertens_crux verify), `aristotle-mertens-dl/` (the downloaded crux), `aristotle-mertupper/`
  (the submitted job). `scratch_mertens.lean` is SUPERSEDED by `BoundedGaps/Mertens.lean`.
- Heavy `native_decide` (MkWitness200) needs `--tstack=2000000` (set in lakefile weakLeanArgs).

## 📁 Key files
- `BoundedGaps/Mertens.lean` — the landed 1D Mertens lower bound + corollaries (NEW this lap).
- `scratch_mertupper.lean` — green helper (a); finish `mertens_prod_upper` here (Next action #1).
- `BoundedGaps/MkWitness200.lean` — `tuple200`, `liminfGap_one_le_1304`, `narrowness_200_le_1304`.
- `ANALYTIC_AXIOM_BURNDOWN.md` — the ladder map; updated (Mertens DONE, next bricks named).
- `PENDING_WORK.md §C` — Aristotle status (job `129257d7` in flight).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. The
Mk-bridge work is complete; the live thread is the analytic-NT burn-down via Mertens. Start at Next
action #1: open `scratch_mertupper.lean` and finish `mertens_prod_upper` (helper (a) is already
green), then port to `BoundedGaps/Mertens.lean` and commit.**

🧵 ➡️ 🧵
