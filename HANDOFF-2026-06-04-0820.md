# Handoff: GPY sub-step (c) CRACKED — sharp Mertens `∑μ²/φ ∼ log x` reduced to 2 Aristotle leaves

**Date**: 2026-06-04 08:20 UTC · **Branch**: `path-a-selberg-nu` · HEAD `e9dc1da` ·
**12 commits this lap** (`ac9ebf9`…`e9dc1da`), all green (full `lake build`, 8275 jobs,
verified at every pre-commit gate), all axiom-clean `[propext, Classical.choice, Quot.sound]`,
all box-local (NEVER pushed). **Supersedes `HANDOFF-2026-06-04-0648.md`.**

## 🎯 What we're doing
Analytic-NT axiom burn-down: turn the GPY/Maynard sieve-asymptotic axioms (`Sieve.lean`
`s1`/`s2`) into real proofs. The handoff identified **sub-step (c)** — sharp Mertens
`∑_{n≤x} μ²(n)/φ(n) ∼ log x` with **leading coefficient EXACTLY 1** — as THE blocker
(prior laps only had the lossy Θ-bound, coeff ≈e^γ). **This lap cracked it.**

## 🧠 Context to carry forward
- **The missing idea** (prior laps + the online request were stuck on it): factor out
  **`ζ(s+1)`, not `ζ(s)`**. With `g=μ²/φ`, define `B := μ ⋆ (id·g)`; then `ζ⋆B = id·g`
  (Dirichlet algebra), giving the keystone `∑_{e∣n} B(e) = n·g(n)`. I reconstructed the
  full textbook proof myself — see `SHARP_MERTENS_RECONSTRUCTION.md`. The online sharp-Mertens
  request is now marked **RESOLVED LOCALLY** (no longer blocking; a textbook cross-check of
  the constant is optional).
- **Full chain, all in `BoundedGaps/SharpMertens.lean` (NEW module, 510 lines), axiom-clean:**
  keystone `sum_divisors_BSharp` → reindex `sum_g_eq_weighted_harmonic`
  (`S(N)=∑_{e≤N}(B(e)/e)·H(⌊N/e⌋)`) → `harmonic_remainder_mem` (`r_e∈[0,1]`) →
  decomposition `sum_g_decomp` (`S=P·logN−Q+R`) → **crux** `P_tendsto_one`
  (`P=∑B/e → 1` via mathlib's Euler product: each local factor `∑'_e b(p^e)=1` EXACTLY)
  → squeeze (`Q/log,R/log→0`) → **capstone** `sharp_mertens_tendsto`:
  `(∑_{n≤N}μ²/φ)/log N → 1`.
- **The whole thing is conditional on ONLY two facts**, both elementary Euler-product
  partial-sum bounds (`summable_norm_bAF_of_bound` / `_log_of_bound` turn them into the
  `Summable` hypotheses): `∑_{e≤N}|b(e)| ≤ C` and `∑_{e≤N}|b(e)||log e| ≤ C`. One-shot
  wrapper `sharp_mertens_of_bounds {C₁ C₂}` takes exactly these and concludes.
- **Why I did NOT prove the two bounds locally:** the `b(e)≠0 ⟺ all prime exponents ≤2`
  Euler expansion (`∏_p(1+2/(p(p-1)))`) is genuinely hard by hand — mathlib only has the
  *squarefree* version (`IsMultiplicative.prodPrimeFactors_one_add_of_squarefree`). This is
  exactly Aristotle's wheelhouse, so I gave it to babs. **Do not duplicate it by hand.**
- **Side win:** ported Aristotle `singular_sum_bounded` (`∑μ²/(φd·d)≤3`) into
  `SingularSeries.lean`, making `∑_{n≤N} n/φ(n) ∼ A·N` (classical totient average order)
  **UNCONDITIONAL** (`sum_self_div_totient_asymptotic_uncond`).
- **Port readiness:** `bAF_prime` (`=1/(p(p-1))`), `bAF_prime_sq` (`=−1/(p(p-1))`),
  `bAF_prime_pow_high` (`=0`) match babs's abstract axioms exactly — replaying babs's proof
  on the concrete `bAF` should be mechanical.

## ✅ State (full `lake build` green, 8275 jobs, all new decls axiom-clean — verified this session)
- `BoundedGaps/SharpMertens.lean` — the complete reduction (above). `sharp_mertens_tendsto`,
  `sharp_mertens_of_bounds` both `#print axioms`-clean.
- `BoundedGaps/SingularSeries.lean` — `singular_sum_bounded` (ported) + unconditional
  `∑n/φ∼A·N`.
- `SHARP_MERTENS_RECONSTRUCTION.md` — the full proof + per-step formalization status.
- Aristotle: **babs `830e5129` RUNNING (~23%, ~40min in)** — `∑_{e≤N}|b e| ≤ 8`.
  `aristotle-blog/Problem.lean` **staged** (the `log`-weighted `≤20` companion), not yet
  submitted (keep ONE job in flight; submit when babs returns).

## 🎬 Next actions
1. **Poll `aristotle show 830e5129-...` (or `aristotle list | grep 830e5129`).** When babs
   completes: download (`aristotle download <uuid> --destination <f>`; it's a **gzip tarball**,
   `tar xzf <f> -C <dir>`), verify `#print axioms` clean in our v4.29.1 kernel, then **port**:
   replay its proof on concrete `bAF` (use `bAF_prime/_sq/_pow_high`, `bAF_isMultiplicative`,
   `bAF_one := bAF_isMultiplicative.1`) to get `∀ N, ∑_{e≤N}‖bAF e‖ ≤ 8`. Feed
   `summable_norm_bAF_of_bound` → discharges hypothesis #1 of `sharp_mertens_of_bounds`.
2. **Immediately submit `aristotle-blog`** (the `log`-weighted bound) the moment babs frees
   the slot: `aristotle submit "Prove blog_bounded in Problem.lean (replace the sorry);
   strategy in header (von Mangoldt route)." --project-dir aristotle-blog`. Note the UUID.
3. When blog returns: port → `summable_norm_bAF_log_of_bound` → discharge hypothesis #2.
   Then `sharp_mertens_of_bounds (ported-h1) (ported-h2)` is **fully unconditional sharp
   Mertens** — a clean axiom-free theorem. Commit.
4. **Then** the real next front (handoff #4, independent of all above): the *weighted* sieve
   sum `∑_{d≤R}(μ²/φ)·F(log d/log R) ∼ (∫₀¹F)·log R` via Abel summation
   (`Mertens.abel_summation_identity`) + Riemann sums, using `sharp_mertens_tendsto` as the
   `F≡1` ingredient. This is what actually feeds `s1`/`alphaBound` (a HUGE axiom — the whole
   GPY sieve calc; sharp Mertens is one deep ingredient, many laps from done).

## ⚠️ Gotchas
- **Box OOM flake** on `lake env lean` (~1/few runs) — re-run, optionally `taskset -c 0-3`.
- **Aristotle abstract-axiom leaves:** babs/blog use `axiom b : ℕ → ℝ` + value axioms; you
  CANNOT instantiate an axiom — you must REPLAY the proof body with concrete `bAF`. The value
  lemmas are pre-built for exactly this.
- `← pow_one p` rewrites ALL nat `p` including inside casts `↑p` — scope with `conv_lhs`
  (see `BSharp_prime'`).
- `Summable.sum_le_tsum` is the method form (`hsum.sum_le_tsum s h`); bare `sum_le_tsum`
  is ENNReal-only. `summable_of_sum_range_le` for nonneg bounded-partial-sums → Summable.
- A second unrelated Aristotle job `spstab`/`8522edf3` is running — NOT ours, ignore it.

## 📁 Key files
- `BoundedGaps/SharpMertens.lean` — THE deliverable (sub-step (c) reduction).
- `BoundedGaps/SingularSeries.lean` — `singular_sum_bounded` + unconditional `∑n/φ∼A·N`.
- `BoundedGaps/Mertens.lean` — `abel_summation_identity:659` (for next front #4),
  `mertens_theta_log` (the crude Θ-core).
- `BoundedGaps/Sieve.lean` — `s1_holds_from_nonprime_asym:1907` / `s2_*:1928` (target axioms),
  `alphaBound:132`/`betaBound:146` (the IsLittleO shapes).
- `SHARP_MERTENS_RECONSTRUCTION.md`, `aristotle-blog/Problem.lean` (staged job).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or
wait for instructions, and don't offer other projects from the KB — this doc IS the chosen
thread (analytic-NT sieve burn-down, sub-step (c) just cracked). Start at Next action #1:
poll/harvest Aristotle babs `830e5129`, port it onto `bAF`, then submit `aristotle-blog`.
Keep ONE Aristotle job in flight at all times.**

🧵 ➡️ 🧵
