# Handoff: SHARP Mertens 1st `∑Λ(n)/n = log N + O(1)` LANDED (coefficient 1, axiom-clean)

**Date**: 2026-06-04 03:28 UTC · **Branch**: `path-a-selberg-nu` · HEAD `4bbb3db` · **17 content commits this lap**, all green (full build 8272 jobs verified at every pre-commit gate), all box-local (NEVER pushed — host pushes). **Supersedes `HANDOFF-2026-06-04-0316.md`** (which is now stale — von Mangoldt + sharp Mertens 1st landed since).

## 🎯 What we're doing
Analytic-NT axiom burn-down (`ANALYTIC_AXIOM_BURNDOWN.md`): driving the 1-D Mertens core
`∑_{n≤N} μ²(n)/φ(n) ~ log N` that underlies the cited s1/s2 sieve axioms. Lower half
(`mertens_lower`) landed earlier. This lap built the full analytic toolkit AND landed the
**sharp Mertens 1st theorem** `∑_{n≤N} Λ(n)/n = log N + O(1)` (von Mangoldt form).

## 🧠 Context to carry forward
- **Why "sharp" matters (the load-bearing insight — see `[[mertens-sharp-vs-crude]]`):** there
  are two Mertens routes in `Mertens.lean`. The Chebyshev-`θ` route (`mertens_first_le`) carries a
  LOSSY `log 4 ≈ 1.386` constant → pushed to Mertens 2nd it gives `∑1/p ≤ 3.4·loglog N` → only
  `∑μ²/φ = O((log N)^3.4)`, NOT the headline `Θ(log N)`. The **von Mangoldt route** gives
  coefficient EXACTLY 1, which is what makes `∑μ²/φ = O(log N)` come out. **Always pursue the
  von Mangoldt/sharp route for the headline; the crude route is a dead end for the constant.**
- **The big unlock this lap:** mathlib's `Mathlib/NumberTheory/Chebyshev.lean` already has the
  HARD piece — `Chebyshev.psi_le_const_mul_self : ψ x ≤ (log 4 + 4)·x` (and `psi_eq_sum_Icc`,
  `abs_psi_sub_theta_le_sqrt_mul_log`, etc.). So I did NOT have to formalize Chebyshev's ψ-bound;
  `mertens_vonMangoldt_upper` just calls it. Check mathlib first for the next bricks too.
- **Consumer-validation pattern (works great, used 3× this lap):** before an Aristotle lemma
  returns, write the downstream proof in a scratch with the lemma's *statement* as a temp `axiom`;
  get it green; port real+consumer together. Mertens-1st, then sharp `∑Λ/n` bounds, all validated
  this way.
- **Lean/tooling gotchas → flushed to personal memory:** `[[lean-rw-sum-image-ho-trap]]` (rw [←sum_image]
  HO fail), `[[aristotle-download-and-verify]]` (download=gzipped tar, no backticks in prompts, always
  `#print axioms`). Also v4.29 renames `div_le_div_iff→div_le_div_iff₀`, `le_div_iff→le_div_iff₀`;
  `Real.hasDerivAt_log` uses `⁻¹`; `Finset.Ico_add_one_right_eq_Icc`+`sum_Ico_eq_sum_range` for reindex.
- **Box OOM flake** ("Cannot allocate memory" on olean load) hits ~once per few builds — always a
  flake, re-run.

## ✅ State — `BoundedGaps/Mertens.lean` (~860 lines; every theorem `#print axioms` = [propext, Classical.choice, Quot.sound], no native_decide, no sorry)
**Sharp Mertens 1st (the headline result this lap):**
- `vonMangoldt_hyperbola` (`∑Λ(n)⌊N/n⌋ = ∑log m`, Aristotle `cc0dfbaf`) — `04ed26a`
- `sum_log_le`, `le_sum_log` (Stirling `N logN−N+1 ≤ ∑log m ≤ N logN`, via `integral_log`) — `8c6d615`
- `cast_div_le_self`, `sub_one_le_cast_div` (floor `N/n−1 ≤ ⌊N/n⌋ ≤ N/n`) — `59c17ac`
- `mertens_vonMangoldt_lower/_upper/_two_sided` (`|∑Λ(n)/n − log N| ≤ log4+4`) — `88f3811`,`7412132`

**Toolkit also landed (the crude route + general tools):** `mertens_prod_upper`/`mertensSummand_eq_prod`,
`chebyshev_theta_le(')`, `abel_div_le`(Aristotle `32baa99f`)+`mertens_first_le(')`, `telescope_tail_*`/
`prime_tail_le`, `hasDerivAt_log_log`+`integral_one_div_x_log_x`+`sum_one_div_n_log_n_le`,
`abel_summation_identity`(Aristotle `431512dd`), `mertens_second_term_bound`.

**Aristotle IN FLIGHT:** `3f137191-...` (`aristotle-pptail/PPTail.lean`) proving
`prime_power_tail_le : ∑_{n≤N, IsPrimePow ∧ ¬Prime} Λ(n)/n ≤ 1`.

## 🎬 Next actions
1. **Poll `aristotle list | grep pptail`** (`3f137191`). If IDLE: download (gzipped tar →
   `tar xzf`), VERIFY in-kernel + `#print axioms`, port. If it FAILED/looks wrong, the bound `≤ 1`
   may be too tight to prove cleanly — consider proving it locally with a looser constant, or via
   `Chebyshev.psi_sub_theta_eq_sum_not_prime` + `abs_psi_sub_theta_le_sqrt_mul_log` (mathlib gives
   the UNWEIGHTED `ψ−θ ≤ √x logx`; the 1/n-weighted tail is what we need, sum it with Abel or
   direct geometric bound). It is NOT on the critical path for a first `∑(log p)/p` bound if you
   accept O(1) — but you need it to make that O(1) explicit.
2. **Land sharp `∑_{p≤N}(log p)/p = log N + O(1)`**: `= ∑Λ(n)/n − (prime-power tail)`; with
   `mertens_vonMangoldt_two_sided` + `prime_power_tail_le` it's a few lines.
3. **Sharp Mertens 2nd `∑_{p≤N} 1/p = log log N + O(1)`** (coeff 1): 2nd Abel step on the SHARP
   `∑(log p)/p` via `abel_summation_identity`. ⚠️ do NOT reuse `mertens_second_term_bound` (it's the
   crude coeff-`(1+1/log2)` bound). For coeff 1, split `A_n·(1/log n − 1/log(n+1)) =
   log n·(…) + (A_n−log n)·(…)`: leading term → `loglog N + O(1)` (coeff 1, via `sum_one_div_n_log_n_le`);
   remainder `(A_n−log n)·(…)` with `A_n−log n = O(1)` TELESCOPES to `O(1)`.
4. Then `∑1/(p−1) ≤ ∑1/p+1` (`prime_tail_le`) + `1+x≤eˣ` ⇒ `∏(1+1/(p−1)) = O(log N)` ⇒
   `∑μ²/φ = O(log N)` ⇒ (with `mertens_lower`) **two-sided `Θ(log N)`** — the headline.

## ⚠️ Gotchas
- Keep ONE Aristotle job in flight (`3f137191` now). When it returns, architect the next (sharp
  Mertens 2nd's term-split lemma is a good candidate).
- The 2nd Abel weight `1/log n` is junk at n=1 (`1/log1 = 1/0 = 0`, NOT antitone at 1→2); `A_1=0`
  kills it but split off n=1 before any weight-monotonicity step.
- `sum_one_div_n_log_n_le` is `∑_{n=3}^N`; the Abel difference-sum is `∑_{n=2}^{N-1}` — mind the
  shift + the `1/(2log2)` n=2 term.

## 📁 Key files
- `BoundedGaps/Mertens.lean` — everything above.
- `PENDING_WORK.md §C` — full Aristotle status + sharp-route plan + 17-commit inventory.
- `ANALYTIC_AXIOM_BURNDOWN.md` — ladder map.
- `aristotle-pptail/PPTail.lean` — in-flight prime-power-tail statement.
- `aristotle-{abel,abelid,vmhyp}-dl/` — downloaded Aristotle proofs (provenance, untracked).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (the
analytic-NT Mertens burn-down). Start at Next action #1: poll `3f137191`; while it finishes, do
Next action #2 (land sharp `∑(log p)/p = log N + O(1)` — `mertens_vonMangoldt_two_sided` is already
landed, so this is `∑Λ/n` minus the tail). The sharp Mertens 1st is DONE; you're assembling toward
`∑μ²/φ = Θ(log N)`.**

🧵 ➡️ 🧵
