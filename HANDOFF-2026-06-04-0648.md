# Handoff: GPY sub-step (b) toolkit COMPLETE + singular-series corner (∑n/φ(n)∼A·N)

**Date**: 2026-06-04 06:48 UTC · **Branch**: `path-a-selberg-nu` · HEAD `cb470f2` ·
**13 commits this lap** (`4ed2a7e`…`cb470f2`), all green (full `lake build`, 8274 jobs,
verified at every pre-commit gate), all box-local (NEVER pushed). **Supersedes
`HANDOFF-2026-06-04-0547.md`.**

## 🎯 What we're doing
Analytic-NT axiom burn-down (`ANALYTIC_AXIOM_BURNDOWN.md`): turn the cited `s1`/`s2`
GPY/Maynard sieve-asymptotic axioms (`Sieve.lean`) into real proofs. Prior laps did
sub-step (a) [algebraic opening] and sub-step (b)'s diagonal reduction. **This lap
finished the sub-step (b) toolkit and opened the singular-series corner.**

## 🧠 Context to carry forward
- **Sub-step decomposition** (a) open square → divisor-lattice ∑_P; (b) CRT lattice
  count = main term + error; (c) Mertens/singular-series summation → `α=I(F)`;
  (d) error/`IsLittleO`. (a) and (b) are now done as **reusable bricks**.
- **The real blocker is sub-step (c), and it is literature-gated.** GPY needs
  `∑_{n≤x} μ²(n)/φ(n) ∼ log x` with **leading coefficient exactly 1**. What we have
  unconditionally (`Mertens.mertens_theta_log`) is only the *lossy* Θ-bound
  `∑μ²/φ ≤ K·log N`, `K≈e^γ` (from the `∏(1+1/(p-1))` route). The sharp `log x+O(1)`
  needs an elementary identity I could not reconstruct (the `n/φ(n)` hyperbola route
  gives the *different* sum `∑μ²/(φd)`; the Möbius-inverse route diverges). **Requested
  the textbook proof in `ON-LINE-REQUEST.md` (2026-06-04) — check for an
  `ON-LINE-FINDINGS-*.md` answer at lap start and act on it.**
- **Why the singular-series corner (`SingularSeries.lean`) even though it's adjacent:**
  `∑n/φ(n)` is NOT GPY's sieve sum (`∑μ²/φ` is), so `∑n/φ(n)∼A·N` is a *classical
  side-result*. But its machinery — multiplicative `IsMultiplicative.eq_iff_eq_on_prime_powers`,
  the Dirichlet hyperbola, Abel/`o(N)` error control — is exactly what sub-step (c)
  reuses. It's a warm-up that builds the toolbox.
- **The sub-step (b) final glue is NOT a quick brick.** `sum_restrict_offdiag_vanish`
  collapses the ∑_P expansion to the coprime diagonal *given* `hvanish` (off-diagonal
  counts = 0), and `lattice_count_offdiag_vanish_Wtrick` discharges `hvanish` *given*
  moduli coprime to `W=∏_{p≤D₀}p` + bounded distinct shifts. BUT `sieveDivisors` (the
  candidate set) is just "all divisors of `n+hᵢ` for `n` in the block" — the
  squarefree / coprime-to-W / R-bounded support that the W-trick needs lives in the
  **λ-weight support** (the `μ(d)` and `F(log d/log R)` in `lambdaTransform`), not in
  `sieveDivisors`. So the glue needs the GPY weight-support plumbing threaded in.
- **Aristotle harvested this lap:** `6515817c` (crtcount, the O(1) interval bound —
  ported as the cross-check witness for `ap_interval_count_bound`) and `627d10e3`
  (the Dirichlet hyperbola — ported). Both verified `#print axioms`-clean in our
  v4.29.1 kernel before porting.

## ✅ State (all axiom-clean `[propext, Classical.choice, Quot.sound]`, full build green)
**`SieveExpansion.lean` — sub-step (b) toolkit (new this lap, appended after
`lattice_count_eq_modEq`):**
- `ap_interval_count_bound` — single-AP interval count, `|#{m∈(A,B]:m≡v[M]} − (B−A)/M| ≤ 1`.
- `lattice_count_main_term` — **the diagonal GPY main term**: on the coprime diagonal,
  `|#{m∈(A,B]: m≡b[W] ∧ ∀i qᵢ∣(m+hᵢ)} − (B−A)/(W·∏q)| ≤ 1`.
- `lattice_count_offdiag_vanish` / `…_of_lt` / `…_Wtrick` — off-diagonal counts vanish
  (general / size form / the real W-trick discharge).
- `lattice_count_pair_offdiag_vanish` (the exact `dᵢ∣∧eᵢ∣` expansion form) +
  `sieveTheta_pair_offdiag_vanish` (s2 weighted sister).
- `sum_restrict_offdiag_vanish` — restricts ∑_P to the coprime diagonal.

**`SingularSeries.lean` — NEW module (wired into `BoundedGaps.lean`):**
- `self_div_totient_eq_sum_moebiusSq_div_totient` — keystone `n/φ(n)=∑_{d∣n}μ²(d)/φ(d)`
  (Dirichlet conv `ζ⋆(μ²/φ)=id/φ`).
- `dirichlet_hyperbola`, `sum_self_div_totient_eq_weighted`,
  `sum_self_div_totient_main_split`, `singularSum_tendsto_of_bounded`,
  `sum_self_div_totient_asymptotic` (**`∑_{n≤N} n/φ(n) ∼ A·N`**, conditional on the
  singular-sum bound `≤3`).

## 🎬 Next actions
1. **Poll `aristotle list | grep 36bb3493`** (job `aristotle-singbound`: the bound
   `∑_{d≤N} μ²(d)/(φ(d)d) ≤ 3`, RUNNING ~25min at handoff). If COMPLETE: download
   (`tar xzf` the gz), verify `#print axioms` in our kernel, port, then instantiate
   `singularSum_tendsto_of_bounded` / `sum_self_div_totient_asymptotic` to make
   `∑n/φ(n)∼A·N` **unconditional**. Then IMMEDIATELY submit the next bounded job.
2. **Check `ON-LINE-FINDINGS-*.md`** (sharp Mertens). If present: harvest it →
   formalize `∑_{n≤x} μ²(n)/φ(n) = log x + O(1)` (coeff 1) → sub-step (c) unblocks.
3. **Sub-step (b) final glue** (real, multi-step): instantiate `sum_restrict_offdiag_vanish`
   at the `sieveSum_selberg_nu_separable_expand` output with `diag := pairwise-coprime
   moduli`, discharging `hvanish` via `…_Wtrick`. Needs: establish from the λ-weight
   support that surviving divisors are squarefree, coprime to `W`, and that shifts are
   bounded by `D₀` — i.e. thread the GPY `W`/`D₀≥H`/admissibility data. This is the
   piece that assembles sub-step (b) into the `s1` main term.
4. **Sub-step (c) Riemann-sum mechanism** (hard analysis, independent of #2's *value*):
   `∑_{d≤R}(μ²/φ)F(log d/log R) ∼ (∫₀¹F)·log R` via `Mertens.abel_summation_identity`
   (partial summation) + Riemann-sum→integral. Worth attempting as a reduction (sharp
   Mertens as hypothesis).

## ⚠️ Gotchas
- **Box OOM flake** on `lake env lean` (~1/few runs, "Cannot allocate memory") — re-run.
- `lake build BoundedGaps.SingularSeries` / `…SieveExpansion` (single module, ~15s) is
  the fast iteration loop; full `lake build` is the commit gate (pre-commit runs it).
- **Aristotle v4.28 → our v4.29.1 drift:** ALWAYS `#print axioms` a downloaded proof in
  our kernel before trusting (a passing `grind` can inject `sorryAx`). Both this lap's
  ports were clean, but check every time. Download is a **gzip tarball** (not a dir
  despite `--destination`) — `tar xzf <file> -C <dir>`.
- `mertensSummand d` (in `Mertens.lean`) is *defeq* to `(moebius d:ℝ)^2/φ d` — bridge
  by `rfl`. Used in `sum_self_div_totient_asymptotic`.
- `ζ` needs `open scoped ArithmeticFunction.zeta` (else silent free-var auto-bind) —
  see staged memory `lean-zeta-notation-autobind`.

## 📁 Key files
- `BoundedGaps/SieveExpansion.lean` — sub-step (a)+(b); the new (b) toolkit at the tail.
- `BoundedGaps/SingularSeries.lean` — the new singular-series corner.
- `BoundedGaps/Mertens.lean` — `mertens_theta_log`/`mertens_isTheta_log` (the Θ core);
  `abel_summation_identity:659` (partial summation for sub-step (c)).
- `BoundedGaps/Sieve.lean` — `:1907` `s1_holds_from_nonprime_asym` & `:1928` `s2_*`
  (the target axioms); `:132/146` `alphaBound`/`betaBound` (the `IsLittleO` shapes).
- `ANALYTIC_AXIOM_BURNDOWN.md` — the map (updated with this lap's status).
- `ON-LINE-REQUEST.md` — sharp-Mertens literature request (open).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor
or wait for instructions, and don't offer other projects from the KB — this doc IS the
chosen thread (the analytic-NT sieve burn-down). Start at Next action #1 (poll/harvest
Aristotle `36bb3493`), check for an `ON-LINE-FINDINGS-*` sharp-Mertens answer (#2), then
drive the sub-step (b) glue / sub-step (c) mechanism (#3/#4). Keep ONE Aristotle job in
flight at all times.**

🧵 ➡️ 🧵
