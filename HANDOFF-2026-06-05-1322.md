# Handoff: `hcorr` re-diagnosed — diagonal leg is PNT-FREE (toolkit landed), off-diagonal needs growing `W`

**Date**: 2026-06-05 13:22 UTC · **Branch**: `path-a-selberg-nu` · HEAD `5e961ea`
6 axiom-clean commits this lap (`c93ed18`…`5e961ea`), full `lake build` green (8300 jobs) at every
pre-commit gate. Never pushed. Supersedes `HANDOFF-2026-06-05-1243.md`. Master `HANDOFF.md` untouched
(repo convention).

## 🎯 What we're doing
Analytic axiom burn-down for the contour-free Path-Y y-space `s1` (built ALONGSIDE the d-space
flagship; Trevor flips the axioms + `B^{±k}` normalisation later). Last lap fully discharged `hBaseW`
(W-coprime sharp Mertens), leaving the off-diagonal correction `hcorr` as the sole `s1` input — which
the lap-8 handoff called "PNT-blocked, a genuine deep wall." **This lap's fresh-mind review found that
verdict is wrong, and rebuilt the strategy around the correct one.**

## 🧠 Context to carry forward — READ THIS, it changes the plan
The lap-8 framing ("all of `hcorr` is PNT-strength, no in-kernel route") conflated two very different
legs of the correction. `S1Correction.yspace_correction_abs_bound_explicit` bounds `|correction|` by:
- **Diagonal leg** `∑_{diag}|∏λλ|·1` — cross-coprime tuples, count error `≤1`, **NO `M` factor**.
- **Off-diagonal leg** `∑_{¬diag}|∏λλ|·M/∏[d,e]` — count `=0` (W-trick); **scales WITH `M`**.

**Key realisation #1 — the scale `x` and level `R=N` are DECOUPLABLE.** `yr_heuristic_main_eq_quadForm_product`
(and the whole main chain) takes `x` as a free parameter (`hcov:(W·N)+2≤x`); the level `R=N` is pinned
only by `F`'s support cutoff. So `x` can be taken *polynomially large* in `N` independently.

**Key realisation #2 — the DIAGONAL leg is PNT-FREE.** The y-space coefficient `ℓ¹`-mass is a fixed
polynomial in `N` *independent of `x`* (the F-cutoff kills every `s>N`, so ≤N terms survive each ≤C —
no Möbius cancellation). With `x ≥ W·N^{6k+2}` ⟹ `M≈N^{6k+2}≥N^{6k+1}`, the diagonal ratio
`(diag weight ≤ ((C·N³)²)^k) / ((φW/W)^k(log N)^k·M) → 0`. **I landed the entire toolkit for this**
(see State). The lap-8 PNT request is moot for this leg.

**Key realisation #3 — the OFF-DIAGONAL leg is the real nut, and it needs a GROWING modulus `W=W(N)`,
NOT PNT.** The scale trick is useless here (`M` cancels vs the main term). Its size is `≈ ε(D₀)·main`
with `ε(D₀)=∑_{p>D₀}1/(p−1)²` (the fraction of singular-series mass shared between two coords at a
prime `p>D₀`; every shared prime exceeds `D₀` by the W-trick). **For FIXED `D₀` (fixed `W`), `ε(D₀)` is
a fixed positive constant ⟹ off-diag is `Θ(main)`, so `hcorr` is effectively FALSE as stated.** The
classical Maynard/GPY fix is `D₀=D₀(N)→∞` slowly (e.g. `log log log N`) so `W=W(N)→∞` and `ε→0`. This
is an architectural change: every current y-space limit theorem (incl. the new
`yspace_s1_sieveSum_div_tendsto_primorial`) fixes `W=∏T` finite.

The contour-free y-space **main term** is correct & unconditional (lap 8) — it's the full `s1` = main +
correction that needs growing `W`. Memory: `[[hcorr-diag-pntfree-offdiag-growing-w]]` (full write-up),
refines `[[s1-derivative-landmine]]`.

## ✅ State — all `[propext, Classical.choice, Quot.sound]`, no sorry, build green (8300)
- **`S1FullLimit.yspace_s1_sieveSum_div_tendsto_primorial`** — top y-space `s1` limit for primorial
  `W=∏T`, `hBaseW` discharged in-kernel (`CoprimeMertens.hBaseW_of_primes_totient`); conditional ONLY
  on `hcorr`.
- **`S1Correction`** (the PNT-free DIAGONAL-leg toolkit, all new this lap):
  - `abs_yLambda_le_level` (`|yLambda R F (log N) d| ≤ d·C·N`, x-independent),
    `yLambda_eq_zero_of_gt_level` (`d>N ⟹ 0`), `sum_abs_yLambda_le_level` (`∑_{d∈R}|yLambda| ≤ C·N³`).
  - `diag_weight_le_prod` (`∑_P|∏λλ| ≤ ∏_i(∑|λ|)²`) — Aristotle cross-check `8681a6f0` (RUNNING).
  - `diag_weight_yLambda_le_poly` (diagonal weight `≤ ((C·N³)²)^k`, x-independent).
  - `abs_diag_correction_le_diag_weight` (count-error glue: `|∑ w·e| ≤ ∑|w|` when `|e|≤1`).
- **`S1DiagonalSize.poly_over_scale_tendsto_zero`** — the scale-trick engine
  (`K·N^p/(c·(log N)^k·M N) → 0` when `M N ≥ N^{p+1}` eventually).
- **`S1DiagonalSize.diag_ratio_tendsto_zero`** — the Leg-1 ratio engine in usable form: takes the
  diagonal correction `g` abstractly (`|g N| ≤ K·N^p`) + `M N ≥ N^{p+1}` and gives
  `g N/(c·(log N)^k·M N) → 0`. So **the entire Leg-1 analytic chain is now abstract-complete**; the
  only remaining work is INSTANTIATION (supply `g` = the actual diagonal correction sum, `c=(φW/W)^k`,
  and the scale-decoupled `M`).
- `PENDING_WORK.md` (lap-9 head) + `ON-LINE-REQUEST.md` rewritten with the corrected diagnosis.

## 🎬 Next actions
1. **Wire Leg 1 (PNT-free, fully in reach — the satisfying milestone).** Add scale-general versions
   (do NOT mutate the existing ones — add alongside) of `S1MainLimit.yspace_s1_heuristic_main_div_sieveB_tendsto`
   and `S1FullLimit.yspace_s1_sieveSum_div_tendsto` taking `x : ℕ → ℝ`, `hcov : ∀ N, (W·N)+2 ≤ x N`
   (the proofs only use `hcov`/`positivity`, so it's threading — replace the hardcoded `(W*N:ℝ)+2`).
   Then prove `diag_correction_ratio_tendsto_zero`: instantiate `abs_diag_correction_le_diag_weight`
   with `e P = count_P − M/∏lcm` (`|e|≤1` from `yspace_diag_count_err`, needs per-P hpos/hcopW from
   Rset membership + the diag filter) to get `|g N| ≤ ((C·N³)²)^k` (via `diag_weight_yLambda_le_poly`),
   then feed `diag_ratio_tendsto_zero` (K=((C)²)^k? actually `((C·N³)²)^k = C^{2k}·N^{6k}`, so `K=C^{2k}`,
   `p=6k`, `c=(φW/W)^k`, `M(N)=(⌊2x⌋−(⌈x⌉−1))/W ≥ N^{6k+1}` for `x ≥ W·N^{6k+2}`). The abstract chain
   is DONE — this is pure instantiation. Discharges the diagonal HALF of `hcorr` with zero analytic input.
2. **Then attack Leg 2 (the real math): re-architect to growing `W = W(N) = ∏_{p≤D₀(N)} p`.** FIRST
   check what `Sieve.s1_holds_from_nonprime_asym` (Sieve.lean:3024) actually expects for the `W`-regime
   before re-stating the y-space limits. The off-diagonal then needs a per-prime singular-series
   mass bound `∑_{p|[d,e]}|λλ|/[d,e] ≤ (C/p)·Q` (likely elementary given W-coprimality; open whether
   it needs sharp `|λ|≤Cd/φ(d)`). `S1OffDiagSize.recip_sq_tail_tendsto_zero` is the `∑_{p>D₀}1/p²→0`
   engine, already proven.
3. **Aristotle**: harvest `8681a6f0` (brick_diagprod) when done — verify in-kernel (it should match the
   local `diag_weight_le_prod`), then submit the next bounded brick (e.g. an average-order
   `∑_{n≤N}σ(n)/φ(n) = O(N·polylog)` to sharpen `sum_abs_yLambda_le_level`, or a Leg-2 sub-lemma).
   `brick_singleprime` (dd98b9c1) is MOOT (hBaseW done) — ignore it.

## ⚠️ Gotchas
- `pow_le_pow_left` is now **`pow_le_pow_left₀`** (v4.29). For `1/n→0` use
  `tendsto_one_div_atTop_nhds_zero_nat` via `exact`, NOT `simpa` (it mangles `1/↑n` to `(↑n)⁻¹`).
- `field_simp` often closes a ring-equality goal outright; a trailing `ring` then errors "no goals".
- Dev fast: `/tmp/x.lean` importing the module, `taskset -c 0-7 lake env lean /tmp/x.lean`. `#print axioms`
  before trusting. Long-line warnings tolerated. Pre-commit hook runs the full `lake build`.

## 📁 Key files
- `BoundedGaps/S1Correction.lean` — the diagonal-leg toolkit (coeff bounds → diag weight → glue).
- `BoundedGaps/S1DiagonalSize.lean` — `poly_over_scale_tendsto_zero` (scale engine).
- `BoundedGaps/S1FullLimit.lean` — `yspace_s1_sieveSum_div_tendsto(_primorial)` (the top `s1` limit).
- `BoundedGaps/S1CountReconcile.lean` — `yr_heuristic_main_eq_quadForm_product` (the FREE-`x` chain).
- `PENDING_WORK.md` (lap-9 head) — the corrected diagnosis + revised priorities.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (contour-free
y-space `s1`: `hBaseW` DONE, `hcorr` now correctly split into a PNT-free diagonal leg with the toolkit
landed, and a growing-`W` off-diagonal leg). Absorb the context, then start at Next action #1: wire the
scale-general main/full limit and prove `diag_correction_ratio_tendsto_zero`.**

🧵 ➡️ 🧵
