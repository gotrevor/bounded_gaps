# Handoff: y-space S1 crystallised to ONE conditional theorem + s1 correction is UNCONDITIONAL (not BV-gated)

**Date**: 2026-06-05 09:56 UTC · **Branch**: `path-a-selberg-nu` · HEAD `dcdb1d6`
8 commits this lap (`648d461`…`dcdb1d6`), full build green at every pre-commit gate (8297 jobs).
Never pushed. Supersedes `HANDOFF-2026-06-05-0911.md`. Master `HANDOFF.md` untouched per repo convention.

## 🎯 What we're doing
Analytic axiom burn-down for the GPY/Maynard sieve. Flagship `bounded_gap_of_Mk_200` rests on 3 math
axioms: `BombieriVinogradov`, `s1_holds_from_nonprime_asym`, `s2_holds_from_prime_asym_at_level`
(verified this lap via `#print axioms` — unchanged, no regression). We build the **contour-free
Path-Y y-space `s1`** ALONGSIDE the d-space flagship (do NOT touch the flagship; Trevor flips the
axioms later). Prior laps built+connected the y-space chain. **This lap crystallised it into ONE
honest conditional theorem, discharged the unconditional half of the correction, corrected a strategic
error, and began the correction decomposition.**

## 🧠 Context to carry forward — read this
- **THE STRATEGIC CORRECTION (most important thing this lap):** the lap-4/5 handoffs repeatedly call
  the `s1` off-diagonal correction "BV-gated (gap C)". **That is WRONG — it conflates s1 with s2.**
  Classically (Maynard/GPY) and per the repo's own
  `archive/findings/ON-LINE-FINDINGS-2026-06-04-gpy-diagonal-asymptotic.md` (line 121: the s1 sum "is
  the **unconditional** case, needs no EH"), **only S2 (the prime weight θ) needs EH/BV** for its level
  of distribution; **S1 (non-prime) is elementary** (divisor sums + singular-series convergence). ⟹
  **the contour-free y-space S1 can be completed with NO analytic axioms** (only `hBaseW` from Aristotle
  + the architectural `B^{±k}` flip). Don't re-inherit the "BV-gated" framing.
- **The y-space chain is now ONE theorem** (`S1FullLimit.yspace_s1_sieveSum_div_tendsto`): the actual
  `sieveSum (selberg_nu_yr_sep …)` at scale `x=W·N+2`, level `R=N`, `/ (sieveB W N ^ k · M)` →
  `mkF_denominator k (∏Fs)` (**α exactly `mkF_denominator`, B^{+k} normalisation**), conditional ONLY
  on the correction tendsto `hcorr` + `hBaseW`. Plus the `(α+o(1))·main` IsLittleO form
  (`yspace_s1_sieveSum_isLittleO`) — the exact shape an eventual y-space `alphaBound` is built from.
- **B^{+k} vs B^{-k} (still Trevor's call):** the flagship `alphaMainTerm = sieveB^{-k}·x/W` is d-space;
  the y-space main term is `sieveB^{+k}·M` (`M≍x/W` the lattice density). Opposite log powers — the flip
  must restate `alphaMainTerm`/`betaMainTerm` to B^{+k}. Architectural; do NOT force it. Memory
  `[[yspace-s1-normalization-Bpm-k]]`. My `S1MainLimit`/`S1FullLimit` define the y-space main term as
  `sieveB W N ^ k · M` and prove α=mkF_den against it cleanly, so the flip becomes mechanical.
- **Why I didn't push further into the correction:** `S1Correction.lean` got the count into the exact
  `lattice_count_main_term`/`lattice_count_offdiag_vanish_Wtrick` shape (single modulus `q i =
  lcm(Pᵢ.1,Pᵢ.2)`, `l=List.finRange k`, `Ioc (⌈x⌉₊-1) ⌊2x⌋₊`). The next step (the `herr`/`hvanish` legs
  of `correction_abs_bound_offdiag`) needs the **W-trick coprimality setup** (pairwise-coprime moduli +
  W-coprimality + shared-prime>shifts), which is a fresh involved sub-project — better with fresh
  context than tangled half-done. The roadmap is in the `S1Correction.lean` file header.

## ✅ State — 4 new files, all axiom-clean `[propext, Classical.choice, Quot.sound]`, build green (8297)
- **`S1MainLimit.lean`** — `yspace_box_quadform_div_tendsto` (`∏quadForm/(log N)^k→(φW/W)^k·mkF_den`),
  **`yspace_s1_heuristic_main_div_sieveB_tendsto`** (heuristic main `/ sieveB^k·M → mkF_den`, α exact),
  `isLittleO_of_div_tendsto` (generic ratio→o helper), `yspace_box_quadform_isLittleO`.
- **`S1DiagonalSize.lean`** — `hyperbola_count_div_tendsto_zero` / `hyperbola_count_isLittleO`:
  `Dₖ(N)=o(N·(log N)^k)`, **unconditional** (the diagonal half of `correction=o(main)`). Pure analysis
  (squeeze the k-D Dirichlet count vs `(1+log N)^{k-1}/(log N)^k→0`).
- **`S1FullLimit.lean`** — **`yspace_s1_sieveSum_div_tendsto`** (the conditional capstone) +
  `yspace_s1_sieveSum_isLittleO` (the `(α+o(1))·main` form).
- **`S1Correction.lean`** — begins the unconditional correction discharge: `sieve_count_eq_lcm_count`
  (two-modulus → lcm count) + **`sieve_count_eq_lattice_count`** (full bridge to the lattice-count
  framework shape). File header = the complete elementary decomposition roadmap.
- `PENDING_WORK.md` lap-6 section (top) carries the strategic correction + concrete attack.

## 🎬 Next actions
1. **Continue the correction decomposition in `S1Correction.lean`** (the unconditional `hcorr` for
   `S1FullLimit`). Define `diag P := the lcm moduli pairwise coprime` and instantiate
   `SieveExpansion.correction_abs_bound_offdiag` on the y-space correction, using
   `sieve_count_eq_lattice_count` to feed `herr` (`lattice_count_main_term`) and `hvanish`
   (`lattice_count_offdiag_vanish_Wtrick`). Take the W-trick coprimality (pairwise-coprime moduli,
   W-coprime, shared-prime>shifts from admissibility) as explicit hypotheses first; construct them from
   the candidate set / `Admissible H` later. Output: `|correction| ≤ ∑_{diag}|∏λλ| + ∑_{¬diag}|∏λλ|·M/∏[d,e]`.
2. **Then the two SIZE bounds** (both elementary, no BV): diagonal `∑_{diag}|∏λλ|=o(M(log R)^k)` (count
   side = `S1DiagonalSize`, DONE; needs the y-space coeff bound `|∏yLambda²|≤C²·count-weight` — note
   `yLambda d = d·∑μ(s/d)F/φ(s)` is NOT uniformly O(1); the bound uses `d/φ(d)` + box→simplex support);
   off-diagonal `∑_{¬diag}|∏λλ|·M/∏[d,e]=o(M(log R)^k)` via the shared-prime `∑_{p>D₀}1/p²→0` factor
   (Mertens, in mathlib). Assembled, these give `hcorr` → `S1FullLimit` unconditional.
3. **Poll Aristotle `65d11d89`** (`brick_coprime_mertens`, the W-coprime sharp Mertens `hBaseW`, ~3h+ at
   31% this lap). If COMPLETE: download, `#print axioms`-verify, port (discharges `hBaseW` threaded
   through the whole chain). A new `aristotle_placeprod` job appeared mid-lap (not mine — leave it).

## ⚠️ Gotchas
- `S1MainLimit`/`S1FullLimit` fix `x = (W*N:ℝ)+2` and level `R=N` so the equality
  `yr_heuristic_main_eq_quadForm_product` (any `x≥W·N+2`) applies and `quadForm`'s `log N` scale aligns.
  The big heuristic-main sum is written out verbatim (verbose); `S1FullLimit`'s IsLittleO corollary
  takes the tendsto as a hypothesis to avoid re-listing the giant `hcorr`.
- Matching the discrete tendsto (`N:ℕ→∞`) to the flagship `alphaBound` (real `y→∞` IsLittleO) needs a
  **discrete→real filter bridge** (compose with `levelN(y)=⌊(y-2)/W⌋`). NOT done — unconditional, on
  the critical path, but bureaucratic; do it when wiring the actual flip.
- Long-line linter warnings on the new files are tolerated (same as S1CountReconcile precedent).
- Develop fast: `/tmp/*.lean` that `import BoundedGaps.S1Correction` (or S1MainLimit), `lake env lean`;
  `taskset -c 0-7 lake build BoundedGaps.X`.

## 📁 Key files
- `BoundedGaps/S1FullLimit.lean` — the honest top-level conditional theorem (tendsto + IsLittleO).
- `BoundedGaps/S1Correction.lean` — the in-progress unconditional correction; **header = the roadmap**.
- `BoundedGaps/S1MainLimit.lean`, `S1DiagonalSize.lean` — the heuristic-main limit + diagonal size.
- `BoundedGaps/SieveExpansion.lean` — `correction_abs_bound_offdiag` (:1226), `lattice_count_main_term`
  (:629), `lattice_count_offdiag_vanish_Wtrick` (:688), `diagonal_weight_le_count` (:1987),
  `hyperbola_count_le` (:2051).
- `BoundedGaps/S1YSpace.lean` — `selberg_nu_yr_sep` (:133), `sieveSum_…_eq_heuristic_add_correction` (:192).
- `PENDING_WORK.md` lap-6 section + `archive/findings/…gpy-diagonal-asymptotic.md` (s1 unconditional).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread (contour-free
y-space `s1`, now ONE conditional theorem; the sole remaining analytic input `hcorr` is ELEMENTARY, not
BV-gated). Absorb the context, then start at Next action #1: continue the `S1Correction.lean`
decomposition — instantiate `correction_abs_bound_offdiag` with the W-trick legs. Do NOT re-inherit the
"BV-gated" framing, and do NOT force the `B^{±k}` flip (Trevor's call).**

🧵 ➡️ 🧵
